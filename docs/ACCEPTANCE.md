---
status: pending
phase: 04-Поставка и приёмка
decision: D-49
started: —
updated: 2026-09-16
---

# Приёмка релиза v1 (UAT-гейт Фазы 4)

Письменный чек-лист ручной приёмки владельца — D-49. Проходится **одной
сессией** на живом столе (SPEC §7.2: «приёмка фазы = зелёная матрица e2e +
ручная приёмка владельцем»), как UAT-гейт verify-work Фазы 4 — та же
механика, что приняла Фазы 1–3 (формат: `.planning/phases/03-.../03-UAT.md`).
Владелец заполняет поля `result`/`note`; автоматизация подготовки — в
шагах каждого пункта.

Порядок не случаен: пункт 1 исполняется на свежей сессии **до** локальной
установки (прогон матрицы сам управляет источниками ввода через
snapshot/restore; чистая сессия — его предусловие), пункты 2–6 — уже с
установленным продуктом, uninstall — последним.

## Tests

### 1. Формальный D-48-гейт: матрица v3 дважды подряд на нетронутой сессии

expected: по процедуре из [docs/ci-runner.md](ci-runner.md) («D-48 double-run
gate»: перелогин владельца → `gh workflow dispatch e2e-matrix --ref <default>
-f fresh_session=true`) ОДИН запуск воркфлоу исполняет два последовательных
прогона матрицы v3 без каких-либо шагов между ними; критерий — оба прогона
`matrix: 31/31 PASS`, conclusion success
result:
note: (механика двойного прогона доказана ранее — run 35108412175; этот пункт
фиксирует формальный свежесессионный гейт)

### 2. Установка с нуля по README (канал релиз-архива, без root)

expected: следуя разделу Install [README.md](../README.md): скачать tar.gz со
страницы Releases, распаковать, `./goswitchctl install` — команда завершается
успехом БЕЗ sudo/root; затем `goswitchctl selfcheck` печатает шесть `ok`
(версия → компонент → unit → движок → конфиг → источник); источник ввода в
GNOME — только goswitch, прежние источники запомнены
result:
note:

### 3. Живой набор жестов в реальном поле ввода

expected: в обычном текстовом поле (например gedit):
одиночный Right Shift — раскладка переключилась (индикатор GNOME сменился);
`ghbdtn` + двойной тап → `привет` (и так же с `GHBDTN`/`сщьввте` — три
регистра); набранная фраза + тройной тап → исправлена целиком; при
выделенном тексте двойной тап исправляет только выделенное; `ghbdtn` +
Shift+RightCtrl → слово исправлено И раскладка переключена; смешанное слово
(`gfbпривет`) + двойной тап → конвертируются только чужие буквы
result:
note:

### 4. Конфиг: правка применяется без рестарта (hot reload)

expected: подключить конфиг по разделу Configuration README
(`systemctl --user edit goswitchd` с `-config ~/.config/goswitch/config.yaml`);
правка валидного значения (например `timeouts.tap_window_ms: 300` → `250`)
применяется без рестарта — `goswitchctl reload` отвечает успехом, поведение
меняется; затем внести опечатку — правка отвергается ЦЕЛИКОМ, демон живёт на
последней хорошей конфигурации, `goswitchctl status` показывает причину в
`config_error=`
result:
note:

### 5. goswitchctl status: счётчики растут

expected: `goswitchctl status` отвечает одной строкой key=value с версией
сборки; после нескольких жестов из пункта 3 счётчик `corrections_done`
вырос; `--json` даёт те же данные для скриптов
result:
note:

### 6. Uninstall — стол восстановлен полностью

expected: `goswitchctl uninstall` останавливает и удаляет unit и компонент
IBus, восстанавливает прежние источники ввода (как до пункта 2); ввод на
столе работает нормально (раскладка переключается системными средствами);
`goswitchctl selfcheck` после удаления — красный по версии (демона нет);
повторная установка при желании работает (идемпотентность)
result:
note:

## Summary

total: 6
passed: 0
issues: 0
pending: 6
skipped: 0
blocked: 0

## Gaps
