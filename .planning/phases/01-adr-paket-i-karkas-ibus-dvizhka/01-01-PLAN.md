---
phase: 01-adr-paket-i-karkas-ibus-dvizhka
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - mise.toml
  - .golangci.yml
  - .gitignore
  - cmd/goswitchd/main.go
  - internal/logging/logging.go
  - internal/logging/logging_test.go
  - engine/address.go
  - engine/types.go
  - engine/keys.go
  - engine/conn.go
  - engine/factory.go
  - engine/engine.go
  - engine/engine_test.go
  - engine/address_test.go
  - engine/wire_test.go
autonomous: true
requirements: [INTEG-01, INTEG-02, INTEG-03, INTEG-04, INTEG-05, INST-04]

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Wire-структуры Component (12 полей) и EngineDesc (17 полей) воспроизводят порядок полей из 01-RESEARCH.md §Pattern 1 дословно — порядок полей есть контракт провода"
    - "Каждый экспортированный D-Bus-обработчик обёрнут recover-шимом: внедрённая паника поглощается, логируется ERROR, возвращается безопасный ответ — юнит-тест с инъекцией паники зелёный (INTEG-05)"
    - "ProcessKeyEvent возвращает false для всех событий без исключения — наблюдатель, транзит по построению (INTEG-02); юнит-тест закрепляет false для bare-модификатора Shift_R"
    - "Трассировка клавиш (keyval/keycode) появляется в логе только при -debug; на уровне INFO пароли не попадают в лог — юнит-тест логгера закрепляет (INST-04)"
    - "Демон работает без root (запуск от пользователя сессии) и не содержит захвата evdev-устройств и путей к устройству инжектора в продуктовом коде — структурный grep-гейт"
    - "Адрес IBus-сокета открыт заново при каждом подключении (никогда не кэшируется): precedence $IBUS_ADDRESS → самый свежий файл ~/.config/ibus/bus/<machine-id>-unix-wayland-0"
    - "mise.toml определяет пины [tools] (go + golangci-lint) и задачи [tasks] build/test/lint/vet/tidy-diff/ci; инструменты ставятся per-project (`mise install`) — research-фолбэк «golangci-lint отсутствует в системе» снят директивой D-09; Makefile в репозитории отсутствует (D-09)"
    - ".golangci.yml — строгий v2-конфиг, адаптированный с эталона владельца: default: all, disable только с однострочным обоснованием, depguard deny unsafe, gocyclo 20 / cyclop 15 / gocognit 30, lll 120, gocritic diagnostic+performance+style, revive, dupl 120, nestif 6, prealloc, formatters gofmt+goimports с local-prefixes github.com/Djarvur/goswitch, exclusions.generated: strict (D-10)"
    - "Каждая задача, производящая Go-код, завершается тройным зелёным: build + test -race + golangci-lint run (канонически — `mise run ci`) — это инвариант конца каждой итерации, не финальный гейт фазы (D-08)"
    - "Чистые единицы адаптера (precedence адреса, декод state→событие, форма wire-структур, recover-шим, гейтинг трассировки) разрабатываются red→green→refactor: падающий тест на утверждении пишется до правки продукта; живой D-Bus handshake (Dial/Auth/Hello/RequestName/RegisterComponent/переподключение) поведенчески не TDD-ируется — его наблюдаемое существует только на живой шине, доказательство — живой гейт verify (integration-verified; D-07)"
    - statement: "goswitchd регистрируется на приватной шине IBus программно (метод org.freedesktop.IBus.RegisterComponent, без XML и без root): `ibus list-engine` содержит goswitch-en и goswitch-ru"
      verification: backstop
    - statement: "Будучи активным источником ввода (активация `ibus engine goswitch-en`), движок получает ProcessKeyEvent и логирует нажатия; обычный набор идёт транзитом; SIGTERM — чистый выход с кодом 0"
      verification: backstop
  artifacts:
    - path: go.mod
      provides: "модуль github.com/Djarvur/goswitch, go 1.23, единственная внешняя зависимость godbus/dbus/v5 v5.2.2"
      contains: "github.com/godbus/dbus/v5"
    - path: mise.toml
      provides: "единственный источник версий инструментов: [tools] go + golangci-lint (пинны) и [tasks] build/test/lint/vet/tidy-diff/ci (D-09)"
      contains: "golangci-lint"
    - path: .golangci.yml
      provides: "строгий v2-конфиг с эталона владельца: default all, depguard deny unsafe, потолки сложности, formatters с local-prefixes модуля (D-10)"
      contains: "local-prefixes"
    - path: engine/types.go
      provides: "wire-структуры Component, EngineDesc, IBusText/AttrList в порядке полей контракта"
    - path: engine/conn.go
      provides: "подключение Dial/Auth/Hello/RequestName + RegisterComponent + цикл переподключения с бэкоффом 1-2 с"
    - path: engine/engine.go
      provides: "объект движка на 3 интерфейсах (IBus.Engine, IBus.Service, DBus.Properties), recover-шим, seam EventHandler"
    - path: internal/logging/logging.go
      provides: "Setup(w, debug) — slog JSON handler, LevelVar, уровни ERROR/WARN/INFO/DEBUG"
    - path: cmd/goswitchd/main.go
      provides: "тонкий main + run(ctx, cfg) error, флаг -debug (default false)"
  key_links:
    - from: cmd/goswitchd/main.go
      to: engine/conn.go
      via: "engine.Run(ctx, cfg) — весь цикл подключение→регистрация→наблюдение→переподключение"
      pattern: "engine\\.Run\\("
    - from: engine/conn.go
      to: engine/factory.go
      via: "ре-экспорт Factory на каждом (пере)подключении; CreateEngine минтит объекты движка"
      pattern: "CreateEngine"
    - from: engine/engine.go
      to: internal/logging/logging.go
      via: "slog.Debug(\"key\", keyval, keycode, release, mods) — только при -debug"
      pattern: "slog\\.Debug"
  prohibitions:
    - "goswitchd НЕ ДОЛЖЕН логировать содержимое нажатий клавиш (keyval/keycode/state) на уровне INFO или без явно включённого флага -debug: трассировка клавиш может захватывать пароли (privacy; PITFALLS security table)"
    - "Продуктовый код демона НЕ ДОЛЖЕН захватывать клавиатуру или потреблять нажатия в Фазе 1: никаких ioctl-захватов evdev-устройств, обращений к устройству инжектора из демона; ProcessKeyEvent всегда false — иначе ломается совместимость с keyd/xremap и Shift+буквы (safety, INTEG-02)"
    - "goswitchd НЕ ДОЛЖЕН передавать данные о нажатиях за пределы локальной машины — никакого сетевого исходящего трафика из демона (privacy)"
    - "Makefile НЕ ДОЛЖЕН создаваться: все проверочные сценарии — задачи mise в mise.toml ([tasks]), гейты вызываются как mise run task; research-фолбэк «golangci-lint отложен» отменён директивами D-09/D-10"
  assumptions:
    - "FLAGGED (research A1): форма сигнала UpdatePreeditText (4 аргумента) не проверена — Фаза 1 preedit не эмитит, риск нулевой; проверить dbus-monitor при первом использовании (Фаза 3)"
    - "FLAGGED (wire): RequestName(org.freedesktop.IBus) на приватной шине может вернуть не PrimaryOwner — полика единственного экземпляра: не-PrimaryOwner → WARN в журнал и выход с ошибкой; сверка с dbus-monitor при отклонении"
    - "FLAGGED (env, снят D-09): golangci-lint отсутствовал в системе (research, Environment Availability) — директива D-09 отменяет фолбэк: mise ставит инструмент per-project; сам mise на машине НЕ установлен (проверено планировщиком round 3: PATH и ~/.local/bin чисты) — Task 1 ставит его официальным инсталлятором user-local (без root), установленная версия фиксируется в SUMMARY"
---

<objective>
Walking skeleton: `goswitchd` запускается, находит приватную шину IBus, проходит Dial/Auth/Hello/RequestName, программно регистрирует компонент с двумя движками (`goswitch-en` с layout us, `goswitch-ru` с layout ru), будучи активным источником ввода логирует каждое нажатие в структурированный JSON-лог и возвращает false (чистый наблюдатель), переживает рестарт ibus-daemon циклом перерегистрации, а паники обработчиков — recover-шимом, и завершается чисто по SIGTERM.

Это трассирующий вертикальный срез Фазы 1: он трогает каждый слой фазы (main → лог → D-Bus адаптер → ibus-daemon → лог) и есть гейт M1 в миниатюре. Все остальные планы фазы строятся поверх этого каркаса.

Purpose: Доказать архитектуру end-to-end первым коммитом D-Bus-слоя (STATE: recover-шим и цикл перерегистрации — day-one элементы; падение движка = смерть ввода рабочего стола). Одновременно это первый кодовый план фазы — здесь же ложится toolchain-каркас владельца: mise.toml и строгий .golangci.yml (D-09/D-10), чтобы все последующие задачи закрывались тройным гейтом D-08.
Output: mise.toml + .golangci.yml, работающий демон `cmd/goswitchd`, адаптер `engine/` (~1k LOC), структурированные логи. CI-workflow, dev systemd-юнит и README — выделены в план 01-05 (checker round 1: file budget).

Scope acceptance (checker round 2, scope_sanity — file budget; пересчитан round 3 под директивы D-07..D-10): files_modified = 16 при пороге предупреждения 10. Прирост против round-2 (12 файлов) разложен на директивы, а не на новую функциональность: +mise.toml и +.golangci.yml — обязательный toolchain-каркас первого кодового таска (D-09/D-10: гейт D-08 требует линт с первой итерации; вынос в 01-05 оставил бы задачи 01-01..01-04 без тройного гейта и без источника версий для CI); +engine/address_test.go, +engine/wire_test.go и переименование recover_test.go → engine_test.go — разложение корпуса по единицам для red-first дисциплины D-07. Превышение принято осознанно: трассер обязан быть одним вертикальным wire-contract-срезом через все слои (main → логгер → адаптер D-Bus → ibus-daemon → лог); горизонтальное разрезание (вынос internal/logging или toolchain-каркаса в отдельный ранний план) нарушило бы tracer-first и создало циклическую зависимость с 01-05 — его локальный гейт компилирует модуль целиком и требует готовый go.mod из ЭТОГО плана. Оценка 48k токенов = 48% контекстного бюджета — ниже целевых 50%. Дальнейшие разрезания снижают доказательность трассера.
</objective>

<execution_context>
@/home/nil/DiskD/W/Djarvur/goswitch/.zcode/gsd-core/workflows/execute-plan.md
@/home/nil/DiskD/W/Djarvur/goswitch/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-adr-paket-i-karkas-ibus-dvizhka/01-CONTEXT.md
@.planning/phases/01-adr-paket-i-karkas-ibus-dvizhka/01-PATTERNS.md
@AGENTS.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Трассер — goswitchd регистрируется на шине IBus и видит клавиши</name>
  <reversibility rating="reversible">Адаптер заменяем за одним стабильным seam (engine.Run); выбор D-01 не зависит от этого кода (CONTEXT D-01 — reversible)</reversibility>
  <files>go.mod, mise.toml, .golangci.yml, .gitignore, cmd/goswitchd/main.go, internal/logging/logging.go, engine/address.go, engine/types.go, engine/keys.go, engine/conn.go, engine/factory.go, engine/engine.go</files>
  <read_first>
  - .planning/phases/01-adr-paket-i-karkas-ibus-dvizhka/01-PATTERNS.md (§ назначений для каждого файла; § Shared Patterns)
  - .planning/phases/01-adr-paket-i-karkas-ibus-dvizhka/01-RESEARCH.md (§ Pattern 1 wire-поверхность — порядок полей Component/EngineDesc дословно; § Pattern 2 константы; § Pattern 5 reconnect+recover; § Pattern 7 логирование; § Anti-Patterns; § Environment Availability — строка golangci-lint снята директивой D-09)
  - .zcode/skills/go-ultimate/SKILL.md (тонкий main / run(ctx, cfg) error; конвенции)
  - .zcode/skills/go-ultimate/assets/cli-simple/main.go (форма main, строки 17-45)
  - [ext] /home/nil/.local/share/ibus-test/test_shift.py (поведенческая модель наблюдателя, строки 25-74; только чтение)
  - [ext] /home/nil/DiskD/W/Djarvur/ass-guard-agent/.golangci.yml (эталон владельца D-10 — база адаптации; v2, default: all, ceilings, exclusions)
  - [ext] /home/nil/DiskD/W/Djarvur/ass-guard-agent/mise.toml (эталон владельца D-09 — форма [tools]/[env]/[tasks], задачи build/test/lint/vet/ci)
  </read_first>
  <action>
  Шаг 0 — toolchain-каркас (D-09/D-10, до первой строки Go-кода). Если mise отсутствует (проверено планировщиком: на машине его нет), установить user-local официальным инсталлятором: curl https://mise.run | sh — ставит в ~/.local/bin/mise без root (jdx/mise, единственная официальная точка распространения); установленную версию записать в SUMMARY. Создать mise.toml по образцу эталона владельца: [tools] go = "1.23" (паритет с директивой go.mod, mise резолвит патч-версию — единственный источник версии инструментария и для локальной разработки, и для CI плана 01-05) и golangci-lint = "2" (мажорный пин, как в эталоне); [env] GOFLAGS = "-trimpath"; [tasks]: build = go build ./..., test = go test -race -count=1 ./..., lint = golangci-lint run, vet = go vet ./..., tidy-diff = go mod tidy плюс git diff --exit-code -- go.mod go.sum, ci с depends = [build, vet, lint, test] — форма имена как в [ext] mise.toml эталона. Выполнить mise install: golangci-lint становится доступен per-project — research-фолбэк «golangci-lint отсутствует, стадийность vet+race» (RESEARCH § Environment Availability) снят директивой D-09. Makefile НЕ создавать (D-09); в .gitignore добавить строку .mise.local.toml. Затем .golangci.yml — адаптация эталона владельца: version "2"; run.timeout 5m, modules-download-mode readonly; linters.default: all; disable-лист переносится с эталона, каждый с однострочным обоснованием (wsl — устарел в v2.2.0, заменён wsl_v5; wsl_v5 — house-style эталона владельца; gomodguard и gomodguard_v2 — пара устаревший плюс renamed-преемник; noinlineerr — противоречит inline-err стилю эталона; modernize — новые gopls-проверки, не входящие в vetted-гейт); settings: depguard deny unsafe с описанием «unsafe ломает memory-safety демона, обрабатывающего нажатия», gocyclo min-complexity 20, cyclop max-complexity 15, gocognit min-complexity 30, lll line-length 120 tab-width 4, varnamelen ignore-names как в эталоне (i j k v r w id ok err ctx t db tc ch fr), tagliatelle json/yaml/toml snake, gocritic enabled-tags diagnostic+performance+style, revive — тот же список правил, что в эталоне, dupl threshold 120, nestif min-complexity 6, prealloc simple+for-loops; exclusions: generated: strict (сгенерированный в плане 01-02 layouts/tables.go понесёт маркер DO-NOT-EDIT первой строкой до package и пропускается линтом автоматически — отдельные path-исключения для генерата НЕ заводятся), presets common-false-positives, paths vendor и third_party (spikes-строка эталона не переносится — таких каталогов в goswitch нет), правила-релаксации тестов из эталона: errcheck на defer Close/Rollback/Fprint/Setenv в *_test.go, unparam, dupl, gosec в *_test.go, exhaustruct плюс exhaustruct_v5 с text .*, errcheck в cmd/ на fmt.Println/os.Exit/log.Fatal; issues: max-issues-per-linter 0, max-same-issues 0, new false; formatters gofmt и goimports с local-prefixes github.com/Djarvur/goswitch — единственное содержательное изменение против эталона (путь модуля). Проверка конфига: mise exec -- golangci-lint config verify.
  Порядок полей в engine/types.go — это контракт провода, переносится из RESEARCH §Pattern 1 дословно, без переупорядочивания: Component (Name "IBusComponent", Attachments, ComponentName "org.freedesktop.IBus.goswitch", Description, Version "0.1.0", License "MIT", Author "Djarvur", Homepage, Exec пустая строка, Textdomain, ObservedPaths, EngineList) и EngineDesc (17 полей; для goswitch-en: EngineName "goswitch-en", LongName "goswitch English (US)", Language "en", Layout "us", Symbol "en"; для goswitch-ru: "goswitch-ru", LongName "goswitch Русская", Language "ru", Layout "ru", Symbol "ru"; Rank 0, Version "0.1.0"). Регистрируются ОБА движка с первого дня — эксперименту D-01 нужны оба. IBusText/AttrList — по RESEARCH (пустой au для простых коммитов; эмиттер CommitText входит в адаптер, коррекция — Фаза 2).
  engine/address.go: Discover() — precedence: переменная окружения IBUS_ADDRESS, иначе самый свежий файл ~/.config/ibus/bus/ (пример живого файла в RESEARCH). Адрес никогда не кэшировать — сокет меняется на каждом поколении ibus-daemon. В ЭТОМ таске — минимальная happy-path версия (env → самый свежий по mtime файл, без суффиксного фильтра и без контракта ошибок); полный precedence-контракт (выбор по суффиксу WAYLAND_DISPLAY/DISPLAY, пустой каталог → именованная ошибка) выгоняется красными тестами в Task 2 (D-07 — минимальная реализация трассера оставляет корпусу подлинные RED).
  engine/conn.go: engine.Run(ctx, cfg) error — цикл: Discover → dbus.Dial → Auth (AuthExternal(uid), fallback AuthCookieSha1) → Hello → RequestName("org.freedesktop.IBus", NameFlagReplaceExisting); результат RequestName проверяется: не-PrimaryOwner/AlreadyOwner → slog.Error и выход с ошибкой (единственный экземпляр). Далее экспорт Factory и вызов org.freedesktop.IBus.RegisterComponent на /org/freedesktop/IBus с аргументом dbus.MakeVariant(component) — имя метода БЕЗ подчёркивания (проверено по строкам libibus; записи вида _RegisterComponent в планировочных документах означают этот же вызов). Потеря шины (закрытие канала Signal() / ErrClosed) → WARN → полный повтор цикла от Discover с бэкоффом 1-2 с с джиттером; выход по ctx.
  engine/factory.go: экспорт на пути /org/freedesktop/IBus/Factory, интерфейс org.freedesktop.IBus.Factory, метод CreateEngine(s engine_name) → (o object_path): минтить путь /org/freedesktop/IBus/Engine/goswitch/<n> с монотонным n, экспортировать туда объект движка, вернуть путь.
  engine/engine.go: объект движка экспортируется на ТРЁХ интерфейсах одновременно (org.freedesktop.IBus.Engine, org.freedesktop.IBus.Service, org.freedesktop.DBus.Properties). Все методы из таблицы RESEARCH §Pattern 1 (ProcessKeyEvent, SetCursorLocation, SetSurroundingText, SetCapabilities, FocusIn, FocusOut, Reset, Enable, Disable, PageUp/PageDown/CursorUp/CursorDown, CandidateClicked, PropertyActivate/Show/Hide, Destroy, Get/GetAll/Set). КАЖДЫЙ экспортированный обработчик обёрнут recover-шимом с defer (поглощение паники + slog.Error + безопасный ответ) — с первого коммита D-Bus-слоя; каждое variant-утверждение только в форме comma-ok. ProcessKeyEvent(u keyval, u keycode, u state) → b: декодировать один раз в EngineEvent{Keyval, Keycode, Release: state&(1<<30)!=0, Mods: state&0xff}, залогировать на DEBUG, передать в EventHandler (если установлен) и НЕМЕДЛЕННО вернуть false — никаких ожиданий в обработчике (анти-паттерн «поздний ответ»; потребление bare-модификаторов запрещено — прототип владельца line 355 escape-пример, Фаза 1 его НЕ копирует). SetSurroundingText — только лог. FocusOut/Reset/Enable — прокинуть в обработчик как lifecycle-события.
  Seam для Фазы 1/3: engine.go объявляет интерфейс EventHandler с методами HandleKey(ev EngineEvent) и HandleLifecycle(kind LifecycleKind) (FocusIn, FocusOut, Reset, Enable, Disable); cfg.Handler может быть nil — тогда движок только логирует.
  engine/keys.go: константы из RESEARCH §Pattern 2 дословно: маски IBUS_SHIFT_MASK 1<<0, IBUS_CONTROL_MASK 1<<2, IBUS_MOD1_MASK 1<<3, IBUS_MOD4_MASK 1<<6, IBUS_MOD5_MASK 1<<7, IBUS_RELEASE_MASK 1<<30; биты capabilities (PREEDIT 1<<0, SURROUNDING 1<<5, SYNC 1<<7); keyval: BackSpace 0xff08, Tab 0xff09, Return 0xff0d, Escape 0xff1b, Shift_L 0xffe1, Shift_R 0xffe2, Control_L 0xffe3, space 0x020.
  internal/logging/logging.go: Setup(w io.Writer, debug bool) — slog.NewJSONHandler на w с LevelVar (LevelDebug при debug, иначе LevelInfo), slog.SetDefault. Контракт уровней: ERROR — ошибки адаптера и исчерпание перерегистрации; WARN — реконнекты, аномалии caps; INFO — жизненный цикл (connected, component registered, engine created, focus in/out, re-registered); DEBUG — трассировка клавиш формата msg="key", keyval="0x..", keycode, release, mods="0x..". Никакого содержимого буфера на INFO (только длины) — правило с первого дня.
  cmd/goswitchd/main.go: тонкий main по паттерну go-ultimate — флаг -debug (по умолчанию выключен), main только парсит флаги и зовёт run(context.Background(), cfg); run(ctx, cfg) error: signal.NotifyContext(SIGINT, SIGTERM), logging.Setup(os.Stderr, cfg.Debug), engine.Run(ctx, cfg); ошибка run печатается в stderr с префиксом goswitchd и os.Exit(1). Тестируемой логики в main нет.
  go.mod: module github.com/Djarvur/goswitch, директива go 1.23, единственная зависимость go get github.com/godbus/dbus/v5@v5.2.2 (yaml.v3 и fsnotify в Фазу 1 НЕ добавлять). Патч-версия бинарного файла go1.27.1 локальна, версия языка в go.mod остаётся 1.23.
  .gitignore: база из PATTERNS (bin/, *.exe, *.test, coverage, кэши инструментов, editors/OS) БЕЗ строки dist/ — dist/ в этом проекте хранит трекуемые упаковочные исходники (systemd unit — план 01-05); пометить это комментарием; плюс строка .mise.local.toml из Шага 0.
  Удалить engine/.gitkeep (каталог наполняется реальными файлами).
  TDD-граница (D-07, заявлена явно): живой D-Bus handshake (Dial/Auth/Hello/RequestName/RegisterComponent/экспорт/цикл переподключения) поведенчески не TDD-ируется — его наблюдаемое существует только на живой шине; доказательство — живой гейт verify (integration-verified). Чистые единицы (precedence адреса, декод state, wire-форма, recover-шим, гейтинг трассировки) в этом таске минимальны, их полный контракт закрывается red→green→refactor корпусом Task 2.
  </action>
  <verify>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise exec -- golangci-lint config verify && mise run build && mise run vet && mise run lint && mise run test && echo TOOLCHAIN-GATES</automated>
    <fails_when>non-zero exit: конфиг .golangci.yml невалиден (config verify), ошибка сборки, замечание vet, находка golangci-lint или падение тестов — тройной гейт D-08 обязан быть зелёным уже на трассере (юнит-тестов ещё нет, go test проходит с no test files)</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && test ! -f Makefile && test ! -f GNUmakefile && test ! -f makefile && grep -q 'golangci-lint = ' mise.toml && grep -q 'default: all' .golangci.yml && grep -q 'unsafe' .golangci.yml && grep -q 'github.com/Djarvur/goswitch' .golangci.yml && grep -q 'generated: strict' .golangci.yml && echo TOOLING-OK</automated>
    <fails_when>существует Makefile/GNUmakefile/makefile (нарушение D-09); mise.toml не пиннит golangci-lint в [tools]; .golangci.yml не содержит default: all, deny unsafe, local-prefixes модуля или generated: strict</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise exec -- go build -o /tmp/goswitchd ./cmd/goswitchd || exit 1; LOG=$(mktemp); FAIL=0; PREV=$(ibus engine); /tmp/goswitchd -debug 2>"$LOG" & PID=$!; REG=1; for i in $(seq 1 50); do if grep -q '"msg":"component registered"' "$LOG"; then REG=0; break; fi; sleep 0.2; done; ibus list-engine | grep -q goswitch || FAIL=1; ibus engine goswitch-en; FOC=1; for i in $(seq 1 50); do if grep -q '"msg":"focus_in"' "$LOG"; then FOC=0; break; fi; sleep 0.2; done; ydotool type --key-delay 40 ghbdtn; grep -q '"msg":"key","keyval":"0x67"' "$LOG" || FAIL=1; ibus engine "$PREV"; kill $PID; wait $PID; DEXIT=$?; test "$REG" -eq 0 && test "$FOC" -eq 0 && test "$FAIL" -eq 0 && test "$DEXIT" -eq 0</automated>
    <fails_when>итоговый test ненулевой: REG=1 — строка "component registered" не появилась в логе за 10 с; FOC=1 — focus_in не появилась за 10 с после активации; FAIL=1 — `ibus list-engine` не содержит goswitch или после инжекции ghbdtn в логе нет строки key с keyval 0x67; DEXIT не 0 — exit-код демона после SIGTERM не 0; провал сборки — немедленный exit 1</fails_when>
  </verify>
  <done>Демон собирается под строгим линтом (D-08/D-10), toolchain-каркас mise на месте (D-09), оба движка регистрируются программно без root, при активации через ibus engine goswitch-en логирует нажатия (наблюдатель, false), восстанавливает прежний движок и выходит чисто по SIGTERM</done>
  <acceptance_criteria>
  - mise.toml: [tools] пиннит go и golangci-lint; определены задачи build/test/lint/vet/tidy-diff/ci; mise install выполняется per-project; Makefile в репо отсутствует
  - .golangci.yml: version "2", default: all, depguard deny unsafe, gocyclo 20 / cyclop 15 / gocognit 30, lll 120, formatters gofmt+goimports с local-prefixes github.com/Djarvur/goswitch, exclusions.generated: strict; каждый disable снабжён однострочным обоснованием
  - mise exec -- golangci-lint config verify зелёный; код трассера проходит строгий линт с первой итерации (D-10)
  - go.mod содержит `go 1.23` и `github.com/godbus/dbus/v5 v5.2.2`; yaml.v3/fsnotify отсутствуют
  - engine/types.go: поля Component идут в порядке Name, Attachments, ComponentName, Description, Version, License, Author, Homepage, Exec, Textdomain, ObservedPaths, EngineList; EngineDesc — 17 полей, Layout на позиции 9, Symbol на позиции 12 (сверка по RESEARCH §Pattern 1)
  - engine/conn.go содержит цикл переподключения: повторный Discover перед каждым Dial (нет кэша адреса) и повторный вызов RegisterComponent после повторного экспорта Factory; бэкофф 1-2 с
  - каждый экспортированный метод объекта движка содержит defer-recover (grep по engine/engine.go находит recover в каждом обработчике)
  - ProcessKeyEvent возвращает литерал false до любых ожиданий; ни один обработчик не блокируется
  - лог-строки INFO жизненного цикла включают messages: connected, component registered, engine created, focus_in, focus_out, re-registered
  - запуск verify-скрипта от имени текущего пользователя (id -u != 0) проходит без sudo
  </acceptance_criteria>
</task>

<task type="tdd">
  <name>Task 2: TDD-корпус чистых единиц адаптера — red → green → refactor (D-07)</name>
  <files>engine/address_test.go, engine/engine_test.go, engine/wire_test.go, internal/logging/logging_test.go</files>
  <read_first>
  - engine/engine.go, engine/keys.go, engine/address.go, engine/types.go (тестируемый код из Task 1)
  - internal/logging/logging.go
  - .zcode/skills/go-ultimate/references/testing.md (vanilla testing, package xxx_test, TestF_suffixCamelCase)
  - .planning/phases/01-adr-paket-i-karkas-ibus-dvizhka/01-RESEARCH.md (§ Validation Architecture — строки таблицы INTEG-05 и INST-04; § Pattern 1 — эталон порядка полей для wire-пинов)
  </read_first>
  <behavior>
  - TestAddress_EnvPrecedence: переменная окружения IBUS_ADDRESS бьёт любые файлы каталога (HOME переопределён на temp-каталог с фейковыми файлами сокетов и подделанными mtime)
  - TestAddress_PicksFreshest: без env выбирается файл с новейшим mtime, отфильтрованный по суффиксу WAYLAND_DISPLAY/DISPLAY — файл с чужим суффиксом игнорируется, даже если он свежее
  - TestAddress_EmptyDirError: пустой/несуществующий каталог при отсутствии env → именованная ошибка, не паника и не пустая строка
  - TestWire_FieldOrder: reflect-пин порядка полей Component (12) и EngineDesc (17) — последовательность Field(i).Name равна RESEARCH §Pattern 1 (порядок полей есть контракт провода)
  - TestEngine_RecoverContainsPanic: вызов каждого экспортированного обработчика с обработчиком-инъектором паники не паникует наружу; на выходе безопасный ответ (false/пустой), в логе ERROR
  - TestEngine_ProcessKeyEventReturnsFalse: для bare-модификатора Shift_R (press и release), для буквы, для组合 с модификаторами — всегда false
  - TestEngine_DecodeState: state с битом 1<<30 → Release=true; Mods маскируется 0xff; keyval константы равны табличным значениям (Shift_R 0xffe2 и др.)
  - TestLogging_JSONShape: каждая запись содержит ключи time, level, msg; уровни ERROR/WARN/INFO/DEBUG фильтруются по LevelVar
  - TestLogging_KeyTraceGated: при Setup(w, false) вызов трассировки клавиш не пишет ничего; при Setup(w, true) — пишет запись msg="key" с keyval/keycode/release/mods
  </behavior>
  <action>
  RED (D-07 — сначала падающий корпус, до правок продукта): написать все четыре тест-файла и прогнать mise exec -- go test ./engine/ ./internal/logging/ -race -count=1. Подлинные RED ожидаются в address-кейсах — минимальная happy-path реализация трассера не умеет суффиксный фильтр (свежайший файл с чужим суффиксом должен игнорироваться) и не даёт именованной ошибки на пустом каталоге; это и есть авторизатор GREEN. Тесты, зелёные с прибытия (пины контракта — wire-порядок полей, happy-path decode, наблюдатель false, recover-шим, гейтинг), зафиксировать в test-коммите как пины (путь «feature exists» из tdd.md: тест значим, поведение уже написано трассером); если НИ ОДИН тест не упал на утверждении — остановиться и усилить корпус: compile-error и пустой прогон RED-гейтом не являются. Commit: test(01-01). GREEN: минимальные правки продукта до зелёного корпуса — выделение чистой функции выбора адреса в engine/address.go (суффиксный фильтр WAYLAND_DISPLAY/DISPLAY, именованная ошибка пустого каталога); остальное поведение уже написано трассером и правится только при реальном падении. Commit: feat(01-01). REFACTOR: чистка без изменения поведения, при необходимости отдельным коммитом refactor(01-01). Сценарии без реальной шины: объект движка конструируется с nil-обработчиком и тестовым логгером; паника инъектируется тестовым EventHandler, паникующим в HandleKey; логгер проверяется через bytes.Buffer (каждая строка парсится как JSON-объект). Имена Test*_ по go-ultimate (внешние пакеты engine_test / logging_test), vanilla testing без testify, все прогоны с -race.
  </action>
  <verify>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise exec -- go test ./engine/ ./internal/logging/ -race -count=1 -v</automated>
    <fails_when>строка FAIL в выводе; или отсутствует строка ok для engine или internal/logging; или в -v листинге нет функций TestAddress_ / TestWire_ / TestEngine_ / TestLogging_</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && ! grep -rEq 'EVIOCGRAB|/dev/uinput' cmd internal engine layouts && echo CLEAN</automated>
    <fails_when>grep находит совпадение в продуктовом коде (вывод не CLEAN) — захват устройств/прямой инжектор допустим только в test/e2e</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise run ci</automated>
    <fails_when>non-zero exit: build, vet, golangci-lint или go test -race по всему модулю — тройной гейт обязателен в конце каждой итерации, не в конце фазы (D-08)</fails_when>
  </verify>
  <done>Корпус зелёный под -race и строгим линтом: precedence адреса (env → суффиксный фильтр → свежайший), wire-порядок, recover-шим инъекцией паники, наблюдатель false, гейтинг трассировки; git-история содержит test(01-01) до feat(01-01) (D-07); mise run ci зелёный (D-08)</done>
  <acceptance_criteria>
  - test-коммит предшествует feat-коммиту; хотя бы один тест упал на утверждении до правки продукта (подлинный RED — суффиксный фильтр и/или пустой каталог), compile-error как RED не засчитывается
  - пины wire-порядка полей Component/EngineDesc зелёные (контракт провода пинирован отражением)
  - engine_test.go содержит тест, где инъектированная паника обработчика не распространяется за пределы метода
  - тест наблюдателя фиксирует false для Shift_R press/release и для букв
  - тест логгера фиксирует отсутствие записей трассировки клавиш при выключенном -debug и наличие при включённом
  - mise run ci проходит без data race и без находок линта (D-08/D-10)
  - в cmd/, internal/, engine/, layouts/ нет обращений к захвату evdev-устройств и путям устройства инжектора (структурный grep-гейт зелёный)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ibus-daemon → goswitchd (D-Bus приватная шина) | демон вызывает наши экспортированные методы с variant-аргументами — единственная точка входа внешних данных |
| goswitchd → stderr/journal | структурированный лог, читаемый владельцем и e2e-стендом |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Information disclosure | internal/logging + engine.ProcessKeyEvent | high | mitigate | трассировка клавиш только за -debug (default off); на INFO только длины буферов; предупреждение в README (план 01-05); юнит-тест гейтинга (Task 2) |
| T-01-02 | Denial of service | engine/engine.go handlers | high | mitigate | recover-шим на каждом экспортированном обработчике + comma-ok на каждом variant-утверждении (паника = смерть ввода всего стола); тест с инъекцией паники |
| T-01-03 | Spoofing | engine/conn.go RequestName | medium | mitigate | результат RequestName проверяется; не-PrimaryOwner → ERROR и выход (гвард единственного экземпляра против дубля движка) |
| T-01-04 | Tampering | регистрация компонента | low | accept | Фаза 1 использует программную регистрацию (runtime only, без XML и системных файлов) — нет файла поставки, который можно подменить; XML-инсталляция и права каталогов — Фаза 4 |
| T-01-05 | Tampering | установка mise (официальный инсталлер mise.run, curl-шелл) | medium | mitigate | mise отсутствует в системе (подтверждено планировщиком) — ставится официальным инсталлером в ~/.local/bin без root; jdx/mise — каноническая точка распространения, влияние ограничено PATH проекта; установленная версия фиксируется в SUMMARY; в CI тот же инсталлер исполняется в эфемерном раннере с contents:read (план 01-05) |
| T-01-SC | Tampering | go get godbus/dbus/v5 | high | mitigate | Package Legitimacy Audit в RESEARCH: godbus v5.2.2 верифицирован по proxy.golang.org (OK, project-locked); новых пакетов сверх таблицы Фаза 1 не вводит |
</threat_model>

<verification>
- Toolchain-гейт (D-09/D-10): mise.toml ([tools] go+golangci-lint, [tasks] build/test/lint/vet/tidy-diff/ci) и строгий .golangci.yml лежат в первом коммите; mise install выполнен; Makefile отсутствует
- Headless-гейт фазы (тот же, что CI): mise run ci — build + vet + test -race + golangci-lint — зелёный (D-08: гейт каждой итерации, не только фазы)
- Живой гейт (трассер): скрипт из Task 1 — регистрация, активация, нажатия в логе, чистый выход
- Структурный гейт: продуктовый код не содержит захвата устройств и путей инжектора (INTEG-02 архитектурно)
</verification>

<success_criteria>
- INTEG-01: программная RegisterComponent работает — `ibus list-engine` видит оба движка goswitch (без root, без XML)
- INTEG-02/INTEG-03 (архитектурная часть): наблюдатель false для всех событий; демон от пользователя сессии; доказательство живой совместимости с keyd — план 03
- INTEG-04 (код): цикл переподключения/перерегистрации с повторным Discover и бэкоффом — в первом коммите D-Bus-слоя; живая проверка — план 03
- INTEG-05: recover-шим на каждом обработчике, доказан юнит-тестом с инъекцией паники
- INST-04: slog JSON, уровни, трассировка клавиш только за -debug (предупреждение в README — план 01-05)
- D-07/D-08/D-09/D-10: строгий TDD-цикл на чистых единицах (test-коммит до feat-коммита), тройной зелёный гейт каждой итерации через mise run ci, mise-задачи вместо Makefile, строгий линт с первого кодового таска
</success_criteria>

## Artifacts this phase produces

Символы, создаваемые ЭТИМ планом (для исключения из drift-верификации reviewer'ом):

- Toolchain-каркас: `mise.toml` (единственный источник версий: [tools] go + golangci-lint; [tasks] build/test/lint/vet/tidy-diff/ci) и `.golangci.yml` (строгий v2-конфиг с эталона владельца; D-09/D-10)
- Модуль `github.com/Djarvur/goswitch` (go.mod, go 1.23); зависимость `github.com/godbus/dbus/v5 v5.2.2`
- Пакет `engine`: `Run(ctx context.Context, cfg Config) error`, `Config` (Component info, Engines, Handler), `Discover() (string, error)`, типы `Component`, `EngineDesc`, `IBusText`, `AttrList`, `EngineEvent`, `EventHandler` (HandleKey/HandleLifecycle), `LifecycleKind`; D-Bus-экспортируемые методы объекта движка: ProcessKeyEvent, SetCursorLocation, SetSurroundingText, SetCapabilities, FocusIn, FocusOut, Reset, Enable, Disable, PageUp, PageDown, CursorUp, CursorDown, CandidateClicked, PropertyActivate, PropertyShow, PropertyHide, Destroy, Get, GetAll, Set; эмиттер сигнала CommitText; константы keyval/масок в keys.go
- Пакет `internal/logging`: `Setup(w io.Writer, debug bool)`
- Бинарник `cmd/goswitchd` с флагом `-debug` (default false)
- Тестовые файлы: engine/engine_test.go, engine/address_test.go, engine/wire_test.go, internal/logging/logging_test.go

(CI-workflow, dev systemd-юнит, README-раздел о конфиденциальности — артефакты плана 01-05, выделенного из этого плана по checker round 1.)

<output>
Create .planning/phases/01-adr-paket-i-karkas-ibus-dvizhka/01-01-SUMMARY.md when done
</output>
