---
phase: 01-adr-paket-i-karkas-ibus-dvizhka
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - layouts/generator/main.go
  - layouts/tables.go
  - layouts/tables_test.go
  - internal/hotkey/fsm.go
  - internal/hotkey/fsm_test.go
autonomous: true
requirements: [CORR-08, TEST-01]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Golden-корпус зелёный: ghbdtn→привет, GHBDTN→ПРИВЕТ, Ghbdtn→Привет и все пары пунктуации обоих shift-уровней, включая асимметричные &↔?, @↔\", #↔№, $↔;, ^↔:, |↔/ (CORR-08)"
    - "Повторная генерация детерминирована: go generate ./layouts не меняет закоммиченного layouts/tables.go (git diff --exit-code)"
    - "CI не зависит от xkb: в CI гоняются только golden-тесты закоммиченного tables.go; генератор — dev-side инструмент"
    - "FSM чист: без горутин и реальных часов — Feed(ev Event, now time.Duration) []Action с инъекцией времени; корпус покрывает границы окна 299/300/301 мс, модификаторное использование, вмешавшуюся клавишу, 4-й тап, FocusOut внутри серии, глитч ibus#2600, серию из 10 тапов (TEST-01)"
    - "Все действия FSM срабатывают на истечении окна различения после последнего тапа (классическая схема D-04), окно по умолчанию 300 мс (D-05)"
  artifacts:
    - path: layouts/generator/main.go
      provides: "codegen CLI: парсинг symbols/us §basic + symbols/ru (default=winkeys + include ru(common)), склейка по имени клавиши, keysym→rune, эмиссия tables.go"
    - path: layouts/tables.go
      provides: "ГЕНЕРИРУЕМЫЙ, закоммиченный: map[rune]rune ENToRU и RUToEN, оба shift-уровня; заголовок с //go:generate"
      contains: "ENToRU"
    - path: layouts/tables_test.go
      provides: "golden-корпус SPEC-примеров + пунктуация"
    - path: internal/hotkey/fsm.go
      provides: "чистая FSM тапов Right Shift: idle→taps1..3, решение на истечении окна"
    - path: internal/hotkey/fsm_test.go
      provides: "детерминированный корпус на синтетических потоках событий с инъекцией времени"
  key_links:
    - from: layouts/tables_test.go
      to: layouts/tables.go
      via: "golden-тесты пинируют экспортируемые maps layouts.ENToRU / layouts.RUToEN"
      pattern: "ENToRU\\["
    - from: internal/hotkey/fsm_test.go
      to: internal/hotkey/fsm.go
      via: "Feed(KeyPress/KeyRelease/TimerExpired/Reset, now) → []Action{Single|Double|Triple}"
      pattern: "\\.Feed\\("
  prohibitions:
    - "Таблицы раскладок НЕ ДОЛЖНЫ быть рукописными или транслитерационными — только генерация из xkb-позиций клавиш: translit ≠ позиция клавиши; рукописная таблица — это ровно тот класс багов, который CORR-08 запрещает (winkeys-кейс из исследования)"
    - "FSM НЕ ДОЛЖЕН выполнять действие на первый тап (мгновенный свитч Caramba) и НЕ ДОЛЖЕН вводить отдельные клавиши для одиночного/двойного: владелец явно отклонил обе схемы (D-04) — решение всегда на истечении окна различения"
  assumptions:
    - "FLAGGED (research A6): встроенная в генератор таблица Cyrillic keysym→Unicode (~70 записей) считается верной — любой неверный символ ловится golden-корпусом в момент генерации, не в рантайме"
---

<objective>
Два чистых пакета фазы без единой строки D-Bus: (1) генерируемые `go:generate` таблицы ЙЦУКЕН↔QWERTY из системных xkb-файлов (us basic + ru default=winkeys) со знаками `[ ] ; ' , . /` на обоих shift-уровнях, зафиксированные golden-корпусом (CORR-08); (2) чистая FSM тапов Right Shift по классической схеме с ожиданием (D-04/D-05) с детерминированным корпусом на синтетических потоках (TEST-01). Оба пакета headless — зелёные в CI без живой сессии.

Зависимость depends_on: [01-01] — не по файлам (они не пересекаются), а по модулю: go.mod появляется в плане 01-01, без него `go test ./layouts/` не собирается; поэтому wave 2, а не wave 1 (checker round 1, dependency_correctness — info; параллельный запуск wave 1 сломал бы сборку).

Purpose: CORR-08 literally «таблицы генерируются go:generate» — это гейт против ручных таблиц; FSM — исполняемая спецификация семантики тапов для ADR-002 (план 04 цитирует корпус).
Output: layouts/ (генератор + сгенерированные таблицы + golden-тесты), internal/hotkey/ (FSM + корпус).
</objective>

<execution_context>
@/home/nil/DiskD/W/Djarvur/goswitch/.zcode/gsd-core/workflows/execute-plan.md
@/home/nil/DiskD/W/Djarvur/goswitch/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-adr-paket-i-karkas-ibus-dvizhka/01-CONTEXT.md (D-04, D-05 — семантика тапов)
@.planning/phases/01-adr-paket-i-karkas-ibus-dvizhka/01-PATTERNS.md (§ layouts-назначение, § fsm-назначения)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Таблицы раскладок — golden-корпус первым, затем генератор</name>
  <reversibility rating="reversible">Сгенерированные данные под pinned golden-тестами; смена варианта раскладки — параметризация генератора</reversibility>
  <precondition>Файлы /usr/share/X11/xkb/symbols/us, /usr/share/X11/xkb/symbols/ru и /usr/include/ibus-1.0/ibuskeysyms.h существуют и читаемы (проверено планировщиком на этой машине)</precondition>
  <files>layouts/tables_test.go, layouts/generator/main.go, layouts/tables.go</files>
  <read_first>
  - .planning/phases/01-adr-paket-i-karkas-ibus-dvizhka/01-RESEARCH.md (§ Pattern 4 — конвейер генерации, полная таблица эффективного маппинга как review-бейзлайн, golden-корпус; § Don't Hand-Roll строка про xkb)
  - /usr/share/X11/xkb/symbols/us (секция xkb_symbols "basic" со строки 4)
  - /usr/share/X11/xkb/symbols/ru (default-директива на строке 9 выбирает winkeys; winkeys включает ru(common), скрытый partial, переопределяет AE03-AE08, AB10, BKSL)
  - /usr/include/ibus-1.0/ibuskeysyms.h (keysym-имя → значение; 104 кириллических define)
  </read_first>
  <behavior>
  - TestGolden_SpecExamples: ENToRU 'g'→'п','h'→'и','b'→'б','d'→'д','t'→'е','n'→'т'; RUToEN обратно; регистры: 'G'→'П', 'Ghbdtn' посимвольно → 'Привет'
  - TestGolden_Punctuation: `[`↔`х`, `]`↔`ъ`, `;`↔`ж`, `'`↔`э`, `,`↔`б`, `.`↔`ю`, `/`↔`.`, `?`↔`,`, `` ` ``↔`ё`, `~`↔`Ё`, `@`↔`"`, `#`↔`№`, `$`↔`;`, `^`↔`:`, `&`↔`?`, `|`↔`/` — в обе стороны через ENToRU/RUToEN
  - TestGolden_TableSize: len(ENToRU) == len(RUToEN), обе ≥ 47 ключей (все позиции us basic)
  </behavior>
  <action>
  СНАЧАЛА layouts/tables_test.go: golden-тесты против API layouts.ENToRU / layouts.RUToEN (map[rune]rune, оба shift-уровня слиты в одну карту: строчные и заглавные). Тесты пишутся из таблицы-бейзлайна RESEARCH §Pattern 4, НЕ из будущего вывода генератора — асимметричные пары (& живёт на AE07-shift в us, а ? на AE07-shift в ru; склейка по позиции клавиши, не по символу) пинируются явно. Затем layouts/generator/main.go: парсить symbols/us секцию xkb_symbols "basic" (самодостаточная) и symbols/ru — default-директива резолвится в winkeys, рекурсивно раскрыть include "ru(common)" (hidden partial внутри symbols/ru), применить переопределения winkeys (AE03-AE08, AB10, BKSL), пропускать kpdl(comma) (keypad-only). Склеить us↔ru по имени xkb-клавиши (TLDE, AE01-AE12, AD01-AD12, AC01-AC11, AB01-AB10, BKSL). Keysym-имя → значение из ibuskeysyms.h; значение → руна: Latin-1 напрямую для 0x20-0xff; кириллица через встроенную в генератор таблицу keysym→Unicode (~70 записей, из defines заголовка). Эмиттировать layouts/tables.go с maps ENToRU/RUToEN и заголовочной строкой //go:generate go run ./generator (директива живёт в генерируемом файле первой строкой — go generate ./layouts находит её). Сгенерированный файл ЗАКОММИТИТЬ; CI гоняет только тесты (без xkb-зависимости). Детерминизм генератора проверяется ТОЛЬКО dev-side командой из verify (go generate ./layouts && git diff --exit-code — вторая automated-команда): отдельного Go-теста, запускающего генератор, в tables_test.go сознательно нет — генератор это package main (импорт в тест невозможен), а exec из теста дублировал бы verify-команду и нарушал бы CI-независимость от xkb-входов (checker round 1, task_completeness). Запустить go generate ./layouts, убедиться в зелёном корпусе и пустом git diff при повторной генерации. Удалить layouts/.gitkeep. Имена тестов Test*_ по go-ultimate.
  </action>
  <verify>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && go test ./layouts/ -run 'TestGolden' -count=1 -v</automated>
    <fails_when>строка FAIL в выводе; или в -v листинге отсутствуют TestGolden_SpecExamples / TestGolden_Punctuation</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && go generate ./layouts && git diff --exit-code -- layouts/tables.go && echo DETERMINISTIC</automated>
    <fails_when>git diff печатает расхождение (exit 1) — повторная генерация отличается от закоммиченного файла (генерация недетерминирована или файл правили руками)</fails_when>
  </verify>
  <done>Таблицы сгенерированы из xkb-позиций, закоммичены, golden-корпус зелёный, повторная генерация — пустой diff</done>
  <acceptance_criteria>
  - layouts/tables.go содержит map-литералы ENToRU и RUToEN и //go:generate директиву; файл помечен как GENERATED
  - golden-тесты покрывают все три регистра SPEC-примеров и все 16 пунктуационных пар из behavior в обе стороны
  - в CI-запуске (go test ./...) генератор не выполняется — только тесты закоммиченного файла
  - generator не импортирует godbus и не зависит от живой сессии
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FSM тапов Right Shift — классика с ожиданием (D-04/D-05)</name>
  <reversibility rating="costly">Семантика D-04/D-05 из CONTEXT: FSM, дефолты конфига и тайминговые e2e-кейсы следующих фаз завязаны на схему; переучивание — координированная правка (CONTEXT оценил D-04 как costly)</reversibility>
  <files>internal/hotkey/fsm.go, internal/hotkey/fsm_test.go</files>
  <read_first>
  - .planning/phases/01-adr-paket-i-karkas-ibus-dvizhka/01-RESEARCH.md (§ Pattern 3 — контракт, состояния, переходы, размеры корпуса; § Code Examples FSM feed)
  - .planning/phases/01-adr-paket-i-karkas-ibus-dvizhka/01-PATTERNS.md (§ fsm.go/fsm_test.go — дельты против прототипа владельца)
  - [ext] /home/nil/.local/share/punto-switcher/punto_engine.py (prior art: _handle_shift_release строки 219-237, rshift_saw_key 357-359; константы 73-74 — только prior art, НЕ образец: DOUBLE_TAP_MS=450/SINGLE_TAP_MS=550 не переносить)
  - internal/hotkey/fsm_test.go по мере написания — корпус из behavior
  </read_first>
  <behavior>
  - TestFSM_SingleAtWindowExpiry: тап (press+release Shift_R) → пауза за пределами окна → TimerExpired(now) возвращает [Single]; действие НЕ возвращается до истечения
  - TestFSM_DoubleAndTriple: 2 тапа с интервалом 299/300 мс → [Double]; 3 тапа → [Triple]
  - TestFSM_WindowEdges: интервал 299 мс — тап продолжает серию; 301 мс — новая серия (первый тап не продолжается)
  - TestFSM_ModifierUse: Shift_R зажат, между press и release — другая клавиша → release НЕ тап, серии нет, действие отсутствует (прототип rshift_saw_key)
  - TestFSM_InterveningKeyCancels: тап, другая клавиша, тап — серия отменена молча, на истечении окна действия нет
  - TestFSM_FourthTapStaysAtThree: 4-й тап в окне → на истечении [Triple] ровно один раз
  - TestFSM_FocusOutDisarms: FocusOut/Reset внутри серии → окно разряжено, действия нет
  - TestFSM_ReleaseWithoutPress: release без press — игнор
  - TestFSM_ShiftGlitch: press Shift_R, буква 1 мс, release (ibus#2600) — модификаторное использование, действия нет
  - TestFSM_BurstOfTen: 10 тапов внутри окна — на истечении [Triple], не цикл
  </behavior>
  <action>
  Контракт (RESEARCH §Pattern 3): чистый объект-функция, БЕЗ горутин и реальных часов. type Event interface{} с видами KeyPress{Keyval uint32}, KeyRelease{Keyval uint32}, TimerExpired, Reset; type Action int с константами Single=1, Double=2, Triple=3. Конструктор NewFSM(window time.Duration); метод Feed(ev Event, now time.Duration) []Action. Состояния idle→taps1→taps2→taps3 (счётчик тапов в текущей серии). Переходы: release Shift_R без вмешавшейся клавиши с момента press и с интервалом от последнего тапа ≤ window → инкремент (максимум 3) и перевод deadline на now+window — АДАПТЕР планирует time.AfterFunc(window) и ре-входит Feed(TimerExpired{}); любая незнаковая клавиша между release-ами отменяет серию молча; любая клавиша при зажатом Shift_R ставит флаг модификаторного использования (release игнорируется); TimerExpired при taps>0 возвращает ровно одно действие и сбрасывает серию; Reset/FocusOut (через Reset-событие от адаптера) разряжает всё. Сначала весь корпус из behavior (RED —_fsm.go ещё нет), затем минимальная реализация до зелёного, затем рефактор без изменения поведения. Окно по умолчанию 300 мс — константа DefaultWindow = 300 * time.Millisecond (D-05); в Фазе 1 не конфигурируется извне (YAML — Фаза 3), параметр window у конструктора уже есть. Тесты — инъекция времени, никаких wall-clock sleep (анти-флак).
  </action>
  <verify>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && go test ./internal/hotkey/ -race -count=1 -v</automated>
    <fails_when>строка FAIL в выводе; или в -v листинге меньше 10 тестовых функций TestFSM_*</fails_when>
  </verify>
  <done>Корпус из ≥10 детерминированных кейсов зелёный под -race; действия только на истечении окна; модификаторное использование и отмены пинированы</done>
  <acceptance_criteria>
  - fsm.go не содержит горутин, каналов, time.Now, time.Sleep (grep чист)
  - Feed возвращает действие ТОЛЬКО на TimerExpired (в behavior-кейсах Single/Double/Triple приходят из TimerExpired)
  - DefaultWindow == 300 * time.Millisecond
  - корпус покрывает все 10 измерений из RESEARCH §Pattern 3 (границы окна, модификатор, вмешательство, 4-й тап, FocusOut, release-без-press, глитч, серия из 10)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| системные xkb/keysym-файлы → генератор (dev-side) | входные данные только для чтения; в рантайме и CI не используются |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | layouts/generator (чтение системных файлов) | low | accept | генератор — dev-side инструмент; вывод закоммичен и пинирован golden-тестами, подмена системного xkb-файла не проходит мимо ревью diff'а сгенерированного файла |
| T-02-02 | Denial of service | internal/hotkey (будущий рантайм) | low | mitigate | FSM чистая и детерминированная — corpus с серией из 10 тапов и глитчем фиксирует отсутствие зацикливания/паники |
</threat_model>

<verification>
- go vet ./... && go build ./... && go test -race ./... зелёный (обе новые пакеты подхватываются CI — workflow плана 01-05)
- Повторная go generate не даёт diff (детерминизм — единственный и достаточный механизм проверки, dev-side)
</verification>

<success_criteria>
- CORR-08 закрыт: таблицы сгенерированы go:generate из xkb symbols, включают знаки `[ ] ; ' , . /` и пунктуацию обоих уровней, golden-корпус зелёный
- TEST-01 (часть): headless-корпус FSM + golden таблиц зелёный под -race без D-Bus и без живой сессии
- Семантика D-04/D-05 исполняемо зафиксирована — ADR-002 (план 04) цитирует корпус как исполняемую спецификацию
</success_criteria>

## Artifacts this phase produces

- Пакет `layouts`: экспортируемые `ENToRU map[rune]rune`, `RUToEN map[rune]rune` (сгенерированные, закоммиченные); `layouts/generator` (main-пакет dev-side CLI); директива //go:generate в tables.go; golden-тесты TestGolden_*
- Пакет `internal/hotkey`: `FSM`, `NewFSM(window time.Duration) *FSM`, `Feed(ev Event, now time.Duration) []Action`, типы событий `KeyPress{Keyval}`, `KeyRelease{Keyval}`, `TimerExpired`, `Reset`, тип `Action` с константами `Single`/`Double`/`Triple`, `DefaultWindow = 300 * time.Millisecond`
- Удалённые плейсхолдеры: layouts/.gitkeep

<output>
Create .planning/phases/01-adr-paket-i-karkas-ibus-dvizhka/01-02-SUMMARY.md when done
</output>
