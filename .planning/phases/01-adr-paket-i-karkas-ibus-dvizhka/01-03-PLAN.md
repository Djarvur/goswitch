---
phase: 01-adr-paket-i-karkas-ibus-dvizhka
plan: 03
type: execute
wave: 3
depends_on: [01-01, 01-02]
files_modified:
  - internal/session/actor.go
  - internal/session/actor_test.go
  - cmd/goswitchd/main.go
  - test/e2e/main.go
  - test/e2e/preflight.go
  - test/e2e/focus_helper.py
  - test/e2e/case_m1.go
  - test/e2e/case_resilience.go
  - test/e2e/README.md
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03, INTEG-02, INTEG-03, INTEG-04, INTEG-05]

estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Сессионный актор сериализует доступ к FSM (один поток событий) и логирует решение FSM записью {\"msg\":\"action\",\"n\":1|2|3} на истечении окна — юнит-тест с фейковыми часами/таймером зелёный"
    - "e2e-раннер — Go-програмта (не go test) с контрактом exit-кода: PASS по кейсам печатается, любой FAIL → код выхода ≠ 0"
    - "Preflight fail-fast: каждая проверка (файл адреса IBus, goswitch в ibus list-engine, доступ на запись к устройству инжектора ядра, импорт gi в /usr/bin/python3, рост лога демона) при провале печатает именованную диагностику и exit 1"
    - "Активация окна перед КАЖДЫМ кейсом через AT-SPI Component.grabFocus хелпером на /usr/bin/python3 (абсолютный путь обязателен — python3 из PATH смотрит на linuxbrew без gi) (TEST-03)"
    - "Хелпер фокуса и инжекция используют только инструменты стенда; продуктовый код демона остаётся чистым от захвата устройств (гейт плана 01 продолжает действовать)"
    - statement: "Гейт M1 зелёный живым прогоном: goswitch активный источник ввода в gsettings (snapshot/restore обоих ключей sources и current), ydotool печатает ghbdtn и тапает Right Shift — в JSON-логе видны события клавиш, решения FSM (single/double/triple) и последовательность lifecycle component registered → focus_in"
      verification: backstop
    - statement: "Живучесть: (a) ibus restart → wait-for-condition по строке re-registered в логе → инжекция снова видна в логе; (b) kill -9 демона → обычный набор через простой источник жив (рабочий стол не потерял ввод) → демон перезапущен раннером и перерегистрировался"
      verification: backstop
  artifacts:
    - path: internal/session/actor.go
      provides: "Actor, реализующий engine.EventHandler: декод → FSM.Feed → time.AfterFunc(window) → лог решения"
    - path: test/e2e/main.go
      provides: "CLI-раннер: флаги -case (m1-gate|ibus-restart|kill9-survive), -pacing ms, -log path; запуск goswitchd как subprocess с -debug и stderr → temp-файл"
    - path: test/e2e/preflight.go
      provides: "fail-fast проверки среды с именованными диагностиками"
    - path: test/e2e/focus_helper.py
      provides: "~30 строк python3-gi: AT-SPI Component.grabFocus окна приложения по имени; shebang /usr/bin/python3"
    - path: test/e2e/case_resilience.go
      provides: "кейсы ibus-restart и kill9-survive с wait-for-condition"
    - path: test/e2e/README.md
      provides: "документация стенда: политика snapshot/restore, keyd-проверка вручную (INTEG-02), инструкция прогона"
  key_links:
    - from: cmd/goswitchd/main.go
      to: internal/session/actor.go
      via: "engine.Config.Handler = session.NewActor(hotkey.NewFSM(hotkey.DefaultWindow)) — проводка актора в seam плана 01"
      pattern: "session\\.NewActor"
    - from: test/e2e/main.go
      to: cmd/goswitchd
      via: "запуск демона subprocess с -debug, stderr → temp-файл, ожидание component registered"
      pattern: "exec\\.Command"
    - from: test/e2e/main.go
      to: test/e2e/focus_helper.py
      via: "exec /usr/bin/python3 focus_helper.py <app> перед каждым кейсом"
      pattern: "/usr/bin/python3"
  prohibitions:
    - "e2e-стенд НЕ ДОЛЖЕН оставлять изменённое состояние живого рабочего стола после прогона: gsettings sources И current обязаны восстанавливаться в teardown даже при падении кейса (defer/восстановление до exit) — иначе владелец теряет свою раскладку (safety)"
    - "Документация стенда НЕ ДОЛЖНА заявлять проверенную совместимость с keyd, пока живая keyd-сессия не проверена вручную: keyd.service на машине failed/disabled, автопроверка невозможна (transparency; INTEG-02 закрывается архитектурно + ручной чек по инструкции)"
  assumptions:
    - "FLAGGED (research A4): ydotool 0.1.8 инжектирует без ydotoold надёжно (бинарник и права проверены, инжекция не гонялась) — первый preflight-кейс стенда это self-test инжекции; fallback: ~150-LOC Go uinput-инжектор (STACK)"
    - "FLAGGED (research A5): восстановления sources+current в teardown достаточно для очистки живого стола — teardown проверяет оба ключа (snapshot обоих обязателен)"
    - "FLAGGED (research A2): семантика keycode на живом IM-пути фиксируется собственным key-trace логом скелета — наблюдение записать в 01-03-SUMMARY для планирования Фазы 2"
---

<objective>
Скелет e2e-стенда (гейт M1) и проводка FSM в демона. Go-раннер (не go test): preflight fail-fast с человеческими диагностиками, самостоятельная активация целевого окна через AT-SPI grabFocus (TEST-03), физическая инжекция ydotool (TEST-02), ассерты по структурированному логу goswitchd через wait-for-condition, snapshot/restore состояния gsettings, кейсы живучести ibus-restart и kill9-survive (INTEG-04/INTEG-05 живьём), отчёт PASS/FAIL с кодом выхода ≠ 0 при падении.

Purpose: гейт M1 «событие видно в логе» — критерий успеха 2 фазы; стенд становится фундаментом матриц Фаз 2-3 и эксперимента D-01 (план 04).
Output: test/e2e/ раннер + хелпер фокуса + 3 кейса, internal/session/ актор, задокументированная INTEG-02.
</objective>

<execution_context>
@/home/nil/DiskD/W/Djarvur/goswitch/.zcode/gsd-core/workflows/execute-plan.md
@/home/nil/DiskD/W/Djarvur/goswitch/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-adr-paket-i-karkas-ibus-dvizhka/01-PATTERNS.md (§ test/e2e назначение, § Shared Patterns)
@.planning/phases/01-adr-paket-i-karkas-ibus-dvizhka/01-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Сессионный актор — проводка FSM в демон</name>
  <files>internal/session/actor.go, internal/session/actor_test.go, cmd/goswitchd/main.go</files>
  <read_first>
  - engine/engine.go (seam EventHandler/EngineEvent/LifecycleKind из плана 01)
  - internal/hotkey/fsm.go (контракт Feed/TimerExpired из плана 02)
  - cmd/goswitchd/main.go (текущая проводка run)
  - .planning/phases/01-adr-paket-i-karkas-ibus-dvizhka/01-RESEARCH.md (§ Pattern 3 — роль адаптера в планировании таймера: time.AfterFunc(window) ре-входит Feed(TimerExpired{}); § Anti-Patterns «Replying late»)
  </read_first>
  <behavior>
  - TestActor_KeyEventsFeedFSM: HandleKey(press/release Shift_R) → на истечении окна логируется {"msg":"action","n":1}
  - TestActor_DoubleTriple: серии из 2/3 тапов → n=2/n=3 ровно один раз
  - TestActor_FocusOutDisarms: HandleLifecycle(FocusOut) внутри серии — действие не логируется, таймер разряжен
  - TestActor_Serialization: параллельные HandleKey из нескольких горутин не дают data race (-race) и не теряют серию (мьютекс/единый вход)
  </behavior>
  <action>
  internal/session/actor.go: NewActor(window time.Duration) *Actor, возвращает объект, реализующий engine.EventHandler (HandleKey, HandleLifecycle). Внутри: мьютекс (единая точка входа — godbus диспетчеризует обработчики на разных горутинах), *hotkey.FSM, текущий дедлайн-таймер. HandleKey: под мьютексом Feed(KeyPress/KeyRelease, now) — now из time.Since(старт) или time.Now().UnixNano(); если FSM перевёл серию (Feed вернул пустой результат, но серия активна), (пере)взвести time.AfterFunc(window, expiry); expiry: под мьютексом Feed(TimerExpired{}, now) → для каждого Action залогировать slog.Info("action", "n", int(a)). HandleLifecycle: FocusOut/Reset → сброс FSM и stop таймера; FocusIn — nothing/лог на DEBUG. Действие одиночного тапа приходит на истечении окна — NEVER в HandleKey (анти-паттерн позднего ответа не касается: ProcessKeyEvent уже вернулся false). cmd/goswitchd/main.go: в run() создать actor := session.NewActor(hotkey.DefaultWindow) и передать в engine.Config.Handler — ровно одна строка проводки, вся логика в акторе. Тесты — фейковые таймеры недоступны извне (AfterFunc реальный): в тестах окно ставить 5-10 мс и ждать wait-for-condition на буфере лога, либо инжектить истечение напрямую экспортированным для теста методом expiry() — выбрать второе: expiry() как экспортируемый метод, вызываемый и AfterFunc, и тестом (детерминизм).
  </action>
  <verify>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && go vet ./... && go build ./... && go test ./internal/session/ -race -count=1 -v</automated>
    <fails_when>non-zero exit vet/build; или FAIL в тестах; или в -v листинге нет функций TestActor_*</fails_when>
  </verify>
  <done>Актор реализует seam плана 01, сериализует FSM, логирует решения action n=1|2|3 на истечении окна, FocusOut разряжает серию; демон проводит актор одной строкой</done>
  <acceptance_criteria>
  - internal/session/actor.go содержит мьютексную сериализацию и time.AfterFunc(window) → Feed(TimerExpired)
  - лог-форма решения: {"msg":"action","n":N} — строка grep-аема стендом
  - cmd/goswitchd/main.go ссылается на session.NewActor (проводка handler)
  - TestActor_Serialization проходит под -race
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: e2e-раннер — preflight, фокус, инжекция, кейс m1-gate</name>
  <precondition>Живая сессия GNOME Wayland активна: echo $XDG_SESSION_TYPE печатает wayland; ibus address возвращает адрес; демон плана 01 собирается</precondition>
  <files>test/e2e/main.go, test/e2e/preflight.go, test/e2e/focus_helper.py, test/e2e/case_m1.go</files>
  <read_first>
  - .planning/phases/01-adr-paket-i-karkas-ibus-dvizhka/01-RESEARCH.md (§ Pattern 6 — семь шагов скелета, готовый shell-скетч; § Pitfalls 2/3/6; § Runtime State Inventory — gsettings snapshot/restore)
  - test/e2e/focus_helper.py по мере написания
  - .planning/phases/01-adr-paket-i-karkas-ibus-dvizhka/01-PATTERNS.md (§ test/e2e — абсолютный путь /usr/bin/python3 обязателен)
  </read_first>
  <action>
  test/e2e/main.go: CLI (не go test — живая сессия, собственный контракт выхода): флаги -case (m1-gate|ibus-restart|kill9-survive — реестр кейсов), -pacing (мс между нажатиями ydotool, дефолт 40, повышать на нагруженной машине), -log (путь к логу демона). Раннер: собирает и запускает goswitchd как subprocess c -debug и stderr → temp-файл (без systemd в скелете), ждёт wait-for-condition строку component registered; выполняет кейс; teardown ВСЕГДА: останов демона, восстановление gsettings. Помощник waitForLog(подстрока, таймаут): поллинг файла лога с малым интервалом — НИКОГДА не фиксированный sleep. Помощники snapshotGsettings/restoreGsettings: gsettings get org.gnome.desktop.input-sources sources И current (оба ключа), restore обоих через defer.
  test/e2e/preflight.go: fail-fast проверки, каждая с именованной диагностикой и exit 1: (1) ibus address непуст; (2) ibus list-engine содержит goswitch-en (после старта демона); (3) файл устройства инжектора ядра доступен на запись (иначе подсказка про группу input); (4) /usr/bin/python3 -c "import gi" успешен (PATH-python3 — linuxbrew без gi, использовать только абсолютный путь); (5) лог демона растёт (heartbeat). Первым кейсом preflight — self-test инжекции (см. flagged A4).
  test/e2e/focus_helper.py: ~30 строк, shebang /usr/bin/python3, gir1.2-atspi: найти окно приложения по имени (gnome-text-editor, /usr/bin/gnome-text-editor есть на машине), вызвать Component.grabFocus; exit 0 при успехе, именованная ошибка иначе. wmctrl НЕ использовать (не установлен, X11-only).
  test/e2e/case_m1.go (m1-gate): snapshot gsettings; добавить в sources ('ibus','goswitch-en') и активировать его как текущий источник (запись sources + current — способ активации gsettings напрямую, НЕ переключение раскладки: мы назначаем активный источник ввода в настройках сессии, это setup стенда, а не свитчинг D-01); запустить gnome-text-editor; focus_helper → grabFocus; ydotool type --key-delay $pacing ghbdtn; ассерты waitForLog: не менее 6 событий "msg":"key"; далее ydotool key --key-delay (окно-скорость: pacing × 3, чтобы тапы попали в 300 мс окно) Shift_R ×2 → waitForLog {"msg":"action","n":2}; lifecycle: строка component registered встречается раньше focus_in (порядок проверять по временным меткам JSON). restoreGsettings в defer. Отчёт: PASS/FAIL по кейсу + при провале выдержка лога.
  Удалить test/e2e/.gitkeep.
  </action>
  <verify>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && go vet ./... && go build ./... && go run ./test/e2e -case m1-gate</automated>
    <fails_when>строка FAIL в отчёте или код выхода ≠ 0; или preflight печатает именованную диагностику и выходит 1</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && head -1 test/e2e/focus_helper.py | grep -q '/usr/bin/python3' && grep -c 'waitForLog\|wait_for' test/e2e/case_m1.go test/e2e/main.go</automated>
    <fails_when>shebang focus_helper.py не /usr/bin/python3; или в кейсе/раннере нет ни одного wait-for-condition вызова (счётчик 0 — стенд построен на sleep)</fails_when>
  </verify>
  <done>m1-gate зелёный живьём: ключи и решения FSM видны в логе при активном goswitch-источнике, окно активируется само, состояние стола восстановлено, код выхода отражает результат</done>
  <acceptance_criteria>
  - раннер запускает демон subprocess с -debug, stderr → temp-файл, и убивает его в teardown
  - snapshot/restore покрывает ОБА ключа gsettings (sources и current), restore в defer выполняется и при FAIL
  - фокус перед каждым кейсом — grabFocus через /usr/bin/python3
  - инжекция только ydotool (--key-delay настраивается флагом -pacing)
  - ассерты — waitForLog по подстрокам "msg":"key", "msg":"action","n":2, порядок lifecycle
  - exit ≠ 0 при любом FAIL кейса
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Кейсы живучести + документация INTEG-02/03 стенда</name>
  <precondition>Кейс m1-gate зелёный (Task 2); ibus CLI доступен (ibus restart поддерживается версией 1.5.29)</precondition>
  <files>test/e2e/case_resilience.go, test/e2e/README.md</files>
  <read_first>
  - test/e2e/main.go, test/e2e/case_m1.go (хелперы раннера из Task 2)
  - .planning/phases/01-adr-paket-i-karkas-ibus-dvizhka/01-RESEARCH.md (§ Pattern 5 — цикл перерегистрации, ibus#2910; § Pattern 6 шаг 6 — оба кейса живучести; § Runtime State Inventory — keyd failed/disabled)
  - docs/SPEC.md §4.4 (требование совместимости с keyd/xremap)
  </read_first>
  <action>
  test/e2e/case_resilience.go — два кейса:
  (a) ibus-restart (INTEG-04): демон работает, активный источник goswitch-en; выполнить ibus restart; waitForLog строки re-registered (раннер ждёт возврата демона к регистрации — цикл плана 01 должен сам переподключиться к НОВОМУ сокету); затем инжекция и waitForLog новых событий "msg":"key" — события текут после рестарта демона шины.
  (b) kill9-survive (INTEG-05): демон работает; kill -9 PID демона (неликвидируемый — именно этот кейс); немедленно переключить активный источник на простой xkb (gsettings sources/current, snapshot уже есть), ydotool type произвольного текста и убедиться, что ввод рабочего стола жив: ассерт не по логу goswitchd, а по тому, что фокус-окно получило символы (AT-SPI читает содержимое поля — python-хелпер расширяется функцией чтения текста, это bridge к Фазе 2); затем раннер перезапускает демон (subprocess respawn — systemd-юнит в скелете не обязателен) и waitForLog component registered.
  Отчёт по кейсам — общий контракт PASS/FAIL + exit ≠ 0.
  test/e2e/README.md: (1) инструкция прогона: go run ./test/e2e -case m1-gate и перечень кейсов, флаг -pacing; (2) политика состояния: стенд мутирует gsettings sources/current живого стола, snapshot/restore обязателен, teardown при Ctrl-C тоже восстанавливает; (3) INTEG-02: архитектурное доказательство совместимости с keyd/xremap (демон не трогает evdev-слой: захват устройств отсутствует, обычный набор идёт транзитом через false — проверено кейсами) + документированная РУЧНАЯ проверка для живой keyd-сессии: включить keyd.service (требует root — вне автомата), убедиться, что набор при активном goswitch идёт без потерь и ремаппинг keyd применяется; сформулировать шаги вручную; (4) INTEG-03: стенд и демон работают от пользователя сессии без root — перечень прав (членство в группе input для стенда, доступ к IBus-сокету).
  </action>
  <verify>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && go vet ./... && go run ./test/e2e -case ibus-restart && go run ./test/e2e -case kill9-survive</automated>
    <fails_when>строка FAIL в любом отчёте или код выхода ≠ 0 у любого кейса (в т.ч. re-registered не появилась после ibus restart — цикл перерегистрации не работает)</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && grep -c 'keyd' test/e2e/README.md && grep -c 'restore\|восстановл' test/e2e/README.md</automated>
    <fails_when>grep не находит упоминание keyd (0) или политику восстановления состояния (0) в README стенда</fails_when>
  </verify>
  <done>Оба кейса живучести зелёные живьём: рестарт ibus-daemon переживается перерегистрацией на новый сокет, kill -9 не роняет ввод рабочего стола; совместимость с keyd документирована как архитектурное доказательство + ручной чек</done>
  <acceptance_criteria>
  - кейс ibus-restart ассертит строку re-registered И последующие события клавиш
  - кейс kill9-survive ассертит живой ввод рабочего стола через простой xkb-источник (не по логу демона) и respawn демона раннером
  - README стенда содержит: инструкцию прогона, политику snapshot/restore, раздел keyd с ручными шагами, раздел прав без root
  - все три кейса дают единый отчёт PASS/FAIL с exit ≠ 0 при FAIL
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| e2e-раннер → живой рабочий стол владельца | стенд инжектирует нажатия и мутирует gsettings — обратимое воздействие на живую сессию |
| ydotool → /dev/uinput (kernel) | физический путь инжекции — инструмент стенда, недоступен продуктовому коду |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering (состояние сессии) | gsettings sources/current | high | mitigate | snapshot ОБОИХ ключей до, restore в defer- teardown при любом исходе; prohibitio must_haves; сбой restore = владелец теряет раскладку |
| T-03-02 | Denial of service (стол) | kill9-survive кейс | medium | mitigate | кейс сначала переключает источник на простой xkb и доказывает живой ввод; инжекция ограничена короткими строками |
| T-03-03 | Information disclosure | лог goswitchd -debug в temp-файле | medium | mitigate | temp-файл в $TMPDIR, удаление в teardown; в CI стенд не выполняется (только живая сессия) |
| T-03-04 | Tampering | /dev/uinput из стенда | low | accept | инжекция — назначение стенда (симуляция человека); права 0660 группа input — штатный механизм; продуктовый код отделён структурным гейтом плана 01 |
</threat_model>

<verification>
- Headless: go vet ./... && go build ./... && go test -race ./... (актор включён)
- Живой прогон: все три кейса m1-gate, ibus-restart, kill9-survive — PASS, exit 0; повторный прогон после сбоя тоже проходит (стенд самовосстанавливает состояние)
- Гейт M1 фазы: «событие видно в логе» при активном goswitch-источнике — зелёный
</verification>

<success_criteria>
- TEST-02: физическая инжекция (ydotool/uinput) → ассерты по логу (чтение поля через AT-SPI — мост Фазы 2, расширенный хелпер уже читает текст в kill9-кейсе)
- TEST-03: стенд сам активирует окно перед каждым кейсом (grabFocus через /usr/bin/python3)
- INTEG-04 живьём: ibus restart → re-registered → события текут
- INTEG-05 живьём: kill -9 → ввод стола жив → respawn → перерегистрация
- INTEG-02/03: транзит без захвата подтверждён кейсами; keyd — архитектурно + документированный ручной чек; всё без root
</success_criteria>

## Artifacts this phase produces

- Пакет `internal/session`: `NewActor(window time.Duration) *Actor`, `(*Actor).HandleKey`, `(*Actor).HandleLifecycle`, `(*Actor).Expiry` (вызов истечения окна — используется AfterFunc и тестами); реализует `engine.EventHandler`
- Бинарник `test/e2e` (CLI-раннер): флаги `-case` (m1-gate, ibus-restart, kill9-survive), `-pacing`, `-log`; контракт exit ≠ 0 при FAIL
- `test/e2e/focus_helper.py`: grabFocus по имени окна; функция чтения текста поля (AT-SPI) — мост к TEST-04 Фазы 2
- Хелперы раннера: waitForLog, snapshot/restore gsettings, preflight, respawn демона
- Документация test/e2e/README.md (прогон, состояние, keyd-ручная проверка, права)
- Удалённый плейсхолдер: test/e2e/.gitkeep

<output>
Create .planning/phases/01-adr-paket-i-karkas-ibus-dvizhka/01-03-SUMMARY.md when done
</output>
