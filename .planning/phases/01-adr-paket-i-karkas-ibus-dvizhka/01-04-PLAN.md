---
phase: 01-adr-paket-i-karkas-ibus-dvizhka
plan: 04
type: execute
wave: 4
depends_on: [01-03]
files_modified:
  - test/e2e/case_d01.go
  - test/e2e/main.go
  - docs/adr/d01-experiment-log.md
  - docs/adr/ADR-001-layout-switching-mechanism.md
  - docs/adr/ADR-002-tap-semantics.md
  - docs/adr/ADR-003-replacement-capability-ladder.md
  - docs/adr/ADR-004-buffer-reset-triggers.md
  - docs/adr/ADR-005-macr-01-owner-checkpoint.md
  - docs/SPEC.md
autonomous: false
requirements: [TEST-02, INTEG-04]

estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Каждая проба эксперимента D-01 записана в docs/adr/d01-experiment-log.md с полями: проба, команда, наблюдаемое, вердикт (switched / not-switched / unavailable) — не менее 4 строк Verdict"
    - "Доказательство переключения — только НАБЛЮДАЕМОЕ: FocusIn/FocusOut наших движков в логе и наблюдаемый текст от последующего ввода; значение dconf само по себе доказательством НЕ является (keyboard.js 46 не синхронизирует внешние изменения)"
    - "ADR-001 написан ПОСЛЕДНИМ из журнала эксперимента: содержит победивший вариант (или Option B с kill-критериями), проигравших кандидатов с наблюдаемыми провалами, и последствия D-03 «один хозяин» если победил внутренний флип"
    - "ADR-005 имеет статус Proposed (решение владельца): рекомендация исполнителя явно помечена как вход, а не решение; код MACR-01 не планируется и не пишется (OPEN-01)"
    - "Каждый ADR содержит разделы Status / Context / Decision / Consequences / Reversibility, формулировки решений D-01..D-06 процитированы из CONTEXT"
    - statement: "Эксперимент D-01 завершён time-boxed прогоном: все пробы прогнаны (или зафиксированы unavailable с причиной), решение выводимо — two-engine при хотя бы одной рабочей пробе, иначе Option B (внутренний флип, подтверждён прототипом владельца)"
      verification: backstop
    - statement: "Владелец просмотрел ADR-пак (гейт M0), решение по MACR-01 зафиксировано в ADR-005, spec-deltas применены к docs/SPEC.md §5 и §4.3, статусы ADR обновлены"
      verification: backstop
  artifacts:
    - path: test/e2e/case_d01.go
      provides: "кейс d01-probe: матрица проб переключения источника с наблюдаемыми ассертами и журналированием"
    - path: docs/adr/d01-experiment-log.md
      provides: "журнал доказательств: по строке на пробу (команда, наблюдаемое, вердикт)"
    - path: docs/adr/ADR-001-layout-switching-mechanism.md
      provides: "механизм переключения раскладки: two-engine vs внутренний флип — из журнала эксперимента"
    - path: docs/adr/ADR-002-tap-semantics.md
      provides: "классика с ожиданием, 300 мс, решение на истечении окна, модификаторная дискриминация, правило 4-го тапа (D-04/D-05)"
    - path: docs/adr/ADR-003-replacement-capability-ladder.md
      provides: "лестница замены: DeleteSurroundingText → Backspace×N (руны, abort-не-мусорить) → clipboard; caps по битам SetCapabilities"
    - path: docs/adr/ADR-004-buffer-reset-triggers.md
      provides: "D-06 дословно: без клика мыши; обязательная сверка surrounding text; best-effort сброс по скачку курсора; spec-delta §4.3"
    - path: docs/adr/ADR-005-macr-01-owner-checkpoint.md
      provides: "OPEN-01: чекпоинт владельца; рекомендация (v2 + keyd-рецепт) — вход, не решение"
  key_links:
    - from: test/e2e/case_d01.go
      to: docs/adr/d01-experiment-log.md
      via: "проба дописывает строку Verdict в журнал — журнал читает ADR-001"
      pattern: "Verdict"
    - from: docs/adr/ADR-001-layout-switching-mechanism.md
      to: docs/adr/d01-experiment-log.md
      via: "ADR-001 ссылается на журнал как на доказательную базу"
      pattern: "d01-experiment-log"
    - from: docs/adr/ADR-004-buffer-reset-triggers.md
      to: docs/SPEC.md
      via: "spec-delta §4.3: текст дельты в ADR, применение к SPEC — после утверждения владельцем"
      pattern: "4\\.3"
  prohibitions:
    - "Механизм переключения НЕ ДОЛЖЕН требовать root, правки системных файлов или ослабления безопасности Shell: unsafe mode НЕ включать — если пробе Shell.Eval он нужен, вердикт пробы «unavailable: требует unsafe mode», не включать его (D-02, safety)"
    - "MACR-01 НЕ ДОЛЖЕН реализовываться или планироваться кодом до решения владельца (OPEN-01); ADR-005 НЕ ДОЛЖЕН подавать рекомендацию исполнителя как решение — только как вход к чекпоинту (transparency)"
    - "ADR-001 НЕ ДОЛЖЕН объявлять two-engine жизнеспособным без наблюдаемого доказательства переключения (FocusIn/FocusOut наших движков плюс наблюдаемый текст) — значение dconf доказательством не является (Pitfall 3)"
  assumptions:
    - "FLAGGED (research A3): GNOME Shell ре-активирует источник goswitch после рестарта ibus-daemon (источники персистентны в gsettings) — кейс ibus-restart плана 03 наблюдает; если НЕ ре-активирует: fallback (poke активации после перерегистрации) записать в ADR-001 как требование к механизму (INTEG-04)"
    - "FLAGGED (context OPEN-01): решение MACR-01 не принимается без владельца — план содержит только авторизацию ADR и чекпоинт, никакого кода"
---

<objective>
Закрыть все архитектурные развилки фазы. (1) Эксперимент D-01 на готовом e2e-стенде: «может ли демон приказать GNOME переключить активный источник ввода?» — матрица проб (gsettings current; синтетические Super+Space/Alt+Shift_L через ydotool; SetGlobalEngine-проба; org.gnome.Shell.Eval без включения unsafe mode; при отрицательных пробах — time-boxed микро-расширение Shell), каждая с наблюдаемым вердиктом в журнал. (2) ADR-пак из пяти решений: ADR-001 пишется ПОСЛЕДНИМ из журнала эксперимента; ADR-002 фиксирует семантику тапов (D-04/D-05); ADR-003 — лестницу замены; ADR-004 — триггеры сброса буфера (D-06, spec-delta §4.3); ADR-005 — чекпоинт владельца по MACR-01/OPEN-01 без кода. (3) Блокирующий чекпоинт владельца (гейт M0): утверждение ADR-пака, решение MACR-01, применение spec-delta к SPEC.

Purpose: критерий успеха 1 фазы — «ADR-пак утверждён владельцем»; STATE-блокер «Decision #1» закрывается kill-criterion спайком до любого кода коррекции.
Output: test/e2e кейс d01-probe, журнал эксперимента, docs/adr/ADR-001..005, обновлённый docs/SPEC.md.
</objective>

<execution_context>
@/home/nil/DiskD/W/Djarvur/goswitch/.zcode/gsd-core/workflows/execute-plan.md
@/home/nil/DiskD/W/Djarvur/goswitch/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-adr-paket-i-karkas-ibus-dvizhka/01-CONTEXT.md (D-01..D-06 дословно, OPEN-01)
@.planning/phases/01-adr-paket-i-karkas-ibus-dvizhka/01-RESEARCH.md (§ Summary — протокол D-01 и исходные данные keyboard.js/ibusManager; § Pattern 8 — состав ADR; § Open Questions)
@docs/SPEC.md (§4.3, §5, §6, §9 — объекты spec-delta)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Эксперимент D-01 — матрица проб переключения источника</name>
  <reversibility rating="reversible">Выбор происходит в начале фазы, до зависимого кода; пробы обратимы (состояние сессии восстанавливается) (CONTEXT D-01 — reversible)</reversibility>
  <precondition>Кейсы плана 03 зелёные (стенд работает: m1-gate, ibus-restart); живая сессия GNOME Wayland активна</precondition>
  <files>test/e2e/case_d01.go, test/e2e/main.go, docs/adr/d01-experiment-log.md</files>
  <read_first>
  - test/e2e/main.go, test/e2e/case_m1.go, test/e2e/case_resilience.go (хелперы раннера: snapshot/restore, waitForLog, фокус, инжекция; реестр кейсов в main.go — дополнить d01-probe)
  - .planning/phases/01-adr-paket-i-karkas-ibus-dvizhka/01-RESEARCH.md (§ Summary — порядок проб и почему; § Pattern 6 шаг 2; § Pitfall 3 — dconf не доказательство; § Open Questions Q1)
  - .planning/phases/01-adr-paket-i-karkas-ibus-dvizhka/01-CONTEXT.md (D-01, D-02, D-03 — критерии допустимости хаков)
  </read_first>
  <action>
  Создать docs/adr/d01-experiment-log.md c заголовком и таблицей: Проба | Команда | Наблюдаемое | Вердикт (switched / not-switched / unavailable). Кейс d01-probe (test/e2e/case_d01.go) использует инфраструктуру стенда (snapshot gsettings sources+current с restore в defer, демон как subprocess, focus_helper, waitForLog) и прогоняет пробы ПО ПОРЯДКУ, каждая — с критерием успеха по НАБЛЮДАЕМОМУ, не по dconf: (1) gsettings-проба: sources содержит оба наших движка; установить current на индекс goswitch-ru; критерий — в логе демона FocusOut(goswitch-en) и FocusIn(goswitch-ru), плюс последующий ввод (ydotool в фокус-окне) выглядит кириллицей (чтение поля AT-SPI-хелпером). (2) ydotool-проба: синтетические комбинации Super+Space и Alt+Shift_L (проверенные живые биндинги GNOME — см. исследование); тот же критерий наблюдения. (3) SetGlobalEngine-проба: ibus engine goswitch-ru; исследованием установлено, что сам по себе он НЕ удовлетворяет two-engine (keyboard.js 46 не применяет XKB на внешний SetGlobalEngine) — проба фиксирует, ЧТО именно наблюдается (смена движка без смены XKB-раскладки), это данные для ADR-001. (4) Shell.Eval-проба: вызов org.gnome.Shell.Eval с activate()-скриптом; без unsafe mode ожидаем отказ — вердикт unavailable с формулировкой причины; unsafe mode НЕ включать (см. prohibitions). (5) Условная time-boxed проба микро-расширения: выполняется ТОЛЬКО если пробы 1-4 не переключили; минимальное расширение (~30 строк JS + metadata.json) в ~/.local/share/gnome-shell/extensions/goswitch-probe@localhost/ (пользовательский каталог, удаление после пробы), включающее вызов InputSourceManager.activate(N) по D-Bus; переключение shell-расширений на живой сессии требует перезапуска GNOME Shell (Wayland: выход/вход) — если это требуется, зафиксировать вердикт unavailable: «требует ре-логина» и НЕ перелогинивать сессию владельца; решение об этой пробе — на усмотрение исполнителя в рамках бокса, всё зафиксировать в журнал. Любая проба с вердиктом switched → двух-наблюдательное подтверждение (FocusIn/FocusOut + текст). Итог: хотя бы одна switched → two-engine механизм доступен; все not-switched/unavailable → Option B (внутренний флип — паттерн работающего прототипа владельца), это НЕ провал эксперимента, а его результат. Журнал дописывается построчно каждым прогоном; кейс завершается PASS при наличии вердикта для каждой выполненной пробы и выводимого решения.
  </action>
  <verify>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && go vet ./... && go build ./... && go run ./test/e2e -case d01-probe</automated>
    <fails_when>строка FAIL в отчёте или код выхода ≠ 0 — в т.ч. если какая-то проба не оставила вердикта в журнале</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && grep -c 'Verdict\|Вердикт' docs/adr/d01-experiment-log.md</automated>
    <fails_when>счётчик меньше 4 — в журнале нет вердикта минимум для четырёх обязательных проб (gsettings, ydotool, SetGlobalEngine, Shell.Eval)</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && gsettings get org.gnome.desktop.input-sources sources</automated>
    <fails_when>вывод не содержит исходного значения (вида [('xkb', 'us'), ('xkb', 'ru')]) — teardown не восстановил состояние стола</fails_when>
  </verify>
  <done>Журнал эксперимента заполнен: у каждой пробы наблюдаемый вердикт; решение two-engine/Option-B выводимо из журнала; состояние сессии восстановлено</done>
  <acceptance_criteria>
  - журнал d01-experiment-log.md содержит ≥ 4 строк вердиктов и колонку наблюдаемого (не dconf-значений)
  - каждая проба со статусом switched подтверждена парой наблюдений FocusIn/FocusOut + текст
  - пробы, требующие unsafe mode или ре-логина, имеют вердикт unavailable с причиной, без выполнения требуемого действия
  - после прогона gsettings sources равен снимку (restore сработал)
  - микро-расширение (если создавалось) удалено из ~/.local/share/gnome-shell/extensions/
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: ADR-пак — 002..005 сначала, ADR-001 последним из журнала</name>
  <reversibility rating="costly">ADR-001 фиксирует контракт переключения; если победит Option B, последствия D-03 «один хозяин» — пользовательский контракт (UX, документация, e2e-кейсы), смена позже требует переучивания (CONTEXT D-03 — costly)</reversibility>
  <precondition>docs/adr/d01-experiment-log.md заполнен (Task 1) — решение выводимо</precondition>
  <files>docs/adr/ADR-001-layout-switching-mechanism.md, docs/adr/ADR-002-tap-semantics.md, docs/adr/ADR-003-replacement-capability-ladder.md, docs/adr/ADR-004-buffer-reset-triggers.md, docs/adr/ADR-005-macr-01-owner-checkpoint.md</files>
  <read_first>
  - .planning/phases/01-adr-paket-i-karkas-ibus-dvizhka/01-CONTEXT.md (D-01..D-06 и OPEN-01 — цитировать дословно)
  - .planning/phases/01-adr-paket-i-karkas-ibus-dvizhka/01-RESEARCH.md (§ Pattern 8 — состав каждого ADR; § Pattern 3 — FSM как исполняемая спецификация; § Pattern 1 — capability ladder и сигналы)
  - docs/adr/d01-experiment-log.md (доказательная база ADR-001)
  - internal/hotkey/fsm_test.go (корпус — цитировать в ADR-002 как исполняемую спецификацию)
  - docs/SPEC.md §4.3, §5, §6 (объекты spec-delta и открытые вопросы)
  </read_first>
  <action>
  Формат каждого ADR (дискреция CONTEXT по формату; состав фиксирован): файлы docs/adr/ADR-00N-slug.md, разделы Status (Proposed — до чекпоинта владельца; обновляется в Task 3), Context, Decision, Consequences, Reversibility (оценки из CONTEXT процитировать).
  ADR-002 Tap semantics: классика с ожиданием (владелец отклонил Caramba-схему и разные клавиши — процитировать D-04); все действия на истечении окна после последнего тапа; окно по умолчанию 300 мс, настраивается (D-05); модификаторная дискриминация; правило 4-го тапа; отсылка к internal/hotkey/fsm_test.go как исполняемой спецификации; NFR-переформулировка (spec-delta §5): «действия без ожидания — < 50 мс; действия Right Shift — не дольше окна различения».
  ADR-003 Replacement capability ladder: решение сейчас, реализация Фаза 2. Уровни: DeleteSurroundingText (сигнал без подтверждения на 1.5.29) → Backspace×N (подсчёт в рунах; при любом сомнении — abort, не мусорить) → clipboard round-trip; выбор уровня по битам capabilities из SetCapabilities (бит surrounding 1<<5); минимум один Chromium-кейс в матрице Фазы 2 подтверждает fallback-уровень.
  ADR-004 Buffer reset triggers: D-06 дословно; клик мыши убран из триггеров (на уровне IME ненаблюдаем); вместо него (1) обязательная сверка буфера с текстом перед курсором перед коррекцией — не совпало, молча ничего не делать; (2) best-effort сброс по скачку позиции курсора в surrounding text; текст spec-delta к §4.3 привести готовым к применению.
  ADR-005 MACR-01 / OPEN-01: Status Proposed (решение владельца, гейт M0); изложить вопрос; рекомендация исполнителя как ВХОД (вынос в v2 через spec-delta + keyd-рецепт в документацию Фазы 4), явно помеченная «не решение»; до решения код не пишется. Рецепт keyd в сам ADR не включать (deferred idea из CONTEXT — кандидат Фазы 4).
  ADR-001 Layout switching mechanism — ПОСЛЕДНИМ: победивший вариант из журнала эксперимента; kill-критерии и что их закрыло; проигравшие кандидаты с наблюдаемыми провалами (включая SetGlobalEngine-находку keyboard.js 46 и gsettings-результат); если Option B — последствия D-03 «один хозяин» (единственный источник goswitch, Right Shift единственный переключатель, Super+Space ничего не переключает, двойное состояние исключено) и требования к UX-документации; наблюдение по A3 (ре-активация источника после рестарта ibus-daemon из кейса ibus-restart плана 03) записать в Consequences; ссылка на docs/adr/d01-experiment-log.md обязательна.
  </action>
  <verify>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && for f in docs/adr/ADR-00{1..5}-*.md; do grep -q '## Status' "$f" && grep -q '## Decision' "$f" && grep -q '## Consequences' "$f" && grep -q '## Reversibility' "$f" || echo "MISSING SECTION: $f"; done; echo CHECKED</automated>
    <fails_when>в выводе появилась строка MISSING SECTION — у какого-то ADR нет обязательного раздела</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && grep -c 'd01-experiment-log' docs/adr/ADR-001-layout-switching-mechanism.md && grep -c 'Proposed' docs/adr/ADR-005-macr-01-owner-checkpoint.md && grep -c 'вход, не решение\|вход, а не решение\|not a decision' docs/adr/ADR-005-macr-01-owner-checkpoint.md</automated>
    <fails_when>ADR-001 не ссылается на журнал эксперимента (0); или ADR-005 не имеет статуса Proposed (0); или рекомендация не помечена как вход, а не решение (0)</fails_when>
  </verify>
  <done>Пять ADR написаны в едином формате; ADR-001 опирается на журнал эксперимента; ADR-005 держит решение за владельцем; тексты spec-delta готовы к применению</done>
  <acceptance_criteria>
  - все 5 файлов существуют с пятью обязательными разделами каждый
  - ADR-002 цитирует D-04/D-05 и ссылается на fsm_test.go
  - ADR-003 фиксирует три уровня лестницы и abort-не-мусорить
  - ADR-004 содержит готовый текст spec-delta §4.3 и оба механизма D-06
  - ADR-005: статус Proposed, рекомендация помечена как вход, MACR-кода в фазе нет
  - ADR-001: решение выводимо из журнала, проигравшие кандидаты с наблюдениями, D-03-последствия при Option B, наблюдение A3
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Гейт M0 — ревью ADR-пака владельцем + применение spec-delta</name>
  <what-built>
  ADR-пак из пяти решений (docs/adr/ADR-001..005) + журнал эксперимента D-01 (docs/adr/d01-experiment-log.md) + работающий каркас демона (планы 01-03). Гейт M0 «Ревью владельца» — критерий успеха 1 фазы. Чекпоинт блокирующий: MACR-01 (OPEN-01) не может быть решён автоматически, а ответ по умолчанию нельзя предполагать.
  </what-built>
  <how-to-verify>
  1. Прочитать docs/adr/d01-experiment-log.md — вердикты проб и выведенное решение.
  2. Прочитать ADR-001: согласиться с механизмом переключения (two-engine или внутренний флип Option B) или потребовать иную пробу.
  3. Прочитать ADR-002/003/004: подтвердить семантику тапов (300 мс, классика с ожиданием), лестницу замены, триггеры сброса буфера.
  4. РЕШИТЬ MACR-01 (ADR-005): (a) вынести в v2 через spec-delta с keyd-рецептом в документации Фазы 4, или (b) оставить во въёмке v1 (тогда Фаза 3 планирует механизм по ADR), или (c) свой вариант.
  5. Утвердить spec-deltas: §5 (тайминги Right Shift vs окна различения) и §4.3 (сброс буфера без клика мыши).
  После ответа владельца исполнитель применяет: тексты дельт в docs/SPEC.md (§5 и §4.3 — готовые тексты из ADR-002/004), статусы ADR Proposed → Accepted (ADR-005 — по решению владельца), при (a) — запись решения в ADR-005 и REQUIREMENTS-трассировку не менять (MACR-01 остаётся Фазы 3 со статусом descoped-by-ADR до formalного spec-delta Фазы 3).
  </how-to-verify>
  <resume-signal>Ответить: «утверждено, MACR в v2» / «утверждено, MACR в v1» / «утверждено с правками: …» / описать возражения</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| e2e-пробы → живая сессия GNOME (gsettings, ibus-daemon,GNOME Shell) | эксперимент мутирует и восстанавливает состояние сессии; пробы Eval/расширений касаются Shell |
| журнал эксперимента → ADR-001 → будущий код Фаз 2-3 | доказательная цепочка решений — подмена наблюдений = подмена архитектуры |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Elevation of privilege / Tampering | Shell.Eval-проба, unsafe mode | high | mitigate | unsafe mode НЕ включать ни при каких условиях; отказ Eval фиксируется вердиктом unavailable; проба микро-расширения — только в пользовательском каталоге, с удалением после, без ре-логина сессии владельца |
| T-04-02 | Tampering (состояние сессии) | gsettings sources/current в пробах | high | mitigate | snapshot/restore стенда (план 03) переиспользуется; teardown-гейт в verify (Task 1, третья команда) |
| T-04-03 | Repudiation (доказательная база) | d01-experiment-log.md | medium | mitigate | журнал пишет кейс построчно (команда+наблюдаемое+вердикт); ADR-001 обязан ссылаться на журнал; запрет объявлять two-engine без наблюдаемого (prohibition) |
| T-04-04 | Tampering | микро-расширение ~/.local/share/gnome-shell/extensions/ | medium | mitigate | условная проба, user-local каталог (без root), удаление после пробы — acceptance criterion Task 1 |
</threat_model>

<verification>
- go vet ./... && go build ./... зелёный; кейс d01-probe PASS
- Журнал: ≥ 4 вердикта, каждый с наблюдаемым (не dconf)
- ADR-пак: 5 файлов, единый формат, ADR-001 ссылается на журнал, ADR-005 Proposed
- Чекпоинт M0 пройдён: решения владельца зафиксированы, spec-deltas применены к docs/SPEC.md, статусы обновлены
</verification>

<success_criteria>
- Критерий 1 фазы закрыт: ADR-пак утверждён владельцем (механизм переключения, семантика тапов, capability ladder, триггеры сброса, судьба MACR-01)
- STATE-блокер «Decision #1» снят kill-criterion спайком до любого кода коррекции (коррекция — Фаза 2)
- OPEN-01: решение MACR-01 принял владелец; кода MACR в фазе нет
- Spec-deltas §5/§4.3 применены к docs/SPEC.md после утверждения
</success_criteria>

## Artifacts this phase produces

- Кейс `test/e2e -case d01-probe` (матрица проб переключения источника)
- Журнал docs/adr/d01-experiment-log.md (доказательная база)
- docs/adr/ADR-001-layout-switching-mechanism.md, ADR-002-tap-semantics.md, ADR-003-replacement-capability-ladder.md, ADR-004-buffer-reset-triggers.md, ADR-005-macr-01-owner-checkpoint.md (статусы Proposed → Accepted по чекпоинту)
- Изменения docs/SPEC.md: §5 (NFR-переформулировка D-05), §4.3 (сброс буфера D-06) — после утверждения владельцем
- Решение владельца по MACR-01, зафиксированное в ADR-005

<output>
Create .planning/phases/01-adr-paket-i-karkas-ibus-dvizhka/01-04-SUMMARY.md when done
</output>
