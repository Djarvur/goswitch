---
phase: 01-adr-paket-i-karkas-ibus-dvizhka
plan: 05
type: execute
wave: 5
depends_on: [01-01, 01-02, 01-03, 01-04]
files_modified:
  - .github/workflows/pr-sanity.yml
  - .github/workflows/security-scheduled.yml
  - .github/dependabot.yml
  - AGENTS.md
  - dist/systemd/user/goswitchd.service
  - README.md
autonomous: true
requirements: [TEST-01, INST-04, INTEG-03]

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "pr-sanity на каждый push/PR: mise install (инструменты из mise.toml — единственный источник версий go + golangci-lint) затем гейты build + vet + golangci-lint + test -race + tidy-diff; отдельный job govulncheck (D-11a). Выбор mise против golangci-lint-action обоснован в плане: action потребовал бы второй пин версии golangci-lint, дрейфующий от mise.toml; mise даёт точный паритет с локальным mise run ci"
    - "Периодическая проверка зависимостей: .github/dependabot.yml (package-ecosystem gomod, weekly) — механизм выбран осознанно: dependabot открывает PR, который автоматически прогоняет полный pr-sanity (build/vet/lint/race/tidy/govulncheck); альтернатива «scheduled job с go list -m -u all» только репортирует и требует ручного триажа (D-11b)"
    - "Периодическая безопасность: .github/workflows/security-scheduled.yml — weekly cron, govulncheck ./...; найденная уязвимость = ненулевой exit = красная джоба (D-11c)"
    - "AGENTS.md после регенерации несёт пять инженерных директив владельца из источника .planning/codebase/CONVENTIONS.md (строгий TDD; зелёная итерация; mise вместо make; строгий golangci-lint с Фазы 1; актуальные GitHub Actions) — прежнее отклонение «golangci-lint отложен» ОТМЕНЕНО директивой D-10 и в источнике отсутствует"
    - "dev systemd user unit: PartOf=graphical-session.target, After=org.freedesktop.IBus.session.GNOME.service, Restart=on-failure; установка руками владельца по README, не скриптом фазы — работа демона в сессии без root (INTEG-03)"
    - "README содержит раздел «Режим отладки и конфиденциальность»: -debug включает трассировку нажатий (keyval/keycode), такие логи могут содержать пароли; плюс dev-инструкция запуска через systemd user unit (INST-04)"
  artifacts:
    - path: .github/workflows/pr-sanity.yml
      provides: "CI sanity: permissions contents:read, concurrency, mise install из mise.toml, шаги build/vet/lint/test -race/tidy-diff, отдельный job govulncheck"
      contains: "golangci-lint"
    - path: .github/workflows/security-scheduled.yml
      provides: "weekly govulncheck с ненулевым exit на уязвостях"
      contains: "govulncheck"
    - path: .github/dependabot.yml
      provides: "weekly обновления go-modules через dependabot"
      contains: "gomod"
    - path: dist/systemd/user/goswitchd.service
      provides: "dev systemd user unit: PartOf=graphical-session.target, After=IBus GNOME user unit, Restart=on-failure"
    - path: README.md
      provides: "раздел «Режим отладки и конфиденциальность» + dev-инструкция запуска"
  key_links:
    - from: .github/workflows/pr-sanity.yml
      to: mise.toml
      via: "mise install читает [tools] и [tasks] — версия инструментария живёт ровно в одном месте (D-09/D-11); setup-go больше не используется"
      pattern: "mise install"
    - from: README.md
      to: dist/systemd/user/goswitchd.service
      via: "dev-инструкция: cp юнита в ~/.config/systemd/user/ + daemon-reload + start"
      pattern: "goswitchd\\.service"
    - from: AGENTS.md
      to: .planning/codebase/CONVENTIONS.md
      via: "раздел Conventions регенерируется из файла-источника generate-claude-md — директивы владельца переживают регенерацию"
      pattern: "GSD:conventions"
  prohibitions:
    - "CI НЕ ДОЛЖЕН запускать e2e-стенд или живые кейсы: push-гейт строго headless (build/vet/lint/test -race/tidy-diff/govulncheck); mise-задачи e2e-* в ci НЕ входят; стенд требует живой GNOME-сессии и выполняется только локально (T-03-03 плана 01-03)"
    - "AGENTS.md НЕ ДОЛЖЕН правиться вручную между маркерами GSD-секций: директивы живут в источнике .planning/codebase/CONVENTIONS.md и переносятся регенерацией"
---
<objective>
CI-пакет по директиве D-11 плюс dev-experience, выделенный из плана 01-01 (checker round 1, scope_sanity). Три механизма D-11 с Фазы 1: (a) .github/workflows/pr-sanity.yml — на каждый push/PR: build + vet + golangci-lint + test -race + tidy-diff, отдельный job govulncheck; инструменты ставятся mise install из mise.toml — единый источник версий (golangci-lint-action отклонён: его поле version продублировало бы пин mise.toml и дрейфовало; mise даёт паритет с локальным mise run ci — D-09/D-10/D-11a); (b) периодическая проверка зависимостей — .github/dependabot.yml (gomod, weekly): dependabot открывает PR, автоматически прогоняющий полный pr-sanity; report-only альтернатива (scheduled go list -m -u all) отклонена (D-11b); (c) .github/workflows/security-scheduled.yml — weekly govulncheck ./..., ненулевой exit на уязвимостях (D-11c). Плюс: регенерация AGENTS.md из уже переписанного владельцем источника .planning/codebase/CONVENTIONS.md (пять инженерных директив; прежнее отклонение «golangci-lint отложен» ОТМЕНЕНО D-10), dev systemd user unit и README-раздел о конфиденциальности -debug.

Purpose: CI — транспорт TEST-01 (headless-зелёный на каждый push) и исполнение D-11 (гейты создаются с Фазы 1 и поддерживаются актуальными); golangci-lint в CI с Фазы 1 через mise. Гейты скилла go-ultimate (build/vet/test -race/tidy/govulncheck) сохранены полностью — меняется только источник инструментов (setup-go → mise install).
Output: .github/workflows/pr-sanity.yml, .github/workflows/security-scheduled.yml, .github/dependabot.yml, перегенерированный раздел Conventions в AGENTS.md, dist/systemd/user/goswitchd.service, README-раздел о конфиденциальности.

Волна 5, depends_on: 01-01..01-04 (checker round 2, dependency_correctness): локальный гейт Task 1 компилирует ВЕСЬ модуль (mise run ci = build + vet + lint + go test -race ./...), поэтому план идёт после всех планов, правящих Go-код — включая intentional-RED фазы TDD в 01-02/01-03, правки test/e2e и расширение mise.toml в 01-03/01-04; параллельных same-wave Go-связей и файловых пересечений с другими планами нет.
</objective>

<execution_context>
@/home/nil/DiskD/W/Djarvur/goswitch/.zcode/gsd-core/workflows/execute-plan.md
@/home/nil/DiskD/W/Djarvur/goswitch/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: GHA-пакет D-11 — pr-sanity + dependabot + security-scheduled; AGENTS.md регенерация</name>
  <precondition>Планы 01-01..01-04 выполнены: mise.toml существует (задачи build/test/lint/vet/tidy-diff/ci плюс e2e-m1/e2e-ibus-restart/e2e-kill9-survive/e2e-d01), весь Go-код фазы зелёный под mise run ci — локальный гейт Task 1 гоняет весь модуль</precondition>
  <files>.github/workflows/pr-sanity.yml, .github/workflows/security-scheduled.yml, .github/dependabot.yml, AGENTS.md</files>
  <read_first>
  - .zcode/skills/go-ultimate/assets/ci/test.yml (гейт-набор и форма скилла: permissions, concurrency, отдельный job govulncheck — адаптация: источник инструментов setup-go заменяется на mise install, гейты зовутся как mise-задачи)
  - .zcode/skills/go-ultimate/references/repo-and-ci.md (форма workflow, ignore-база)
  - mise.toml (единственный источник версий go+golangci-lint и имён задач — CI зовёт ровно эти задачи; план 01-01)
  - .planning/codebase/CONVENTIONS.md (уже переписан владельцем: пять инженерных директив — источник раздела Conventions AGENTS.md; регенерация переносит его)
  - .planning/phases/01-adr-paket-i-karkas-ibus-dvizhka/01-PATTERNS.md (§ CI workflow shape)
  - AGENTS.md (маркеры GSD:conventions-start/end — раздел Conventions и его источник)
  </read_first>
  <action>
  .github/workflows/pr-sanity.yml: on push branches [main] + pull_request + workflow_dispatch; permissions contents: read; concurrency group pr-sanity-${{ github.event_name }}-${{ github.ref }} с cancel-in-progress; checkout@v4. Установка инструментов: официальный инсталлер mise (curl https://mise.run в шеле, ~/.local/bin добавляется в GITHUB_PATH), затем mise install — читает mise.toml, ставит go и golangci-lint ровно тех версий, что локально (выбор против golangci-lint-action зафиксирован: единый источник пина, паритет с mise run ci; действие потребовало бы вторую версию в inputs). Гейты отдельными шагами: mise run build; mise run vet; mise run lint (golangci-lint в CI с Фазы 1 — D-10/D-11a); mise run test; mise run tidy-diff (go mod tidy + git diff --exit-code -- go.mod go.sum — tidy-гейт из гейт-набора скилла). Отдельный job govulncheck: та же mise-установка, затем mise exec -- go run golang.org/x/vuln/cmd/govulncheck@latest ./... (network-side инструмент: скачивание и vuln DB только в CI, локальный гейт его не зеркалит; если @latest потребует новее toolchain — поднять пин go в mise.toml вместе с директивой go.mod, одно место). Матрицу совместимости НЕ добавлять (приложение, не библиотека — скилл сам предписывает удалить compat-job для приложений).
  .github/workflows/security-scheduled.yml: on schedule cron еженедельно (например 0 6 * * 1) + workflow_dispatch; permissions contents: read; один job: mise-установка (как выше) и mise exec -- go run golang.org/x/vuln/cmd/govulncheck@latest ./... — govulncheck завершается ненулевым кодом при найденных уязвимостях, джоба краснеет (D-11c).
  .github/dependabot.yml: version 2; updates: package-ecosystem gomod, directory /, schedule interval weekly (D-11b). Обоснование выбора (зафиксировать комментарием в файле): dependabot открывает PR на обновление, каждый PR автоматически прогоняет полный pr-sanity (включая линт, race и govulncheck) — обновление не сольётся без зелёного гейта; scheduled-джоба с go list -m -u all лишь репортирует и требует ручного триажа.
  AGENTS.md: источник .planning/codebase/CONVENTIONS.md уже переписан владельцем под директивы (строгий TDD; зелёная итерация build+test -race+lint; mise вместо make; строгий golangci-lint с Фазы 1; актуальные GitHub Actions) — записей об отклонении golangci-lint в нём больше нет (отменено D-10). Обновить AGENTS.md регенерацией: node .zcode/gsd-core/bin/gsd-tools.cjs query generate-claude-md из корня репо; убедиться, что раздел Conventions несёт строки директив; вручную между маркерами AGENTS.md не править.
  </action>
  <verify>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && grep -q 'mise install' .github/workflows/pr-sanity.yml && grep -q 'golangci-lint' .github/workflows/pr-sanity.yml && grep -q 'tidy-diff' .github/workflows/pr-sanity.yml && grep -q 'govulncheck' .github/workflows/pr-sanity.yml && grep -q 'contents: read' .github/workflows/pr-sanity.yml && grep -q 'schedule' .github/workflows/security-scheduled.yml && grep -q 'govulncheck' .github/workflows/security-scheduled.yml && grep -q 'gomod' .github/dependabot.yml && echo WORKFLOWS-OK</automated>
    <fails_when>любой grep не находит свой элемент — отсутствует один из механизмов D-11: pr-sanity (mise install, lint, tidy-diff, govulncheck, permissions), scheduled security (cron + govulncheck) или dependabot gomod</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise run ci && mise run tidy-diff && echo LOCAL-GATES</automated>
    <fails_when>non-zero exit любой из команд — то, что запускает CI (кроме network-side govulncheck), обязано проходить локально; или tidy-гейт нашёл незакоммиченное расхождение go.mod/go.sum</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && grep -q 'Строгий TDD' AGENTS.md && grep -q 'mise' AGENTS.md && grep -q 'golangci-lint' AGENTS.md && echo CONVENTIONS-CARRIED</automated>
    <fails_when>раздел Conventions в AGENTS.md не несёт строки директив (Строгий TDD / mise / golangci-lint) — регенерация не подхватила переписанный источник CONVENTIONS.md</fails_when>
  </verify>
  <done>Три механизма D-11 в репо: pr-sanity (build + vet + golangci-lint + test -race + tidy-diff + job govulncheck, инструменты из mise.toml), weekly dependabot gomod, weekly scheduled govulncheck с ненулевым exit на уязвимостях; AGENTS.md несёт директивы владельца после регенерации; локальный прогон того же пути зелёный</done>
  <acceptance_criteria>
  - pr-sanity.yml: mise install (не setup-go), шаги build/vet/lint/test -race/tidy-diff, отдельный job govulncheck, permissions contents: read, concurrency, без матрицы совместимости
  - security-scheduled.yml: weekly cron + govulncheck; ненулевой exit на уязвимостях роняет джобу
  - .github/dependabot.yml: gomod weekly; комментарий-обоснование выбора против report-only джобы
  - AGENTS.md (раздел Conventions) содержит строки директив (Строгий TDD, mise, golangci-lint) после регенерации из CONVENTIONS.md
  - mise-задачи e2e-* в pr-sanity НЕ вызываются (живой стенд остаётся локальным)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: dev systemd unit + предупреждение о конфиденциальности в README</name>
  <files>dist/systemd/user/goswitchd.service, README.md</files>
  <read_first>
  - .planning/phases/01-adr-paket-i-karkas-ibus-dvizhka/01-RESEARCH.md (§ Runtime State Inventory — systemd-строка: PartOf=graphical-session.target, After=org.freedesktop.IBus.session.GNOME.service)
  - .planning/phases/01-adr-paket-i-karkas-ibus-dvizhka/01-PATTERNS.md (§ systemd dev unit goswitchd.service)
  - README.md (текущее состояние)
  </read_first>
  <action>
  dist/systemd/user/goswitchd.service (dev-форма): [Unit] Description=goswitch layout engine; PartOf=graphical-session.target; After=org.freedesktop.IBus.session.GNOME.service. [Service] ExecStart=%h/go/bin/goswitchd (dev-путь go install); Restart=on-failure (покрывает неустранимый kill -9). [Install] WantedBy=graphical-session.target. Установка юнита в сессию — руками владельца по README-инструкции, НЕ скриптом фазы.
  README.md: добавить раздел «Режим отладки и конфиденциальность»: флаг -debug включает трассировку нажатий клавиш (keyval/keycode) в JSON-лог; такие логи могут содержать пароли; в production-режиме stderr уходит в journal — не оставлять -debug включённым постоянно. Плюс короткая dev-инструкция запуска: go install ./cmd/goswitchd && cp dist/systemd/user/goswitchd.service ~/.config/systemd/user/ && systemctl --user daemon-reload && systemctl --user start goswitchd.
  </action>
  <verify>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && grep -q 'PartOf=graphical-session.target' dist/systemd/user/goswitchd.service && grep -q 'Restart=on-failure' dist/systemd/user/goswitchd.service && grep -q 'After=org.freedesktop.IBus.session.GNOME.service' dist/systemd/user/goswitchd.service && echo UNIT-OK</automated>
    <fails_when>grep не находит PartOf=graphical-session.target, After=org.freedesktop.IBus.session.GNOME.service или Restart=on-failure в юните</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && test "$(grep -c '\-debug' README.md)" -ge 1 && grep -q 'goswitchd.service' README.md && grep -qi 'парол' README.md && echo README-OK</automated>
    <fails_when>README не упоминает -debug (счётчик 0), не ссылается на goswitchd.service в dev-инструкции или не содержит предупреждения о паролях</fails_when>
  </verify>
  <done>dev-юнит привязан к graphical-session и перезапускается при падении; README предупреждает о трассировке клавиш и содержит инструкцию dev-запуска без root</done>
  <acceptance_criteria>
  - юнит содержит PartOf=graphical-session.target, After=org.freedesktop.IBus.session.GNOME.service, Restart=on-failure, WantedBy=graphical-session.target
  - README содержит раздел о конфиденциальности -debug с упоминанием паролей и инструкцию установки user-юнита
  - установка юнита не автоматизирована скриптом фазы (руками по README)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow-файлы → среда выполнения GitHub Actions | CI исполняет код репо на каждый push/PR — права и границы задаёт workflow |
| CI-раннер → module proxy (golang.org/x/vuln) и инсталлер mise (mise.run) | инструменты скачиваются при каждом прогоне |
| dependabot → репо | бот открывает PR на обновления зависимостей — PR проходят полный pr-sanity |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | govulncheck через go run @latest (непиннированная версия инструмента) | medium | mitigate | golang.org/x/vuln — first-party модуль Go team; исполнение в эфемерном раннере с permissions: contents: read и без секретов; пиннирование версии — при первом обновлении инструмента |
| T-05-02 | Elevation of privilege | права workflow-токена | low | mitigate | permissions: contents: read на уровне каждого workflow (pr-sanity и security-scheduled) — минимальные права по умолчанию |
| T-05-03 | Tampering | установка mise в CI (curl-инсталлер mise.run) | medium | mitigate | официальный домен mise.run (jdx/mise — каноническая точка распростновения), эфемерный раннер, contents: read, без секретов; та же установка воспроизводима локально (план 01-01, T-01-05) |
| T-05-04 | Tampering | dependabot-PR на обновление godbus | low | mitigate | каждый dependabot-PR автоматически прогоняет полный pr-sanity (build/vet/lint/race/tidy-diff/govulncheck) — обновление не попадает в main без зелёного гейта; в Фазе 1 зависимость одна (godbus v5.2.2) |
| T-05-SC | Tampering | go run golang.org/x/vuln/cmd/govulncheck@latest и mise install в CI | low | mitigate | package-legitimacy: официальный first-party модуль Go team и официальный инсталлер jdx/mise (не npm/pip/cargo реестры), доставка через module proxy с checksum-верификацией; новых runtime-зависимостей демону план не добавляет |
</threat_model>

<verification>
- Локально зелёный тот же путь, что и CI (кроме network-side govulncheck): mise run ci && mise run tidy-diff
- Структурно: все три механизма D-11 присутствуют (pr-sanity с lint, security-scheduled, dependabot gomod); инструменты ставятся только из mise.toml (mise install)
- AGENTS.md несёт строки директив владельца после регенерации из .planning/codebase/CONVENTIONS.md
</verification>

<success_criteria>
- TEST-01 (инфраструктурная часть): headless CI (build/vet/golangci-lint/test -race/tidy-diff/govulncheck) гоняет unit/golden-корпус фазы на каждый push — зелёный
- D-11: pr-sanity + периодическая проверка зависимостей (dependabot weekly) + периодическая безопасность (weekly govulncheck) — все три механизма созданы с Фазы 1
- INST-04 (документационная часть): предупреждение о трассировке клавиш опубликовано в README
- INTEG-03 (dev-часть): dev systemd user unit после graphical-session.target, установка и работа без root
- Гейты скилла go-ultimate: полный набор сохранён (build/vet/test -race/tidy/govulncheck) и усилен golangci-lint с Фазы 1 (D-10) — отклонений нет
</success_criteria>

## Artifacts this phase produces

- CI `.github/workflows/pr-sanity.yml` (push/PR: build + vet + golangci-lint + test -race + tidy-diff, отдельный job govulncheck; mise install — единственный источник версий)
- Scheduled `.github/workflows/security-scheduled.yml` (weekly govulncheck, ненулевой exit на уязвимостях)
- `.github/dependabot.yml` (weekly gomod; выбор против report-only джобы зафиксирован)
- dev-юнит `dist/systemd/user/goswitchd.service`
- README-раздел «Режим отладки и конфиденциальность» + dev-инструкция запуска
- Перегенерированный раздел Conventions в AGENTS.md (пять инженерных директив владельца из CONVENTIONS.md)

<output>
Create .planning/phases/01-adr-paket-i-karkas-ibus-dvizhka/01-05-SUMMARY.md when done
</output>
