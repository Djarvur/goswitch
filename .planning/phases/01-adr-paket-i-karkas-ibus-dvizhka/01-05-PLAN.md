---
phase: 01-adr-paket-i-karkas-ibus-dvizhka
plan: 05
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - .github/workflows/test.yml
  - .planning/codebase/CONVENTIONS.md
  - AGENTS.md
  - dist/systemd/user/goswitchd.service
  - README.md
autonomous: true
requirements: [TEST-01, INST-04, INTEG-03]

estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "CI на каждый push/PR выполняет полный гейт-набор скилла go-ultimate: go build ./..., go vet ./..., go test -race -cover ./..., go mod tidy + git diff --exit-code -- go.mod go.sum, job govulncheck (TEST-01: headless-зелёный в CI)"
    - "Единственное отклонение от CI-гейтов скилла — golangci-lint (инструмент не установлен на целевой машине; research явно разрешает стадийность vet+test -race) — зафиксировано в .planning/codebase/CONVENTIONS.md, источнике раздела Conventions файла AGENTS.md, переживающем регенерацию generate-claude-md"
    - "dev systemd user unit: PartOf=graphical-session.target, After=org.freedesktop.IBus.session.GNOME.service, Restart=on-failure; установка руками владельца по README, не скриптом фазы — работа демона в сессии без root (INTEG-03)"
    - "README содержит раздел «Режим отладки и конфиденциальность»: -debug включает трассировку нажатий (keyval/keycode), такие логи могут содержать пароли; плюс dev-инструкция запуска через systemd user unit (INST-04)"
  artifacts:
    - path: .github/workflows/test.yml
      provides: "CI: permissions contents:read, concurrency по event, checkout+setup-go go-version-file: go.mod; шаги build/vet/test -race -cover; шаг modules are tidy; отдельный job govulncheck"
      contains: "govulncheck"
    - path: .planning/codebase/CONVENTIONS.md
      provides: "запись отклонения CI-гейтов (golangci-lint staging) — источник раздела Conventions в AGENTS.md"
      contains: "golangci-lint"
    - path: dist/systemd/user/goswitchd.service
      provides: "dev systemd user unit: PartOf=graphical-session.target, After=IBus GNOME user unit, Restart=on-failure"
    - path: README.md
      provides: "раздел «Режим отладки и конфиденциальность» + dev-инструкция запуска"
  key_links:
    - from: .github/workflows/test.yml
      to: go.mod
      via: "setup-go go-version-file: go.mod — версия инструментария живёт ровно в одном месте"
      pattern: "go-version-file"
    - from: README.md
      to: dist/systemd/user/goswitchd.service
      via: "dev-инструкция: cp юнита в ~/.config/systemd/user/ + daemon-reload + start"
      pattern: "goswitchd\\.service"
    - from: AGENTS.md
      to: .planning/codebase/CONVENTIONS.md
      via: "раздел Conventions регенерируется из файла-источника generate-claude-md — запись отклонения переживает регенерацию"
      pattern: "GSD:conventions"
  prohibitions:
    - "CI НЕ ДОЛЖЕН запускать e2e-стенд или живые кейсы: push-гейт строго headless (build/vet/test/tidy/govulncheck); стенд требует живой GNOME-сессии и выполняется только локально (T-03-03 плана 01-03)"
    - "AGENTS.md НЕ ДОЛЖЕН правиться вручную между маркерами GSD-секций: отклонение фиксируется в источнике .planning/codebase/CONVENTIONS.md и переносится регенерацией"
---

<objective>
Выделенный из плана 01-01 (checker round 1, scope_sanity: 15 файлов на пороге деградации) dev-experience-пакет: (1) headless CI-workflow с ПОЛНЫМ гейтом-набором скилла go-ultimate — build, vet, test -race -cover, tidy-гейт, govulncheck (два последних в исходном плане опускались без записи об отклонении — восстановлены как cost-free); (2) dev systemd user unit для живой сессии; (3) README с предупреждением о конфиденциальности -debug. Отклонение от гейтов скилла ровно одно — golangci-lint (стадийность vet+test -race разрешена research) — и оно фиксируется в переживающем регенерацию источнике раздела Conventions файла AGENTS.md.

Purpose: CI — транспорт TEST-01 (headless-зелёный на каждый push); восстановление tidy/govulncheck закрывает project_skill_compliance (cost-free гейты не отбрасываются, остальные отклонения записываются в AGENTS.md).
Output: .github/workflows/test.yml, dist/systemd/user/goswitchd.service, README-раздел о конфиденциальности, запись в AGENTS.md (Conventions).
</objective>

<execution_context>
@/home/nil/DiskD/W/Djarvur/goswitch/.zcode/gsd-core/workflows/execute-plan.md
@/home/nil/DiskD/W/Djarvur/goswitch/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CI-workflow с полным гейтом go-ultimate + запись отклонения в AGENTS.md</name>
  <precondition>go.mod существует (план 01-01 выполнен — CI использует go-version-file: go.mod)</precondition>
  <files>.github/workflows/test.yml, .planning/codebase/CONVENTIONS.md, AGENTS.md</files>
  <read_first>
  - .zcode/skills/go-ultimate/assets/ci/test.yml (полный гейт-набор скилла: build, vet, test -race -cover, tidy-гейт, job govulncheck — копировать форму дословно)
  - .zcode/skills/go-ultimate/references/repo-and-ci.md (форма workflow, ignore-база)
  - .planning/phases/01-adr-paket-i-karkas-ibus-dvizhka/01-PATTERNS.md (§ CI workflow shape — tidy check в форме; «keep govulncheck only if it doesn't complicate the skeleton» — отдельный job не усложняет)
  - .planning/phases/01-adr-paket-i-karkas-ibus-dvizhka/01-RESEARCH.md (§ Environment Availability / Runtime State Inventory — golangci-lint отсутствует → стадийность vet+test -race)
  - AGENTS.md (маркеры GSD:conventions-start/end — раздел Conventions и его источник)
  </read_first>
  <action>
  .github/workflows/test.yml — копия структуры скилла assets/ci/test.yml дословно по гейтам: on push branches [main] + pull_request + workflow_dispatch; permissions contents: read; concurrency group test-${{ github.event_name }}-${{ github.ref }} с cancel-in-progress; checkout@v4 + setup-go@v5 с go-version-file: go.mod. Job test с шагами: go build ./...; go vet ./...; go test -race -cover ./...; шаг «modules are tidy»: go mod tidy && git diff --exit-code -- go.mod go.sum — tidy-гейт восстановлен (checker round 1, project_skill_compliance: cost-free гейт не отбрасывался без причины). Job vuln (отдельный, из актива скилла): go run golang.org/x/vuln/cmd/govulncheck@latest ./... — govulncheck исполняется только в CI (network-fetch инструмента и vuln DB), локальный гейт его не зеркалит. Матрицу совместимости НЕ добавлять (приложение, не библиотека — скилл сам предписывает удалить compat-job для приложений).
  Единственное отклонение от гейтов скилла — golangci-lint: инструмент не установлен на целевой машине, research явно разрешает стадийность vet+test -race с добавлением пиннированного golangci-lint позже. Зафиксировать отклонение в месте, переживающем регенерацию AGENTS.md: .planning/codebase/CONVENTIONS.md — файл-источник раздела ## Conventions (маркер GSD:conventions source:CONVENTIONS.md; регенерация переносит содержимое в AGENTS.md; вручную между маркерами AGENTS.md не править). Текст записи: CI-гейты скилла go-ultimate — build / vet / test -race -cover / go mod tidy + git diff --exit-code / govulncheck — включены с Фазы 1; golangci-lint отложен до появления инструмента на целевой машине (разрешённая research-ом стадийность); при добавлении — пиннированная версия в отдельном job. Затем обновить сам AGENTS.md регенерацией: node .zcode/gsd-core/bin/gsd-tools.cjs query generate-claude-md из корня репо — и убедиться, что раздел Conventions несёт запись (source-файл уже обновлён, регенерация его подхватит).
  </action>
  <verify>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && grep -q 'go-version-file' .github/workflows/test.yml && grep -q 'go mod tidy' .github/workflows/test.yml && grep -q 'git diff --exit-code' .github/workflows/test.yml && grep -q 'govulncheck' .github/workflows/test.yml && grep -q 'contents: read' .github/workflows/test.yml && echo GATES-PRESENT</automated>
    <fails_when>любой grep не находит свой гейт — workflow не воспроизводит полный набор скилла (go-version-file, tidy, govulncheck, permissions)</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && go vet ./... && go build ./... && go test -race -count=1 ./... && go mod tidy && git diff --exit-code -- go.mod go.sum && echo LOCAL-GATES</automated>
    <fails_when>non-zero exit любой из команд — то, что запускает CI (кроме network-side govulncheck), обязано проходить локально; или tidy-гейт нашёл незакоммиченное расхождение go.mod/go.sum</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && grep -q 'golangci-lint' .planning/codebase/CONVENTIONS.md && grep -q 'golangci-lint' AGENTS.md && echo DEVIATION-NOTED</automated>
    <fails_when>grep не находит golangci-lint в источнике Conventions или в AGENTS.md — отклонение CI-гейтов не зафиксировано в месте, видимом агентам</fails_when>
  </verify>
  <done>CI воспроизводит полный гейт-набор скилла (build/vet/test-race-cover/tidy/govulncheck), локальный прогон того же пути зелёный, отклонение golangci-lint зафиксировано в AGENTS.md через источник, переживающий регенерацию</done>
  <acceptance_criteria>
  - .github/workflows/test.yml: go-version-file: go.mod, permissions contents: read, шаги build+vet+test -race -cover, шаг modules are tidy, отдельный job govulncheck
  - матрица совместимости отсутствует (приложение)
  - .planning/codebase/CONVENTIONS.md содержит запись об отклонении golangci-lint с причиной и условием добавления
  - AGENTS.md (раздел Conventions) несёт ту же запись после регенерации
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: dev systemd unit + предупреждение о конфиденциальности в README</name>
  <files>dist/systemd/user/goswitchd.service, README.md</files>
  <read_first>
  - .planning/phases/01-adr-paket-i-karkas-ibus-dvizhka/01-RESEARCH.md (§ Runtime State Inventory — systemd-строка: PartOf=graphical-session.target, After=org.freedesktop.IBus.session.GNOME.service)
  - .planning/phases/01-adr-paket-i-karkas-ibus-dvizhka/01-PATTERNS.md (§ systemd dev unit goswitchd.service)
  - README.md (текущее состояние)
  </read_first>
  <action>
  dist/systemd/user/goswitchd.service (dev-форма): [Unit] Description=goswitch layout engine; PartOf=graphical-session.target; After=org.freedesktop.IBus.session.GNOME.service. [Service] ExecStart=%h/go/bin/goswitchd (dev-путь go install); Restart=on-failure (покрывает неустранимый kill -9). [Install] WantedBy=graphical-session.target. Установка юнита в сессию — руками владельца по README-инструкции, НЕ скриптом фазы.
  README.md: добавить раздел «Режим отладки и конфиденциальность»: флаг -debug включает трассировку нажатий клавиш (keyval/keycode) в JSON-лог; такие логи могут содержать пароли; в production-режиме stderr уходит в journal — не оставлять -debug включённым постоянно. Плюс короткая dev-инструкция запуска: go install ./cmd/goswitchd && cp dist/systemd/user/goswitchd.service ~/.config/systemd/user/ && systemctl --user daemon-reload && systemctl --user start goswitchd.
  </action>
  <verify>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && grep -q 'PartOf=graphical-session.target' dist/systemd/user/goswitchd.service && grep -q 'Restart=on-failure' dist/systemd/user/goswitchd.service && grep -q 'After=org.freedesktop.IBus.session.GNOME.service' dist/systemd/user/goswitchd.service && echo UNIT-OK</automated>
    <fails_when>grep не находит PartOf=graphical-session.target, After=org.freedesktop.IBus.session.GNOME.service или Restart=on-failure в юните</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && test "$(grep -c '\-debug' README.md)" -ge 1 && grep -q 'goswitchd.service' README.md && grep -qi 'парол' README.md && echo README-OK</automated>
    <fails_when>README не упоминает -debug (счётчик 0), не ссылается на goswitchd.service в dev-инструкции или не содержит предупреждения о паролях</fails_when>
  </verify>
  <done>dev-юнит привязан к graphical-session и перезапускается при падении; README предупреждает о трассировке клавиш и содержит инструкцию dev-запуска без root</done>
  <acceptance_criteria>
  - юнит содержит PartOf=graphical-session.target, After=org.freedesktop.IBus.session.GNOME.service, Restart=on-failure, WantedBy=graphical-session.target
  - README содержит раздел о конфиденциальности -debug с упоминанием паролей и инструкцию установки user-юнита
  - установка юнита не автоматизирована скриптом фазы (руками по README)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow-файл → среда выполнения GitHub Actions | CI исполняет код репо на каждый push/PR — права и границы задаёт workflow |
| CI-раннер → module proxy (golang.org/x/vuln) | инструмент govulncheck скачивается go run @latest при каждом прогоне |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | govulncheck через go run @latest (непиннированная версия инструмента) | medium | mitigate | golang.org/x/vuln — first-party модуль Go team; исполнение в эфемерном раннере с permissions: contents: read и без секретов; пиннирование версии — при первом обновлении инструмента |
| T-05-02 | Elevation of privilege | права workflow-токена | low | mitigate | permissions: contents: read на уровне workflow (форма скилла) — минимальные права по умолчанию |
| T-05-SC | Tampering | установка инструмента в CI (go run golang.org/x/vuln/cmd/govulncheck@latest) | low | mitigate | package-legitimacy: официальный first-party модуль Go team (не npm/pip/cargo реестр), доставка через go module proxy с checksum-верификацией; новых runtime-зависимостей демону план не добавляет |
</threat_model>

<verification>
- Локально зелёный тот же путь, что и CI (кроме network-side govulncheck): go vet ./... && go build ./... && go test -race ./... && go mod tidy && git diff --exit-code -- go.mod go.sum
- Структурно: workflow содержит все гейты скилла, go-version-file, минимальные permissions
- Отклонение golangci-lint видимо в AGENTS.md (переносится регенерацией из .planning/codebase/CONVENTIONS.md)
</verification>

<success_criteria>
- TEST-01 (инфраструктурная часть): headless CI гоняет unit/golden-корпус фазы на каждый push — зелёный
- INST-04 (документационная часть): предупреждение о трассировке клавиш опубликовано в README
- INTEG-03 (dev-часть): dev systemd user unit после graphical-session.target, установка и работа без root
- Гейты скилла go-ultimate: tidy и govulncheck восстановлены; единственное отклонение (golangci-lint) задокументировано в AGENTS.md
</success_criteria>

## Artifacts this phase produces

- CI `.github/workflows/test.yml` (полный гейт-набор скилла: build / vet / test -race -cover / tidy / govulncheck; go-version-file)
- dev-юнит `dist/systemd/user/goswitchd.service`
- README-раздел «Режим отладки и конфиденциальность» + dev-инструкция запуска
- Запись в `.planning/codebase/CONVENTIONS.md` → раздел Conventions в AGENTS.md (отклонение golangci-lint)

<output>
Create .planning/phases/01-adr-paket-i-karkas-ibus-dvizhka/01-05-SUMMARY.md when done
</output>
