---
phase: 04-postavka-i-priemka
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/e2e-matrix.yml
  - test/e2e/focus_helper.py
  - scripts/d48-nightly-dispatch.sh
  - docs/ci-runner.md
autonomous: true
gap_closure: true
gap_ids: [G-4-2]
requirements: [INST-01]
# scope_note: план доставляет переопределённый владельцем формальный гейт D-48 (G-4-2, 04-UAT.md —
# директива 2026-09-17 «придумай, как тестировать это без участия человека»): автономный ночной
# прогон. requirements указывает INST-01 как ближайшее фазовое покрытие (прецедент scope_note
# 04-05/04-06/04-08).

estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Workflow e2e-matrix падает БЫСТРО и внятно на занятом столе: шаг idle-desktop preflight идёт для ВСЕХ триггеров ПЕРЕД fresh-session-префлайтом, вызывает новый режим focus_helper.py focused-app и при ответе ≠ «(none)» роняет job сообщением «desktop busy: <app> holds focus — the matrix needs an idle desktop» вместо 31 непонятного witness-падения (G-4-2, п.1 fix direction)"
    - "Soft mode для schedule: когда триггер — schedule и idle/fresh-session префлайт не прошёл, job завершается НЕЙТРАЛЬНО (::warning::-аннотация + D48_SKIP=1 + exit 0), шаги прогонов и выгрузка артефакта пропущены; workflow_dispatch сохраняет жёсткий гейт. Падение САМИХ прогонов при пройденных префлайтах остаётся красным для всех триггеров (ночной красный = регрессионный сигнал, не шум)"
    - "Триггер schedule с ночным cron (01:10 UTC) подразумевает fresh_session-семантику (условие fresh-session-префлайта включает github.event_name == 'schedule'); комментарий в workflow фиксирует: schedule исполняет определение DEFAULT branch — активируется после мержа фазы в main (до мержа работает machine-side диспетчер через D48_REF)"
    - "focus_helper.py имеет режим focused-app: имя приложения, держащего сфокусированный фрейм (той же focus-first логикой фреймов-до-спуска из 04-08, без спуска в поддеревья и без полного обхода), либо «(none)», когда сфокусированного фрейма нет нигде; живо доказан на текущем столе (прецедент 04-04/04-08 — headless RED невозможен)"
    - "scripts/d48-nightly-dispatch.sh: минимальный bash (set -euo pipefail), гвард gh (presence + gh auth status), exec gh workflow run e2e-matrix.yml --repo Djarvur/goswitch --ref \"${D48_REF:-main}\" -f matrix=test/e2e/cases/matrix-v3.yaml -f fresh_session=true; lint-гейт — bash -n (shellcheck в mise [tools] НЕТ — новых tool-зависимостей не заводить)"
    - "docs/ci-runner.md содержит раздел «Автономный ночной гейт (D-48 v2)»: три copy-paste блока разовой машинной настройки (GDM autologin /etc/gdm3/custom.yaml; root-таймер 03:50 systemctl restart gdm; systemd USER units — раннер на graphical-session.target + oneshot-диспетчер сессии с ExecStart скрипта диспатча), заметный security-нотаутологин, note о семантике доказательства (автоматный зелёный двойной прогон на автологин-свежей сессии; fresh_session-префлайт остаётся машинным свидетелем свежести)"
    - "Ничего в репозитории не исполняет sudo/root: root-блоки (custom.yaml, системные юниты/таймер) — документация для ручного применения владельцем; план НЕ исполняет машинные шаги (юниты, gdm restart, gh workflow run)"
  artifacts:
    - path: test/e2e/focus_helper.py
      provides: "режим focused-app (cmd_focused_app): имя приложения сфокусированного фрейма или «(none)»; docstring-таблица и usage-строка дополнены; контракт idle-префлайта задокументирован"
    - path: .github/workflows/e2e-matrix.yml
      provides: "schedule-cron 01:10 UTC + idle-desktop preflight (все триггеры, fail-fast «desktop busy», soft mode для schedule) + soft-armed fresh-session preflight + D48_SKIP-гейт на шагах прогонов и артефакта; двойной прогон v3 и матричный выбор не тронуты"
    - path: scripts/d48-nightly-dispatch.sh
      provides: "ин-репо диспатч-скрипт гейта: gh-гвард, D48_REF:-main, matrix-v3 + fresh_session=true; exec-бит"
    - path: docs/ci-runner.md
      provides: "раздел «Автономный ночной гейт (D-48 v2)»: три блока разовой настройки + security-ноута + семантика доказательства; устаревший пункт «Триггер — workflow_dispatch только» обновлён"
  key_links:
    - from: .github/workflows/e2e-matrix.yml (idle-desktop preflight)
      to: test/e2e/focus_helper.py (focused-app)
      via: "/usr/bin/python3 test/e2e/focus_helper.py focused-app — «(none)» = стол свободен, иначе «desktop busy: <app>»"
      why: "один и тот же AT-SPI-примитив отвечает и за 31 кейс матрицы, и за быстрый вердикт о занятости стола"
    - from: systemd user unit goswitch-d48-dispatch (документирован в docs/ci-runner.md)
      to: scripts/d48-nightly-dispatch.sh
      via: "ExecStart=%h/goswitch/scripts/d48-nightly-dispatch.sh (EnvironmentFile=-%h/.config/goswitch/d48-dispatch.env задаёт D48_REF)"
      why: "свежая сессия сама диспатчит гейт — человек не набирает команду никогда"
    - from: root timer d48-nightly-relogin.timer (03:50)
      to: GDM autologin (custom.yaml)
      via: "systemctl restart gdm → autologin → graphical-session.target → user-юниты (раннер + диспетчер)"
      why: "единственный нечеловеческий путь к свежей графической сессии; оба конца применяет владелец руками"
---

<objective>
Закрыть G-4-2: формальный гейт D-48 больше не требует человека в цикле (директива владельца 2026-09-17: «придумай, как тестировать это без участия человека»).

Purpose: приёмочное доказательство D-48 («матрица зелёная дважды подряд на нетронутой сессии») становится автономным ночным прогоном — GDM autologin + root-таймер пересоздают сессию, systemd user-юниты поднимают раннер и диспатчер, свежая сессия сама диспатчит `e2e-matrix` с `fresh_session=true`; ин-репо часть (idle-preflight, schedule, диспатч-скрипт, документация) делает прогон быстрым, внятным и безопасным на занятом столе.

Output: изменённые `.github/workflows/e2e-matrix.yml` и `test/e2e/focus_helper.py`, новый `scripts/d48-nightly-dispatch.sh`, новый раздел в `docs/ci-runner.md`. Машинные шаги (autologin, root-таймер, user-юниты) план только ДОКУМЕНТИРУЕТ — их применяет владелец; живое доказательство плана — режим focused-app на текущем столе.
</objective>

<execution_context>
@/home/nil/DiskD/W/Djarvur/goswitch/.zcode/gsd-core/workflows/execute-plan.md
@/home/nil/DiskD/W/Djarvur/goswitch/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-postavka-i-priemka/04-UAT.md (G-4-2 — полный диагноз, evidence, fix direction; диагноз НЕ пере-проводить, скоуп НЕ расширять — ровно пять пунктов из постановки)
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-postavka-i-priemka/04-PATTERNS.md (§ focus_helper.py — конвенции хелпера, docstring-таблица как контракт-документация; § e2e-matrix.yml — конвенции workflow)
@.github/workflows/e2e-matrix.yml (текущий гейт: fresh-session префлайт, два прогона, concurrency e2e-gnome, заголовочный комментарий с T-02-07-01)
@test/e2e/focus_helper.py (focused_frame_witness :183-225 — фреймы-до-спуска, переиспользуемая форма; docstring-таблица :1-93; dispatch main() :568-599)
@docs/ci-runner.md (раздел «D-48 double-run gate», раздел systemd user unit — прецедент юнита раннера, раздел «Безопасность»)
</context>

<tasks>

<task type="auto">
  <name>Task 1: focus_helper.py — режим focused-app (примитив idle-префлайта)</name>
  <files>test/e2e/focus_helper.py</files>
  <read_first>test/e2e/focus_helper.py — focused_frame_witness (:183-225, форма «фреймы-до-спуска» из 04-08), applications() (:118-125), docstring-таблица субкоманд (:1-93), main() dispatch и usage-строка (:568-599); .planning/phases/04-postavka-i-priemka/04-08-SUMMARY.md (контекст focus-first фикса G-4-1)</read_first>
  <action>
    Добавить cmd_focused_app(): обход applications(); для каждого приложения взять ПРЯМЫХ детей (фреймы) в том же best-effort try/except-обёрточном стиле, что focused_frame_witness; на каждом фрейме проверить состояние Atspi.StateType.FOCUSED; первый же сфокусированный фрейм — напечатать имя приложения (app.get_name(), а при пустом — «(unnamed)») и вернуть None. Спуска в поддеревья фрейма НЕТ и полного обхода НЕТ: вопрос «занят ли стол» решается на уровне фреймов (та же дисциплина «фреймы до спуска», что 04-08; full_walk_witness остаётся эксклюзивом witness-режима). Если ни у одного приложения нет сфокусированного фрейма — напечатать «(none)».
    Упрощение по постановке осознанное: shell/mutter-поверхности НЕ различаются на «модальные/немодальные» — печатается имя любого приложения со сфокусированным фреймом, «(none)» при отсутствии; интерпретация ответа — забота вызывающего (workflow-префлайта), не хелпера.
    Дополнить docstring-таблицу субкоманд записью для focused-app: назначение (примитив idle-desktop preflight'а e2e-matrix, G-4-2/план 04-09), контракт «имя приложения сфокусированного фрейма либо (none)», и что режим живо-проверяемый (gi/AT-SPI требует живой сессии — прецедент 04-04/04-08).
    Добавить dispatch-плечо в main(): len(argv) == 2 и argv[1] == "focused-app"; расширить usage-строку. Другие режимы, walk(), full_walk_witness и exhaustive-контракты не трогать.
    TDD-дисциплина: режим НЕ покрыт headless RED — python-поведение хелпера живо-доказываемое по домовому прецеденту (gi/Atspi недоступен headless, тестовой py-инфраструктуры в репо нет); пиннуемое — py_compile + живой прогон в verify.
  </action>
  <verify>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && /usr/bin/python3 -m py_compile test/e2e/focus_helper.py && out1=$(/usr/bin/python3 test/e2e/focus_helper.py focused-app) && out2=$(/usr/bin/python3 test/e2e/focus_helper.py focused-app) && [ -n "$out1" ] && [ "$out1" = "$out2" ] && printf 'FOCUSED-APP-OK: %s\n' "$out1" && mise run ci</automated>
    <fails_when>ноль строк, больше одной строки, расхождение двух вызовов подряд, некомпилируемый py или красный mise run ci; на живом столе владельца ожидаем имя реально сфокусированного приложения (сейчас стол занят — «(none)» тоже валидный ответ, если ни один фрейм не сфокусирован)</fails_when>
  </verify>
  <done>focused-app стабильно печатает ровно одну строку — имя приложения, держащего сфокусированный фрейм, либо «(none)»; docstring-таблица и usage дополнены; живой вывод (значение) зафиксирован в SUMMARY; mise run ci зелёный.</done>
</task>

<task type="auto">
  <name>Task 2: e2e-matrix.yml — ночной schedule + idle-desktop preflight с soft mode</name>
  <files>.github/workflows/e2e-matrix.yml</files>
  <read_first>.github/workflows/e2e-matrix.yml (весь файл: блок on:, заголовочный комментарий с T-02-07-01, fresh-session preflight, два шага прогона, upload-artifact); docs/ci-runner.md раздел «D-48 double-run gate»; 04-UAT.md G-4-2 fix direction пп. 1 и «soft mode»</read_first>
  <action>
    Зависит от Task 1 (префлайт вызывает focused-app).
    Блок on:: добавить триггер schedule с cron "10 1 * * *" и русским комментарием: cron в UTC; ночной автономный гейт D-48 (G-4-2, план 04-09); scheduled workflows исполняют ОПРЕДЕЛЕНИЕ default branch — активируется после мержа фазы в main (до мержа гейт диспатчит machine-side диспетчер из docs/ci-runner.md через D48_REF); слот выровнен под root-таймер 03:50 machine-local (UTC+3) из документации — ~20 минут после автологина, внутри 30-минутного окна свежести; schedule подразумевает fresh_session-семантику.
    Обновить устаревший заголовочный комментарий: «триггер — ТОЛЬКО workflow_dispatch» → workflow_dispatch + ночной schedule; соображение T-02-07-01 в силе — push/PR-триггеров по-прежнему нет, schedule исполняет только определение защищённого default branch, диспатч остаётся правом ролей с write.
    Новый шаг «idle-desktop preflight (G-4-2)» СРАЗУ после actions/checkout (до Install mise — python3-gi машинный, ставить инструменты ради вердикта о занятости стола не нужно), БЕЗ if: — исполняется для всех триггеров. Скрипт: запустить /usr/bin/python3 test/e2e/focus_helper.py focused-app, снять вывод; триггер передавать через env GITHUB_EVENT_NAME (канон WR-04 — без ${{ }}-интерполяции в shell). Если ответ ≠ «(none)»: при GITHUB_EVENT_NAME = schedule — напечатать ::warning:: с текстом «desktop busy: <app> holds focus — the matrix needs an idle desktop» (суффикс про soft mode), дописать D48_SKIP=1 в GITHUB_ENV и выйти 0; иначе — ::error:: с тем же текстом и exit 1. При «(none)» — строка «desktop idle» и дальше. Литеральные формы (D48_SKIP=1, текст «the matrix needs an idle desktop») держать ТОЛЬКО в исполняемых строках, не в комментариях — на них завязаны count-гейты verify.
    Fresh-session preflight: условие шага расширить до inputs.fresh_session == true || github.event_name == 'schedule'. Три точки отказа внутри (нет графической сессии; непарсимый Timestamp; возраст > D48_MAX_SESSION_AGE_SEC) свернуть в одну локальную shell-функцию с сообщениями БЕЗ изменений семантики для диспатча: при GITHUB_EVENT_NAME = schedule — ::warning:: с тем же текстом + D48_SKIP=1 + exit 0 (нейтральный ночной исход, пока машинная настройка не применена), иначе — прежний ::error:: + exit 1.
    Гейт skip на последующих шагах: оба шага прогонов (run #1, run #2) получают if: env.D48_SKIP != '1'; шаг Upload e2e report — if: always() && env.D48_SKIP != '1' (иначе upload-artifact v4 упадёт на отсутствующем файле и окрасит нейтральный scheduled-прогон). Падение САМИХ прогонов soft mode не ловит — при пройденных префлайтах любое падение прогона краснит job при любом триггере (ночной красный = регрессионный сигнал).
    Выбор матрицы и двойной прогон не менять: для schedule inputs.matrix пуст и существующий if/elif/else уже выбирает v3; шаги run #1/#2 остаются подряд без вмешательства (D-48/T-04-06-02); concurrency e2e-gnome без cancel-in-progress — второй диспатч (диспетчер сессии + cron) встаёт в очередь.
  </action>
  <verify>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && grep -q 'cron: "10 1 \* \* \*"' .github/workflows/e2e-matrix.yml && grep -q "focused-app" .github/workflows/e2e-matrix.yml && grep -q "github.event_name == 'schedule'" .github/workflows/e2e-matrix.yml && grep -q "D48_SKIP=1" .github/workflows/e2e-matrix.yml && [ "$(grep -c 'the matrix needs an idle desktop' .github/workflows/e2e-matrix.yml)" = "2" ] && [ "$(grep -c "env.D48_SKIP != '1'" .github/workflows/e2e-matrix.yml)" = "3" ] && [ "$(grep -c 'mise run e2e-matrix-v3' .github/workflows/e2e-matrix.yml)" = "2" ] && echo NIGHTLY-WIRED && mise run ci</automated>
    <fails_when>нет NIGHTLY-WIRED: отсутствует cron/schedule-условие/D48_SKIP-механика; «the matrix needs an idle desktop» не ровно 2 (error+warning arms); гейт env.D48_SKIP не ровно на 3 шагах (run #1, run #2, upload); двойной прогон v3 повреждён (count ≠ 2); mise run ci красный. Поведение schedule-vs-dispatch headless не проверяемо — проверяется структурой + живым диспатчем владельца</fails_when>
  </verify>
  <done>Workflow несёт ночной schedule (fresh_session-семантика подразумевается), idle-preflight идёт первым для всех триггеров и роняет диспатч внятным «desktop busy» на занятом столе, scheduled-провалы префлайтов завершаются нейтрально (warning + exit 0, прогон и артефакт пропущены), диспатч сохраняет жёсткий гейт, двойной прогон v3 не тронут, mise run ci зелёный.</done>
</task>

<task type="auto">
  <name>Task 3: диспатч-скрипт scripts/d48-nightly-dispatch.sh + docs «Автономный ночной гейт (D-48 v2)»</name>
  <files>scripts/d48-nightly-dispatch.sh, docs/ci-runner.md</files>
  <read_first>docs/ci-runner.md (весь файл: разделы systemd user unit, «D-48 double-run gate», «Безопасность» — устаревающий пункт «Триггер — workflow_dispatch только»); .github/workflows/e2e-matrix.yml после Task 2 (cron-слот, soft mode — на них ссылается документация); docs/ci-runner.md юнит goswitch-ci-runner.service как прецедент формы user-юнита</read_first>
  <action>
    Создать scripts/d48-nightly-dispatch.sh (каталог scripts/ новый), chmod +x. Минимальный bash без излишеств — shellcheck в mise [tools] НЕТ, новых tool-зависимостей не заводить, lint-гейт — bash -n (по постановке). Шапка-комментарий: назначение (G-4-2/план 04-09, диспетчер сессии гоняет свежую сессию — человек не набирает команду), ссылка на docs/ci-runner.md. Тело: set -euo pipefail; ref="${D48_REF:-main}" (комментарий: до мержа фазы override-файл задаёт фаза-ветку, дефолт main — пост-мерж состояние); гвард: command -v gh и gh auth status с именованными сообщениями в stderr и exit 1; затем exec gh workflow run e2e-matrix.yml --repo Djarvur/goswitch --ref "$ref" -f matrix=test/e2e/cases/matrix-v3.yaml -f fresh_session=true (--repo обязателен: юнит стартует с произвольным cwd, gh не резолвит репозиторий из $HOME).
    В docs/ci-runner.md добавить раздел «Автономный ночной гейт (D-48 v2)» (план 04-09, G-4-2) после раздела «D-48 double-run gate»:
    - Вводка: директива владельца 2026-09-17 — убрать человека из цикла; цепочка: root-таймер 03:50 restart gdm → GDM autologin → свежая сессия → graphical-session.target поднимает user-юниты (раннер + диспетчер) → диспетчер через ~2 мин диспатчит гейт с fresh_session=true.
    - Заметный security-нотаут: autologin на личной машине — осознанный tradeoff владельца (любой у консоли входит в сессию; владелец применяет зная это); НИЧЕГО в репозитории не исполняет sudo — все root-файлы из блоков ниже владелец применяет руками.
    - Блок 1 — GDM autologin: /etc/gdm3/custom.yaml, секция [daemon]: AutomaticLoginEnable=true и AutomaticLogin=<логин владельца>; строка про tradeoff рядом.
    - Блок 2 — root-таймер: /etc/systemd/system/d48-nightly-relogin.service (Unit: Description; Service: Type=oneshot, ExecStart=/usr/bin/systemctl restart gdm) и /etc/systemd/system/d48-nightly-relogin.timer (OnCalendar=*-*-* 03:50:00, [Install] WantedBy=timers.target); команды systemctl daemon-reload, systemctl enable --now d48-nightly-relogin.timer, проверка systemctl list-timers; примечание: restart gdm разбирает сессию, autologin возвращает пользователя, user-юниты стартуют сами.
    - Блок 3 — systemd USER units: (а) раннер — существующий goswitch-ci-runner.service из раздела выше уже висит на graphical-session.target (After=, Restart=on-failure) и ПЕРЕИСПОЛЬЗУЕТ вручную зарегистрированный каталог ~/actions-runner — достаточно systemctl --user enable goswitch-ci-runner; (б) диспетчер сессии — ~/.config/systemd/user/goswitch-d48-dispatch.service: [Unit] Description + PartOf=graphical-session.target; [Service] Type=oneshot, ExecStartPre=/usr/bin/sleep 120 (гвард ~2 мин — дать столу и раннеру устояться), ExecStart=%h/goswitch/scripts/d48-nightly-dispatch.sh (примечание: подставить фактический путь рабочей копии репозитория на этой машине), EnvironmentFile=-%h/.config/goswitch/d48-dispatch.env (опционально, вне git: строка D48_REF=<ветка> для до-мерж диспатчей, дефолт main); [Install] WantedBy=graphical-session.target; команды systemctl --user daemon-reload / enable goswitch-d48-dispatch и проверка journalctl --user -u goswitch-d48-dispatch.
    - Ин-репо пояс (post-merge): workflow-cron 01:10 UTC — второй, избыточный путь (GitHub сам диспатчит определение default branch); concurrency e2e-gnome ставит его в очередь за диспетчер-прогоном; мягкий режим делает незадачливый слот нейтральным (::warning:: + exit 0); примечание про UTC против machine-local 03:50 (при другом часовом поясе — сдвинуть cron).
    - Семантика доказательства: формальное доказательство D-48 = автоматный зелёный двойной прогон на автологин-свежей сессии; fresh_session-префлайт (loginctl ≤ 30 мин) остаётся МАШИННЫМ свидетелем свежести — по директиве владельца 2026-09-17. До применения машинной настройки scheduled-прогоны завершаются нейтрально (мягкий режим) — без красного ночного шума.
    Обновить устаревший пункт раздела «Безопасность»: «Триггер — workflow_dispatch только» → workflow_dispatch + ночной schedule (исполняет только определение default branch — защищённая ветка, изменения через PR; fork/push-триггеры отсутствуют, T-02-07-01 в силе); дополнить: юнит диспетчера исполняет только ин-репо скрипт с gh-гвардом, root-часть — исключительно ручное применение владельцем.
    TDD-дисциплина: workflow-yaml, systemd-доки и shell-сетап не юнит-тестируемы headless; пиннуемое — bash -n, exec-бит, структурные greps (по постановке). Машинные шаги (применение юнитов, gdm restart, живой gh workflow run) план НЕ исполняет.
  </action>
  <verify>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && bash -n scripts/d48-nightly-dispatch.sh && test -x scripts/d48-nightly-dispatch.sh && grep -q "gh auth status" scripts/d48-nightly-dispatch.sh && grep -q -- "--repo Djarvur/goswitch" scripts/d48-nightly-dispatch.sh && grep -q "fresh_session=true" scripts/d48-nightly-dispatch.sh && grep -q 'D48_REF:-main' scripts/d48-nightly-dispatch.sh && grep -q 'Автономный ночной гейт (D-48 v2)' docs/ci-runner.md && grep -q "AutomaticLoginEnable" docs/ci-runner.md && grep -q "d48-nightly-relogin" docs/ci-runner.md && grep -q "goswitch-d48-dispatch" docs/ci-runner.md && grep -q "D48_REF" docs/ci-runner.md && mise run ci && echo D48-V2-WIRED</automated>
    <fails_when>нет D48-V2-WIRED: скрипт отсутствует/не исполняемый/не проходит bash -n/нет gh-гварда или fresh_session=true; в docs нет раздела D-48 v2 или одного из трёх блоков настройки; mise run ci красный</fails_when>
  </verify>
  <done>Скрипт существует, исполняемый, bash -n чист, гвардит gh-аутентификацию и диспатчит matrix-v3 + fresh_session=true на ${D48_REF:-main} через --repo; docs несут раздел «Автономный ночной гейт (D-48 v2)» с тремя copy-paste блоками, security-ноутом и семантикой доказательства; устаревший пункт «Триггер» обновлён; mise run ci зелёный.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions (schedule/dispatch) → живой стол владельца | workflow исполняет код репозитория на машине владельца; множество триггеров определяет, кто может это запустить |
| root-часть машинной настройки (доки) → сессия | root-юниты/таймер применяет владелец руками; репозиторий возит только документацию |
| юнит диспетчера сессии → gh workflow run | нечеловеческий аутентифицированный диспатч из сессии владельца |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-09-01 | Elevation/Information Disclosure | GDM autologin + unattended-сессия (root-таймер + user-юниты) | high | accept | Осознанный tradeoff владельца на личной машине — применяется вручную из docs/ci-runner.md «Автономный ночной гейт (D-48 v2)» с проговорённым tradeoff; НИЧЕГО в репозитории не исполняет sudo; существующие контролы раннера в силе (регистрация только в Djarvur/goswitch, runner-group рекомендация) |
| T-04-09-02 | Tampering (расширение триггерной поверхности) | e2e-matrix schedule-триггер | low | mitigate | Schedule исполняет ТОЛЬКО определение default branch (защищённая ветка, изменения через PR); push/PR-триггеры остаются отсутствующими (T-02-07-01); permissions: contents: read и concurrency e2e-gnome не меняются |
| T-04-09-03 | Denial of Service (ночной красный шум, двойной диспатч) | cron + диспетчер сессии | low | mitigate | Soft mode для scheduled-префлайтов (::warning:: + D48_SKIP + exit 0); concurrency без cancel-in-progress ставит второй диспатч в очередь; D48_SKIP-гейт пропускает прогоны и артефакт, сохраняя нейтральный вывод |
</threat_model>

<verification>
- NIGHTLY-WIRED (Task 2) и D48-V2-WIRED (Task 3) — структурные гейты workflow/скрипта/доков зелёные
- /usr/bin/python3 -m py_compile test/e2e/focus_helper.py + живой смок focused-app (два стабильных вызова; значение зафиксировано в SUMMARY) — Task 1
- mise run ci зелёный в каждой задаче (директива 2 — зелёная итерация)
- Граница плана: машинные шаги (custom.yaml, root-таймер, user-юниты, gdm restart, живой gh workflow run, формальный ночной прогон) планом НЕ исполняются — владелец применяет их из docs/ci-runner.md ПОСЛЕ плана/мержа; это и есть переопределённое доказательство D-48
</verification>

<success_criteria>
G-4-2 закрыт в объёме постановки (5 пунктов fix direction): (1) schedule-cron с подразумеваемой fresh_session-семантикой и комментарием про default branch post-merge; (2) idle-desktop preflight для всех триггеров перед fresh-session-префлайтом — fail-fast «desktop busy: <app> holds focus — the matrix needs an idle desktop», мягкий режим для schedule (warning + exit 0, без красного ночного шума), жёсткий гейт для диспатча; (3) focused-app в focus_helper.py — фреймы-до-спуска, «(none)» при пустом столе, живо доказан; (4) scripts/d48-nightly-dispatch.sh — bash -n чист, gh-гвард, D48_REF:-main, matrix-v3 + fresh_session=true, --repo; (5) docs «Автономный ночной гейт (D-48 v2)» — три блока разовой настройки, security-ноута, семантика доказательства. Двойной прогон v3 и продуктовый код не тронуты; зелёная итерация (директива 2) соблюдена. gap_ids: [G-4-2].
</success_criteria>

<output>
Create `.planning/phases/04-postavka-i-priemka/04-09-SUMMARY.md` when done
</output>
