---
phase: 04-postavka-i-priemka
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/install/install.go
  - internal/install/install_test.go
  - cmd/goswitchctl/main.go
  - test/e2e/case_install.go
  - test/e2e/main.go
  - mise.toml
autonomous: true
requirements: [INST-01]

estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`goswitchctl install` ставит движок без root одним запуском (D-39): component XML в `~/.config/ibus/component/goswitch.xml`, systemd user unit в `~/.config/systemd/user/goswitchd.service`, компонент в кеше реестра (env write-cache), юнит active, прежние источники сохранены, input-sources = только goswitch (D-40), активация `ibus engine goswitch-en` — живой install-cycle доказывает всю цепочку"
    - "КАЖДЫЙ запуск `ibus write-cache` из goswitch несёт `IBUS_COMPONENT_PATH=$HOME/.config/ibus/component` — юнит-пин корня Падения 1 (голый write-cache выселяет user-компонент из кеша)"
    - "`goswitchctl uninstall` — полный откат (D-42): stop/disable юнита, юнит и XML удалены, кеш перезаписан с env, источники ВОССТАНОВЛЕНЫ из сохранённых с машинной сверкой readback, state-файл удалён; `~/.config/goswitch/` по умолчанию НЕ трогается, `--purge` удаляет и его"
    - "install идемпотентен: повторный install поверх установленного НЕ перезаписывает исходный бэкап источников и не ломает состояние; повреждённый/отсутствующий state-файл при uninstall → безопасный фолбэк `[('xkb', 'us')]` с отчётом — клавиатура не кирпичится (ASVS V5)"
    - "Живой install-cycle e2e: install → юнит active → движок реально зарегистрирован (ListActiveEngines) → коррекция `ghbdtn`→`привет` через УСТАНОВЛЕННОГО unit-демона (спавн демона стенда отключён — single-instance ctlsvc) → `goswitchctl status` отвечает → повторный install безвреден → uninstall → стол восстановлен (verifyRestored-стиль)"
    - "Путь демона в юните — абсолютный путь `goswitchd` рядом с `goswitchctl` (dirname(os.Executable)); отсутствие goswitchd — внятный отказ с подсказкой, никаких `%h` и поиска по $PATH (ASVS V14)"
  artifacts:
    - path: internal/install/install.go
      provides: "пакет install: Runner-шов (ctx, name, args, env, stdin) + execRunner + дедлайн на вызов; Install(ctx) — полная последовательность D-39/D-40; Uninstall(ctx, purge) — полный откат D-42; writeAtomic(path, data, perm); рендер component XML (зеркало wire-идентичности, ПУСТОЙ <exec>) и user unit (абсолютный ExecStart)"
    - path: internal/install/install_test.go
      provides: "корпус на fakeRunner (записывает argv+env) + t.TempDir как $HOME: последовательность, env-пин каждого write-cache, идентичность XML/юнита, идемпотентность, corrupt-state фолбэк"
    - path: cmd/goswitchctl/main.go
      provides: "подкоманды install / uninstall [--purge] с собственным бюджетом времени (НЕ ctlCallTimeout); обновлённый errUsage"
    - path: test/e2e/case_install.go
      provides: "живой кейс install-cycle реестра pickCase: полный жизненный цикл через установленного демона"
  key_links:
    - from: cmd/goswitchctl/main.go
      to: internal/install/install.go
      via: "тонкий клиент: CLI только парсит флаги и печатает, вся логика в пакете (PATTERNS § cmd/goswitchctl)"
      pattern: "install\\.(Install|Uninstall)\\("
    - from: internal/install/install.go
      to: "ibus write-cache (env)"
      via: "IBUS_COMPONENT_PATH на КАЖДОМ вызове — Падение 1; env строится с дисциплиной withEnv (позже-дубликаты не воскресают)"
      pattern: "IBUS_COMPONENT_PATH"
    - from: "install save (install-state.json)"
      to: "uninstall restore"
      via: "state-файл ~/.local/share/goswitch/install-state.json (0600, атомарно) — пара save/restore один контракт; НИКОГДА в ~/.config/goswitch (Падение 8)"
      pattern: "install-state\\.json"
  prohibitions:
    - statement: "install/uninstall НЕ ДОЛЖНЫ писать или требовать что-либо вне $HOME — ни root, ни системные пути; нарушение ломает без-root модель INTEG-03 и публичное обещание установки"
      status: flagged
      verification: judgment
    - statement: "Установщик НЕ ДОЛЖЕН трогать чужие компоненты IBus и системные юниты: env write-cache сканирует весь user-каталог — чужие user-компоненты переживают наш install/uninstall; системный /usr/share/ibus/component не читается-не пишется"
      status: flagged
      verification: judgment
    - statement: "Uninstall НЕ ДОЛЖЕН оставлять стол без ввода: восстановление источников обязано валидировать форму сохранённого значения ДО gsettings set и падать в безопасный xkb-фолбэк при любом сомнении"
      status: flagged
      verification: judgment
  assumptions:
    - "FLAGGED (probe INST-01 / unclassified — у фазы нет SPEC-покрытия рёбер): приёмка «чистая Ubuntu 24.04» эмулируется живым столом без goswitch-артефактов (research Runtime State Inventory: юнит не установлен, ~/.config/goswitch нет, 0 тегов) + e2e-снимок/восстановление; действительно чистая машина — ручная сессия владельца по README (D-49, UAT); владелец подтверждает трактовку на verify-гейте"
    - "FLAGGED (research A5): `gsettings set org.gnome.desktop.input-sources sources \"[('ibus', 'goswitch-en')]\"` принимается с ibus-записью — формат следует верифицированной xkb-паре, живьём в этой сессии не прогонялся; первый живой прогон install-cycle — решающая проба, при отказе установка активирует движок через `ibus engine` и источник переключается репортом (эскалация владельцу, не тихий обход)"
    - "FLAGGED (research A6): `ibus restart` подхватывает cache-written user-компоненты (цепочка fresh-XML→env-cache→restart→list-engine не прогонялась одним зондом); финальный шаг install — bounded-wait ListActiveEngines — немедленно вскрывает ложность, если это не так"
    - "FLAGGED (research A3 / Pitfall 4): one-shot write-cache same-second flake (mtime-гонка) лечится verify+один повтор в install; если флейк систематический — цикл проверки переключается на содержимое кеша, а не list-engine"
---

<objective>
Трассер фазы: `goswitchctl install` ставит goswitch как IBus engine + systemd user service без root и приводит стол к «одному хозяину» (D-39/D-40), а `goswitchctl uninstall` полностью откатывает установку (D-42) — живой install-cycle e2e гоняет весь жизненный цикл через установленного демона. Это INST-01-ядро, на которое надстраиваются selfcheck (04-02), релиз-каналы (04-03) и доки (04-07).

**As a** пользователь без root-доступа, **I want** поставить goswitch одной командой `goswitchctl install` и убрать его `goswitchctl uninstall` без следа, **so that** инструмент не требует администратора и не ломает мою среду, если я решу его удалить.

Purpose: INST-01 — фундамент поставки; все живые находки research (Падения 1/2/4/8/9) запираются юнит-корпусом и живым кейсом.
Output: internal/install (пакет + корпус), подкоманды install/uninstall у goswitchctl, живой кейс install-cycle + mise-задача e2e-install-cycle.
</objective>

<execution_context>
@/home/nil/DiskD/W/Djarvur/goswitch/.zcode/gsd-core/workflows/execute-plan.md
@/home/nil/DiskD/W/Djarvur/goswitch/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-postavka-i-priemka/04-CONTEXT.md (D-39, D-40, D-42 — заперты)
@.planning/phases/04-postavka-i-priemka/04-RESEARCH.md (§ Pattern 1/2, § Pitfalls 1/2/4/8/9, § Code Examples install-sequence, § Runtime State Inventory)
@.planning/phases/04-postavka-i-priemka/04-PATTERNS.md (§ internal/install, § internal/install/*_test.go, § cmd/goswitchctl, § test/e2e/main.go install-cycle, § dist/systemd/user/goswitchd.service — образцы с номерами строк)
@docs/adr/ADR-001-layout-switching-mechanism.md (модель «один хозяин» — D-40)
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Трассер — internal/install (Runner-шов с env) + goswitchctl install/uninstall + живой кейс install-cycle: XML→env-cache→юнит→один-хозяин→активация, откат стола (D-39/D-40/D-42, INST-01)</name>
  <reversibility rating="costly">D-39/D-40 — публичный CLI-контракт и UX-модель установки ADR-001 (reversibility из CONTEXT); смена последовательности/семантики после публикации = поломка пользовательских установок</reversibility>
  <precondition>Живая GNOME Wayland-сессия: непусто $WAYLAND_DISPLAY, ibus-daemon отвечает (ibus address), префлайт go run ./test/e2e -case m1-gate зелёный. Юнит goswitchd НЕ установлен (systemctl --user is-active goswitchd → inactive; иначе кейс сначала снимает существующее состояние в снимок)</precondition>
  <files>internal/install/install.go, internal/install/install_test.go, cmd/goswitchctl/main.go, test/e2e/case_install.go, test/e2e/main.go, mise.toml</files>
  <read_first>
  - .planning/phases/04-postavka-i-priemka/04-RESEARCH.md (§ Pattern 1 — env-cache и верификация; § Pattern 2 — XML зеркалит wire-идентичность, пустой <exec>; § Code Examples — Install sequence; § Pitfalls 1/2/4/8/9)
  - .planning/phases/04-postavka-i-priemka/04-PATTERNS.md (§ internal/install — Runner-шов clipboard.go:43-69 с расширением env, дедлайн :134-139, execRunner :151-171, withEnv surface.go:248-258, snapshot/restore main.go:412-475, fallbackEngine main.go:499-511; § cmd/goswitchctl — диспетчер :57-84, заметка про собственный дедлайн; § dist-шаблон :10-23)
  - internal/clipboard/clipboard.go (канонический subprocess-пакет: шов, дедлайн, execRunner)
  - internal/clipboard/clipboard_test.go (fakeRunner-паттерн — запись argv, расширить env)
  - cmd/goswitchctl/main.go (текущий диспетчер, errUsage :39, ctlCallTimeout :32)
  - cmd/goswitchd/main.go (идентичность движков :135-136 — источник имён для XML)
  - engine/types.go (wire-идентичность компонента :96-101; Version «0.1.0» — схема-версия, НЕ сборочная)
  - test/e2e/main.go (run/pickCase/реестр :196-233, caseListUsage :87-93, snapshotDesktop/restoreGsettings/verifyRestored :412-475)
  - test/e2e/preflight.go (listActiveEnginesContain :178-205 — форма live-пробы для финального шага install)
  - test/e2e/case_ctl.go (кейс гоняет собранные бинарники — образец для install-cycle)
  - dist/systemd/user/goswitchd.service (семантика юнита: PartOf/After/Restart/WantedBy)
  - docs/SPEC.md §7 (принцип «полный цикл без человека»)
  </read_first>
  <behavior>
  - TestInstall_Sequence (fakeRunner, t.TempDir как $HOME): порядок вызовов = gsettings get sources → запись install-state.json → mkdir+запись component XML → `ibus write-cache` (с env) → верификация `ibus list-engine` содержит goswitch-en → запись user unit → `systemctl --user daemon-reload` → `systemctl --user enable --now goswitchd` → `ibus restart` → bounded-wait live-регистрации (шов activeEngines) → `gsettings set org.gnome.desktop.input-sources sources "[('ibus', 'goswitch-en')]"` → `ibus engine goswitch-en`
  - TestInstall_EveryWriteCacheCarriesEnv: КАЖДЫЙ записанный вызов ibus write-cache имеет env с IBUS_COMPONENT_PATH=$HOME/.config/ibus/component (дубликат-дисциплина withEnv — позже-назначения не воскресают)
  - TestInstall_ComponentXMLMirrorsWireIdentity: XML содержит name org.freedesktop.IBus.goswitch, оба движка goswitch-en (longname «goswitch English (US)», language en, layout us) и goswitch-ru («goswitch Русская», ru, ru), version 0.1.0, license MIT, author Djarvur, homepage github.com/Djarvur/goswitch, ПУСТОЙ <exec>; файл 0644, записан атомарно (writeAtomic: temp в том же каталоге + rename)
  - TestInstall_UnitAbsoluteExecStart: ExecStart = абсолютный путь goswitchd = filepath.Join(filepath.Dir(os.Executable()), "goswitchd"); семантика dist-шаблона сохранена (PartOf=graphical-session.target, After=org.freedesktop.IBus.session.GNOME.service, Restart, WantedBy); нет %h, нет shell-интерполяции
  - TestInstall_DaemonBinaryMissing: goswitchd отсутствует рядом с бинарником → ошибка с подсказкой, НОЛЬ вызовов systemctl/gsettings
  - TestInstall_StateFileOutsideConfigDir: state в ~/.local/share/goswitch/install-state.json (0600), содержит ДОСЛОВНО раннее значение sources
  - TestInstall_SecondInstallKeepsOriginalBackup: state-файл уже существует → повторный install НЕ перезаписывает его (бэкап первой установки неприкосновенен)
  - TestUninstall_FullRollback: stop → disable → удаление юнита → daemon-reload → удаление XML → env write-cache → ibus restart → gsettings set восстановленного значения → best-effort активация восстановленного источника → удаление state-файла; ~/.config/goswitch НЕ тронут
  - TestUninstall_CorruptStateFallsBack: state с мусорным значением → restore подставляет "[('xkb', 'us')]", ошибка НЕ фатальна для отката, в выводе виден фолбэк
  - Живой кейс install-cycle (Task 1-объём): сборка обоих бинарников в tmp стенда → goswitchctl install → systemctl --user is-active goswitchd == active → ibus list-engine содержит goswitch-en → ListActiveEngines содержит движок → goswitchctl uninstall → юнит/XML отсутствуют, sources равны снимку (машиная сверка)
  </behavior>
  <action>
  RED (D-07): internal/install/install_test.go — корпус behavior против пустого пакета (файл-заглушка install.go с пустыми типами для подлинного RED на утверждениях, стиль 02-03). Прогнать mise exec -- go test ./internal/install/ -race -count=1 — RED; red-evidence (gsd check tdd-red-evidence). Commit: test(04-01).
  GREEN — internal/install/install.go по образцу clipboard: тип Runner func(ctx, name, args []string, env []string, stdin []byte) ([]byte, error) (расширение шова env — Падение 1 юнит-пинится корпусом); New(opts...) с WithRunner (шов тестов) и WithHome (t.TempDir) и шовом activeEngines func(ctx) ([]string, error) (прод-реализация — dial по форме preflight.go:178-205 через engine; пакет импортирует engine для адреса — та же module, допустимо); execRunner с захватом stdout/stderr и ошибкой argv+stderr; call() с package-level дедлайном (cmdTimeout-образец; НО install-контекст живёт дольше — дедлайн НА ВЫЗОВ, а общий бюджет задаёт CLI); writeAtomic(path, data, perm).
  Install(ctx) выполняет последовательность из behavior ДОСЛОВНО (каждый шаг — именованная ошибка с подсказкой фикса, стиль префлайта): resolve home/пути; gsettings get sources (сохранить raw-строку); state-файл: существует → НЕ трогать (идемпотентный бэкап), нет → writeAtomic 0600; renderComponentXML (значения из engine/types.go:96-101 и main.go:135-136; <exec> ПУСТ — systemd единственный супервизор, двойной spawn ломит single-instance guard ctlsvc; Падение из research Pattern 2); writeAtomic 0644 в ~/.config/ibus/component/goswitch.xml (mkdir -p); ibus write-cache с env (withEnv-дисциплина surface.go:248-258); верификация: ibus list-engine содержит goswitch-en, иначе ОДИН повтор write-cache и повторная проверка (Pitfall 4), затем отказ с подсказкой; renderUnit + writeAtomic 0644 в ~/.config/systemd/user/goswitchd.service; systemctl --user daemon-reload; systemctl --user enable --now goswitchd; ibus restart; bounded-wait (≤20 s, poll ~500 ms) live-регистрации через activeEngines-шов; gsettings set sources "[('ibus', 'goswitch-en')]" (D-40 «один хозяин»); ibus engine goswitch-en (активация = SetGlobalEngine; gsettings current рантаймом игнорируется — живая находка Фазы 1).
  Uninstall(ctx, purge): systemctl --user stop/disable goswitchd (идемпотентно-терпимо к «not loaded»); удалить юнит; daemon-reload; удалить XML; env write-cache (сохраняет чужие user-компоненты); ibus restart; restore: прочитать state, валидация формы (начинается с «[», заканчивается «]», непусто) → gsettings set дословного значения, ИНАЧЕ фолбэк "[('xkb', 'us')]" с отчётом (ASVS V5); best-effort активация восстановленного источника (вывод формы fallbackEngine main.go:499-511, ошибка не фатальна); удалить state; purge=true → удалить ~/.config/goswitch и ~/.local/share/goswitch (D-42).
  CLI cmd/goswitchctl/main.go: таймаут ctlCallTimeout остаётся ТОЛЬКО на D-Bus-ветках (status/reload/correct) — перестроить run() так, чтобы install/uninstall получали собственный контекст installTimeout = 120 s (ibus restart + старт юнита; PATTERNS-заметка); case "install"/"uninstall" с flag.NewFlagSet, uninstall --purge (fs.Bool); вывод — шаг-за-шагом пути/вердикты (D-20/D-21: без пользовательского текста); errUsage расширен: usage: goswitchctl status [--json] | reload | correct | install | uninstall [--purge].
  test/e2e/case_install.go: кейс install-cycle (объём Task 1) по образцу case_ctl.go: собрать goswitchctl+goswitchd в tmp стенда (go build -o), НО спавн демона стенда ОТКЛЮЧЁН для этого кейса (single-instance: org.djarvur.goswitch и IBus-регистрация принадлежат unit-демону; executor находит место отключения в run()-флоу main.go); шаги-утверждения из behavior; весь кейс под снимком стенда (run()-defer restoreGsettings+verifyRestored) + собственный defer: uninstall best-effort при любом исходе (Pitfall 9); register "install-cycle" в pickCase + caseListUsage В ОДНОЙ правке (реестр+usage-дисциплина). mise.toml: [tasks.e2e-install-cycle] по конвенции (описание с план-рефом и трейлером «(live GNOME session; NOT in ci)»). mise run ci зелёный; живой прогон e2e-install-cycle. Commit: feat(04-01).
  </action>
  <verify>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise exec -- go test ./internal/install/ -race -count=1</automated>
    <fails_when>строка FAIL в выводе или non-zero exit — корпус install/uninstall не зелёный под -race</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise run ci</automated>
    <fails_when>non-zero exit — build/vet/golangci-lint/test -race не зелёные (D-08)</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise exec -- go test ./cmd/... ./internal/... -race -count=1</automated>
    <fails_when>non-zero exit — регресс существующих пакетов (goswitchctl-диспетчер перестроен)</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise run e2e-install-cycle</automated>
    <fails_when>non-zero exit или отсутствие PASS install-cycle в выводе; либо после uninstall sources ≠ снимку / юнит или XML остались — живая цепочка install→откат не работает</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise run e2e-ctl</automated>
    <fails_when>non-zero exit — регресс status/reload/correct после перестройки диспетчера</fails_when>
  </verify>
  <done>Install выполняет полную последовательность D-39/D-40 с env на каждом write-cache и идентичным wire-XML; uninstall полностью откатывает с валидацией state; корпус зелёный под -race; живой install-cycle проходит install→active→registered→uninstall→restored; mise run ci зелёный; история test до feat</done>
  <acceptance_criteria>
  - internal/install/install.go экспортирует Install и Uninstall; каждая запись ibus write-cache в проде и корпусе несёт IBUS_COMPONENT_PATH (греп-пин в тесте)
  - component XML: name org.freedesktop.IBus.goswitch, движки goswitch-en/goswitch-ru с longname/language/layout из main.go:135-136, ПУСТОЙ <exec>; юнит: абсолютный ExecStart без %h
  - state-файл только в ~/.local/share/goswitch/ (0600); повторный install не перезаписывает исходный бэкап
  - uninstall: corrupt state → gsettings set "[('xkb', 'us')]" + отчёт; ~/.config/goswitch сохранён без --purge, удалён с --purge
  - goswitchctl: install/uninstall не наследуют ctlCallTimeout (собственный бюджет ≥ 60 s); errUsage содержит обе подкоманды
  - install-cycle живьём: systemctl is-active = active, list-engine и ListActiveEngines видят goswitch; после uninstall — юнита/XML нет, sources восстановлены (машиная сверка)
  - e2e-ctl остаётся PASS (регресс-арест существующего CLI)
  </acceptance_criteria>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Твёрдость контура — идемпотентность install-over-install, retry write-cache (Pitfall 4), идемпотентный uninstall, юнит-хайджек-контроль (ASVS V14)</name>
  <reversibility rating="reversible">Внутренние гарантии пакета; публичный контракт не меняется</reversibility>
  <files>internal/install/install.go, internal/install/install_test.go, cmd/goswitchctl/main.go</files>
  <read_first>
  - .planning/phases/04-postavka-i-priemka/04-RESEARCH.md (§ Pitfall 4 — same-second flake; § Security Domain — V14 unit hijack: atomic overwrite + verify content hash)
  - internal/install/install.go (результат Task 1 — где укреплять)
  - internal/install/install_test.go (корпус — расширяемый fakeRunner)
  - cmd/goswitchctl/main.go (флаг --purge уже в Task 1; здесь — только если корпус вскрыл дыры проводки)
  </read_first>
  <behavior>
  - TestInstall_OverInstallIdempotent: второй install поверх установленного (fakeRunner: list-engine уже содержит goswitch, unit уже active) завершается без ошибок; state-файл НЕ перезаписан; XML/юнит перезаписаны атомарно тем же содержимым
  - TestInstall_WriteCacheRetryOnce: первый list-engine-ответ без goswitch → ровно ОДИН повторный write-cache (с env) → второй ответ с goswitch → успех; обе попытки с env; после повтора отказ — внятная ошибка с подсказкой
  - TestInstall_UnitHijackOverwritten: в ~/.config/systemd/user/goswitchd.service лежит чужое содержимое → install атомарно перезаписывает и СВЕРЯЕТ содержимое после записи (read-back == отрендеренное) — расхождение = ошибка (ASVS V14)
  - TestUninstall_Idempotent: второй uninstall при уже удалённых артефактах — зелёный no-op без ошибок
  - TestUninstall_PurgeRemovesUserConfig: purge=true удаляет ~/.config/goswitch и ~/.local/share/goswitch; purge=false сохраняет ~/.config/goswitch
  </behavior>
  <action>
  RED (D-07): расширить install_test.go кейсами behavior против текущего пакета — падения на утверждениях (повторный install сейчас перезапишет state / retry отсутствует / read-back сверки нет). Прогон — RED; red-evidence. Commit: test(04-01).
  GREEN — internal/install/install.go: retry-контур write-cache из Task 1 получает пин «ровно один повтор» (счётчик попыток, юнит-пин); после записи XML и юнита — read-back сверка содержимого (writeAtomic возвращает записанное, отдельная перечитка файла; расхождение → именованная ошибка); повторный install: state существует → skip-save (уже из Task 1, закрепить корпусом), systemctl enable --now на уже-active юните — терпимо; uninstall: tolerate «not loaded»/«not found» на каждом шаге удаления (идемпотентность). REFACTOR без смены поведения. mise run ci зелёный. Commit: feat(04-01).
  </action>
  <verify>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise exec -- go test ./internal/install/ -race -count=1</automated>
    <fails_when>строка FAIL — кейсы идемпотентности/retry/сверки не зелёные</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise run ci</automated>
    <fails_when>non-zero exit — зелёная итерация D-08 не достигнута</fails_when>
  </verify>
  <done>Идемпотентность install-over-install и повторного uninstall запинена корпусом; retry write-cache ровно один; перезапись юнита сверяется read-back; всё под -race</done>
  <acceptance_criteria>
  - install_test.go содержит TestInstall_OverInstallIdempotent, TestInstall_WriteCacheRetryOnce, TestInstall_UnitHijackOverwritten, TestUninstall_Idempotent, TestUninstall_PurgeRemovesUserConfig — все зелёные
  - retry write-cache делает ровно один повтор (счётчик вызовов в fakeRunner — пин), каждая попытка с env
  - writeAtomic + read-back сверка на XML и юните; чужое содержимое юнита не переживает install молча
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Полный жизненный цикл живьём — коррекция и статус через УСТАНОВЛЕННОГО демона, повторная установка, машинный откат (INST-01, «полный цикл без человека»)</name>
  <reversibility rating="reversible">Расширение живого кейса; контракты не меняются</reversibility>
  <precondition>Task 1/2 зелёные; живая сессия (как Task 1)</precondition>
  <files>test/e2e/case_install.go, test/e2e/main.go</files>
  <read_first>
  - test/e2e/case_install.go (кейс Task 1 — расширяемый скелет)
  - test/e2e/case_word.go (инжекция+оракул слова через движок — образец шага коррекции)
  - test/e2e/case_ctl.go (шаг статуса против живого демона)
  - test/e2e/main.go (focusWitness/readback-оракулы, не зависящие от пайпа логов демона)
  - .planning/phases/04-postavka-i-priemka/04-RESEARCH.md (§ Pitfall 9 — снимок/восстановление вокруг install- e2e)
  </read_first>
  <action>
  Расширить кейс install-cycle до полного жизненного цикла: после install-утверждений Task 1 — (1) коррекция через установленного демона: открыть entry-поверхность (zenity), injectText("ghbdtn"), двойной shift-r, readback-оракул поля == "привет" (демон стенда НЕ запущен — работает unit-демон; оракуль независим от пайпа логов); (2) goswitchctl status против unit-демона отвечает строкой с mode=; (3) повторный goswitchctl install — exit 0, стол не сломан; (4) повторная коррекция по-прежнему работает; (5) uninstall → юнит inactive/отсутствует, XML отсутствует, ibus list-engine без goswitch, sources == снимку (verifyRestored-стиль). Отказ любого шага = exit ≠ 0 кейса; defer-uninstall остаётся страховкой. Обновить caseListUsage, если менялось имя/описание (реестр+usage вместе). Живой прогон зелёный. Commit: feat(04-01).
  </action>
  <verify>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise run e2e-install-cycle</automated>
    <fails_when>non-zero exit — любая ступень цикла упала: коррекция через unit-демона, status, повторный install, откат</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise run ci</automated>
    <fails_when>non-zero exit — зелёная итерация D-08 не достигнута</fails_when>
  </verify>
  <done>Живой цикл: install → коррекция ghbdtn→привет через unit-демона → status → повторный install безвреден → повторная коррекция → uninstall → стол восстановлен; всё без участия человека</done>
  <acceptance_criteria>
  - case_install.go: шаг коррекции даёт readback "привет" через УСТАНОВЛЕННОГО демона (демон стенда не спавнится — пин в коде кейса)
  - шаг goswitchctl status отвечает mode=… против org.djarvur.goswitch, которым владеет unit-демон
  - повторный install в кейсе даёт exit 0; после него коррекция работает снова
  - после uninstall: systemctl --user is-active goswitchd ≠ active, goswitch.xml нет, list-engine без goswitch, sources равны снимку
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| установщик → OS-состояние пользователя | install/uninstall пишут в $HOME-состояние (ibus кеш, systemd user manager, gsettings) — ошибки тут ломают ввод сессии |
| state-файл → gsettings restore | сохранённое значение источников — единственный путь восстановления ввода; повреждение = кирпич клавиатуры |
| чужие инструменты → ibus-кеш | голый write-cache третьих сторон выселяет наш компонент (Падение 1) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01-01 | Tampering | юнит-хайджек (чужой pre-created goswitchd.service) | high | mitigate | атомарная перезапись + read-back сверка содержимого (ASVS V14); пин TestInstall_UnitHijackOverwritten |
| T-04-01-02 | DoS | повреждённый state-файл → клавиатура без ввода после uninstall | high | mitigate | shape-валидация ДО gsettings set + фолбэк [('xkb','us')] с отчётом (ASVS V5); пин TestUninstall_CorruptStateFallsBack |
| T-04-01-03 | Tampering | инъекция gvariant через state-файл в restore | high | mitigate | restore только значений, прошедших форму-валидацию (скобки/непустота); мусор → фолбэк, никогда дословная установка |
| T-04-01-04 | Information Disclosure | вывод install/uninstall | medium | mitigate | только пути/вердикты/причины, без пользовательского текста (D-20/D-21) |
| T-04-01-05 | DoS | выселение компонента чужим голым write-cache | medium | mitigate | selfcheck-ремонт (04-02) + README документирует команду только целиком (04-07) |
</threat_model>

<verification>
- mise run ci зелёный (build + vet + golangci-lint + test -race, D-08)
- mise exec -- go test ./internal/install/ -race -count=1 — весь корпус (последовательность, env-пин, идемпотентность, фолбэки)
- mise run e2e-install-cycle — живой полный цикл на столе владельца
- mise run e2e-ctl — регресс существующего CLI
</verification>

<success_criteria>
- INST-01-ядро доказано: установка без root одной командой, полный откат, идемпотентность — юнит-корпус + живой цикл
- Все живые ловушки research запинены: env на каждом write-cache (Падение 1), рестарт-видимость (Падение 2), retry (Падение 4), state вне ~/.config/goswitch (Падение 8), снимок-страховка (Падение 9)
- «Один хозяин» ADR-001/D-40 исполняется установщиком и откатывается uninstall
</success_criteria>

<artifacts_produced>
## Artifacts this plan produces (символы плана 04-01)

- `internal/install`: пакет — типы `Runner` (с env-параметром), `Installer` (+ `WithRunner`/`WithHome`/activeEngines-шов), методы `Install(ctx)`, `Uninstall(ctx, purge bool)`, `writeAtomic(path, data, perm)`; рендеры component XML и user unit
- `cmd/goswitchctl`: подкоманды `install`, `uninstall [--purge]`; `installTimeout` бюджет; расширенный `errUsage`
- test/e2e: кейс реестра `install-cycle` (case_install.go)
- mise-задача `e2e-install-cycle`
- Пути-контракты: `~/.config/ibus/component/goswitch.xml`, `~/.config/systemd/user/goswitchd.service`, `~/.local/share/goswitch/install-state.json`
</artifacts_produced>

<output>
Create `.planning/phases/04-postavka-i-priemka/04-01-SUMMARY.md` when done
</output>
