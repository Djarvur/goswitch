---
phase: 04-postavka-i-priemka
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - test/e2e/surface.go
  - test/e2e/matrix.go
  - test/e2e/preflight.go
  - test/e2e/cases/matrix-v3.yaml
  - docs/ci-runner.md
autonomous: true
requirements: [INST-01]
# scope_note: план доставляет критерий №2 фазы (матрица полной широты) — формально это приёмочное
# доказательство поставляемого INST-01-продукта; выделенного REQ-ID у критерия №2 нет (TEST-04 закрыт
# в Фазе 2 с расширением в Фазе 3), поэтому requirements указывает INST-01 как ближайшее покрытие фазы.

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Матрица v3 (D-47) существует как НОВЫЙ YAML по прецеденту v1→v2: matrix-v2.yaml НЕ тронута (замороженный эталон Фазы 3 — git-диф пуст); v3 добавляет поверхность gedit и оконный режим Chromium (x11/XWayland) к полной широте v2"
    - "Драйвер gedit — полный lifecycle-trio (open/await-input/pid-readback/close) с изоляцией сессии через XDG-перенаправление (спайк решает точный набор флагов — gedit не имеет --standalone); AT-SPI имя приложения и anchor-поведение СПИКИРОВАНЫ, не предположены («пин фактического» — прецедент 02-05)"
    - "Драйвер chromium-x11 — второй спавн семейства chromium с флагом --ozone-platform=x11 на существующем наборе (свежий --user-data-dir в tmp стенда, --force-renderer-accessibility); фактический уровень лестницы/selection-поведение x11-режима СПИКИРОВАНЫ отдельно от Wayland-режима (Падение 7: IM-путь другой — zwp_text_input_v3 vs IBus input context)"
    - "Словарь поверхностей расширен закрытой валидацией: matrixSurfaces() + openMatrixSurface + matrixSurfaceApp + matrixSurfaceReportsAnchor — имя вне словаря падает на ВАЛИДАЦИИ кейса, не на живом прогоне (matrix.go:310-316)"
    - "Префлайт содержит проверку gedit (LookPath + подсказка установки + spawn/witness/close happy path) — детектор «warning sign» Падения 6"
    - "Полный прогон v3 зелёный живьём (go run ./test/e2e -matrix test/e2e/cases/matrix-v3.yaml): тело v2 + gedit-кейсы + chromium-x11 кейсы, PASS-отчёт, exit 0"
  artifacts:
    - path: test/e2e/surface.go
      provides: "startGedit/waitGeditInput/readGeditText/closeGedit (GTE-образец с XDG-изолированием по итогам спайка) + x11-вариант chromium-драйвера; новые surfaceKind; пины AT-SPI имён"
    - path: test/e2e/matrix.go
      provides: "словарь поверхностей gedit + chromium-x11; открытие и app-name/anchor-карты по спайк-вердиктам"
    - path: test/e2e/preflight.go
      provides: "checkGeditLaunch (LookPath + fix-hint + happy path)"
    - path: test/e2e/cases/matrix-v3.yaml
      provides: "v3-корпус: заголовок-комментарий (план-реф, дельта схемы, документированные исключения) + multi-doc кейсы"
    - path: docs/ci-runner.md
      provides: "требование gedit к машине раннера (sudo apt install gedit, universe) — Падение 6"
  key_links:
    - from: test/e2e/cases/matrix-v3.yaml
      to: test/e2e/matrix.go
      via: "имена поверхностей gedit/chromium-x11 валидируются закрытым словарём — кейс с опечаткой падает на загрузке"
      pattern: "gedit|chromium-x11"
    - from: test/e2e/surface.go
      to: test/e2e/matrix.go
      via: "драйверные константы AT-SPI имён синхронны matrixSurfaceApp — комментарий re-pin-both-together (:21-26)"
      pattern: "matrixSurfaceApp"
  prohibitions:
    - statement: "v2 НЕ ДОЛЖНА меняться — замороженный принятый эталон Фазы 3; любое «улучшение» старых кейсов едёт только в v3 (D-47)"
      status: flagged
      verification: judgment
    - statement: "Новые поверхности НЕ ДОЛЖНЫ получать ожидания по аналогии (уровень лестницы/anchor/имена) без живого спайка — «пин фактического, никогда предположения» (прецеденты 02-05/03-03)"
      status: flagged
      verification: judgment
    - statement: "Драйверы НЕ ДОЛЖНЫ трогать сессию владельца GTE/gedit (draft-store, single-instance) — изоляция XDG-перенаправлением в tmp стенда (урок 02-02)"
      status: flagged
      verification: judgment
  assumptions:
    - "FLAGGED (research A4): изоляция gedit решаема XDG-перенаправлением (аналог GTE 02-02) — gedit без --standalone, спайк решает флаг-набор; при провале — альтернативный механизм изолирования ищет спайк, эскалация если класса решения нет"
    - "FLAGGED (research A1): chromium --ozone-platform=x11 даёт жизнеспособную XWayland/IBus-input-context поверхность — запуск проверен, сокет-уровень верифицирован не до конца; спайк Task 1 решает, при провале x11-уровень документируется как исключение в заголовке v3 (критерий №2 покрывается фактически доступными оконными режимами, вердикт за владельцем на verify)"
    - "FLAGGED (состав v3 — discretion CONTEXT): точный набор кейсов gedit/x11 определяется ПОСЛЕ спайка; запланированный минимум: gedit — слово en-ru + три регистра + ru-en + фраза + смешанное слово (+select при якоре), x11 — слово en-ru + регистры + ряд фактического уровня; v2-тело копируется целиком (superset)"
---

<objective>
Расширение матрицы до v3 (D-47, критерий №2 фазы): поверхность gedit (GTK3-поколение IM/AT-SPI — действительно другое, не GTK4-клон) и оконный режим Chromium x11/XWayland (другой IM-путь). Спайк-пин фактического поведения впереди составления кейсов; v2 заморожена.

**As a** пользователь gedit или Chromium-в-XWayland, **I want** коррекция раскладки работает и в моём приложении, **so that** матрица приёмки покрывает мой сценарий, а не только GTK4-поверхности разработчиков.

Purpose: критерий №2 роадмапа — полная матрица с capability-tier покрытием (gnome-text-editor, gedit, Chrome, оконные режимы Chromium); база двойного прогона D-48 (04-06).
Output: драйверы gedit + chromium-x11, расширенный словарь/префлайт, matrix-v3.yaml, живой зелёный прогон v3.
</objective>

<execution_context>
@/home/nil/DiskD/W/Djarvur/goswitch/.zcode/gsd-core/workflows/execute-plan.md
@/home/nil/DiskD/W/Djarvur/goswitch/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-postavka-i-priemka/04-CONTEXT.md (D-47 — заперт; discretion: состав кейсов после исследования поверхностей)
@.planning/phases/04-postavka-i-priemka/04-RESEARCH.md (§ Matrix v3 surfaces — gedit GTK3/не установлен, Chrome нативный Wayland; § Pattern 5; § Pitfalls 6/7; § A1/A4)
@.planning/phases/04-postavka-i-priemka/04-PATTERNS.md (§ surface.go — GTE-изоляция :117-139, lifecycle-trio, chromium-флаги :63-83, surfaceKind :44-52; § matrix.go — четыре точки :42-52/:314-316/:806-836/:1113-1121, anchor-карта :921-923; § matrix-v3.yaml — форма v2; § preflight.go)
@test/e2e/cases/matrix-v2.yaml (замороженный эталон — источник тела v3)
@docs/ci-runner.md (книга раннера — куда добавить требование gedit)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Спайки + драйверы — gedit (XDG-изоляция, пин AT-SPI имени/anchor) и chromium-x11 (пин фактического уровня), словарь + префлайт (D-47)</name>
  <reversibility rating="reversible">Расширение стенда; словарь валидации — точечное расширение существующей карты</reversibility>
  <precondition>gedit УСТАНОВЛЕН: /usr/bin/gedit существует (владелец: sudo apt install gedit на столе=раннере — docs/ci-runner.md обновляет требование). Живая сессия, префлайт стенда зелёный</precondition>
  <files>test/e2e/surface.go, test/e2e/matrix.go, test/e2e/preflight.go, docs/ci-runner.md</files>
  <read_first>
  - .planning/phases/04-postavka-i-priemka/04-RESEARCH.md (§ Pitfall 6 — gedit GTK3 + изоляция неизвестна; § Pitfall 7 — x11 как НОВЫЙ уровень; § A1/A4)
  - .planning/phases/04-postavka-i-priemka/04-PATTERNS.md (§ surface.go — ОБЯЗАТЕЛЬНО: GTE-драйвер :117-139 и lifecycle-trio waitGTEInput/readGTEText/closeGTE, chromium-флаги :63-83, surfaceKind-прецедент :44-52, closeEntrySurface case_m1.go:128-147; § matrix.go — 4 точки расширения; § preflight.go — checks-table и checkGnomeTextEditorLaunch :159-174)
  - test/e2e/surface.go (весь — драйверные конвенции, withEnv :248-258, gteReadAttempts/:152-155)
  - test/e2e/matrix.go (matrixSurfaces :42-52, валидация :310-316, openMatrixSurface :806-836, matrixSurfaceApp :1113-1121, matrixSurfaceReportsAnchor :921-923)
  - test/e2e/preflight.go (таблица проверок, бинарный check)
  - test/e2e/case_select.go (спайк-форма пинирования anchor-поведения поверхностей — прецедент 03-03)
  - docs/ci-runner.md (раздел требований к машине)
  </read_first>
  <action>
  СПАЙК gedit (перед кодом драйвера, вердикты в SUMMARY): (а) изоляция — запуск gedit с XDG_DATA_HOME/XDG_CONFIG_HOME в tmp стенда (gedit не имеет --standalone); подобрать набор: окно рождается, AT-SPI-дерево живо, инстанс владельца не затронут; (б) пин AT-SPI имени приложения (bridge-путь — матрица матчит по нему); (в) пин anchor-поведения: ctrl+a пушит ли cursor/anchor (канон select-all из 03-03) — решает select-кейсы v3 или их исключение; (г) пин фактического уровня лестницы (caps у gedit-клиента — GTK3-поколение может отличаться).
  СПАЙК chromium-x11: спавн с --ozone-platform=x11 ПЛЮС существующий флаг-набор (свежий --user-data-dir в tmp, --force-renderer-accessibility); пин AT-SPI имени (рядом с chromiumAppName — комментарий re-pin-both-together), пин уровня лестницы и selection-поведения x11-режима ОТДЕЛЬНО от Wayland (Падение 7 — идентичные expect_level для двух режимов = предположение, запрещено).
  КОД по вердиктам: surface.go — startGedit/waitGeditInput/readGeditText/closeGedit (GTE-образец, XDG-env из вердикта (а)); x11-драйвер — второй спавн chromium-семейства (surfaceKind-расширение прецедентом :44-52, closeEntrySurface получает no-op-кейс). matrix.go — matrixSurfaces() + surfaceChromiumX11Name + matrixSurfaceGedit; openMatrixSurface — кейс на драйвер; matrixSurfaceApp — имена из спайка; matrixSurfaceReportsAnchor — gedit-вердикт. preflight.go — checkGeditLaunch (LookPath + подсказка установки + happy path спавн/witness/close). docs/ci-runner.md — требование gedit в раздел машины (sudo apt install gedit, universe). Прогон юнит/сборка: mise run ci; живой smoke обеих поверхностей (pattern runSurfaceSmoke из 02-02 или открытие через -matrix с одним кейсом — по наличию инструмента). Commit: feat(04-05) (спайк-вердикты — в SUMMARY и комментариях констант).
  </action>
  <verify>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise run ci</automated>
    <fails_when>non-zero exit — зелёная итерация D-08 не достигнута (драйверы в сборке)</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && grep -q "gedit" test/e2e/matrix.go && grep -q "x11" test/e2e/matrix.go && grep -q "gedit" test/e2e/preflight.go && grep -q "gedit" docs/ci-runner.md && echo VOCAB-OK</automated>
    <fails_when>отсутствие VOCAB-OK — словарь/префлайт/доки не расширен</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && git diff --exit-code -- test/e2e/cases/matrix-v2.yaml && echo V2-FROZEN</automated>
    <fails_when>отсутствие V2-FROZEN — замороженный эталон Фазы 3 изменён (нарушение D-47)</fails_when>
  </verify>
  <done>Обе поверхности водятся стендом с изоляцией; AT-SPI имена, anchor и уровни запинены спайком (вердикты в SUMMARY); словарь+префлайт+доки расширены; v2 не тронута; mise run ci зелёный</done>
  <acceptance_criteria>
  - surface.go: startGedit/waitGeditInput/readGeditText/closeGedit + x11-спавн chromium; изоляция через XDG в tmp стенда; AT-SPI-имена — константы со ссылкой на спайк-вердикт
  - matrix.go: gedit и chromium-x11 в matrixSurfaces(); matrixSurfaceApp и matrixSurfaceReportsAnchor несут СПИК-значения (не скопированные с других поверхностей)
  - expect_level пины x11 отличается от Wayland-пин, если спайк показал отличие; совпадение допустимо только при живом подтверждении обоих
  - preflight: checkGeditLaunch с fix-hint «sudo apt install gedit»; docs/ci-runner.md содержит требование gedit
  - matrix-v2.yaml без изменений (git diff пуст)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: matrix-v3.yaml — superset v2 + gedit-набор + chromium-x11 набор; живой зелёный полный прогон (D-47, критерий №2)</name>
  <reversibility rating="reversible">Тест-данные; v2 остаётся эталоном</reversibility>
  <precondition>Task 1 зелёный (драйверы + спайк-пины); gedit установлен</precondition>
  <files>test/e2e/cases/matrix-v3.yaml</files>
  <read_first>
  - test/e2e/cases/matrix-v2.yaml (весь — тело копируется; заголовок-комментарий :1-27 — образец для v3-шапки)
  - .planning/phases/04-postavka-i-priemka/04-PATTERNS.md (§ matrix-v3.yaml — форма кейса :27-42; правила D-47)
  - test/e2e/matrix.go (валидация closed-vocabulary — имена из Task 1; канон select-all «ctrl+a» из 03-03)
  - SUMMARY 04-05 Task 1 (спайк-вердикты: anchor gedit, уровень x11 — определяют состав select/level кейсов)
  </read_first>
  <action>
  Создать test/e2e/cases/matrix-v3.yaml: шапка-комментарий (план-реф 04-05; дельта vs v2: +gedit GTK3-поверхность, +chromium-x11 оконный режим; документированные исключения — например select-кейсы gedit при отсутствии anchor-пуша по вердикту спайка, с обоснованием). Тело: ВСЕ кейсы v2 скопированы дословно (superset — полная ширина критерия №2) + gedit-набор (минимум: gedit-word-en-ru, gedit-word-upper, gedit-word-capital, gedit-word-ru-en, gedit-phrase, gedit-word-mixed; select-кейсы только при спайк-подтверждённом якоре) + chromium-x11-набор (минимум: x11-word-en-ru, x11-word-upper, x11-word-capital, ряд фактического уровня лестницы — expect_level из спайка). Имена поверхностей — только из словаря Task 1. Живой прогон: go run ./test/e2e -matrix test/e2e/cases/matrix-v3.yaml — полный PASS, exit 0 (mise-задача e2e-matrix-v3 добавляется планом 04-06 — прогон прямой командой, соответствующей будущей задаче). mise run ci зелёный. Commit: feat(04-05).
  </action>
  <verify>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && go run ./test/e2e -matrix test/e2e/cases/matrix-v3.yaml</automated>
    <fails_when>non-zero exit или любой FAIL в PASS/FAIL-отчёте — v3 не зелёная живьём</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && v2=$(grep -c '^name:' test/e2e/cases/matrix-v2.yaml); v3=$(grep -c '^name:' test/e2e/cases/matrix-v3.yaml); echo "v2=$v2 v3=$v3"; test "$v3" -ge $((v2 + 10))</automated>
    <fails_when>non-zero exit — v3 не superset: прирост меньше 10 кейсов (минимум 6 gedit + 4 x11 не набран)</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && git diff --exit-code -- test/e2e/cases/matrix-v2.yaml && echo V2-FROZEN</automated>
    <fails_when>отсутствие V2-FROZEN — эталон Фазы 3 изменён</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise run ci</automated>
    <fails_when>non-zero exit — зелёная итерация D-08 не достигнута</fails_when>
  </verify>
  <done>v3 существует как superset: все кейсы v2 + gedit-ряд + x11-ряд; полный живой прогон PASS exit 0; v2 не тронута; шапка документирует дельту и исключения</done>
  <acceptance_criteria>
  - matrix-v3.yaml: шапка с план-рефом, дельтой схемы и исключениями (прецедент v2 :1-27); каждый кейс — закрытый словарь поверхностей
  - superset: все 21 v2-кейсов присутствуют (сверка по именам) + минимум 6 gedit + минимум 4 x11 кейсов
  - select-кейсы gedit присутствуют ТОЛЬКО при спайк-подтверждённом anchor (иначе — документированное исключение в шапке)
  - expect_level x11-кейсов равен спайк-вердикуту, не Wayland-копии без подтверждения
  - живой прогон v3: exit 0, ноль FAIL
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| спавн поверхностей → сессия владельца | gedit/chromium-инстансы стенда не должны трогать данные/сессию владельца (draft-store, single-instance) |
| спайк-вердикты → кейсы | ожидания в YAML без живой пробы = ложные PASS/FAIL приёмки |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-05-01 | DoS | серийные убийства поверхностей вешают мост a11y (урок 02-06) | medium | mitigate | lifecycle-trio с pid-based close/reap; никаких SIGKILL-серий; префлайт-детектор |
| T-04-05-02 | Tampering | кейсы по предположению (уровень/якорь «как у соседней поверхности») | high | mitigate | запрет «пин фактического»: каждое ожидание новой поверхности — из спик-вердикта Task 1 (прецедент 02-05); acceptance-критерий явно это проверяет |
| T-04-05-03 | DoS | отсутствие gedit валит кейсы целиком | low | mitigate | префлайт checkGeditLaunch с fix-hint до прогона (Падение 6 warning-sign) |
</threat_model>

<verification>
- mise run ci зелёный (D-08)
- go run ./test/e2e -matrix test/e2e/cases/matrix-v3.yaml — полный живой PASS, exit 0
- git diff --exit-code -- test/e2e/cases/matrix-v2.yaml — эталон заморожен
- grep-гейты словаря/префлайта/док (VOCAB-OK)
</verification>

<success_criteria>
- D-47 исполнен: v3 — новый YAML, v2 заморожена, gedit + x11 добавлены с спайк-пинами
- Критерий №2 подготовлен: полная широта (GTE, gedit, Chrome Wayland + x11) зелёная одним прогоном — база двойного прогона 04-06
- Требование gedit к раннеру документировано (docs/ci-runner.md)
</success_criteria>

<artifacts_produced>
## Artifacts this plan produces (символы плана 04-05)

- test/e2e/surface.go: драйверы `startGedit`/`waitGeditInput`/`readGeditText`/`closeGedit`, x11-спавн chromium (`surfaceKind`-расширение), константы AT-SPI имён gedit/x11
- test/e2e/matrix.go: словарные имена `gedit`/`chromium-x11` (matrixSurfaces, matrixSurfaceApp, matrixSurfaceReportsAnchor, openMatrixSurface)
- test/e2e/preflight.go: `checkGeditLaunch`
- test/e2e/cases/matrix-v3.yaml: v3-корпус (superset v2 + gedit + x11)
- docs/ci-runner.md: требование gedit к машине раннера
- Спайк-вердикты (в SUMMARY): изоляция gedit, AT-SPI имена, anchor-поведение, фактические уровни лестницы обоих новых режимов
</artifacts_produced>

<output>
Create `.planning/phases/04-postavka-i-priemka/04-05-SUMMARY.md` when done
</output>
