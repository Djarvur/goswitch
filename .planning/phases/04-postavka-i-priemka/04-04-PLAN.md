---
phase: 04-postavka-i-priemka
plan: 04
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - test/e2e/focus_helper.py
  - test/e2e/perf.go
  - test/e2e/perf_test.go
  - test/e2e/main.go
  - mise.toml
autonomous: true
requirements: [INST-03]

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Выделенный perf-прогон (mise-задача e2e-perf) гоняет ОДНОРОДНЫЙ горячий кейс N=40 повторов (комбо Shift+RightCtrl: исправление слова + переключение раскладки одним аккордом, D-43) и печатает отчёт p50/p95/p99 латентности + VmRSS-контрольные точки + VmHWM (пик RSS) — с блоком методики, называющим механизм свидетеля и его квант (D-44)"
    - "Окно измерения — только ydotool→AT-SPI: t0 непосредственно перед инжекцией аккорда, t1 = прибытие наблюдения AT-SPI (событие text-changed резидентного свидетеля); внутренний tap→commit-лог в приёмку НЕ входит (D-44)"
    - "Бюджет — гейт, не метрика: p95 ≥ 50 мс ИЛИ VmHWM ≥ 50 МБ (50*1024 kB) → exit ≠ 0 (INST-03); отчёт сохраняется артефактом perf-report.txt"
    - "Свидетель НЕ квантован измеряемым бюджетом: резидентный событийный слушатель object:text-changed (один старт-кост, таймстемп на событие); запасной режим — резидентный line-protocol опросник с названным квантом (Падение 3: текущий per-poll spawn стоит 60–80 мс и физически не разрешает <50 мс)"
    - "Память по D-45: VmHWM читается один раз в конце прогона из /proc/<pid>/status демона (pid-идентичность стенда, не pgrep); VmRSS снимается в контрольных точках (после старта, конец прогона); демон для прогона спавнится БЕЗ -debug (прод-форма: числа честные)"
  artifacts:
    - path: test/e2e/focus_helper.py
      provides: "новый субрежим свидетеля: резидентный событийный listener object:text-changed (строка-на-событие с таймстемпом) + резервный резидентный опросник с фиксированным квантом; exit-коды и /usr/bin/python3 — конвенции файла"
    - path: test/e2e/perf.go
      provides: "percentile(samples, p) (sort+index), readVmStatus(pid) (VmHWM/VmRSS парсер /proc), резидентный witness-клиент (spawn+line-reader), perf-кейс: цикл N повторов, t0/t1, отчёт, бюджет-гейт"
    - path: test/e2e/perf_test.go
      provides: "юнит-корпус чистой математики: percentile на фиксированных выборках (точные значения p50/p95/p99), парсер VmHWM/VmRSS (фикстура-строки, отсутствие строки = ошибка), бюджет-гейт (p95≥50ms → ошибка)"
    - path: mise.toml
      provides: "[tasks.e2e-perf] — выделенный perf-прогон (live GNOME session; NOT in ci)"
  key_links:
    - from: test/e2e/perf.go
      to: test/e2e/focus_helper.py
      via: "резидентный witness-процесс: один spawn на прогон, чтение строк-событий; t1 = таймстемп прибытия наблюдения (D-44)"
      pattern: "text-changed|witness"
    - from: test/e2e/perf.go
      to: "/proc/<pid>/status"
      via: "VmHWM-оракул по pid демона стенда (s.daemon.Process.Pid) — kernel-tracked пик, одна читка в конце (D-45)"
      pattern: "VmHWM"
  prohibitions:
    - statement: "Число латентности НЕ ДОЛЖНО добываться внутренним tap→commit-логом демона или опросным свидетелем с квантом ≥ измеряемого бюджета — только окно ydotool→AT-SPI с названным в методике механизмом (D-44, честная верхняя оценка)"
      status: flagged
      verification: judgment
    - statement: "Perf-прогон НЕ ДОЛЖЕН проходить молча при превышении бюджета — p95 ≥ 50 мс или VmHWM ≥ 50 МБ обязаны давать exit ≠ 0 (бюджет — гейт приёмки, не справочная метрика)"
      status: flagged
      verification: judgment
    - statement: "Выборка НЕ ДОЛЖНА размазываться по разнородным кейсам — одна выборка, один горячий кейс (D-43); Right-Shift тап-жесты НЕ подменяют горячий кейс: их задержка содержит структурное окно различения 300 мс (SPEC §5) и к бюджету <50 мс не относится"
      status: flagged
      verification: judgment
  assumptions:
    - "FLAGGED (probe INST-03 / unclassified — у фазы нет SPEC-покрытия рёбер): трактовка «реакции на горячую клавишу» — WAITLESS-жест. SPEC §5 (спека-дельта Фазы 1): waitless-действия < 50 мс; действия Right Shift ограничены окном различения 300 мс. Горячий кейс D-43 «коррекция слова + переключение раскладки» = КОМБО Shift+RightCtrl (один аккорд, без окна различения — SWCH-02). Первый живой шаг Task 2 пинирует семантику возбуждения комбо одним -debug-трейсом (диагностика методики, НЕ приёмочное число); если комбо окажется окна-ожидающим — эскалация владельцу на verify-гейте, бюджет не переопределяется молча"
    - "FLAGGED (research A2): AT-SPI object:text-changed события достижимы и дают суб-10 мс квант (стандартный набор событий at-spi2 2.52, не реализован в стенде); запасной режим — резидентный опросник ~5 мс с названным квантом; выбор механизма фиксируется в блоке методики отчёта"
    - "FLAGGED (research): spawn ydotool < 10 мс (измерен живьём) — остаётся в окне как честная верхняя оценка (D-44)"
---

<objective>
Измерительная приёмка INST-03 (D-43/D-44/D-45): выделенный perf-прогон с резидентным событийным свидетелем AT-SPI, однородная выборка горячего кейса (комбо-аккорд), отчёт p50/p95/p99 + VmHWM/VmRSS, бюджет как гейт (exit ≠ 0). Числа из отчёта публикуются таблицей в README (D-46, план 04-07).

**As a** владелец бюджета «< 50 мс / < 50 МБ», **I want** измеренные p95 латентности горячего кейса и пик RSS демона с опубликованной методикой, **so that** обещание производительности подтверждено числом, а не мнением.

Purpose: INST-03 — критерий №3 роадмапа, подтверждённый измерением.
Output: witness-режим focus_helper.py, test/e2e/perf.go (+юнит-корпус), mise-задача e2e-perf, perf-report.txt с числами для README.
</objective>

<execution_context>
@/home/nil/DiskD/W/Djarvur/goswitch/.zcode/gsd-core/workflows/execute-plan.md
@/home/nil/DiskD/W/Djarvur/goswitch/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-postavka-i-priemka/04-CONTEXT.md (D-43, D-44, D-45, D-46 — заперты; discretion: N повторов и состав горячего кейса пинируется методикой)
@.planning/phases/04-postavka-i-priemka/04-RESEARCH.md (§ Pattern 4 — образец perf-цикла и readVmHWM; § Pitfall 3 — квантование свидетеля; § Pitfall 5 — флип без text-события; § Don't Hand-Roll — percentile и память)
@.planning/phases/04-postavka-i-priemka/04-PATTERNS.md (§ perf.go — тайминг-кнобы case_m1.go:13-24, инжекция main.go:631-666, watchdog :172-192, pid-идентичность; § focus_helper.py — конвенции)
@docs/SPEC.md §5 (NFR: waitless < 50 мс; Right Shift ≤ окно различения — основа трактовки D-43)
@test/e2e/case_ctl.go (образец -config-прогона с прод-параметрами демона)
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Резидентный событийный свидетель focus_helper.py + чистая математика perf (percentile, VmHWM/VmRSS) — юнит-корпус впереди (D-44/D-45)</name>
  <reversibility rating="reversible">Измерительная обвязка стенда; контракт чисел фиксирует методика (D-44)</reversibility>
  <precondition>Живая сессия: /usr/bin/python3 с gi доступен (существующие режимы хелпера работают — e2e-ctl зелёный)</precondition>
  <files>test/e2e/focus_helper.py, test/e2e/perf.go, test/e2e/perf_test.go</files>
  <read_first>
  - .planning/phases/04-postavka-i-priemka/04-RESEARCH.md (§ Pattern 4 — readVmHWM-скетч ДОСЛОВНО; § Pitfall 3 — запрет per-poll spawn; § Don't Hand-Roll — s[int(float64(len(s)-1)*p)])
  - .planning/phases/04-postavka-i-priemka/04-PATTERNS.md (§ focus_helper.py — INPUT_ROLES/walk/char_count :66-110, exit-коды :56, /usr/bin/python3 :53-54, docstring-таблица субкоманд 1-57; § perf.go — кнобы/инжекция/watchdog)
  - test/e2e/focus_helper.py (весь — где subtree-примитивы и диспетчер субкоманд)
  - test/e2e/case_m1.go (контрапункт: focusWitness :290-297 — чего НЕ делать; witnessPoll :19-21)
  - test/e2e/main.go (runCmd :519-532, injectKeys/injectText :631-666 — t0 ставится непосредственно перед вызовом)
  - test/e2e/matrix_test.go (форма юнит-тестов в пакете main стенда)
  </read_first>
  <behavior>
  - TestPercentile_Exact (perf): выборка [10..100 шаг 10] (n=10): p50 == 50, p95 == 100, p99 == 100; формула s[int(float64(len(s)-1)*p)] — пин точных значений
  - TestPercentile_SingleSample: [42] → все перцентили 42
  - TestReadProcStatus (perf): фикстура-строки "VmHWM:\t 12345 kB" и "VmRSS:\t 6789 kB" → 12345, 6789; строка VmHWM отсутствует → ошибка "no VmHWM line"
  - TestPerfBudgetGate: gate(p95=49ms, vmhwm=49*1024kB) == nil; gate(p95=50ms, …) != nil; gate(p95=10ms, vmhwm=50*1024kB) != nil — обе стороны бюджета
  - Живой smoke свидетеля: spawn /usr/bin/python3 focus_helper.py <режим> → injectText("a") в zenity через ydotool → строка-событие приходит ≤ 2 s с таймстемпом; остановка witness-процесса по pid
  - Резервный режим (если событийный нестабилен на столе, A2): резидентный опросник с квантом 5 мс — та же строка-протокол; выбор зафиксирован комментарием в perf.go и в отчёте
  </behavior>
  <action>
  RED (D-07): test/e2e/perf_test.go — корпус чистой математики против заглушек perf.go (percentile/readProcStatus/budgetGate — пустые тела для подлинного RED). Прогон mise exec -- go test ./test/e2e/ -run 'TestPercentile|TestReadProcStatus|TestPerfBudgetGate' -race -count=1 — RED; red-evidence. Commit: test(04-04).
  GREEN — test/e2e/perf.go: percentile(samples []time.Duration, p float64) time.Duration — сортировка копии + индекс s[int(float64(len(s)-1)*p)] (никаких статистических зависимостей — Don't Hand-Roll); readProcStatus(pid) (VmHWM, VmRSS int64 kB, error) — парсинг /proc/<pid>/status strings.CutPrefix по research Pattern 4; budgetGate(p95, vmhwm) error; каркас резидентного witness-клиента: spawnOnce /usr/bin/python3 focus_helper.py <режим> (pid-идентичность), line-reader с каналом строк, shutdown по pid (никогда pkill/pgrep -f — anti-pattern research).
  test/e2e/focus_helper.py: новый субрежим (имя по диспетчерской конвенции файла): Atspi.EventListener.register("object:text-changed") + main loop; строка-на-событие: ISO-таймстемп + имя приложения + pid если доступен; переиспользуются INPUT_ROLES/applications/walk; docstring-таблица субкоманд дополнена строкой режима с живыми оговорками; exit-коды 0/1/2 сохранены. Резервный субрежим — резидентный опросник: тот же line-протокол, квант — именованная константа 5 мс (назвать в docstring: методика D-44 требует названного кванта). Живой smoke из behavior прогоняется вручную mise-задачей ещё НЕ существует — прогон через go run ./test/e2e -case perf не готов (Task 2): smoke допускается точечным прогоном хелпера из терминала с фиксацией вердикта в SUMMARY (событийный vs опросник — выбор по A2). mise run ci зелёный. Commit: feat(04-04).
  </action>
  <verify>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise exec -- go test ./test/e2e/ -run 'TestPercentile|TestReadProcStatus|TestPerfBudgetGate' -race -count=1 -v</automated>
    <fails_when>non-zero exit или отсутствие PASS TestPercentile_Exact/TestReadProcStatus/TestPerfBudgetGate в -v-выводе — чистая математика perf не запинена</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise run ci</automated>
    <fails_when>non-zero exit — зелёная итерация D-08 не достигнута</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && grep -q "text-changed" test/e2e/focus_helper.py && grep -v '^#' test/e2e/focus_helper.py | grep -c "EventListener" | grep -qv '^0$' && echo WITNESS-MODE-OK</automated>
    <fails_when>отсутствие WITNESS-MODE-OK — событийный режим свидетеля не добавлен в хелпер</fails_when>
  </verify>
  <done>Юнит-корпус математики зелёный (точные перцентили, парсер /proc, бюджет-гейт с обеих сторон); хелпер несёт резидентный событийный режим (+определён резервный); живой smoke свидетеля зафиксирован вердиктом в SUMMARY</done>
  <acceptance_criteria>
  - perf.go: percentile/readProcStatus/budgetGate экспортированы в пакете стенда; формула перцентилей — s[int(float64(len(s)-1)*p)] (греп-пин)
  - focus_helper.py: субрежим с Atspi.EventListener "object:text-changed"; строка-на-событие с таймстемпом; docstring дополнен; резидентный опросник-резерв с константой кванта
  - witness-клиент: один spawn на прогон, pid-based shutdown; ни одного per-poll python-spawn в цикле измерений (греп-пин: цикл не зовёт focusWitness)
  - perf_test.go пинит p50/p95/p99 на выборке 10 и обе стороны бюджета (50ms / 50*1024kB)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Perf-кейс — N=40 повторов горячего комбо-кейса, t0/t1 ydotool→AT-SPI, отчёт + бюджет-гейт + mise e2e-perf (D-43/D-44/D-45, INST-03)</name>
  <reversibility rating="reversible">Методика — контракт чисел; смена методики = перезапуск прогона и обновление README-таблицы</reversibility>
  <precondition>Task 1 зелёный; живая сессия; демон стенда спавнится БЕЗ -debug (прод-форма — числа честные)</precondition>
  <files>test/e2e/perf.go, test/e2e/main.go, mise.toml</files>
  <read_first>
  - .planning/phases/04-postavka-i-priemka/04-CONTEXT.md (D-43 — однородная выборка; D-44 — окно и методика; D-45 — память)
  - .planning/phases/04-postavka-i-priemka/04-RESEARCH.md (§ Pattern 4 — t0/t1 и honest upper bound; § Pitfall 5 — флип без text-события решается комбо-кейсом: текст-событие коррекции закрывает окно)
  - .planning/phases/04-postavka-i-priemka/04-PATTERNS.md (§ perf.go — кнобы-константы, watchdog :172-192 бюджет повышается perf-константой, никогда не снимается; § main.go — реестр pickCase + caseListUsage в одной правке)
  - test/e2e/case_word.go (цикл: поле → инжекция → оракул; очистка поля между повторами)
  - test/e2e/main.go (инжекция :631-666, runCmd, watchdog, run()-флоу со спавном демона и снимком)
  - test/e2e/case_m1.go (виден ВЫЗОВ комбо-инжекции? если инжекция аккорда ещё не существует — заглянуть в кейс combo из 03-04: файл кейса комбо в test/e2e/ — grep -l combo)
  </read_first>
  <action>
  Первый живой шаг (пин семантики, assumption-флаг): ОДИН диагностический прогон с -debug-трейсом — комбо Shift+RightCtrl возбуждается НЕМЕДЛЕННО (без окна различения); вердикт в SUMMARY. Если комбо окна-ожидающее — СТОП и эскалация (SUMMARY + вердикт; бюджет не переопределять молча).
  test/e2e/perf.go — кейс perf (register в pickCase + caseListUsage одной правкой): спавн демона БЕЗ -config и БЕЗ -debug (вшитые дефолты, прод-форма); открыть zenity entry-поверхность; VmRSS-контрольная №1 после старта демона. Цикл N=40 (константа-кноб в блоке таймингов файла): очистить поле (select-all+delete инжекцией), injectText("ghbdtn"), quiesce, t0 := time.Now() НЕПОСРЕДСТВЕННО перед инжекцией аккорда, инжекция shift+rctrl (форма — из кейса combo 03-04), t1 = строка-событие свидетеля, соответствующая появлению «привет» (сопоставление по ожидаемому тексту с поля readback-подтверждением повтора), выборка i = t1-t0. Watchdog прогона — perf-специфичная константа (N×бюджет поля×запас), никогда не снимать. Конец: VmRSS-контрольная №2, readProcStatus → VmHWM. Отчёт в stdout + файл perf-report.txt: блок методики (механизм свидетеля + квант, окно ydotool→AT-SPI, включая spawn ydotool; N; поверхность; прод-форма демона), строки p50/p95/p99 (мс, 1 знак), vmrss_start_kb/vmrss_end_kb, vm_hwm_kb, budget verdict. Гейт: budgetGate(p95, vmhwm) — ошибка = exit ≠ 0 кейса. mise.toml: [tasks.e2e-perf] (конвенция описания + «(live GNOME session; NOT in ci)»). Прогон живьём: отчёт зелёный под бюджетом; числа из живого отчёта — в SUMMARY (сырьё README-таблицы D-46 для 04-07). mise run ci зелёный. Commit: feat(04-04).
  </action>
  <verify>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise run e2e-perf</automated>
    <fails_when>non-zero exit — бюджет пробит (p95 ≥ 50 мс / VmHWM ≥ 50 МБ) или кейс упал; отсутствие строк p50/p95/p99/vm_hwm_kb в выводе — отчёт не сформирован</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && test -f perf-report.txt && grep -q "p95" perf-report.txt && grep -q "vm_hwm_kb" perf-report.txt && grep -q "witness" perf-report.txt && echo REPORT-OK</automated>
    <fails_when>отсутствие REPORT-OK — артефакт отчёта не создан или без методики/чисел</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise run ci</automated>
    <fails_when>non-zero exit — зелёная итерация D-08 не достигнута</fails_when>
  </verify>
  <done>perf-кейс живьём: 40 однородных повторов комбо-кейса, p95 < 50 мс, VmHWM < 50 МБ, exit 0; perf-report.txt с блоком методики; mise run ci зелёный; числа в SUMMARY для README</done>
  <acceptance_criteria>
  - реестр pickCase содержит perf; caseListUsage обновлён той же правкой; mise-задача e2e-perf существует
  - цикл измерений не содержит per-poll python-spawn (греп-пин) и не использует witnessPoll-петлю матрицы
  - t0 ставится непосредственно перед инжекцией аккорда (код-ревью пин: inject вызов сразу после t0)
  - отчёт содержит: методику с механизмом свидетеля и квантом, p50/p95/p99, vmrss_start/end, vm_hwm_kb, бюджет-вердикт
  - budgetGate даёт exit ≠ 0 при p95 ≥ 50 мс или VmHWM ≥ 50*1024 kB (юнит-пин Task 1)
  - демон прогона без -debug (пин в коде кейса)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| свидетель → отчёт | числа приёмки производятся наблюдателем; квант наблюдателя ≥ бюджета = невидимый дефект (Падение 3) |
| демон → /proc | память читается по pid демона стенда — pid-спуфинг исключён прямым полем Process.Pid |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-04-01 | Information Disclosure | perf-отчёт содержит набранный текст | low | mitigate | отчёт — числа/кванты/КБ/вердикты; содержимое поля не печатается (D-20/D-21) |
| T-04-04-02 | DoS | wedged свидетель вешает прогон | medium | mitigate | witness-клиент под watchdog прогона; таймаут строки-события = шаг прогона падает внятно, не молча |
| T-04-04-03 | Tampering | «постановочные» числа (демон в debug, прогретое поле) | low | mitigate | прод-форма демона без -debug (пин в кейсе), очистка поля перед каждым повтором, методика в отчёте называет условия |
</threat_model>

<verification>
- mise exec -- go test ./test/e2e/ -run 'TestPercentile|TestReadProcStatus|TestPerfBudgetGate' -race -count=1 — корпус математики
- mise run e2e-perf — живой прогон зелёный под бюджетом, perf-report.txt создан
- mise run ci зелёный (D-08)
</verification>

<success_criteria>
- INST-03 доказан измерением: p95 < 50 мс (окно ydotool→AT-SPI, комбо-кейс D-43), VmHWM < 50 МБ (D-45), гейт exit ≠ 0
- Свидетель не квантован бюджетом (Падение 3 закрыт резидентным событийным режимом)
- Числа и методика зафиксированы для README-таблицы (D-46 → 04-07)
</success_criteria>

<artifacts_produced>
## Artifacts this plan produces (символы плана 04-04)

- test/e2e/perf.go: `percentile`, `readProcStatus` (VmHWM/VmRSS), `budgetGate`, witness-клиент (spawnOnce/line-reader/pid-shutdown), кейс реестра `perf`, perf-кнобы (N, кванты, watchdog)
- test/e2e/focus_helper.py: субрежимы резидентного свидетеля — событийный `object:text-changed` + резервный опросник (квант-константа)
- test/e2e/perf_test.go: TestPercentile_Exact/SingleSample, TestReadProcStatus, TestPerfBudgetGate
- mise-задача `e2e-perf`; артефакт прогона `perf-report.txt` (методика + p50/p95/p99 + VmRSS/VmHWM + вердикт бюджета)
</artifacts_produced>

<output>
Create `.planning/phases/04-postavka-i-priemka/04-04-SUMMARY.md` when done
</output>
