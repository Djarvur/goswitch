---
phase: 04-postavka-i-priemka
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - .goreleaser.yaml
  - .github/workflows/release.yml
  - mise.toml
autonomous: true
requirements: [INST-01]

estimate:
  tokens: 16000
  raw_tokens: 16000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "goreleaser-конфиг собирает ОБА бинарника (goswitchd + goswitchctl) одной конфигурацией: `mise exec -- goreleaser check` зелёный, snapshot-сборка производит дистрибутив с прошитой версией (goswitchd -version ≠ dev) — D-38"
    - "Прошивка версии rides на дефолтных ldflags goreleaser (-X main.version/main.commit/main.date) — кастомных build-скриптов нет; go install-канал собирается без прошивки и честно показывает dev (D-37/D-38)"
    - "Release-workflow: тег v* → mise install → goreleaser release; permissions contents: write ТОЛЬКО в этом workflow (pr-sanity/e2e-matrix остаются contents: read); goreleaser запинен версией в mise.toml [tools] (Падение 10, D-09/D-11)"
    - "Релизные ассеты: tar.gz (оба бинарника) + checksums.txt (sha256) — целостность артефактов проверяема; go.mod/go.sum НЕ изменяются (goreleaser — CI-инструмент, НЕ зависимость демона, D-38)"
  artifacts:
    - path: .goreleaser.yaml
      provides: "version: 2; builds goswitchd (./cmd/goswitchd) + goswitchctl (./cmd/goswitchctl), goos [linux], goarch [amd64]; archives tar.gz; checksum checksums.txt; changelog с фильтром docs/test/chore"
    - path: .github/workflows/release.yml
      provides: "on push tags ['v*'], permissions contents: write, mise install, mise exec -- goreleaser release --clean; concurrency release; шапка-комментарий с решениями (D-38, Падение 10)"
    - path: mise.toml
      provides: "goreleaser = \"2.18.1\" под [tools] — единый источник версий локально и в CI"
  key_links:
    - from: .goreleaser.yaml
      to: cmd/goswitchd/main.go
      via: "дефолтные ldflags прошивают main.version/main.commit/main.date в var из 04-02 — без этих var прошивка молча теряется; зависимость 04-02"
      pattern: "main\\.version"
    - from: .github/workflows/release.yml
      to: mise.toml
      via: "CI ставит goreleaser через mise install из пина [tools] — локально и CI одна версия (прецедент 01-05)"
      pattern: "mise install"
  prohibitions:
    - statement: "goreleaser НЕ ДОЛЖЕН появиться в go.mod ни в каком виде — CI/билд-инструмент не является зависимостью демона (D-38, рантайм-принцип «stdlib + godbus + минимум»)"
      status: flagged
      verification: judgment
    - statement: "release-workflow НЕ ДОЛЖЕН наследовать широкие токены или secrets сверх GITHUB_TOKEN — область записи ограничена этим workflow (Падение 10)"
      status: flagged
      verification: judgment
  assumptions:
    - "FLAGGED (discretion A7, решение планировщика): v1-релиз — amd64-only (целевая платформа Ubuntu 24.04 amd64; arm64 добавляется по запросу — комментарий в конфиге); тег-схема semver, релиз v1 — v1.0.0; ассеты tar.gz с ОБОИМИ бинарниками (install требует пару goswitchctl+goswitchd рядом) + checksums.txt; owner подтвердит на verify-гейте (категория «на усмотрение» из CONTEXT)"
    - "FLAGGED (загрузка workflow): release.yml резолвится от default branch (прецедент 02-07) — до мержа фазы тег не публикуется; первый тег v1.0.0 ставит план 04-07 после зелёных гейтов"
---

<objective>
Релизный конвейер (D-38): goreleaser собирает и публикует релизы — GitHub Releases-ассеты с checksums и канал `go install github.com/Djarvur/goswitch/cmd/goswitchd@vX.Y.Z`; версия прошивается дефолтными ldflags в var из 04-02 (D-37). Подготовка каналов INST-01: пользователь ставит либо бинарник из релиза, либо go install.

**As a** пользователь проекта, **I want** релиз с прошитой версией в GitHub Releases и канал go install, **so that** установка воспроизводима, а баг-репорты привязаны к точной сборке.

Purpose: INST-01 (канал «бинарник из GitHub releases» + `go install`) — критерий №1 роадмапа.
Output: .goreleaser.yaml, .github/workflows/release.yml, goreleaser-пин в mise.toml; snapshot-сборка доказана локально.
</objective>

<execution_context>
@/home/nil/DiskD/W/Djarvur/goswitch/.zcode/gsd-core/workflows/execute-plan.md
@/home/nil/DiskD/W/Djarvur/goswitch/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-postavka-i-priemka/04-CONTEXT.md (D-37, D-38 — заперты; discretion: формат ассетов/архитектуры/тег-схема)
@.planning/phases/04-postavka-i-priemka/04-RESEARCH.md (§ Standard Stack — goreleaser v2.18.1; § Pattern 3 — дефолтные ldflags; § Code Examples — минимальный .goreleaser.yaml; § Pitfall 10; § Package Legitimacy Audit — Approved)
@.planning/phases/04-postavka-i-priemka/04-PATTERNS.md (§ .goreleaser.yaml и § release.yml — аналоги и скелеты)
@.planning/phases/04-postavka-i-priemka/04-02-SUMMARY.md (var version/commit/date в cmd/goswitchd — цель прошивки)
@.github/workflows/pr-sanity.yml (скелет: mise install, минимальные права)
@.github/workflows/e2e-matrix.yml (non-push триггер + concurrency-конвенция)
</context>

<tasks>

<task type="auto">
  <name>Task 1: .goreleaser.yaml (две сборки, дефолтная прошивка) + goreleaser-пин в mise + snapshot-доказательство прошивки (D-37/D-38)</name>
  <reversibility rating="reversible">Конфиг инструмента (reversibility D-38 из CONTEXT)</reversibility>
  <precondition>04-02 завершён: cmd/goswitchd содержит var version/commit/date — без них прошивка молча потеряется</precondition>
  <files>.goreleaser.yaml, mise.toml</files>
  <read_first>
  - .planning/phases/04-postavka-i-priemka/04-RESEARCH.md (§ Code Examples — минимальный .goreleaser.yaml ДОСЛОВНО как основа; § Pattern 3 — дефолтные ldflags; § Installation — mise use)
  - .planning/phases/04-postavka-i-priemka/04-PATTERNS.md (§ mise.toml — конвенция [tools] и [tasks]; No Analog Found — .goreleaser.yaml)
  - mise.toml ([tools] — место пина; [env] GOFLAGS="-trimpath" уже детерминирует сборки)
  - cmd/goswitchd/main.go (var version/commit/date из 04-02 — приёмник прошивки)
  - go.mod (go 1.23, 3 прямых зависимости — НЕ трогать)
  </read_first>
  <action>
  Создать .goreleaser.yaml по образцу research «Minimal .goreleaser.yaml» с решениями discretion (A7, зафиксировать в комментарии конфига): version: 2; builds: goswitchd (main ./cmd/goswitchd) и goswitchctl (main ./cmd/goswitchctl), goos [linux], goarch [amd64] (комментарий: целевая платформа Ubuntu 24.04 amd64; +arm64 — по запросу владельца); БЕЗ кастомных ldflags — дефолтные уже прошивают main.version/main.commit/main.date (research Pattern 3); archives: format tar.gz, ОДИН архив с обоими бинарниками (goswitchctl install требует пару рядом — разрешение демона через dirname(os.Executable)); checksum: name_template checksums.txt; changelog: sort asc, exclude ['^docs:', '^test:', '^chore:']; release: GitHub Releases (дефолт-цель под GITHUB_TOKEN). Шапка-комментарий: D-37/D-38, goreleaser НЕ зависимость демона (go.mod не меняется). mise.toml: goreleaser = "2.18.1" под [tools] (единый источник версий, mise registry → aqua:goreleaser/goreleaser). Проверки: mise exec -- goreleaser check (валидность конфига); mise exec -- goreleaser build --snapshot --clean → в dist/ лежат оба бинарника; снятие показаний: snapshot-бинарник goswitchd -version печатает НЕ-dev значение (snapshot подставляет служебную версию — доказательство, что -X доходит); go mod tidy-диф чист (goreleaser не в зависимостях — Падение из research «D-38 locks this»). mise run ci зелёный. Commit: feat(04-03).
  </action>
  <verify>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise exec -- goreleaser check</automated>
    <fails_when>non-zero exit или строки с "error"/"invalid" — конфиг невалиден</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise exec -- goreleaser build --snapshot --clean && find dist -name goswitchd -type f | head -1</automated>
    <fails_when>non-zero exit или пустый вывод find — snapshot-сборка не произвела goswitchd</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise exec -- go mod tidy && git diff --exit-code -- go.mod go.sum && echo TIDY-CLEAN</automated>
    <fails_when>отсутствие TIDY-CLEAN в выводе — goreleaser утёк в зависимости (нарушение D-38)</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise run ci</automated>
    <fails_when>non-zero exit — зелёная итерация D-08 не достигнута</fails_when>
  </verify>
  <done>goreleaser check зелёный; snapshot собирает оба бинарника; прошивка версии доходит (goswitchd -version ≠ dev в snapshot); go.mod/go.sum нетронуты; goreleaser запинен в mise</done>
  <acceptance_criteria>
  - .goreleaser.yaml: version: 2, два builds с main-путями ./cmd/goswitchd и ./cmd/goswitchctl, goos [linux], goarch [amd64], БЕЗ поля ldflags (дефолтная прошивка)
  - один tar.gz-архив на релиз с обоими бинарниками; checksums.txt (sha256)
  - mise.toml [tools] содержит goreleaser = "2.18.1"
  - git diff по go.mod/go.sum после tidy пуст (греп-пин D-38)
  - snapshot-бинарник goswitchd -version печатает не-dev значение
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: .github/workflows/release.yml — тег v* → goreleaser release, contents:write только здесь (D-38, Падение 10)</name>
  <reversibility rating="reversible">CI-конфиг; публикация происходит только на тег (ставит 04-07)</reversibility>
  <files>.github/workflows/release.yml</files>
  <read_first>
  - .planning/phases/04-postavka-i-priemka/04-PATTERNS.md (§ release.yml — скелет pr-sanity :36-50, триггер/concurrency-форма e2e-matrix :24-38, шапка-комментарии)
  - .planning/phases/04-postavka-i-priemka/04-RESEARCH.md (§ Pitfall 10 — права и версионный дрейф; § Architecture Patterns — RELEASE-ветка диаграммы)
  - .github/workflows/pr-sanity.yml (шаги mise install; contents: read — НЕ трогать)
  - .github/workflows/e2e-matrix.yml (форма non-push триггера + concurrency с обоснованием)
  - .goreleaser.yaml (результат Task 1)
  </read_first>
  <action>
  Создать .github/workflows/release.yml: шапка-комментарий (назначение; D-38; Падение 10 — область записи только здесь; тег-схема v1.0.0 из discretion A7); on: push: tags: ['v*']; permissions: contents: write (ТОЛЬКО этот workflow); concurrency: group release (без cancel-in-progress — публикация не должна рваться); job (runs-on ubuntu-latest): checkout@v4 → Install mise (curl https://mise.run | sh) → PATH → mise install (goreleaser из пина mise.toml — прецедент 01-05: CI и локально один источник версий) → mise exec -- goreleaser release --clean. Никаких secrets сверх GITHUB_TOKEN. Проверка структуры греп-гейтами (verify) + сверка, что pr-sanity.yml и e2e-matrix.yml остались contents: read (дрейф прав исключён). Регистрация от default branch (прецедент 02-07): до мержа фазы workflow не резолвится — первый тег ставит 04-07 после зелёных гейтов; заметка в SUMMARY. mise run ci зелёный. Commit: feat(04-03).
  </action>
  <verify>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && grep -q "tags: \['v\*'\]" .github/workflows/release.yml && grep -q "contents: write" .github/workflows/release.yml && grep -q "goreleaser release --clean" .github/workflows/release.yml && grep -q "mise install" .github/workflows/release.yml && echo RELEASE-WF-OK</automated>
    <fails_when>отсутствие RELEASE-WF-OK в выводе — триггер/права/команда/mise-шаг отсутствуют</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && grep -c "contents: read" .github/workflows/pr-sanity.yml .github/workflows/e2e-matrix.yml && ! grep -q "contents: write" .github/workflows/pr-sanity.yml .github/workflows/e2e-matrix.yml && echo SCOPE-OK</fails_when>
    <fails_when>отсутствие SCOPE-OK — права записи протекли в другие workflow (Падение 10)</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise run ci</automated>
    <fails_when>non-zero exit — зелёная итерация D-08 не достигнута</fails_when>
  </verify>
  <done>Release-workflow валиден структурно: тег v*, contents:write только здесь, mise-установка goreleaser из пина, goreleaser release --clean; остальные workflow не расширили права</done>
  <acceptance_criteria>
  - .github/workflows/release.yml содержит on.push.tags ['v*'], permissions contents: write, шаги mise install и mise exec -- goreleaser release --clean
  - pr-sanity.yml и e2e-matrix.yml содержат contents: read и НЕ содержат contents: write
  - шапка-комментарий цитирует D-38 и Падение 10 (конвенция обоих workflow)
  - секреты: только GITHUB_TOKEN (никаких дополнительных secrets-блоков)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| тег → релиз-артефакт | публикация по тегу создаёт публичные артефакты — подмена/битые ассеты подрывают доверие к каналу |
| CI-токен → GitHub Releases | release-workflow владеет правом записи в релизы — область должна быть ограничена |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-03-01 | Tampering | подмена релизных артефактов | high | mitigate | goreleaser checksums.txt (sha256) публикуется с ассетами; go install-канал — целостность через checksum DB proxy.golang.org |
| T-04-03-02 | Elevation | токен release-workflow | high | mitigate | permissions: contents: write ТОЛЬКО в release.yml; pr-sanity/e2e-matrix остаются contents: read (греп-гейт SCOPE-OK) |
| T-04-03-03 | Tampering | goreleaser-инструмент (поставка) | low | mitigate | версия запинена в mise.toml (2.18.1, legitimacy-аудит research: Approved, dual-source); CI ставит из пина |
| T-04-03-04 | DoS | версионный дрейф локально vs CI | low | mitigate | единый источник mise.toml [tools] + mise install в CI (прецедент 01-05) |
</threat_model>

<verification>
- mise exec -- goreleaser check зелёный
- mise exec -- goreleaser build --snapshot --clean — оба бинарника в dist/, версия прошита (goswitchd -version ≠ dev)
- go mod tidy-диф пуст (goreleaser не в зависимостях, D-38)
- греп-гейты release.yml (триггер/права/команда) + SCOPE-OK по остальным workflow
</verification>

<success_criteria>
- D-38 исполнен: goreleaser ведёт сборку/публикацию, демон не получил ни одной новой зависимости
- Оба канала INST-01 готовы: GitHub Releases-ассеты (tar.gz + checksums) и go install pkg@tag (совместимость module как есть)
- Права записи ограничены release-workflow (Падение 10)
</success_criteria>

<artifacts_produced>
## Artifacts this plan produces (символы плана 04-03)

- `.goreleaser.yaml` — конфиг: builds goswitchd/goswitchctl, linux/amd64, tar.gz-архив с обоими бинарниками, checksums.txt, changelog-фильтры
- `.github/workflows/release.yml` — workflow: тег v*, contents: write (только здесь), mise install, goreleaser release --clean
- `mise.toml` — [tools] goreleaser = "2.18.1"
- Решения discretion (в комментариях конфига + SUMMARY): amd64-only v1, тег-схема semver v1.0.0, ассеты tar.gz+checksums
</artifacts_produced>

<output>
Create `.planning/phases/04-postavka-i-priemka/04-03-SUMMARY.md` when done
</output>
