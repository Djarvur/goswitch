---
phase: 04-postavka-i-priemka
plan: 07
type: execute
wave: 4
depends_on: ["04-02", "04-03", "04-04"]
files_modified:
  - README.md
  - README.ru.md
  - docs/ACCEPTANCE.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [INST-01]

estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "README.md (EN, первичен) и README.ru.md (RU, синхрон) — двуязычная пара (D-50): что это, установка БЕЗ root (канал релиз-архива: скачать tar.gz → goswitchctl install ОДНОЙ командой; канал go install: две команды @vX.Y.Z → goswitchctl install), что делает install (XML+юнит+«один хозяин»+активация), selfcheck/uninstall (--purge), конфиг (ссылка docs/CONFIG.md; до первого конфига — вшитые дефолты), perf-таблица (D-46: p50/p95/p99 + RSS peak из ЖИВОГО прогона e2e-perf), предупреждение приватности -debug, troubleshooting (выселение кеша → selfcheck чинит)"
    - "Нигде в доках НЕ документируется голый `ibus write-cache` как пользовательский шаг (Падение 1 — выселяет компонент); установка = только goswitchctl install"
    - "docs/ACCEPTANCE.md — письменный чек-лист приёмки владельца D-49 (формат 03-UAT: нумерованные пункты expected/result/note + Summary-блок): установка с нуля по README на живом столе → selfcheck → живой набор всех жестов (флип/слово/фраза/выделение/комбо/смешанное/reload) → status → uninstall → стол восстановлен"
    - "WINDOWS-ледger сверен с решениями владельца 2026-09-15: #2 → fixed (владелец подтвердил на verify-гейте Фазы 3), #4 → waive с причиной (принятое платформенное ограничение GTK4-Wayland); open_count == 0 — ship-гейт не заблокирован (Pitfall 11)"
    - "Релиз v1 опубликован: тег v1.0.0 (semver, discretion A7) → release-workflow → GitHub Releases с tar.gz (оба бинарника) + checksums.txt; версия ассета проверяема (goswitchd -version == v1.0.0); канал go install виден в module proxy (v1.0.0.info)"
  artifacts:
    - path: README.md
      provides: "двуязычная пара (EN первичен): полный публичный фронт продукта с perf-таблицей и одно-командной установкой"
    - path: README.ru.md
      provides: "русская пара README.md — синхрон по разделам (D-50)"
    - path: docs/ACCEPTANCE.md
      provides: "чек-лист UAT-гейта фазы (D-49) — та же механика, что закрыла Фазы 1–3"
    - path: .planning/WINDOWS.md
      provides: "сверенный ledger: #2 fixed, #4 waived с причиной, open_count 0"
  key_links:
    - from: README.md
      to: perf-report.txt (04-04)
      via: "числа таблицы — из живого прогона e2e-perf на момент релиза (D-46: актуализация — часть релизного цикла)"
      pattern: "p95"
    - from: README.md/README.ru.md
      to: goswitchctl install (04-01)
      via: "инструкция установки = ОДНА команда; никаких ручных ibus/systemctl шагов"
      pattern: "goswitchctl install"
    - from: тег v1.0.0
      to: .github/workflows/release.yml (04-03)
      via: "тег запускает публикацию; ассеты содержат ОБА бинарника — инструкция README исполнима с чистой машины"
      pattern: "v1\\.0\\.0"
  prohibitions:
    - statement: "Доки НЕ ДОЛЖНЫ документировать голый `ibus write-cache` как шаг пользователя и НЕ ДОЛЖНЫ разбивать установку на ручные ibus/systemctl шаги — только goswitchctl install (Падение 1: ручной голый write-cache выселяет компонент)"
      status: flagged
      verification: judgment
    - statement: "EN и RU README НЕ ДОЛЖНЫ расходиться по фактам (команды/числа/версии) — рассинхрон = врущие доки (D-50); проверяется сверкой команд и чисел таблицы"
      status: flagged
      verification: judgment
    - statement: "Тег релиза НЕ ДОЛЖЕН ставиться до зелёных гейтов (ci + install-cycle + perf под бюджетом + матрица v3 зелёная) — публикация непроверенного = публичный акт с откатом через удаление релиза"
      status: flagged
      verification: judgment
  assumptions:
    - "FLAGGED (D-49 — механика гейта): чек-лист проходится владельцем на verify-work фазы (одна сессия по §7.2 спеки — та же механика, что закрыла Фазы 1–3); план публикует чек-лист и релиз, проход — гейт владельца"
    - "FLAGGED (порядок релиза): тег v1.0.0 ставится планом ДО ручной приёмки — UAT владельца ставит продукт с настоящего релиза (как пользователь); при находках UAT — фикс и re-tag v1.0.1 (нормальный релизный цикл); владелец видит порядок на verify-гейте"
    - "FLAGGED (discretion): расположение чек-листа — docs/ACCEPTANCE.md отдельным файлом (не раздел README): чек-лист — артефакт прохода, не инструкция пользователя; структура по формату 03-UAT"
---

<objective>
Публикация и приёмка (D-49/D-50/D-46 + ship-гигиена): двуязычный README с одно-командной установкой и живой perf-таблицей, письменный чек-лист приёмки владельца, сверка WINDOWS-ledger, и сам релиз v1 — тег v1.0.0 публикует ассеты через конвейер 04-03. Завершает M4: продукт ставится по документированной инструкции и готов к ручной приёмке.

**As a** новый пользователь, **I want** двуязычный README, где установка с нуля — одна команда, а числа производительности опубликованы, **so that** я решаюсь поставить инструмент за минуты и знаю, что обещания подтверждены; **as a** владелец, **I want** письменный чек-лист и опубликованный релиз, **so that** приёмка v1 — управляемая сессия, а не импровизация.

Purpose: критерий №4 роадмапа (ручная приёмка + опубликованные доки) и финализация INST-01 (релизные каналы наполнены реальным релизом).
Output: README.md + README.ru.md (двуязычная пара), docs/ACCEPTANCE.md, сверенный WINDOWS-ledger, опубликованный релиз v1.0.0.
</objective>

<execution_context>
@/home/nil/DiskD/W/Djarvur/goswitch/.zcode/gsd-core/workflows/execute-plan.md
@/home/nil/DiskD/W/Djarvur/goswitch/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-postavka-i-priemka/04-CONTEXT.md (D-46, D-49, D-50 — заперты; discretion: расположение чек-листа)
@.planning/phases/04-postavka-i-priemka/04-RESEARCH.md (§ Anti-Patterns — запрет документировать голый write-cache; § Pitfall 11 — WINDOWS-ledger; § State of the Art — README перезапись)
@.planning/phases/04-postavka-i-priemka/04-PATTERNS.md (§ README — обязательства контента; § docs/ACCEPTANCE.md — формат 03-UAT)
@.planning/phases/04-postavka-i-priemka/04-01-SUMMARY.md (install/uninstall/selfcheck — факты для инструкции)
@.planning/phases/04-postavka-i-priemka/04-04-SUMMARY.md (числа perf-прогона для таблицы D-46)
@.planning/phases/03-frazy-vydelenie-pereklyuchenie-i-konfiguratsiya/03-UAT.md (формат чек-листа)
@README.md (текущий минимальный — переписывается)
@docs/CONFIG.md (публичный конфиг-контракт — на что ссылается README)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Двуязычный README (D-50) — установка одной командой, perf-таблица из живого прогона (D-46), приватность (INST-01)</name>
  <reversibility rating="costly">Опубликованный контент (reversibility D-50 из CONTEXT); рассинхрон пары = врущие доки</reversibility>
  <precondition>04-01/02 (install/uninstall/selfcheck факты), 04-03 (каналы), 04-04 (perf-report.txt с живыми числами) завершены</precondition>
  <files>README.md, README.ru.md</files>
  <read_first>
  - .planning/phases/04-postavka-i-priemka/04-PATTERNS.md (§ README — обязательства контента дословно; текущая структура :11, :13-22, :29-34 — ручные cp-шаги уходят)
  - .planning/phases/04-postavka-i-priemka/04-RESEARCH.md (§ Anti-Patterns — «never document bare ibus write-cache»; § Don't Hand-Roll не применим — это доки)
  - README.md (текущий: предупреждение приватности :13-22 сохранить и развернуть)
  - docs/CONFIG.md (конфиг-контракт — ссылка из README; НЕ дублировать таблицу)
  - perf-report.txt (живые числа 04-04 — источник таблицы)
  - .planning/phases/04-postavka-i-priemka/04-02-SUMMARY.md (selfcheck — шесть шагов: что обещать в troubleshooting)
  </read_first>
  <action>
  Переписать README.md (EN, первичен): (1) что это — 2–3 строки + как работает (IBus engine, без root, не конфликтует с keyd/xremap); (2) Requirements: Ubuntu 24.04 GNOME Wayland; (3) Install — канал A (релиз-архив): скачать tar.gz со странице Releases, распаковать, запустить goswitchctl install — ВСЁ (компонент IBus, systemd user unit, переключение источников, активация — одной командой; без root); канал B (go install): go install github.com/Djarvur/goswitch/cmd/goswitchd@v1.0.0 и .../cmd/goswitchctl@v1.0.0, затем goswitchctl install; версия сборки при этом канале — dev (честная оговорка D-37); (4) Verify: goswitchctl selfcheck — шесть проверок; (5) Usage: жесты кратко (ссылка на docs/), goswitchctl status/reload/correct; (6) Configuration: до первого ~/.config/goswitch/config.yaml демон на вшитых дефолтах (install НЕ генерирует конфиг — решение фазы), схема — docs/CONFIG.md, hot reload; (7) Performance: таблица p50/p95/p99 (мс) + RSS peak (МБ) ИЗ perf-report.txt + строка методики (окно ydotool→AT-SPI, комбо-кейс, N) + примечание об обновлении каждым релизом (D-46); (8) Privacy: INFO — счётчики/причины; -debug трассирует КАЖДУЮ клавишу (пароли!) — расширить текущее предупреждение :13-22; (9) Troubleshooting: «движок пропал из списка» → goswitchctl selfcheck (чинит кеш сам); «не работает в GTK4-приложении после перехвата» → платформенное ограничение; (10) Uninstall: goswitchctl uninstall (--purge удаляет и конфиг); (11) License MIT. НИ ОДНОГО ручного шага ibus write-cache/systemctl в инструкции (Падение 1). README.ru.md — русский зеркальный перевод, синхрон команд/чисел/версий дословно (те же команды, та же таблица). Числа таблицы — из существующего perf-report.txt (свежие: при необходимости перегнать mise run e2e-perf). mise run ci зелёный. Commit: docs(04-07).
  </action>
  <verify>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && grep -q "goswitchctl install" README.md && grep -q "goswitchctl install" README.ru.md && grep -q "selfcheck" README.md && grep -q "uninstall" README.md && grep -q "p95" README.md && grep -q "p95" README.ru.md && echo README-COMPLETE</automated>
    <fails_when>отсутствие README-COMPLETE — пара неполна (нет установки/самопроверки/отката/perf-таблицы в обоих языках)</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && ! grep -v '^#' README.md | grep -v '^>' | grep -q "ibus write-cache" && ! grep -v '^#' README.ru.md | grep -v '^>' | grep -q "ibus write-cache" && echo NO-BARE-CACHE</automated>
    <fails_when>отсутствие NO-BARE-CACHE — голый write-cache задокументирован как шаг (Падение 1, запрет)</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && en=$(grep -c 'goswitchctl install' README.md); ru=$(grep -c 'goswitchctl install' README.ru.md); echo "en=$en ru=$ru"; test "$en" -eq "$ru" && test "$en" -ge 2</automated>
    <fails_when>non-zero exit — команды установки разошлись между языками или канал A/B не описаны</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise run ci</automated>
    <fails_when>non-zero exit — зелёная итерация D-08 не достигнута</fails_when>
  </verify>
  <done>Двуязычная пара с одно-командной установкой (оба канала), selfcheck/uninstall, perf-таблицей из живого прогона, приватностью и troubleshooting; команды и числа синхронны; голого write-cache нет</done>
  <acceptance_criteria>
  - README.md и README.ru.md существуют; оба содержат goswitchctl install (оба канала), selfcheck, uninstall --purge, таблицу p50/p95/p99 + RSS
  - числа таблицы совпадают с perf-report.txt (сверка вручную при ревью — пин в SUMMARY с датой прогона)
  - предупреждение -debug сохранено и расширено (каждая клавиша видна — пароли)
  - ни один язык не содержит голого ibus write-cache или ручных systemctl-шагов установки (grep-пин)
  - счётчики команд установки в EN == RU (синхрон пары)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: docs/ACCEPTANCE.md — чек-лист приёмки владельца (D-49) + сверка WINDOWS-ledger (Pitfall 11)</name>
  <reversibility rating="reversible">Документ-гейт; ledger-глагоны идемпотентны к повтору с вердиктом</reversibility>
  <files>docs/ACCEPTANCE.md, .planning/WINDOWS.md</files>
  <read_first>
  - .planning/phases/03-frazy-vydelenie-pereklyuchenie-i-konfiguratsiya/03-UAT.md (формат: нумерованные пункты expected/result/note + Summary total/passed/issues/pending)
  - .planning/phases/04-postavka-i-priemka/04-CONTEXT.md (D-49 дословно: установка с нуля по README → живой набор всех жестов → uninstall)
  - .planning/WINDOWS.md (ledger: #2 открыт при закрытии владельцем; #4 открыт при принятии ограничения)
  - .planning/STATE.md (решения владельца 2026-09-15: WINDOWS #2 и #4 закрыты — строки для причин)
  - docs/SPEC.md §7.2 (ручная приёмка — одна сессия)
  - README.md (результат Task 1 — чек-лист ссылается на его шаги)
  </read_first>
  <action>
  docs/ACCEPTANCE.md по формату 03-UAT: назначение (UAT-гейт фазы 4, D-49, одна сессия по SPEC §7.2); нумерованные пункты с expected/result/note: (1) установка с нуля по README на живом столе (канал релиз-архива; expected: goswitchctl install без root, selfcheck шесть ok); (2) живой набор жестов в реальном поле: одиночный Right Shift — флип с индикатором; двойной — слово ghbdtn→привет (три регистра); тройной — фраза; при выделении — выделенный текст; Shift+RightCtrl — комбо; смешанное слово — точечная конверсия; (3) конфиг: правка ~/.config/goswitch/config.yaml + goswitchctl reload — применение без рестарта; (4) goswitchctl status — счётчики растут; (5) uninstall — стол восстановлен, ввод работает; (6) Summary-блок (total/passed/issues/pending). Владелец заполняет result/note в своей сессии.
  WINDOWS-ledger сверка (Pitfall 11 — ship-гейт читает open_count): gsd-tools windows fixed 2 (deviation закрыта: владелец подтвердил чтение D-24 на verify-гейте Фазы 3, STATE 2026-09-15); gsd-tools windows waive 4 "owner accepted GTK4-Wayland forwarded-events platform limitation (UAT 2026-09-15, REQUIREMENTS MACR-01 caveat)" — причины из решений владельца в STATE.md. После глагонов: frontmatter open_count == 0, waived/fixed счётчики согласованы; сверка grep-гейтом. Commit: docs(04-07).
  </action>
  <verify>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && grep -q "expected:" docs/ACCEPTANCE.md && grep -q "goswitchctl install" docs/ACCEPTANCE.md && grep -q "uninstall" docs/ACCEPTANCE.md && grep -q "Summary" docs/ACCEPTANCE.md && echo CHECKLIST-OK</automated>
    <fails_when>отсутствие CHECKLIST-OK — чек-лист без формата или обязательных секций</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && grep -q "open_count: 0" .planning/WINDOWS.md && echo LEDGER-CLEAN</automated>
    <fails_when>отсутствие LEDGER-CLEAN — ledger не сверен (ship-гейт будет заблокирован)</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise run ci</automated>
    <fails_when>non-zero exit — зелёная итерация D-08 не достигнута</fails_when>
  </verify>
  <done>Чек-лист D-49 в repo (формат 03-UAT, шесть групп пунктов с expected/result/note + Summary); ledger: #2 fixed, #4 waived с причиной, open_count 0</done>
  <acceptance_criteria>
  - docs/ACCEPTANCE.md: пункты установки/жестов/конфига/статуса/uninstall с expected-полями и Summary-блоком
  - .planning/WINDOWS.md frontmatter: open_count: 0; строка #4 несёт причину waive со ссылкой на решение владельца 2026-09-15
  - ledger-изменения произведены глагонами gsd-tools (не ручной правкой статусов в таблице)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Релиз v1.0.0 — гейты → тег → публикация → проверка ассетов и канала go install (D-37/D-38, INST-01)</name>
  <reversibility rating="costly">Публичный акт: релиз удаляем, но факт публикации виден; до тега — только зелёные гейты</reversibility>
  <precondition>ВСЕ гейты зелёные на HEAD: mise run ci; mise run e2e-install-cycle; mise run e2e-perf (бюджет); матрица v3 зелёная локально; двойной прогон 04-06 доказан; README/чек-лист запечены. gh аутентифицирован, remote = github.com/Djarvur/goswitch</precondition>
  <files>.planning/WINDOWS.md</files>
  <read_first>
  - .github/workflows/release.yml (конвейер 04-03 — что ожидать на теге)
  - .goreleaser.yaml (ассеты: tar.gz с обоими бинарниками + checksums.txt)
  - .planning/phases/04-postavka-i-priemka/04-03-SUMMARY.md (решения discretion: v1.0.0, amd64, tar.gz)
  - docs/ci-runner.md (если release-workflow ещё не на default branch — регистрация workflow от default branch, прецедент 02-07)
  </read_first>
  <action>
  Финальная сверка гейтов (каждый — зелёный или задача не стартует; любой красный = СТОП и фикс): mise run ci; mise run e2e-install-cycle; mise run e2e-perf; go run ./test/e2e -matrix test/e2e/cases/matrix-v3.yaml. Убедиться, что workflow release зарегистрирован от default branch (если фаза ещё не смержена — bootstrap: тег не ставить до мержа; зарегистрировать workflow прецедентом 02-07). Поставить аннотированный тег: git tag -a v1.0.0 -m "goswitch v1.0.0" && git push origin v1.0.0. Наблюдение: gh run watch (workflow release) до conclusion success. Проверки публикации: gh release view v1.0.0 — ассеты содержат tar.gz и checksums.txt; скачать архив во временный каталог, распаковать, запустить goswitchd -version → печатает v1.0.0 (прошивка D-37 на реальном артефакте); сверить checksum: sha256sum -c checksums.txt (в каталоге загрузки). Канал go install: curl -s https://proxy.golang.org/github.com/!djarvur/goswitch/@v/v1.0.0.info — версия видна (proxy подхватывает с задержкой до минут; при лаге — повторить, зафиксировав в SUMMARY). Все вердикты (run id, контрольные суммы, -version вывод) — в SUMMARY. При провале публикации — чинить конвейер и re-tag НЕ трогая v1.0.0 (использовать v1.0.1 после фикса; удаление неудачного релиза/тега — допустимый откат, отметить в SUMMARY). Commit: docs(04-07) (если менялись заметки/доки по итогам).
  </action>
  <verify>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && gh release view v1.0.0 --json assets --jq '.assets[].name' | grep -q "checksums.txt" && gh release view v1.0.0 --json assets --jq '.assets[].name' | grep -q "tar.gz" && echo RELEASE-ASSETS-OK</automated>
    <fails_when>отсутствие RELEASE-ASSETS-OK — релиз не опубликован или ассеты неполны</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && gh release view v1.0.0 --json assets --jq -r '.assets[].url' >/dev/null && d=$(mktemp -d) && gh release download v1.0.0 -D "$d" -p "*.tar.gz" && tar -xzf "$d"/*.tar.gz -C "$d" && "$d"/goswitchd -version | grep -q "v1.0.0" && echo STAMPED-OK</automated>
    <fails_when>отсутствие STAMPED-OK — ассет не скачивается/не распаковывается или версия не прошита (goswitchd -version без v1.0.0)</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && curl -s https://proxy.golang.org/github.com/!djarvur/goswitch/@v/v1.0.0.info | grep -q "v1.0.0" && echo PROXY-OK</automated>
    <fails_when>отсутствие PROXY-OK — module proxy не видит тег (канал go install недоступен); повторить после задержки proxy, при стойком отсутствии — эскалация в SUMMARY</fails_when>
  </verify>
  <done>Релиз v1.0.0 опубликован конвейером: ассеты (tar.gz оба бинарника + checksums) на месте, контрольная сумма сходится, версия прошита (v1.0.0), канал go install виден proxy; до тега все гейты были зелёные</done>
  <acceptance_criteria>
  - перед тегом: ci, install-cycle, perf (под бюджетом), v3-матрица — зелёные (вердикты в SUMMARY)
  - gh release view v1.0.0: tar.gz + checksums.txt; скачанный goswitchd -version печатает v1.0.0
  - sha256sum -c checksums.txt проходит по скачанному архиву
  - proxy.golang.org отдаёт v1.0.0.info — канал go install github.com/Djarvur/goswitch/cmd/goswitchd@v1.0.0 жив
  - SUMMARY: run id релиза, контрольные суммы, вывод -version, даты гейтов
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README → действия пользователя | неверная инструкция = сломанный стол читателя; рассинхрон языков = выборочная ложь |
| тег → публичный релиз | публикация неот проверенного кода необратима по доверию (удаление не стирает историю скачиваний) |
| ledger → ship-гейт | несведённый ledger блокирует /gsd-ship (Pitfall 11) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-07-01 | Tampering | README документирует разрушительные ручные шаги (голый write-cache) | high | mitigate | запрет + grep-гейт NO-BARE-CACHE в обоих языках (Падение 1 — доступность IME пользователя) |
| T-04-07-02 | Tampering | рассинхрон EN/RU фактов (D-50) | medium | mitigate | grep-гейт равенства команд установки; сверка чисел таблицы — acceptance-критерий с пином в SUMMARY |
| T-04-07-03 | Tampering | публикация непроверенного релиза | high | mitigate | precondition-гейт: тег только после ci + install-cycle + perf + v3 зелёные; неудача — v1.0.1 после фикса, не тихий re-tag |
| T-04-07-04 | Information Disclosure | perf-таблица/чек-лист раскрывают лишнее | low | mitigate | числа и вердикты, без содержимого полей (D-20/D-21) |
</threat_model>

<verification>
- README-гейты: README-COMPLETE, NO-BARE-CACHE, синхрон команд EN==RU
- CHECKLIST-OK + LEDGER-CLEAN (open_count: 0)
- Релиз: RELEASE-ASSETS-OK, STAMPED-OK (v1.0.0 в ассете), PROXY-OK (канал go install)
- mise run ci зелёный (D-08)
</verification>

<success_criteria>
- Критерий №4 фазы готов: двуязычные доки опубликованы, чек-лист приёмки в repo, владелец проходит UAT-гейт по D-49 (одна сессия §7.2)
- INST-01 завершён: оба канала наполнены реальным релизом v1.0.0 с проверяемой версией и checksums
- Ship-гигиена: WINDOWS open_count == 0
</success_criteria>

<artifacts_produced>
## Artifacts this plan produces (символы плана 04-07)

- README.md (переписан, EN первичен) + README.ru.md (новый, RU) — публичный фронт: установка/самопроверка/откат/perf-таблица/приватность/troubleshooting
- docs/ACCEPTANCE.md — чек-лист приёмки D-49 (формат 03-UAT)
- .planning/WINDOWS.md — ledger сверен: #2 fixed, #4 waived, open_count 0
- Публичные артефакты: тег v1.0.0, GitHub Release (tar.gz + checksums.txt), proxy-видимость v1.0.0 (канал go install)
</artifacts_produced>

<output>
Create `.planning/phases/04-postavka-i-priemka/04-07-SUMMARY.md` when done
</output>
