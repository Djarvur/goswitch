---
phase: 04-postavka-i-priemka
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - test/e2e/focus_helper.py
  - test/e2e/matrix.go
  - test/e2e/quiesce_test.go
autonomous: true
gap_closure: true
gap_ids: [G-4-1]
requirements: [INST-01]
# scope_note: план чинит harness-дефект G-4-1 (04-UAT.md), из-за которого формальный D-48-гейт
# (критерий №2 фазы — двойной зелёный на нетронутой сессии) ронял ЗДОРОВЫЕ кейсы; requirements
# указывает INST-01 как ближайшее фазовое покрытие (прецедент scope_note 04-05/04-06).

estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "witness-проба (focus_helper.py witness) отвечает через focus-first обход: у каждого приложения состояние FOCUSED проверяется на фреймах ДО спуска в детей; обходится только поддерево сфокусированного фрейма; полный обход всех нод всех приложений выполняется ТОЛЬКО как фолбэк, когда сфокусированного фрейма нет ни у одного приложения (G-4-1 fix direction, п.1)"
    - "Семантика witness не изменилась: грамматика строки app:role:chars=N, предпочтение INPUT_ROLES (TEXT/PASSWORD_TEXT/ENTRY) над фреймом, arms «фрейм-fallback с chars=-1» и «(none)» — заблокированная сессия (shell PASSWORD_TEXT, прецедент 01-03) отвечает как раньше, потому что фолбэк — это сегодняшний код дословно"
    - "Бюджет квиеса сходится арифметически (защита в глубину, fix direction п.2): witnessProbeTimeout поднят 4s→10s, пол масштабируемости запинен корпусом (RED→GREEN); инвариант «окно witnessQuieceAttempts*surfaceFocusWait покрывает полные пробы всех попыток плюс межпробные паузы» запинен; witnessQuiesceWindow — единый источник для дедлайна и текста ошибки"
    - "Живой смок на desktop: witness со сфокусированной ENTRY-поверхностью отвечает exit 0 строкой ENTRY-класса заметно быстрее 10s-пола; witness после закрытия поверхности отвечает promptly (фрейм-fallback или (none)) — фолбэк-рука живая, не клин"
    - "mise run ci зелёный (директива 2 — зелёная итерация); формальный fresh-session D-48-диспетч — шаг владельца ПОСЛЕ плана и в план НЕ входит"
  artifacts:
    - path: test/e2e/focus_helper.py
      provides: "focus-first witness-обход (фреймы до спуска, поддеревья только сфокусированных фреймов, полный обход — фолбэк) + docstring-контракт с G-4-1-обоснованием"
    - path: test/e2e/matrix.go
      provides: "witnessProbeTimeout = 10s (защита в глубину) + witnessQuiesceWindow (именованная константа окна квиеса, комментарий с G-4-1-доказательством)"
    - path: test/e2e/quiesce_test.go
      provides: "headless-корпус бюджета квиеса: пол масштабируемости пробы (RED→GREEN) + инвариант покрытия окна (прецедент watchdog_test.go)"
  key_links:
    - from: test/e2e/matrix.go
      to: test/e2e/focus_helper.py
      via: "matrixQuiesce зовёт /usr/bin/python3 {helper} witness (matrix.go:706); фокус-first делает пробу быстрой при БОЛЬШИХ деревьях, 10s-пол страхует остальные режимы деградации моста"
      pattern: "witness"
    - from: test/e2e/quiesce_test.go
      to: test/e2e/matrix.go
      via: "корпус читает witnessProbeTimeout / witnessQuieceAttempts / surfaceFocusWait / witnessQuiesceWindow — константы как контракт"
      pattern: "witnessProbeTimeout"
  prohibitions:
    - statement: "Focus-first НЕ ДОЛЖЕН применяться к exhaustive-обходам (focused_inputs/focused-inputs, cmd_text, focused-input-pid): их контракт — перечислить ВСЕ кандидатов (лаг фокуса 02-02 — сфокусированная нода может жить в НЕсфокусированном фрейме, который focus-first пропустит); exhaustive-воркаер стоит стендового отказа от инжекции"
      status: flagged
      verification: judgment
    - statement: "witness-проба НЕ ДОЛЖНА читать/печатать текст поверхности — только роль и счётчик символов (грамматика app:role:chars=N; приватность логов D-20/D-21)"
      status: enforced
      verification: automated
  assumptions:
    - "FLAGGED (масштаб доказательства): локальный живой смок доказывает корректность обхода на столе с обычными деревьями; масштабируемость на больших fresh-session деревьях (~6100 нод shell+gjs, голый обход ~3.0s — profiling 2026-09-17 в 04-UAT.md) доказывается владельцем формальным D-48-диспетчем ПОСЛЕ плана; локальный замер пробы прилагается как слабое свидетельство"
    - "FLAGGED (мультифокус): когда несколько поверхностей одновременно репортят FOCUSED (лаг моста), focus-first отвечает первой INPUT_ROLES-нодой из поддерева сфокусированного фрейма в порядке desktop→frames; полный порядок кандидатов и отказ от инжекции остаются зоной exhaustive focused-inputs (02-02 контракт) — единственные потребители голого witness (matrixQuiesce, focusWitness) нуждаются в prompt-ответе, не в перечислении"

---

<objective>
Закрыть G-4-1: a11y witness-проба выходит за бюджет на больших desktop-деревьях — голый обход всех нод (~3.0s на свежесессионных 6100 нод shell+gjs) пересекает witnessProbeTimeout 4s, и 30s-гейт квиеса роняет здоровые кейсы формального D-48-прогона (8/31 и 3/31 в попытках 1-2, 04-UAT.md).

**As a** e2e-стенд, **I want** witness-пробу, которая находит сфокусированную поверхность через поддеревья сфокусированных фреймов, не обходя весь стол, **so that** гейт квиеса отвечает в бюджете на столе любого размера и формальный D-48-гейт перестаёт падать на здоровых кейсах.

Purpose: harness-дефект масштабируемости, не продуктовый регресс — починка разблокирует приёмочный гейт фазы (критерий №2).
Output: focus-first witness в focus_helper.py (полный обход — только фолбэк), witnessProbeTimeout 10s + witnessQuiesceWindow в matrix.go (защита в глубину), headless-корпус quiesce_test.go (RED→GREEN), живой смок.
</objective>

<execution_context>
@/home/nil/DiskD/W/Djarvur/goswitch/.zcode/gsd-core/workflows/execute-plan.md
@/home/nil/DiskD/W/Djarvur/goswitch/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-postavka-i-priemka/04-UAT.md (G-4-1 — полный диагноз, profiling-доказательство, fix direction; диагноз НЕ пере-проводить, скоуп НЕ расширять)
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-postavka-i-priemka/04-PATTERNS.md (§ focus_helper.py — конвенции хелпера; § perf.go — timing-knob convention, именованные константы в шапке файла)
@test/e2e/matrix.go (матрица: witnessProbeTimeout/witnessQuieceAttempts :682-691, matrixQuiesce :693-720)
@test/e2e/focus_helper.py (cmd_witness :144-164, walk :115-133, applications :106-112, focused_inputs :213-230 — exhaustive-контракт, docstring-таблица :1-81)
@test/e2e/watchdog_test.go (прецедент headless-корпуса чистой логики — форма для quiesce_test.go)
@.planning/phases/04-postavka-i-priemka/red-evidence/04-04-task1-red.json (форма RED-свидетельства)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Корпус бюджета квиеса (RED→GREEN) + защита в глубину: witnessProbeTimeout 4s→10s, witnessQuiesceWindow</name>
  <files>test/e2e/quiesce_test.go, test/e2e/matrix.go</files>
  <read_first>
    - test/e2e/matrix.go:682-720 — witnessProbeTimeout (4s, комментарий про live finding 02-06), witnessQuieceAttempts (2), matrixQuiesce (цикл probe→overall-check→sleep; ошибка печатает witnessQuieceAttempts*surfaceFocusWait)
    - test/e2e/watchdog_test.go — прецедент: корпус чистой логики без живого стола, комментарий-шапка называет план и причину
    - test/e2e/case_m1.go:13-24 — timing-knob convention: именованные константы, никогда не sprinkle
    - .planning/phases/04-postavka-i-priemka/red-evidence/04-04-task1-red.json — форма RED-свидетельства (command/exitCode/expected/actual/output)
  </read_first>
  <behavior>
    - TestWitnessProbeBudget_ScalabilityFloor: witnessProbeTimeout >= 10*time.Second. RED при нынешних 4s (G-4-1: голый обход ~3.0s на свежесессионном дереве 6100 нод + любая добавочная латентность пересекал 4s; пол 10s = измеренный худший случай с ~3x запасом). GREEN после поднятия константы.
    - TestMatrixQuiesce_WindowCoversProbes: time.Duration(witnessQuieceAttempts)*(witnessProbeTimeout+witnessPoll) <= witnessQuiesceWindow — окно квиеса покрывает ПОЛНУЮ пробу на каждую попытку плюс межпробные паузы (при 2 попытках: 2*(10s+100ms)=20.2s <= 30s). Инвариант ловит будущий дрейф констант (кто-то поднимет пробу до 20s — корпус падает до живого прогона).
    - TestMatrixQuiesce_WindowSource: witnessQuiesceWindow == witnessQuieceAttempts*surfaceFocusWait (пин единого источника: дедлайн цикла и текст ошибки читают одну константу; == 30*time.Second при сегодняшних 2×15s).
  </behavior>
  <action>
    Строгий TDD (директива 1 CONVENTIONS.md): сначала RED, потом GREEN.

    RED СТУПЕНЧАТЫЙ (правка по чекеру: два оконных теста ссылаются на ещё не существующую witnessQuiesceWindow — их RED-форма это ошибка КОМПИЛЯЦИИ пакета, и она не должна маскировать чистое падение первого теста).
    RED шаг 1: создай test/e2e/quiesce_test.go (package main) по образцу watchdog_test.go — шапка-комментарий называет план 04-08 и G-4-1 (04-UAT.md: harness-дефект масштабируемости witness-пробы, попытки 35142914380/35188570260) — ТОЛЬКО с TestWitnessProbeBudget_ScalabilityFloor. Прогони `go test ./test/e2e/ -run 'WitnessProbeBudget' -race -count=1 -v` — чистое падение УТВЕРЖДЕНИЯ (4s < 10s), билд зелёный. Сохрани RED-свидетельство шага 1 в .planning/phases/04-postavka-i-priemka/red-evidence/04-08-task1-red.json по форме 04-04-task1-red.json (command/exitCode/targetTest/expected/actual/output — транскрипция вывода).
    RED шаг 2: добавь в тот же файл TestMatrixQuiesce_WindowCoversProbes и TestMatrixQuiesce_WindowSource. Прогони `go test ./test/e2e/ -run 'MatrixQuiesce' -race -count=1 -v` — ожидаемая RED-форма: ошибка компиляции `undefined: witnessQuiesceWindow`; допиши её в тот же red-evidence JSON второй записью.

    GREEN (правки ТОЛЬКО в блоке констант и комментариев matrix.go:682-720, логика цикла matrixQuiesce не меняется):
    - witnessProbeTimeout = 10 * time.Second; комментарий расширь: помимо клина реапа 02-06, пол масштабируемости G-4-1 — голый полный обход свежесессионного дерева (3673 ноды gnome-shell + 2437 gjs, ~3.0s) пересекал прежние 4s; 10s держит запас против измеренного худшего случая; фокус-first обход хелпера (Task 2) держит реальные пробы субсекундными — константа теперь защита в глубину.
    - Добавь именованную константу witnessQuiesceWindow = witnessQuieceAttempts * surfaceFocusWait; используй её в matrixQuiesce и в тексте ошибки (сейчас выражение дублируется в :702 и :713-714). Комментарий: окно покрывает полные пробы всех попыток плюс межпробные паузы (инвариант запинен в quiesce_test.go); дедлайн проверяется после пробы, поэтому максимум одна проба переходит номинальное окно — верхняя граница остаётся у вочдога кейса (caseTimeout).
    - witnessQuieceAttempts оставь 2 — ревизия арифметики зафиксирована комментарием: 2 попытки по (10s проба + 100ms пауза) = 20.2s внутри окна 30s. Существующее имя с опечаткой НЕ переименовывать (работающий lint, ренейм — чистый churn).
    - witnessPoll из case_m1.go переиспользуется как есть (100ms).
  </action>
  <verify>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && test -f .planning/phases/04-postavka-i-priemka/red-evidence/04-08-task1-red.json && go test ./test/e2e/ -run 'WitnessProbeBudget|MatrixQuiesce' -race -count=1 && mise run ci</automated>
  </verify>
  <done>RED-свидетельство записано (шаг 1: пол 10s падал на 4s чистым падением утверждения; шаг 2: оконные тесты — ошибкой компиляции undefined: witnessQuiesceWindow); witnessProbeTimeout = 10s; witnessQuiesceWindow — единый источник дедлайна и текста ошибки; корпус quiesce_test.go зелёный; mise run ci зелёный.</done>
  <acceptance_criteria>
    - test/e2e/quiesce_test.go содержит три теста из behavior-блока и проходит при -race
    - matrix.go: witnessProbeTimeout == 10*time.Second; выражение witnessQuieceAttempts*surfaceFocusWait встречается в исполняемом коде ровно один раз (в объявлении witnessQuiesceWindow)
    - red-evidence/04-08-task1-red.json существует и описывает ОБЕ RED-формы: чистое падение утверждения TestWitnessProbeBudget_ScalabilityFloor на 4s И ошибку компиляции undefined: witnessQuiesceWindow у оконных тестов
    - go build ./... && go test -race ./... && golangci-lint run — зелёные (mise run ci)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Focus-first witness-обход в focus_helper.py (полный обход — только фолбэк) + живой смок</name>
  <files>test/e2e/focus_helper.py</files>
  <read_first>
    - test/e2e/focus_helper.py:106-164 — applications() (порядок desktop), walk() (BFS одного дерева, best-effort на исчезающих нодах), cmd_witness (нынешняя семантика: fallback = первая сфокусированная нода ЛЮБОЙ роли; INPUT_ROLES предпочитаются; печать app:role:chars=N; arms chars=-1 и (none))
    - test/e2e/focus_helper.py:213-241 — focused_inputs()/focused_input(): ДОКУМЕНТИРОВАННЫЙ exhaustive-контракт (лаг фокуса 2026-09-12: shell-оверлей держит реальный фокус, пока нода кейса ещё репортит focused) — не трогать
    - test/e2e/focus_helper.py:1-81 — docstring-таблица: контрактная документация каждого режима с live-verified оговорками (прецедент 04-04)
    - .planning/phases/04-postavka-i-priemka/04-UAT.md — G-4-1: числа (3673+2437 нод, обход ~3.0s, пробы 3.8-6.3s против <1s на здоровой сессии)
    - .planning/phases/04-postavka-i-priemka/04-PATTERNS.md § focus_helper.py — конвенции: /usr/bin/python3, exit-коды 0/1/2
  </read_first>
  <behavior>
    - Приложение со сфокусированным фреймом: witness находит INPUT_ROLES-нод внутри ПОДДЕРЕВА только сфокусированного фрейма и печатает app:role:chars=N — не спускаясь в несфокусированные фреймы и не обходя другие приложения целиком (проверка FOCUSED на фрейме ДО всякого спуска).
    - Ни у одного приложения нет сфокусированного фрейма: witness выполняет СТАРЫЙ полный обход всех приложений дословно — ответ (input-строка, фрейм-fallback chars=-1, или (none)) совпадает с сегодняшним бит-в-бит; заблокированная сессия (shell PASSWORD_TEXT, 01-03) обслуживается этим же фолбэком.
    - Порядок и грамматика вывода не изменились: desktop-порядок приложений, child-порядок фреймов, предпочтение INPUT_ROLES над первой сфокусированной нодой любой роли.
  </behavior>
  <action>
    Измени ТОЛЬКО witness-путь focus_helper.py (python-хелпер по house-прецеденту 04-04 доказывается живьём — headless RED для него невозможен: CI без python3-gi/a11y-шины; headless TDD-дисциплину плана несёт корпус Task 1).

    Focus-first проход: для каждого приложения из applications() (desktop-порядок) перебери ПРЯМЫХ детей приложения (фреймы, child-порядок) и проверь состояние FOCUSED фрейма ДО всякого спуска в детей. Только для сфокусированного фрейма прогони существующий walk() по его поддереву с нынешней по-нодовой логикой ДОСЛОВНО: per-node FOCUSED-проверка, предпочтение INPUT_ROLES, запись первой сфокусированной ноды любой роли в fallback. Первая найденная INPUT_ROLES-нода печатает app:role:chars=N (char_count как сегодня) — и witness завершается.

    Фолбэк: вынеси тело сегодняшней cmd_witness в отдельную функцию полного обхода БЕЗ изменений логики и вызывай её только когда focus-first проход не нашёл ничего ни у одного приложения; её ответ (input-строка / фрейм-fallback chars=-1 / (none)) становится ответом cmd_witness. Так семантика фазы 01-03 (locked-session shell PASSWORD_TEXT) сохраняется по построению — фолбэк ЕСТЬ сегодняшний код.

    Явное не-цель (запрет из must_haves): focused_inputs()/focused_input()/cmd_text/cmd_focused_input_pid и прочие exhaustive-потребители walk() НЕ трогать — их контракт перечислять ВСЕХ кандидатов (лаг фокуса 02-02: сфокусированная нода в несфокусированном фрейме), focus-first её пропустил бы; exhaustive focused-inputs остаётся стендовым отказом от инжекции.

    Docstring-таблицу (режим witness, строки 4-10) дополни: focus-first обход — у каждого приложения FOCUSED проверяется на фреймах до спуска, обходятся только поддеревья сфокусированных фреймов, полный обход — фолбэк; обоснование G-4-1 (свежесессионные деревья gnome-shell 3673 + gjs 2437 нод делали голый обход ~3.0s и пересекали бюджет пробы стенда); оговорка о мультифокусе (focus-first отвечает первой сфокусированной фреймом INPUT_ROLES-нодой; exhaustive перечисление — zone focused-inputs).

    Живой смок (desktop доступен): /usr/bin/python3 -m py_compile на файл; затем спавни zenity --entry (запиши pid; закрывай по pid — никогда pkill/pgrep -f, прецедент anti-pattern), замерь wall-time witness-вызова — обязан ответить exit 0 одной строкой ENTRY-класса заметно быстрее 10s-пола (локальный замер запиши в SUMMARY как слабое свидетельство масштабируемости); закрой zenity по pid и прогони witness ещё раз — prompt-ответ (фрейм-fallback или (none)) доказывает живость фолбэк-руки.
  </action>
  <verify>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && /usr/bin/python3 -m py_compile test/e2e/focus_helper.py && mise run ci</automated>
  </verify>
  <done>witness отвечает через поддеревья сфокусированных фреймов; полный обход исполняется только при отсутствии сфокусированных фреймов везде; вывод и arms совпадают с прежними; exhaustive-обходы не тронуты; живой смок (ENTRY-строка + живой фолбэк) зелёный; mise run ci зелёный.</done>
  <acceptance_criteria>
    - cmd_witness проверяет FOCUSED на прямых детях приложения до спуска; walk вызывается только на поддеревьях сфокусированных фреймов либо в функции фолбэка
    - тело прежнего cmd_witness сохранено дословно как фолбэк-функция (arms (none) и chars=-1 живы)
    - focused_inputs/focused_input/cmd_text/cmd_focused_input_pid/cmd_focused_text_pid/cmd_grab_input_pid не содержат focus-first-логики
    - живой смок: witness со сфокусированным zenity-entry — exit 0, строка с ролью ENTRY, wall-time записан; witness после закрытия — prompt-ответ
    - /usr/bin/python3 -m py_compile и mise run ci — зелёные
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| e2e-стенд → живой desktop | стенд спавнит поверхности (zenity) и читает a11y-дерево через D-Bus; план новых доверенных границ не создаёт — изменения только в test/e2e (harness), продуктовая поверхность не тронута |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-08-01 | Information Disclosure | focus_helper.py witness | low | mitigate | witness печатает только роль и счётчик символов (app:role:chars=N), никогда текст поверхности — грамматика сохраняется дословно (D-20/D-21); acceptance_criteria Task 2 пиняет arms |
| T-04-08-02 | Denial of Service | matrixQuiesce (matrix.go) | low | mitigate | тяжёлый фолбэк-обход при отсутствии фокуса ограничен witnessProbeTimeout=10s (пол запинен корпусом) и окном witnessQuiesceWindow; верхняя граница — вочдог кейса; инвариант покрытия окна в quiesce_test.go ловит дрейф констант |
| T-04-08-03 | Tampering | focus_helper.py (e2e-артефакт, не продукт) | low | accept | хелпер исполняется только владельцем стенда из рабочего дерева; изменение — тот же файл, что и сегодня, без новых источников ввода; риск эквивалентен статус-кво остальных e2e-скриптов репо |
</threat_model>

<verification>
- go build ./... && go test -race ./... && golangci-lint run — зелёные (mise run ci, директива 2)
- Корпус quiesce_test.go: пол пробы + инвариант окна — зелёные при -race; RED-свидетельство в red-evidence/04-08-task1-red.json
- /usr/bin/python3 -m py_compile test/e2e/focus_helper.py — зелёный
- Живой смок Task 2: ENTRY-строка в бюджете + prompt-ответ фолбэка — зафиксированы в SUMMARY с wall-time
- matrixQuiesce не менял структуру цикла: probe → overall-check → sleep; изменились только константа пробы и именованное окно
</verification>

<success_criteria>
G-4-1 закрыт в объёме fix direction: (1) witness-проба больше не обходит весь стол при наличии сфокусированного фрейма — focus-first проход находит поверхность через поддеревья сфокусированных фреймов, полный обход остался фолбэком с прежней семантикой (включая locked-session PASSWORD_TEXT); (2) witnessProbeTimeout 10s с запиненным полом и инвариантом окна квиеса — защита в глубину; (3) exhaustive-обходы и продуктовый код не тронуты; (4) зелёная итерация (директива 2) соблюдена. Формальный fresh-session D-48-диспетч — следующий шаг владельца ПОСЛЕ этого плана (в план не входит).
</success_criteria>

<output>
Create `.planning/phases/04-postavka-i-priemka/04-08-SUMMARY.md` when done
</output>
