---
phase: 04-postavka-i-priemka
plan: 06
type: execute
wave: 4
# wave_note: depends_on alone gives a formal minimum of wave 3 (04-04 w2, 04-05 w1); mise.toml
# also overlaps 04-03 (wave 3), and the file-conflict rule bumps this plan to wave 4.
depends_on: ["04-04", "04-05"]
files_modified:
  - .github/workflows/e2e-matrix.yml
  - mise.toml
  - docs/ci-runner.md
autonomous: true
requirements: [INST-01]
# scope_note: план доставляет критерий №2 фазы (двойной прогон на нетронутой сессии, D-48) — приёмочный
# гейт поставки; requirements указывает INST-01 как ближайшее фазовое покрытие (см. scope_note 04-05).

estimate:
  tokens: 14000
  raw_tokens: 14000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "e2e-workflow гоняет v3 ДВАЖДЫ ПОДРЯД в одном job: два последовательных шага mise run e2e-matrix-v3, любое падение любого прогона = exit ≠ 0 job (D-48 — первый прогон не должен портить состояние для второго)"
    - "Вход fresh_session (bool, default false): при true префлайт-шаг сверяет возраст GNOME-сессии раннера (loginctl) с порогом (30 минут, константа с комментарием) и роняет job с подсказкой перелогина — операционализация «нетронутой сессии» (research A8/Open Question 2)"
    - "mise-задача e2e-matrix-v3 существует (единый интерфейс локально и в CI, D-09): go run ./test/e2e -matrix test/e2e/cases/matrix-v3.yaml с описанием по конвенции"
    - "docs/ci-runner.md документирует процедуру гейта: перелогин владельца → dispatch с fresh_session=true → оба прогона зелёные (шаг-за-шагом, включая возвращение раннера после перелогина)"
    - "Первый dispatch-прогон механики доказан живьём на green106: оба последовательных v3-прогона зелёные в одном запуске (fresh_session=false — доказательство механики; свежесессионный прогон остаётся шагом приёмки владельца)"
  artifacts:
    - path: .github/workflows/e2e-matrix.yml
      provides: "matrix-путь v3 по умолчанию (env MATRIX, никогда shell-интерполяция — WR-04), двойной прогон, вход fresh_session + loginctl-префлайт"
    - path: mise.toml
      provides: "[tasks.e2e-matrix-v3] — интерфейс двойного прогона"
    - path: docs/ci-runner.md
      provides: "процедура D-48-гейта (перелогин → dispatch fresh_session=true → двойной зелёный)"
  key_links:
    - from: .github/workflows/e2e-matrix.yml
      to: mise.toml
      via: "оба шага прогона зовут mise run e2e-matrix-v3 — CI и локально одна команда (D-09, прецедент pr-sanity)"
      pattern: "e2e-matrix-v3"
  prohibitions:
    - statement: "Двойной прогон НЕ ДОЛЖЕН пересоздавать сессию/пересоздавать окружение между прогонами №1 и №2 — суть проверки в том, что первый прогон НЕ портит состояние для второго (D-48); вмешательство между прогонами = подмена проверки"
      status: flagged
      verification: judgment
    - statement: "Свежесть сессии НЕ ДОЛЖНА декларироваться без проверки: fresh_session=true обязан сверять фактический возраст сессии и падать при превышении порога"
      status: flagged
      verification: judgment
  assumptions:
    - "FLAGGED (research A8): «свежеподнятая GNOME-сессия раннера» операционализирована как перелогин владельца + loginctl-возраст ≤ 30 минут на момент dispatch (порог — константа с комментарием, владелец может сменить); раннер живёт в графической сессии владельца (green106) — перелогин возвращает и раннер; процедуру и порог владелец подтверждает на verify-гейте"
    - "FLAGGED (механика vs гейт): план доказывает механику двойного прогона на ТЕКУЩЕЙ сессии (fresh_session=false); формальный D-48-гейт (свежая сессия) исполняется на приёмке фазы владельцем по процедуре из docs/ci-runner.md — разделение зафиксировано, чтобы не выдавать механику за гейт"
---

<objective>
Операционализация D-48: двойной подряд прогон матрицы v3 на нетронутой сессии раннера как CI-гейт. Workflow e2e-matrix получает v3-путь, второй последовательный прогон и проверку свежести сессии; mise-задача — единый интерфейс; книга раннера — процедуру гейта.

**As a** принимающая сторона, **I want** двойной прогон полной матрицы на свежей сессии без вмешательства между прогонами, **so that** доказано: первый прогон не портит состояние для второго — матрица зелёная не случайно.

Purpose: критерий №2 роадмапа (двойной зелёный на нетронутой сессии) — CI-механика гейта M4.
Output: расширенный e2e-matrix.yml (v3 + double run + fresh_session), mise-задача e2e-matrix-v3, процедура в docs/ci-runner.md, первый живой двойной прогон на green106.
</objective>

<execution_context>
@/home/nil/DiskD/W/Djarvur/goswitch/.zcode/gsd-core/workflows/execute-plan.md
@/home/nil/DiskD/W/Djarvur/goswitch/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-postavka-i-priemka/04-CONTEXT.md (D-48 — заперт дословно)
@.planning/phases/04-postavka-i-priemka/04-RESEARCH.md (§ A8/Open Question 2 — свежесть сессии; § Pitfall 10; § Validation Architecture — критерий 2)
@.planning/phases/04-postavka-i-priemka/04-PATTERNS.md (§ e2e-matrix.yml — входы :26-30, env-guard :69-77, concurrency :36-38)
@.planning/phases/04-postavka-i-priemka/04-05-SUMMARY.md (matrix-v3.yaml существует; спайк-вердикты поверхностей)
@.github/workflows/e2e-matrix.yml (текущая форма)
@docs/ci-runner.md (книга раннера)
</context>

<tasks>

<task type="auto">
  <name>Task 1: mise-задача e2e-matrix-v3 + двойной прогон в workflow + fresh_session-префлайт + процедура в книге раннера (D-48)</name>
  <reversibility rating="reversible">CI/таск-конфигурация; гейт-семантика задана D-48</reversibility>
  <precondition>matrix-v3.yaml существует и зелёная локально (04-05); green106 онлайн (gh api /repos/Djarvur/goswitch/actions/runners)</precondition>
  <files>.github/workflows/e2e-matrix.yml, mise.toml, docs/ci-runner.md</files>
  <read_first>
  - .github/workflows/e2e-matrix.yml (весь — входы, env-гварды MATRIX, concurrency e2e-gnome без cancel-in-progress, шаги mise)
  - .planning/phases/04-postavka-i-priemka/04-PATTERNS.md (§ e2e-matrix.yml — все три точки расширения; § mise.toml — конвенция описаний задач)
  - mise.toml (e2e-matrix/e2e-matrix-v2 — прецеденты описаний)
  - docs/ci-runner.md (структура книги: куда пишется процедура гейта)
  - test/e2e/README.md (run book стенда — согласование формулировок)
  </read_first>
  <action>
  mise.toml: [tasks.e2e-matrix-v3] по прецеденту v1→v2 — run = "go run ./test/e2e -matrix test/e2e/cases/matrix-v3.yaml", описание с план-рефом 04-06 и трейлером «(live GNOME session; NOT in ci)» — локальный интерфейс тот же, что в CI.
  .github/workflows/e2e-matrix.yml: (а) дефолт matrix-пути — v3 (вход/ENV сохраняет форму WR-04: MATRIX через env, не shell-интерполяция); (б) НОВЫЙ вход fresh_session (bool, default false, описание: «D-48 gate: set true only after owner relogin»); (в) шаг префлайта свежести: если fresh_session=true — прочитать возраст GNOME-сессии раннера через loginctl (текущий пользователь runner; Since-время сессии vs now), порог 30 минут — именованная переменная шага с комментарием; превышение = шаг падает с подсказкой «owner relogin required (D-48)»; (г) ДВА последовательных шага прогона: «matrix v3 run #1» и «matrix v3 run #2» — оба mise run e2e-matrix-v3 (env-guard сохранён), МЕЖДУ ними никаких шагов пересоздания/очистки — суть D-48; любое падение = exit ≠ 0 job; concurrency e2e-gnome сохранён без cancel-in-progress. Шапка-комментарий workflow дополнена обоснованием двойного прогона (D-48 цитатой).
  docs/ci-runner.md: раздел «D-48 double-run gate»: пошаговая процедура — (1) владелец завершает сессию и перелогинивается (раннер возвращается user-менеджером), (2) dispatch workflow с fresh_session=true и matrix=v3, (3) критерий успеха — оба прогона зелёные в одном запуске; примечание о том, что свежесть проверяется loginctl-префлайтом. mise run ci зелёный. Commit: feat(04-06).
  </action>
  <verify>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && grep -q "e2e-matrix-v3" mise.toml && grep -q "e2e-matrix-v3" .github/workflows/e2e-matrix.yml && grep -c "mise run e2e-matrix-v3" .github/workflows/e2e-matrix.yml | grep -q '^2$' && grep -q "fresh_session" .github/workflows/e2e-matrix.yml && grep -q "loginctl" .github/workflows/e2e-matrix.yml && echo DOUBLE-RUN-WIRED</automated>
    <fails_when>отсутствие DOUBLE-RUN-WIRED — задача/вход/префлайт отсутствуют или шагов прогона не два</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && grep -q "relogin" docs/ci-runner.md && grep -q "fresh_session" docs/ci-runner.md && echo RUNNER-DOC-OK</automated>
    <fails_when>отсутствие RUNNER-DOC-OK — процедура гейта не документирована</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise run ci</automated>
    <fails_when>non-zero exit — зелёная итерация D-08 не достигнута</fails_when>
  </verify>
  <done>Workflow: v3-дефолт, два последовательных прогона без вмешательства между ними, fresh_session-префлайт с порогом; mise-задача существует; процедура в книге раннера; ci зелёный</done>
  <acceptance_criteria>
  - mise.toml содержит [tasks.e2e-matrix-v3] с run-командой -matrix test/e2e/cases/matrix-v3.yaml
  - e2e-matrix.yml: ровно ДВА шага mise run e2e-matrix-v3 подряд; между ними нет шагов очистки/пересоздания (визуальная сверка шага — пин в SUMMARY)
  - вход fresh_session default false; префлайт-шаг с loginctl и порогом 30 минут падает с подсказкой relogin при превышении
  - MATRIX-путь проходит через env (WR-04 не нарушен — греп shell-интерполяции в run-строках отсутствует)
  - docs/ci-runner.md: процедура из трёх шагов с критерием успеха
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Живое доказательство механики — dispatch на green106, оба последовательных v3-прогона зелёные одним запуском</name>
  <reversibility rating="reversible">CI-прогон; повторяем по требованию</reversibility>
  <precondition>Workflow с изменениями Task 1 доступен для dispatch (после мержа фазы — либо bootstrap-PR с одним файлом workflow по прецеденту 02-07 d815e72, если dispatch нужен до мержа; gh аутентифицирован)</precondition>
  <files>.github/workflows/e2e-matrix.yml</files>
  <read_first>
  - .planning/phases/04-postavka-i-priemka/04-CONTEXT.md (D-48 дословно)
  - docs/ci-runner.md (обновлённый раздел гейта + регистрация/токены раннера — разделы 02-07)
  - .planning/phases/02-korrektsiya-slova-en-ru/02-CONTEXT.md (D-19 — прецедент bootstrap-dispatch с фаза-ветки)
  </read_first>
  <action>
  Dispatch механики: gh workflow run e2e-matrix (refresh inputs по текущей форме workflow; matrix=v3, fresh_session=false — доказательство МЕХАНИКИ двойного прогона; свежесессионная форма остаётся шагом приёмки, см. assumption). Дождаться (gh run watch / gh run view --log): job содержит оба прогона; вердикт — оба зелёные, run conclusion success. Если первый прогон портит состояние второму (второй красный при зелётом первом) — это НАХОДКА D-48-качества: зафиксировать, диагностировать (отчёты обоих прогонов), чинить стенд/демона, повторить dispatch; не ослаблять второй прогон. Результаты (run id, счётчики кейсов обоих прогонов) — в SUMMARY; оговорка механика-vs-гейт — там же. Если dispatch недоступен до мержа — bootstrap-PR с workflow-файлом по прецеденту 02-07 и dispatch --ref фаза-ветка. Commit при правках по находкам: fix(04-06).
  </action>
  <verify>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && gh run list --workflow e2e-matrix --limit 1 --json conclusion,displayTitle --jq '.[0].conclusion' | grep -q success</automated>
    <fails_when>вывод не success — последний запуск job упал (первый или второй прогон красный)</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && log=$(mktemp) && gh run view --workflow e2e-matrix --log >"$log" && test -s "$log" && grep -v '^$' "$log" | grep -ci 'FAIL' | grep -q '^0$' && echo NO-FAIL-ROWS</automated>
    <fails_when>non-zero exit — gh не отдал лог (auth/сеть/нет запусков: гейт обязан ПАДАТЬ, а не проходить формально по пустому вводу), лог пуст, или в нём есть FAIL-строки (падение кейса в любом из двух прогонов)</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise run ci</automated>
    <fails_when>non-zero exit — зелёная итерация D-08 не достигнута (включая fix-коммиты по находкам двойного прогона)</fails_when>
  </verify>
  <done>Один dispatch-запуск на green106: оба последовательных v3-прогона зелёные, conclusion success; счётчики и run id в SUMMARY; механика двойного прогона доказана живьём</done>
  <acceptance_criteria>
  - последний run e2e-matrix workflow: conclusion success
  - лог запуска содержит оба шага прогона и ноль FAIL-строк в обоих
  - SUMMARY: run id, количество кейсов прогона №1 и №2 (равное — superset v3), оговорка механика-vs-гейт (свежесессионный прогон — шаг приёмки)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dispatch → сессия владельца | двойной прогон работает в графической сессии владельца-раннера — падение job не должно оставлять стол поломанным |
| заявка свежести → факт | fresh_session без проверки = декларация, не гейт |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-06-01 | Spoofing | свежесть сессии заявлена, но не проверена | medium | mitigate | loginctl-префлайт с порогом и падением (D-48 операционализация A8) |
| T-04-06-02 | DoS | первый прогон портит состояние второму (утечка поверхностей/демонов) | medium | mitigate | сам D-48-гейт: второй прогон обязателен и без вмешательства; находка чинится, не ослабляется |
| T-04-06-03 | Elevation | токен workflow расширен заодно с доработкой | low | mitigate | e2e-matrix остаётся contents: read (греп-гейт 04-03 SCOPE-OK продолжает действовать) |
</threat_model>

<verification>
- греп-гейт DOUBLE-RUN-WIRED (задача + два шага + вход + loginctl)
- RUNNER-DOC-OK (процедура в книге)
- gh run list: последний e2e-matrix conclusion success; лог без FAIL-строк
- mise run ci зелёный (D-08)
</verification>

<success_criteria>
- D-48 операционализован и доказан механически: двойной подряд зелёный прогон v3 одним CI-запуском
- Свежесть сессии проверяема (fresh_session + loginctl), процедура владельца документирована
- Формальный свежесессионный гейт готов к исполнению на приёмке фазы (без вмешательства между прогонами)
</success_criteria>

<artifacts_produced>
## Artifacts this plan produces (символы плана 04-06)

- mise-задача `e2e-matrix-v3`
- .github/workflows/e2e-matrix.yml: вход `fresh_session`, loginctl-префлайт свежести (порог 30 минут — константа шага), шаги «matrix v3 run #1/#2», дефолт пути v3
- docs/ci-runner.md: раздел «D-48 double-run gate» (процедура перелогин → dispatch → критерий)
</artifacts_produced>

<output>
Create `.planning/phases/04-postavka-i-priemka/04-06-SUMMARY.md` when done
</output>
