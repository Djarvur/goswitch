---
phase: 04-postavka-i-priemka
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - internal/install/selfcheck.go
  - internal/install/selfcheck_test.go
  - cmd/goswitchctl/main.go
  - cmd/goswitchd/main.go
  - cmd/goswitchd/main_test.go
  - internal/ctlsvc/ctlsvc.go
  - internal/ctlsvc/ctlsvc_test.go
  - internal/session/actor.go
  - internal/session/actor_test.go
  - test/e2e/case_install.go
# scope_note: 10 файлов, но 8 из них — точечные однострочные вставки (var version / флаг /
# один токен renderStatus / одно поле Status); содержательные новые файлы — selfcheck.go + selfcheck_test.go.
autonomous: true
requirements: [INST-01]

estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`goswitchctl selfcheck` — сквозной живой контроль D-41: шесть шагов (версия → компонент виден ibus → user unit active → движок реально зарегистрирован → конфиг валиден → источник ввода goswitch), каждый печатает ok ИЛИ FAIL с подсказкой фикса (стиль e2e-preflight), exit ≠ 0 при любом красном"
    - "Шаг «компонент виден» чинится сам: перед красным вердиктом selfcheck перезапускает env-cache (write-cache с IBUS_COMPONENT_PATH) и перепроверяет — РОВНО один ремонт, не цикл (D-41, Падение 1)"
    - "`goswitchd -version` печатает версию сборки и выходит 0; release-сборки goreleaser прошивают тег/коммит/дату, сборки `go install` честно показывают dev (D-37)"
    - "`goswitchctl status` содержит токен version= со сборкой демона; однострочный key=value-канон не сломан (config_error остаётся последним полем), --json парсит токен без клиентских правок (D-37)"
    - "Живой install-cycle прогоняет selfcheck зелёным сразу после install (шаг добавлен в кейс 04-01)"
  artifacts:
    - path: internal/install/selfcheck.go
      provides: "Selfcheck(ctx, w io.Writer) error — таблица проверок с подсказками; ремонт env-cache на шаге компонента; шовы-пробы (activeEngines, ctlsvc status) для корпуса"
    - path: internal/install/selfcheck_test.go
      provides: "корпус: зелёный путь, каждый красный путь с подсказкой, ремонт-путь (первый list-engine без goswitch → env-cache повтор → зелёный), конфиг без файла = зелёный defaults"
    - path: cmd/goswitchd/main.go
      provides: "пакетные var version/commit/date (dev-фолбэк) + флаг -version (печать + exit 0)"
    - path: internal/session/actor.go
      provides: "поле Version в session.Status — демон прошивает свою сборку в статус"
    - path: internal/ctlsvc/ctlsvc.go
      provides: "токен version= в renderStatus (первым токеном; грамматика пар не нарушена)"
  key_links:
    - from: cmd/goswitchctl/main.go
      to: internal/install/selfcheck.go
      via: "тонкий клиент: selfcheck-диспетчеризация с собственным бюджетом (60 s), логика в пакете"
      pattern: "\\.Selfcheck\\("
    - from: internal/ctlsvc/ctlsvc.go
      to: internal/session/actor.go
      via: "renderStatus печатает st.Version — значение приходит из Status-снапшота актора, демон передаёт прошитую версию при конструировании"
      pattern: "version="
    - from: internal/install/selfcheck.go
      to: "engine live-проба"
      via: "шаг «движок реально зарегистрирован» — ListActiveEngines-dial по форме preflight.go:178-205 (шов для корпуса)"
      pattern: "ListActiveEngines"
  prohibitions:
    - statement: "selfcheck НЕ ДОЛЖЕН молча проходить при красном состоянии — каждый провал виден строкой FAIL с подсказкой и ненулевым кодом; «типу зелёного» нет места"
      status: flagged
      verification: judgment
    - statement: "selfcheck НЕ ДОЛЖЕН печатать пользовательский текст (содержимое конфига/поля) — только пути, вердикты и причины (D-20/D-21)"
      status: flagged
      verification: judgment
  assumptions:
    - "FLAGGED (probe INST-01 / unclassified): срок «установка самопроверяется» трактуется как selfcheck зелёный НЕПОСРЕДСТВЕННО после install на живом столе (не в отдельной чистой сессии); владельца проверяет UAT-шаг selfcheck после README-установки (D-49)"
    - "FLAGGED (планировочное решение по research Pattern 3): wire-компонент engine.NewComponent остаётся на схема-версии «0.1.0» (метаданные компонента ≠ сборочная версия); прошитая сборка видна только через goswitchd -version и status version= — осознанный выбор, зафиксировать в SUMMARY"
---

<objective>
Сквозная самопроверка установки (D-41) и прошивка версии в бинарники и статус (D-37): пользователь после установки одной командой `goswitchctl selfcheck` видит, что именно сломано и как чинить; любая сборка идентифицируема — база разбора баг-репортов публичного инструмента.

**As a** пользователь, у которого «что-то не работает», **I want** `goswitchctl selfcheck` показывает построчный зелёный/красный вердикт с подсказкой фикса, **so that** диагностика установки не требует чтения логов и issue-трекера.

Purpose: INST-01 (self-check после установки — критерий №1 роадмапа) + D-37 (база идентификации сборок для релиз-каналов 04-03).
Output: internal/install/selfcheck.go + корпус, goswitchd -version, токен version= в статусе, selfcheck-шаг в install-cycle.
</objective>

<execution_context>
@/home/nil/DiskD/W/Djarvur/goswitch/.zcode/gsd-core/workflows/execute-plan.md
@/home/nil/DiskD/W/Djarvur/goswitch/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-postavka-i-priemka/04-CONTEXT.md (D-37, D-41 — заперты)
@.planning/phases/04-postavka-i-priemka/04-RESEARCH.md (§ Pattern 3 — прошивка goreleaser-дефолтами; § Pitfall 2 — cache-visible ≠ daemon-visible; § Validation Architecture D-37)
@.planning/phases/04-postavka-i-priemka/04-PATTERNS.md (§ ctlsvc — renderStatus :132-157 и грамматика :125-131; § cmd/goswitchd — флаги :25-35; § preflight — checks-table :30-52 и listActiveEnginesContain :178-205)
@.planning/phases/04-postavka-i-priemka/04-01-SUMMARY.md (пакет internal/install, шовы, кейс install-cycle)
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Версия сборки — var version/commit/date + goswitchd -version + токен version= в статусе (D-37)</name>
  <reversibility rating="reversible">Механика stamping-а (reversibility D-37 из CONTEXT)</reversibility>
  <files>cmd/goswitchd/main.go, cmd/goswitchd/main_test.go, internal/session/actor.go, internal/session/actor_test.go, internal/ctlsvc/ctlsvc.go, internal/ctlsvc/ctlsvc_test.go</files>
  <read_first>
  - .planning/phases/04-postavka-i-priemka/04-RESEARCH.md (§ Pattern 3 — дефолтные ldflags goreleaser: -X main.version/main.commit/main.date; var version = "dev" с фолбэком)
  - .planning/phases/04-postavka-i-priemka/04-PATTERNS.md (§ cmd/goswitchd — блок флагов :25-35; § ctlsvc — renderStatus-токены :132-157, parseStatusLine уже генерик :159-173)
  - cmd/goswitchd/main.go (блок флагов; место конструирования session — куда передать версию)
  - internal/session/actor.go (структура Status, место поля Version)
  - internal/ctlsvc/ctlsvc.go (renderStatus — список фиксированных токенов)
  - internal/ctlsvc/ctlsvc_test.go (корпус однострочника — расширение)
  </read_first>
  <behavior>
  - TestVersionString (cmd/goswitchd): versionString() при пустых commit/date печатает ровно "goswitchd dev"; при прошитых version/commit/date — "goswitchd v1.0.0 abc123 2026-01-01" (формат: имя версия [коммит] [дата] без висячей пунктуации)
  - TestVersionFlag: флаг -version → печать строки версии и выход 0 БЕЗ запуска демона (ранний выход в main до run)
  - TestStatusCarriesVersion (session): Status-снапшот актора содержит Version, равный переданному при конструировании
  - TestRenderStatusVersionToken (ctlsvc): строка статуса содержит "version=dev" (или прошитое) ПЕРВЫМ токеном; config_error по-прежнему последний; пары без пробелов (грамматика :125-131)
  - TestParseStatusLineParsesVersion (cmd/goswitchctl, если корпус клиента рядом — иначе grep-гейт): parseStatusLine извлекает version без правок клиента (обобщённый парсер уже умеет)
  </behavior>
  <action>
  RED (D-07): cmd/goswitchd/main_test.go — TestVersionString/TestVersionFlag против текущего main (versionString нет — заглушка-файл для подлинного RED); actor_test/ctlsvc_test — кейсы Version. Прогон mise exec -- go test ./cmd/goswitchd/ ./internal/session/ ./internal/ctlsvc/ -race -count=1 — RED; red-evidence. Commit: test(04-02).
  GREEN — cmd/goswitchd/main.go: пакетные var version = "dev", var commit = "", var date = "" (goreleaser дефолтно прошивает main.version/main.commit/main.date — кастомные ldflags НЕ нужны, research Pattern 3); флаг -version bool: печать versionString() в stdout и os.Exit(0) до запуска демона; versionString() собирает строку из поведения. Версия передаётся в session при конструировании (точку входа подбирает executor по месту session.New/WithStatus-провайдера в main.go). internal/session/actor.go: поле Version string в Status (провайдер-интерфейс снапшота не меняется — research § ctlsvc). internal/ctlsvc/ctlsvc.go: renderStatus добавляет "version=" + st.Version первым токеном; комментарий грамматики обновлён. Клиент cmd/goswitchctl БЕЗ правок (parseStatusLine генерик) — если репр-кейс клиента существует рядом, он остаётся зелёным. mise run ci зелёный. Commit: feat(04-02).
  Осознанное решение (research Pattern 3, зафиксировать в SUMMARY): engine.NewComponent остаётся на схема-версии "0.1.0" — метаданные IBus-компонента не связаны со сборочной версией; прошивка видна только в -version/status.
  </action>
  <verify>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise exec -- go test ./cmd/goswitchd/ ./internal/session/ ./internal/ctlsvc/ -race -count=1</automated>
    <fails_when>строка FAIL — корпус версии/статуса не зелёный</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise run ci</automated>
    <fails_when>non-zero exit — зелёная итерация D-08 не достигнута</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise exec -- go run ./cmd/goswitchd -version</automated>
    <fails_when>non-zero exit или вывод не содержит "goswitchd dev" — флаг версии не работает в dev-сборке</fails_when>
  </verify>
  <done>-version печатает "goswitchd dev" в dev-сборке и выходит 0; статус несёт version= первым токеном; канон key=value и config_error-позиция не сломаны; корпус зелёный под -race</done>
  <acceptance_criteria>
  - cmd/goswitchd/main.go содержит пакетные var version/commit/date (dev-фолбэк) и флаг -version с ранним выходом 0
  - renderStatus печатает version= первым токеном; ctlsvc_test пинит форму строки; config_error остаётся замыкающим полем
  - session.Status содержит Version, заполненное от конструкции демона
  - cmd/goswitchctl/main.go НЕ изменён ради version (генерик-парсер) — если менялся, причина в SUMMARY
  - go run ./cmd/goswitchd -version печатает "goswitchd dev", exit 0
  </acceptance_criteria>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: goswitchctl selfcheck — шесть живых шагов D-41 с подсказками и ремонтом env-cache (INST-01)</name>
  <reversibility rating="reversible">Контракт диагностики (reversibility D-41 из CONTEXT)</reversibility>
  <precondition>Task 1 зелёный; install 04-01 работает (selfcheck тестируется против установленного состояния)</precondition>
  <files>internal/install/selfcheck.go, internal/install/selfcheck_test.go, cmd/goswitchctl/main.go, test/e2e/case_install.go</files>
  <read_first>
  - .planning/phases/04-postavka-i-priemka/04-CONTEXT.md (D-41 дословно — состав шагов)
  - .planning/phases/04-postavka-i-priemka/04-RESEARCH.md (§ Pitfall 1 — ремонт через env-cache; § Pitfall 2 — cache-visible ≠ daemon-visible: шаг компонента проверяет list-engine (кеш), шаг регистрации — ListActiveEngines (живой демон), это РАЗНЫЕ проверки)
  - .planning/phases/04-postavka-i-priemka/04-PATTERNS.md (§ preflight — checks-table :30-52, диагностика с подсказкой :59-66, listActiveEnginesContain :178-205; § cmd/goswitchctl — собственный бюджет для не-D-Bus подкоманд)
  - internal/install/install.go (Runner-шов, env-cache-вызовы, пути — переиспользовать)
  - test/e2e/preflight.go (форма live-пробы ListActiveEngines)
  - test/e2e/case_install.go (кейс — куда вставить selfcheck-шаг)
  - internal/config (Load — валидация конфига для шага 5; strict-декод D-33: пустой ЯВНЫЙ конфиг = отказ, отсутствующий = defaults)
  </read_first>
  <behavior>
  - TestSelfcheck_AllGreen (fakeRunner/шовы): шесть строк "ok …" в порядке D-41 (version → component → unit → engine → config → source), возврат nil
  - TestSelfcheck_ComponentRepair: первый ответ list-engine без goswitch → selfcheck перезапускает write-cache С ENV → второй ответ с goswitch → "ok component" (ремонт прозрачен в выводе); ровно один повтор
  - TestSelfcheck_ComponentRedAfterRepair: обе попытки без goswitch → "FAIL component-visible: …" с подсказкой (goswitchctl install), возврат ошибки
  - TestSelfcheck_EachRedPath: для каждого шага — красный сценарий даёт FAIL-строку с подсказкой фикса (unit inactive → systemctl --user enable --now goswitchd; движок не зарегистрирован → journalctl --user -u goswitchd; конфиг невалиден → путь и причина парса; источник не goswitch → goswitchctl install)
  - TestSelfcheck_ConfigNoFileGreen: ~/.config/goswitch/config.yaml отсутствует → "ok config (defaults: no config file)" — зелёный, demon на вшитых дефолтах (осознанное решение 04-планировщика: install НЕ генерирует конфиг)
  - TestSelfcheck_StatusProbeSeam: шаг version берётся из ctlsvc-статуса живого демона (шов-функция; демон не отвечает → FAIL с подсказкой запуска юнита)
  - Живой шаг: install-cycle после install прогоняет goswitchctl selfcheck — exit 0, шесть ok
  </behavior>
  <action>
  RED (D-07): internal/install/selfcheck_test.go — корпус behavior против заглушки selfcheck.go. Прогон mise exec -- go test ./internal/install/ -race -count=1 — RED; red-evidence. Commit: test(04-02).
  GREEN — internal/install/selfcheck.go: Selfcheck(ctx, w io.Writer) error — таблица проверок (preflight-паттерн checks-table): каждая строка "ok NAME" или "FAIL NAME: <подсказка>"; шаги: (1) version — шов ctlStatus func(ctx) (string, error): ответ содержит version=…; демона нет → FAIL с подсказкой systemctl --user start goswitchd; (2) component-visible — ibus list-engine содержит goswitch-en; красный → ОДИН ремонт write-cache(env) → перепроверка (Падение 1); (3) unit-active — systemctl --user is-active goswitchd == "active"; (4) engine-registered — шов activeEngines (форма preflight :178-205): содержит goswitch-en; (5) config-valid — файл ~/.config/goswitch/config.yaml существует → config.Load валиден (ошибка парса → FAIL с путём и причиной), отсутствует → зелёный defaults; (6) input-source — gsettings get org.gnome.desktop.input-sources sources содержит ('ibus', 'goswitch-en'). Все вызовы через существующий Runner-шов с env и дедлайном; вывод — только вердикты/пути (D-20/D-21). CLI: case "selfcheck" с собственным бюджетом selfcheckTimeout = 60 s (вне ctlCallTimeout); errUsage расширен "| selfcheck". test/e2e/case_install.go: после install — шаг goswitchctl selfcheck (exit 0, шесть ok в stdout). mise run ci зелёный; живой e2e-install-cycle. Commit: feat(04-02).
  </action>
  <verify>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise exec -- go test ./internal/install/ -race -count=1</automated>
    <fails_when>строка FAIL — корпус selfcheck не зелёный</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise run ci</automated>
    <fails_when>non-zero exit — зелёная итерация D-08 не достигнута</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise run e2e-install-cycle</automated>
    <fails_when>non-zero exit или отсутствие selfcheck-строк ok в выводе кейса — живая самопроверка после install не зелёная</fails_when>
  </verify>
  <done>Selfcheck гоняет шесть шагов D-41 с ремонт-путём env-cache, красные пути с подсказками покрыты корпусом; install-cycle прогоняет его зелёным живьём; exit-контракт ≠ 0 при любом FAIL</done>
  <acceptance_criteria>
  - internal/install/selfcheck.go экспортирует Selfcheck(ctx, io.Writer) error; порядок шагов = D-41
  - каждый красный путь печатает FAIL с конкретной подсказкой (пин корпуса); ремонт компонента — ровно один env-cache повтор
  - отсутствующий конфиг = зелёный defaults (install не генерирует конфиг — решение зафиксировано)
  - goswitchctl: selfcheck-диспетчеризация с бюджетом 60 s; errUsage содержит selfcheck
  - case_install.go: selfcheck-шаг после install — exit 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| selfcheck → OS-состояние | пробы читают живое состояние (ibus, systemd, gsettings) и ОДИН раз ремонтируют кеш — запись ограничена env-cache |
| вывод диагностики → терминал | пользовательский вывод не должен раскрывать содержимое конфига/поля |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-02-01 | DoS | selfcheck-ремонт зацикливается на повторяющемся выселении | medium | mitigate | ровно один повтор env-cache, затем красный вердикт — пин TestSelfcheck_ComponentRedAfterRepair |
| T-04-02-02 | Information Disclosure | вывод selfcheck | medium | mitigate | только вердикты/пути/причины парса, без содержимого конфига (D-20/D-21) |
| T-04-02-03 | Spoofing | ложный зелёный при проверке не того состояния | low | mitigate | component (кеш, list-engine) и engine (живой демон, ListActiveEngines) — РАЗНЫЕ шаги (Падение 2), оба обязательны |
</threat_model>

<verification>
- mise run ci зелёный (D-08)
- mise exec -- go test ./internal/install/ ./cmd/goswitchd/ ./internal/session/ ./internal/ctlsvc/ -race -count=1
- go run ./cmd/goswitchd -version печатает "goswitchd dev", exit 0
- mise run e2e-install-cycle — install → selfcheck шесть ok → коррекция → uninstall
</verification>

<success_criteria>
- D-41 исполнен дословно: шесть шагов, зелёный/красный с подсказкой, ремонт env-cache
- D-37-механика готова к прошивке goreleaser (04-03): var version/commit/date + -version + status version=
- install-cycle теперь самопроверяется — критерий №1 роадмапа (self-check после установки) доказан живьём
</success_criteria>

<artifacts_produced>
## Artifacts this plan produces (символы плана 04-02)

- `internal/install`: `Selfcheck(ctx, io.Writer) error` + шовы `ctlStatus`/`activeEngines` (selfcheck.go)
- `cmd/goswitchd`: пакетные `var version`/`var commit`/`var date` (dev-фолбэк), флаг `-version`, `versionString()`
- `internal/session`: поле `Version` в `session.Status`
- `internal/ctlsvc`: токен `version=` в renderStatus (первым)
- `cmd/goswitchctl`: подкоманда `selfcheck` (бюджет `selfcheckTimeout`), расширенный errUsage
- test/e2e: selfcheck-шаг кейса install-cycle
</artifacts_produced>

<output>
Create `.planning/phases/04-postavka-i-priemka/04-02-SUMMARY.md` when done
</output>
