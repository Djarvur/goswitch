---
phase: 02-korrektsiya-slova-en-ru
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/correct/buffer.go
  - internal/correct/direction.go
  - internal/correct/convert.go
  - internal/correct/verify.go
  - internal/correct/plan.go
  - internal/correct/buffer_test.go
  - internal/correct/direction_test.go
  - internal/correct/convert_test.go
  - internal/correct/verify_test.go
  - internal/correct/plan_test.go
autonomous: true
requirements: [CORR-04, CORR-05, CORR-07]

estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Направление чистого слова определяется по составу букв: латиница → ENtoRU, кириллица → RUtoEN, букв нет → отказ; смешанное слово (любая одна чужая буква) → отказ (D-16 базис); цифры и пунктуация нейтральны — «смешанным» слово не делают (CORR-04)"
    - "Конверсия сохраняет регистр посимвольно: ghbdtn→привет, GHBDTN→ПРИВЕТ, Ghbdtn→Привет, привет→ghbdtn, ПРИВЕТ→GHBDTN, Привет→Ghbdtn; цифры и прочие (не-буквенные) руны токена идут через таблицу тождественно: `ghbdtn2026`→`привет2026`, `ghbdtn,`→`привет,` (CORR-05, D-14/D-15)"
    - "Токен = максимальный запуск буквенно-ёмких клавиш (объединение EN+RU слоёв по позиции) и цифр: `ghbdtn,` — один токен (запятая ёмкая — RU 'б'), `;` — ёмкая (RU 'ж'), `/` и `-` — граница; пробел завершает слово, но НЕ выкидывает его из буфера (D-13/D-14/D-15)"
    - "BuildPlan вычисляет ОБА уровня лестницы в рунах (не байтах): уровень 1 {Offset:-(tail+n), NChars:tail+n, Commit:converted+tail}, уровень 2 {Backspaces:tail+n, Commit:converted+tail}; кириллический токен из 6 рун — счёт 6, не 12 (CORR-07, ADR-003, Pitfall 2)"
    - "Сверка ADR-004 — суффиксное правило: текст перед курсором обязан ЗАКАНЧИВАТЬСЯ токеном; хвост после токена в тексте перед курсором НЕ допустим; короче токена — не совпадение"
    - "После успешной коррекции ReplaceToken заменяет токен в буфере на исправленный (инвариант «буфер == текст перед курсором»), повторный двойной тап конвертирует обратно — toggle"
    - "Пакет чист: ни одного импорта godbus/времени/горутин — весь корпус гоняется headless под -race без живой сессии"
  artifacts:
    - path: internal/correct/buffer.go
      provides: "чистый Buffer: Push/Backspace/HardReset/Token/Tail/ReplaceToken, хранение []rune, границы токенов по буквенно-ёмким клавишам (letterCapable)"
    - path: internal/correct/direction.go
      provides: "type Dir + Detect(token []rune) (Dir, bool): классификация рун через unicode.Latin/Cyrillic, смешанное слово → отказ (D-16), нейтральность цифр/пунктуации (D-15)"
    - path: internal/correct/convert.go
      provides: "Convert(token []rune, dir Dir) ([]rune, bool): буквы через layouts.ENToRU/RUToEN (регистр сохраняется конструктивно), цифры и не-буквенные руны — тождественно"
    - path: internal/correct/verify.go
      provides: "MatchesSuffix(textBeforeCursor, token []rune) bool — суффиксное правило сверки ADR-004"
    - path: internal/correct/plan.go
      provides: "Level/Plan/BuildPlan(token, tail, converted []rune, caps uint32): уровень по биту CapSurroundingText, арифметика диапазона в рунах для обоих уровней (формулы уровня 2 используются планом 02-05 без переделки)"
  key_links:
    - from: internal/correct/convert.go
      to: layouts/tables.go
      via: "per-rune lookup layouts.ENToRU/RUToEN — обе карты, оба shift-уровня на позицию"
      pattern: "ENToRU|RUToEN"
    - from: internal/correct/plan.go
      to: engine/keys.go
      via: "BuildPlan выбирает уровень по биту CapSurroundingText (1 << 5) — константа без D-Bus-импорта самого транспорта"
      pattern: "CapSurroundingText"
    - from: internal/correct/buffer.go
      to: internal/correct/direction.go
      via: "Token() возвращает руны токена — вход Detect/Convert; заменяется ReplaceToken после коррекции (toggle-инвариант)"
      pattern: "\\.Token\\(\\)"
  prohibitions:
    - statement: "Определение направления НЕ ДОЛЖНО использовать словари, частотные или лингвистические эвристики — только состав буфера по скрипту букв (CORR-04; SPEC §11: словари — для автокоррекции, которая отвергнута)"
      status: resolved
      verification: judgment
  assumptions:
    - "FLAGGED (edge-probe, CORR-01, unclassified): проба границ «последнего слова» не классифицирована автоматикой — предположение: набор решений D-13/D-14/D-15/D-16 + ADR-002/003/004 полностью определяет выделение слова; остаточные неклассифицированные границы покрываются корпусом buffer_test.go и e2e-матрицей (02-06)"
    - "FLAGGED (edge-probe, CORR-05, unclassified): регистро-сохранение не классифицировано автоматикой — предположение: таблицы Фазы 1 несут оба shift-уровня на позицию ('G':'П' и 'g':'п'), регистр сохраняется конструктивно; корпус пинит три регистра в обе стороны"
---

<objective>
Чистый пакет internal/correct — вся словесная логика фазы как headless-библиотека с полным TDD-корпусом: буфер-фраза с границами токенов (D-13/D-14/D-15), детектор направления с отказом смешанного слова (D-16), конверсия с сохранением регистра, суффиксная сверка ADR-004 и арифметика лестницы ОБЕИХ уровней в рунах (формулы уровня 2 поставляются здесь, подключение — план 02-05).

Purpose: CORR-04/CORR-05/CORR-07 (формулы) доказываются корпусом без D-Bus и живой сессии; проводка в движок/актёр — трассер 02-03, надстройки — 02-04..02-06. Это базис, на котором каждый следующий план фазы строится без переделки формул.
Output: пакет internal/correct (5 файлов + 5 файлов корпуса), зелёный headless под -race.
</objective>

<execution_context>
@/home/nil/DiskD/W/Djarvur/goswitch/.zcode/gsd-core/workflows/execute-plan.md
@/home/nil/DiskD/W/Djarvur/goswitch/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-korrektsiya-slova-en-ru/02-CONTEXT.md (D-13, D-14, D-15, D-16 — заперты)
@.planning/phases/02-korrektsiya-slova-en-ru/02-PATTERNS.md (§ internal/correct — контракт чистоты по образцу internal/hotkey/fsm.go)
@docs/adr/ADR-003-replacement-capability-ladder.md
@docs/adr/ADR-004-buffer-reset-triggers.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Буфер-фраза с границами токенов + детектор направления + конверсия — полный корпус D-13..D-16 (CORR-04/CORR-05, D-07)</name>
  <reversibility rating="reversible">Чистые структуры данных за стабильными сигнатурами Push/Token/Detect/Convert; смена представления — локальная правка пакета</reversibility>
  <files>internal/correct/buffer.go, internal/correct/direction.go, internal/correct/convert.go, internal/correct/buffer_test.go, internal/correct/direction_test.go, internal/correct/convert_test.go</files>
  <read_first>
  - .planning/phases/02-korrektsiya-slova-en-ru/02-RESEARCH.md (§ Pattern 2 — буфер, § Буквенно-ёмкие клавиши — выведенный список дословно, § Code Examples — классификация направления, § Common Pitfalls 2)
  - .planning/phases/02-korrektsiya-slova-en-ru/02-PATTERNS.md (§ internal/correct — контракт чистоты по образцу internal/hotkey/fsm.go:1-11)
  - internal/hotkey/fsm.go (стиль package-дока и чистого состояния)
  - layouts/tables.go (ENToRU/RUToEN — обе карты, оба shift-уровня; образец translateStrict в layouts/tables_test.go:9-19)
  - docs/adr/ADR-003-replacement-capability-ladder.md, docs/adr/ADR-004-buffer-reset-triggers.md
  </read_first>
  <behavior>
  - TestBuffer_TokenRules: Push('g'..'n') → Token()=="ghbdtn", Tail()==""; затем Push(' ') → Token() остаётся "ghbdtn" (D-13 — последнее слово живёт после пробела), Tail()==" "; Backspace() выталкивает последнюю руну; HardReset() → Token()==nil
  - TestBuffer_TwoTokens: "ghbdtn privet" → Token()=="privet" (текущий токен выигрывает); "ghbdtn privet " → Token()=="privet", Tail()==" "
  - TestBuffer_WordAfterSpace (D-13): "ghbdtn" + ' ' → Token()=="ghbdtn", Tail()==" "; "ghbdtn vj" → Token()=="vj", Tail()==""; "ghbdtn vj " → Token()=="vj", Tail()==" "
  - TestBuffer_PunctuationToken (D-14): "ghbdtn," → Token()=="ghbdtn," (запятая на буквенно-ёмкой клавише — RU 'б'); "ghbdtn;" → Token()=="ghbdtn;" (';' ёмкая — RU 'ж'); "ghbdtn/" → Token()=="ghbdtn", Tail()=="/" ('/' — не ёмкая: обе стороны символы); "ghbdtn-" → Token()=="ghbdtn", Tail()=="-"
  - TestBuffer_DigitsInToken (D-15): "ghbdtn2026" → Token()=="ghbdtn2026"; "2026" → Token()=="2026"
  - TestBuffer_BackspacePop: "ghbdtn v" → Backspace×1 → Token()=="ghbdtn" (вернулись к прошлому слову), Tail()==""; Backspace×8 → буфер пуст
  - TestBuffer_ReplaceTokenToggle: после ReplaceToken("привет") буфер "ghbdtn " → "привет "; Token()=="привет" — повторная коррекция конвертирует обратно (инвариант «буфер == текст перед курсором»)
  - TestDetect_PureScript (CORR-04): "ghbdtn"→(ENtoRU,true); "привет"→(RUtoEN,true); "ghbdtn2026"→(ENtoRU,true) — направление только по буквам; "2026"→(_,false)
  - TestDetect_MixedRefused (D-16): "gfbпривет"→(_,false); "приветg"→(_,false); "ghbdtnп"→(_,false) — любая одна чужая буква достаточна
  - TestDetect_NeutralNotMixed (D-15/D-16): "ghbdtn2026"→(ENtoRU,true); "привет2026"→(RUtoEN,true); "ghbdtn,;"→(ENtoRU,true) — пунктуация/цифры нейтральны
  - TestDetect_BothShiftLevels: "GHBdtN"→(ENtoRU,true); "ПриВет"→(RUtoEN,true)
  - TestConvert_CasePreserved (CORR-05): ghbdtn→привет, GHBDTN→ПРИВЕТ, Ghbdtn→Привет, привет→ghbdtn, ПРИВЕТ→GHBDTN, Привет→Ghbdtn; "ghbdtn2026"→"привет2026" (цифры тождественно); "ghbdtn,"→"привет," (не-буквенная руна токена сохраняется, НЕ конвертируется в 'б' — пример D-14); немапленная буква → (nil,false)
  </behavior>
  <action>
  RED (D-07): создать пакет-заглушку, чтобы корпус падал на утверждениях, не на компиляции: internal/correct — три файла с doc-комментариями, цитирующими D-13/D-14/D-15/D-16 + ADR-003/004 (стиль package-дока internal/hotkey/fsm.go:1-11), и типами-заглушками: Buffer{} с методами-нулями, Detect→(0,false), Convert→(nil,false). Весь корпус behavior в трёх *_test.go (package correct_test, vanilla testing, t.Parallel() на каждом тесте и субтесте, имена Test*_ — go-ultimate). Прогнать mise exec -- go test ./internal/correct/ -race -count=1 — подлинный RED на утверждениях; записать red-evidence (gsd check tdd-red-evidence). Commit: test(02-01).
  GREEN — НИ ОДНОГО импорта D-Bus/времени/горутин; всё в []rune:
  (1) buffer.go — Buffer{runes []rune; curStart int; lastStart, lastLen int}; Push(r): если letterCapable(r)||unicode.IsDigit(r) — расширить/начать текущий токен, иначе — завершить текущий (lastStart/lastLen = координаты, если len>0) и положить руну в буфер как границу; letterCapable(r) = unicode.IsLetter(r) || unicode.IsLetter(layouts.ENToRU[r]) || unicode.IsLetter(layouts.RUToEN[r]) (comma-ok по обеим картам — выведено из принципа D-14: клавиша ёмкая, если хотя бы один слой даёт букву; вывод сверен с RESEARCH § Буквенно-ёмкие клавиши). Token() — текущий токен, а если пуст — последний завершённый (D-13), с запоминанием координат для ReplaceToken. Tail() — руны после конца Token() до конца буфера. Backspace() — вытолкнуть последнюю руну (честно: и токен, и хвост, и прошлые слова; координаты токенов пересчитываются от конца). HardReset() — очистить всё. ReplaceToken(repl []rune) — заменить диапазон Token() на repl, хвост сохранить (toggle-инвариант).
  (2) direction.go — type Dir int; const (ENtoRU Dir = iota + 1; RUtoEN); Detect(token []rune) (Dir, bool): счёт рун unicode.Is(unicode.Latin, r) / unicode.Is(unicode.Cyrillic, r); обе >0 → (0,false) — смешанное, тихий отказ (D-16); только EN → ENtoRU; только RU → RUtoEN; букв нет → (0,false). Тело — по образцу RESEARCH § Code Examples (классификация направления).
  (3) convert.go — Convert(token []rune, dir Dir) ([]rune, bool): per-rune lookup (образец translateStrict layouts/tables_test.go:9-19): БУКВЫ через layouts.ENToRU/RUToEN по dir — регистр сохраняется конструктивно (карты несут оба уровня); цифры и прочие (не-буквенные) руны токена — тождественно; немапленная буква → (nil,false).
  Прогон корпуса зелёный. Commit: feat(02-01). REFACTOR при очевидных улучшениях без смены поведения.
  </action>
  <verify>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise exec -- go test ./internal/correct/ -race -count=1</automated>
    <fails_when>строка FAIL в выводе или non-zero exit — корпус буфера/направления/конверсии не зелёный под -race</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && ! grep -rn 'github.com/godbus' internal/correct/ && ! grep -rn '"time"' internal/correct/ && echo PURE-PACKAGE</automated>
    <fails_when>non-zero exit — пакет импортирует godbus или time (нарушение контракта чистоты)</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise run ci</automated>
    <fails_when>non-zero exit — build/vet/golangci-lint/test -race не зелёные (D-08); новый пакет обязан проходить максимально строгий линт (D-10)</fails_when>
  </verify>
  <done>Пакет internal/correct существует и чист (без godbus/time/горутин); корпус D-13..D-16 зелёный headless под -race; история test(02-01) до feat(02-01)</done>
  <acceptance_criteria>
  - internal/correct содержит buffer.go/direction.go/convert.go и не импортирует godbus и time (grep-гейт в verify)
  - buffer_test.go пинит: пробел завершает слово, но не выкидывает его из буфера (D-13); ',' и ';' в токене, '/' и '-' — граница (D-14); цифры в токене (D-15); Backspace-pop к прошлому слову; ReplaceToken-toggle
  - direction_test.go пинит: одна чужая буква → отказ; цифры и пунктуация не делают слово смешанным; оба регистра
  - convert_test.go пинит все три регистра в обе стороны и пример D-14 "ghbdtn,"→"привет,"
  - mise run ci зелёный
  </acceptance_criteria>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Суффиксная сверка ADR-004 + арифметика лестницы ОБЕИХ уровней в рунах (CORR-07)</name>
  <reversibility rating="costly">Формула диапазона — контракт CORR-07 («ровно по диапазону»); смена после живых кейсов и матрицы требует согласованно менять корпус, актёр и матрицу</reversibility>
  <files>internal/correct/verify.go, internal/correct/plan.go, internal/correct/verify_test.go, internal/correct/plan_test.go</files>
  <read_first>
  - .planning/phases/02-korrektsiya-slova-en-ru/02-RESEARCH.md (§ Pattern 1 — арифметика лестницы с хвостом, § Pattern 4 — суффиксное правило сверки, § Code Examples — MatchesSuffix, § Common Pitfalls 1/2)
  - internal/correct/buffer.go (Token/Tail — входы BuildPlan после Task 1)
  - engine/keys.go (CapSurroundingText = 1 << 5 — бит выбора уровня)
  - docs/adr/ADR-003-replacement-capability-ladder.md, docs/adr/ADR-004-buffer-reset-triggers.md
  </read_first>
  <behavior>
  - TestMatchesSuffix (ADR-004): ("abc ghbdtn", "ghbdtn")→true; ("abc ghbdtn ", "ghbdtn")→false (хвост после токена в тексте перед курсором НЕ допустим — сверка ждёт токен последним); ("gfb", "ghbdtn")→false (короче)
  - TestBuildPlan_BothLevels (CORR-07): token="ghbdtn", tail=" ", converted="привет", caps с CapSurroundingText → Level1{Offset:-(1+6)=-7, NChars:1+6=7, Commit:"привет "}; caps без бита → Level2{Backspaces:1+6=7, Commit:"привет "}; tail="" вырождается в Offset:-6, NChars:6, Commit:"привет"
  - TestBuildPlan_RunesNotBytes (Pitfall 2): token="привет" (12 байт, 6 рун), tail="" → Backspaces==6 (не 12)
  - TestPlan_TailArithmetic (CORR-07, Pitfall 1): token="ghbdtn", tail=" " → Level1{Offset:-7, NChars:7, Commit:"привет "}; token="привет" (RU→EN), tail=" " → Level2{Backspaces:7, Commit:"ghbdtn "} — счёт в рунах; токен без хвоста — вырожденный случай
  </behavior>
  <action>
  RED (D-07): verify_test.go/plan_test.go с кейсами behavior против заглушек (MatchesSuffix→false, BuildPlan→нулевая Plan) — падения на утверждениях. Прогон mise exec -- go test ./internal/correct/ -race -count=1, red-evidence. Commit: test(02-01).
  GREEN: (1) verify.go — MatchesSuffix(textBeforeCursor, token []rune) bool через slices.Equal суффикса (тело RESEARCH § Code Examples): текст перед курсором обязан ЗАКАНЧИВАТЬСЯ токеном.
  (2) plan.go — type Level int (Level1/Level2); Plan{Level; Offset int32; NChars uint32; Backspaces int; Commit []rune}; BuildPlan: caps&engine.CapSurroundingText != 0 → Level1{Offset:-(tail+n), NChars:tail+n, Commit:converted+tail}; иначе Level2{Backspaces:tail+n, Commit:converted+tail}. ГЕОМЕТРИЯ (планировочная поправка к RESEARCH Pattern 1, уровень 1): удалять РОВНО ТОКЕН при непустом хвосте нельзя — после удаления курсор встаёт ПОСЛЕ хвоста и CommitText вставит слово за хвостом (`abc  привет` с потерей порядка); строковый пин D-13 (`ghbdtn ` → `привет `) разрешает только удаление токен+хвост и перекоммит исправленное+хвост. Все счётчики — len([]rune) (Pitfall 2). Сопроводить комментарий в plan.go ссылкой на D-13/D-14 и на это геометрическое обоснование. Commit: feat(02-01). REFACTOR при очевидных улучшениях без смены поведения.
  </action>
  <verify>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise exec -- go test ./internal/correct/ -run 'TestMatchesSuffix|TestBuildPlan|TestPlan' -race -count=1 -v</automated>
    <fails_when>строка FAIL в выводе — корпус сверки/арифметики не зелёный</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise run ci</automated>
    <fails_when>non-zero exit — тройной гейт не зелёный (D-08)</fails_when>
  </verify>
  <done>MatchesSuffix и BuildPlan обоих уровней пиниты корпусом (руны, хвост, вырожденный случай); формулы уровня 2 готовы к подключению планом 02-05 без переделки</done>
  <acceptance_criteria>
  - verify_test.go пинит суффиксное правило включая отказ при хвосте после токена в тексте перед курсором
  - plan_test.go пинит хвостовую арифметику обоих уровней в рунах, включая кириллический токен (6 рун, не 12 байт) и вырожденный tail==""
  - internal/correct по-прежнему без godbus/time-импортов (grep-гейт Task 1 зелёный)
  - mise run ci зелёный
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (нет новых транспортных границ) | чистая библиотека без D-Bus/ввода-вывода; границы появятся в планах 02-03..02-05 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01-01 | Tampering | арифметика диапазона (plan.go) | high | mitigate | счёт строго в рунах (Pitfall 2), хвостовая геометрия (Pitfall 1) — корпус TestBuildPlan_*/TestPlan_* пинит обе ошибки до любого живого прогона |
| T-02-01-02 | Tampering | границы слова (buffer.go) | medium | mitigate | letterCapable выведена из объединения EN+RU слоёв (принцип D-14) и сверена с RESEARCH § Буквенно-ёмкие клавиши; корпус пинит ','/';' внутри и '/'/'-' как границу |
</threat_model>

<verification>
- mise run ci зелёный (build + vet + golangci-lint + test -race, D-08/D-10)
- Корпус internal/correct зелёный headless: направления, регистры, границы слова, сверка, арифметика обоих уровней
- Чистота пакета: grep-гейт на godbus/time-импорты
</verification>

<success_criteria>
- CORR-04 (чистое направление + нейтральность), CORR-05 (регистр посимвольно), CORR-07 (формулы диапазона обоих уровней в рунах) — доказаны корпусом
- D-13/D-14/D-15/D-16 реализованы дословно на уровне логики
- Формулы уровня 2 и суффиксная сверка поставлены заранее: планы 02-03 (уровень 1) и 02-05 (уровень 2) подключают их без переделки
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (символы плана 02-01)

- Пакет `internal/correct`: `Buffer`, `NewBuffer`, `(*Buffer).Push/Backspace/HardReset/Token/Tail/ReplaceToken`, `letterCapable`; `Dir`, `ENtoRU`, `RUtoEN`, `Detect`; `Convert`; `MatchesSuffix`; `Level`, `Level1`, `Level2`, `Plan`, `BuildPlan` — формулы ОБЕИХ уровней лестницы
- Полный корпус пяти *_test.go: границы слова D-13/D-14/D-15, смешанное D-16, три регистра в обе стороны, суффиксная сверка, хвостовая арифметика в рунах
</artifacts_produced>

<output>
Create `.planning/phases/02-korrektsiya-slova-en-ru/02-01-SUMMARY.md` when done
</output>
