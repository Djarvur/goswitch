---
phase: 02-korrektsiya-slova-en-ru
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/correct/buffer.go
  - internal/correct/direction.go
  - internal/correct/convert.go
  - internal/correct/verify.go
  - internal/correct/plan.go
  - internal/correct/buffer_test.go
  - internal/correct/direction_test.go
  - internal/correct/convert_test.go
  - internal/correct/verify_test.go
  - internal/correct/plan_test.go
  - engine/engine.go
  - engine/engine_test.go
  - engine/factory.go
  - internal/session/actor.go
  - internal/session/actor_test.go
  - test/e2e/case_word.go
  - mise.toml
autonomous: true
requirements: [CORR-01, CORR-04, CORR-05, CORR-07]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Двойной тап Right Shift исправляет последнее слово EN→RU сквозным конвейером: живой e2e-кейс word-en-ru (zenity, инжекция ydotool, чтение результата) даёт ghbdtn→привет; три регистра пинятся юнит-корпусом (CORR-01, CORR-05)"
    - "Направление чистого слова определяется по составу букв: латиница → ENtoRU, кириллица → RUtoEN, букв нет → тихий отказ; цифры и пунктуация нейтральны (CORR-04)"
    - "Замена уровня 1 (клиент с битом CapSurroundingText) удаляет токен+хвост и коммитит исправленный_токен+хвост одной транзакцией — ровно по диапазону неверного текста, без визуального прыжка (CORR-07)"
    - "Перед любой заменой выполняется сверка ADR-004: RequireSurroundingText → ожидание свежего SetSurroundingText ≤ 100 мс (таймер актёра, ответ ProcessKeyEvent всегда мгновенный) → суффиксное сравнение; несовпадение или таймаут → тихий отказ, ни один символ не удаляется (D-20)"
    - "Слово, уже отделённое пробелом, исправляется: буфер помнит последнее слово и хвост; живой кейс word-after-space даёт `ghbdtn ` → `привет ` (D-13)"
    - "Токен = максимальный запуск буквенно-ёмких клавиш (объединение EN+RU слоёв) и цифр: `ghbdtn,` — один токен, исправляется как `привет,`; `/`, `-`, пробел — граница (D-14); цифры идут через таблицу тождественно, направление — только по буквам: `ghbdtn2026` → `привет2026` (D-15)"
    - "Отказ коррекции логируется на INFO причиной без содержимого слова (reason: empty-buffer | no-letters | mixed-script | verify-mismatch | verify-timeout | no-surrounding); детали удачной коррекции (исход→результат, уровень лестницы, latency) — только в -debug; на INFO — только счётчики (D-20/D-21)"
    - "После успешной коррекции буфер хранит исправленный токен (инвариант «буфер == текст перед курсором»), повторный двойной тап конвертирует обратно — toggle"
  artifacts:
    - path: internal/correct/buffer.go
      provides: "чистый Buffer: Push/Backspace/HardReset/Token/Tail/ReplaceToken, хранение []rune, границы токенов по буквенно-ёмким клавишам"
    - path: internal/correct/direction.go
      provides: "type Dir + Detect(token []rune) (Dir, bool): классификация рун через unicode.Latin/Cyrillic, смешанное слово → отказ (D-16 базис)"
    - path: internal/correct/convert.go
      provides: "Convert(token []rune, dir Dir) ([]rune, bool): буквы через layouts.ENToRU/RUToEN (регистр сохраняется конструктивно), цифры и прочие руны токена — тождественно (пример D-14 `ghbdtn,`→`привет,`)"
    - path: internal/correct/verify.go
      provides: "MatchesSuffix(textBeforeCursor, token []rune) bool — суффиксное правило сверки ADR-004"
    - path: internal/correct/plan.go
      provides: "Level/Plan/BuildPlan(token, tail, converted []rune, caps uint32): уровень по биту CapSurroundingText, арифметика диапазона в рунах (оба уровня)"
    - path: engine/engine.go
      provides: "эмиттеры DeleteSurroundingText(offset int32, nchars uint32) и RequireSurroundingText() симметрично CommitText; EventHandler расширен HandleSurroundingText/HandleCapabilities/AttachEngine"
    - path: internal/session/actor.go
      provides: "двухфазная коррекция в актёре: Double→токен→направление→Require→(таймер 100 мс)→сверка→удаление+коммит; кормление буфера печатными клавишами; INFO/D-21-логи"
    - path: test/e2e/case_word.go
      provides: "живые кейсы word-en-ru и word-after-space (zenity, оракул stdout)"
  key_links:
    - from: internal/session/actor.go
      to: internal/correct/buffer.go
      via: "актёр кормит Buffer печатными клавишами и берёт Token()/Tail() при решении Double"
      pattern: "\\.Token\\(\\)"
    - from: internal/session/actor.go
      to: engine/engine.go
      via: "actёра sink = attached engine: RequireSurroundingText → SetSurroundingText (handler) → DeleteSurroundingText + CommitText"
      pattern: "AttachEngine"
    - from: internal/correct/plan.go
      to: engine/keys.go
      via: "BuildPlan выбирает уровень по биту CapSurroundingText (1 << 5)"
      pattern: "CapSurroundingText"
  prohibitions:
    - statement: "Коррекция НЕ ДОЛЖНА срабатывать без явного жеста — никакой автокоррекции при наборе: владелец осознанно отверг автокоррекцию (Out of Scope в REQUIREMENTS.md); конвейер запускается только решением FSM Double"
      status: resolved
      verification: judgment
    - statement: "Журнал НЕ ДОЛЖЕН содержать содержимое набранных/исправленных слов на уровне INFO — только причины отказов без слов и счётчики; исход→результат и трассировка — исключительно -debug (D-20/D-21)"
      status: resolved
      verification: judgment
  assumptions:
    - "FLAGGED (edge-probe, CORR-01, unclassified): проба границ «последнего слова» не классифицирована автоматикой — предположение: набор решений D-13/D-14/D-15/D-16 + ADR-002/003/004 полностью определяет выделение слова; остаточные неклассифицированные границы покрываются корпусом buffer_test.go и e2e-матрицей (02-04)"
    - "FLAGGED (edge-probe, CORR-05, unclassified): регистро-сохранение не классифицировано автоматикой — предположение: таблицы Фазы 1 несут оба shift-уровня на позицию ('G':'П' и 'g':'п'), регистр сохраняется конструктивно; корпус пинит три регистра в обе стороны"
    - "FLAGGED (research A1): zenity 4.0.1 (GTK4) применяет DeleteSurroundingText — если нет, кейс word-en-ru падает живым прогоном и лестница вырождается в уровень 2 (самодостаточен по ADR-003); матрица 02-04 закрепит фактический уровень"
---

<objective>
Трассер ядра ценности: двойной тап Right Shift исправляет последнее слово EN→RU (`ghbdtn`→`привет`) в живом поле ввода zenity — сквозной конвейер буфер→направление→сверка→лестница (уровень 1) через эмиттеры engine/, с логами D-20/D-21. Вторая задача расширяет срез границами слова владельца: слово после пробела (D-13, хвостовая арифметика), пунктуация в токене (D-14), цифры (D-15).

**As a** пользователь, набирающий в неправильной раскладке, **I want** двойным нажатием Right Shift исправить последнее слово с сохранением регистра, **so that** не перепечатывать его вручную — и замена идёт ровно по диапазону неверного текста, без прыжка курсора.

Purpose: CORR-01 — ядро продукта; каждый последующий план фазы расширяет этот доказанный срез (направления, уровень 2, матрица).
Output: пакет internal/correct (чистая логика, D-Bus-free), два эмиттера engine/, двухфазная коррекция в актёре, живые кейсы word-en-ru / word-after-space, mise-задачи e2e-word / e2e-word-space.
</objective>

<execution_context>
@/home/nil/DiskD/W/Djarvur/goswitch/.zcode/gsd-core/workflows/execute-plan.md
@/home/nil/DiskD/W/Djarvur/goswitch/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-korrektsiya-slova-en-ru/02-CONTEXT.md (D-13, D-14, D-15, D-20, D-21 — заперты)
@.planning/phases/02-korrektsiya-slova-en-ru/02-PATTERNS.md (§ internal/correct, § actor, § engine — образцы с номерами строк)
@docs/adr/ADR-003-replacement-capability-ladder.md
@docs/adr/ADR-004-buffer-reset-triggers.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Трассер — двойной Right Shift исправляет ghbdtn→привет в zenity: буфер→направление→сверка→DeleteSurroundingText+CommitText (CORR-01/CORR-04/CORR-05/CORR-07, D-07)</name>
  <reversibility rating="costly">Сечение EventHandler (HandleSurroundingText/HandleCapabilities/AttachEngine) — кросс-модульный контракт engine↔session; смена позже требует согласованной правки в обоих пакетах и стенде</reversibility>
  <precondition>Живая GNOME Wayland-сессия на этой машине доступна: непусто $WAYLAND_DISPLAY, ibus-daemon отвечает (ibus address), префлайт стенда `go run ./test/e2e -case m1-gate` зелёный на текущем HEAD</precondition>
  <files>internal/correct/buffer.go, internal/correct/direction.go, internal/correct/convert.go, internal/correct/verify.go, internal/correct/plan.go, internal/correct/buffer_test.go, internal/correct/direction_test.go, internal/correct/convert_test.go, internal/correct/verify_test.go, internal/correct/plan_test.go, engine/engine.go, engine/engine_test.go, engine/factory.go, internal/session/actor.go, internal/session/actor_test.go, test/e2e/case_word.go, mise.toml</files>
  <read_first>
  - .planning/phases/02-korrektsiya-slova-en-ru/02-RESEARCH.md (§ Pattern 1 — арифметика лестницы, § Pattern 2 — буфер, § Pattern 4 — двухфазная сверка, § Common Pitfalls 1/2/4, § Буквенно-ёмкие клавиши — выведенный список)
  - .planning/phases/02-korrektsiya-slova-en-ru/02-PATTERNS.md (§ internal/correct — контракт чистоты по образцу internal/hotkey/fsm.go; § actor — timer re-entry actor.go:84-108; § engine — образец эмиттера engine.go:310-317)
  - engine/engine.go (ProcessKeyEvent:128-142, SetSurroundingText:152-160, SetCapabilities:162-170, CommitText:307-317, EventHandler:66-71)
  - engine/keys.go (CapSurroundingText=1<<5, KeyBackSpace/Tab/Return/Escape/Space, modsMask)
  - engine/types.go (IBusText, NewIBusText)
  - engine/factory.go (CreateEngine binds conn/path — точка AttachEngine)
  - internal/session/actor.go (весь; мьютекс-сериализация, ExpiryAt — место решения)
  - internal/session/actor_test.go (captureLogs/syncBuffer/tapShift — расширяемый корпус)
  - layouts/tables.go (ENToRU/RUToEN — обе карты, оба shift-уровня)
  - test/e2e/case_m1.go (startZenity/waitZenityEntry/closeZenity/focusWitness — драйвер кейса)
  - test/e2e/main.go (pickCase-реестр:165-179, waitForLog:486, injectText/injectKeys/pressKey:559-594, runCaseWatchdog:141)
  - docs/adr/ADR-003-replacement-capability-ladder.md, docs/adr/ADR-004-buffer-reset-triggers.md
  </read_first>
  <behavior>
  - TestBuffer_TokenRules: Push('g'..'n') → Token()=="ghbdtn", Tail()==""; затем Push(' ') → Token() остаётся "ghbdtn" (D-13 — последнее слово живёт после пробела), Tail()==" "; Backspace() выталкивает последнюю руну; HardReset() → Token()==nil
  - TestBuffer_TwoTokens: "ghbdtn privet" → Token()=="privet" (текущий токен выигрывает); "ghbdtn privet " → Token()=="privet", Tail()==" "
  - TestBuffer_PunctuationToken (D-14): "ghbdtn," → Token()=="ghbdtn," — запятая на буквенно-ёмкой клавише (RU 'б'), входит в токен; "ghbdtn/" → Token()=="ghbdtn", Tail()=="/" ('/' — не ёмкая: обе стороны символы); "ghbdtn-" → Token()=="ghbdtn", Tail()=="-"
  - TestBuffer_DigitsInToken (D-15): "ghbdtn2026" → Token()=="ghbdtn2026"; "2026" → Token()=="2026"
  - TestDetect_PureScript (CORR-04): "ghbdtn"→(ENtoRU,true); "привет"→(RUtoEN,true); "ghbdtn2026"→(ENtoRU,true) — направление только по буквам; "2026"→(_,false); "gfbпривет"→(_,false) — смешанное, тихий отказ (D-16)
  - TestConvert_CasePreserved (CORR-05): ghbdtn→привет, GHBDTN→ПРИВЕТ, Ghbdtn→Привет, привет→ghbdtn, ПРИВЕТ→GHBDTN, Привет→Ghbdtn; "ghbdtn2026"→"привет2026" (цифры тождественно); "ghbdtn,"→"привет," (пример D-14: не-буквенная руна токена сохраняется)
  - TestMatchesSuffix (ADR-004): ("abc ghbdtn", "ghbdtn")→true; ("abc ghbdtn ", "ghbdtn")→false (хвост после токена в тексте перед курсором НЕ допустим — свера ждёт токен последним); ("gfb", "ghbdtn")→false (короче)
  - TestBuildPlan_BothLevels (CORR-07): token="ghbdtn", tail=" ", converted="привет", caps с CapSurroundingText → Level1{Offset:-(1+6)=-7, NChars:1+6=7, Commit:"привет "}; caps без бита → Level2{Backspaces:1+6=7, Commit:"привет "}; tail="" вырождается в Offset:-6, NChars:6, Commit:"привет"
  - TestBuildPlan_RunesNotBytes (Pitfall 2): token="привет" (12 байт, 6 рун), tail="" → Backspaces==6 (не 12)
  - TestActor_DoubleTapCorrects (session, fake-sink): кормление печатных нажатий g..n → двойной тап → expiry → на sink наблюдается RequireSurroundingText; затем HandleSurroundingText("abc ghbdtn", 9) → на sink ровно DeleteSurroundingText(-6,6) (токен без хвоста) и CommitText("привет"); в логе INFO {"msg":"correction","outcome":"done"}; в -debug запись с "level":1 первой после msg
  - TestActor_VerifyPaths (D-20): свера не сошлась → INFO {"msg":"correction skipped","reason":"verify-mismatch"}, НИ ОДНОГО вызова DeleteSurroundingText/CommitText на sink; таймаут 100 мс (инъекция времени таймера) → reason:"verify-timeout"; пустой буфер → reason:"empty-buffer"; ни одна INFO-запись не содержит содержимого слова
  </behavior>
  <action>
  RED (D-07): создать пакеты-заглушки, чтобы корпус падал на утверждениях, не на компиляции. internal/correct: пять файлов с doc-комментариями, цитирующими D-13/D-14/D-15/D-16 + ADR-003/004 (стиль package-дока internal/hotkey/fsm.go:1-11), и типами-заглушками: Buffer{} с методами-нулями, Detect→(0,false), Convert→(nil,false), MatchesSuffix→false, BuildPlan→нулевая Plan. Затем весь корпус behavior в пяти *_test.go (package correct_test, vanilla testing, t.Parallel() на каждом тесте и субтесте, имена Test*_ — go-ultimate). internal/session/actor_test.go: расширить фейковым sink-интерфейсом (двойник эмиттеров engine: RequireSurroundingText/DeleteSurroundingText/CommitText — вызовы записываются слайсом под мьютексом, стиль syncBuffer actor_test.go:27) и тестами TestActor_DoubleTapCorrects/TestActor_VerifyPaths против текущего актёра (методы-заглушки: HandleSurroundingText/HandleCapabilities/AttachEngine добавляются как no-op — поэтому RED падает на утверждениях: коррекции нет, вызовов sink нет). Прогнать mise exec -- go test ./internal/correct/ ./internal/session/ -race -count=1 — подлинный RED на утверждениях; записать red-evidence (gsd check tdd-red-evidence). Commit: test(02-01).
  GREEN — чистый пакет internal/correct (НИ ОДНОГО импорта D-Bus/времени/горутин; всё в []rune):
  (1) buffer.go — Buffer{runes []rune; curStart int; lastStart, lastLen int}; Push(r): если letterCapable(r)||unicode.IsDigit(r) — расширить/начать текущий токен, иначе — завершить текущий (lastStart/lastLen = координаты, если len>0) и положить руну в буфер как границу; letterCapable(r) = unicode.IsLetter(r) || unicode.IsLetter(layouts.ENToRU[r]) || unicode.IsLetter(layouts.RUToEN[r]) (comma-ok по обеим картам — выведено из принципа D-14: клавиша ёмкая, если хотя бы один слой даёт букву; вывод сверен с RESEARCH § Буквенно-ёмкие клавиши); Token() — текущий токен, а если пуст — последний завершённый (D-13), с запоминанием координат для ReplaceToken; Tail() — руны после конца Token() до конца буфера; Backspace() — вытолкнуть последнюю руну (честно: и токен, и хвост, и прошлые слова; координаты токенов пересчитываются от конца); HardReset() — очистить всё; ReplaceToken(repl []rune) — заменить диапазон Token() на repl, хвост сохранить (инвариант «буфер == текст перед курсором», toggle-повторным тапом).
  (2) direction.go — type Dir int; const (ENtoRU Dir = iota + 1; RUtoEN); Detect(token []rune) (Dir, bool): счёт рун unicode.Is(unicode.Latin, r) / unicode.Is(unicode.Cyrillic, r); обе >0 → (0,false) — смешанное, тихий отказ (D-16); только EN → ENtoRU; только RU → RUtoEN; букв нет → (0,false). Тело — по образцу RESEARCH § Code Examples.
  (3) convert.go — Convert(token []rune, dir Dir) ([]rune, bool): per-rune lookup (образец translateStrict layouts/tables_test.go:9-19): БУКВЫ через layouts.ENToRU/RUToEN по dir — регистр сохраняется конструктивно (карты несут оба уровня); цифры и прочие (не-буквенные) руны токена — тождественно (пример D-14 `ghbdtn,`→`привет,` пинит: запятая сохраняется, НЕ конвертируется в 'б'); немапленная буква → (nil,false).
  (4) verify.go — MatchesSuffix(textBeforeCursor, token []rune) bool через slices.Equal суффикса (тело RESEARCH § Code Examples): текст перед курсором обязан ЗАКАНЧИВАТЬСЯ токеном.
  (5) plan.go — type Level int (Level1/Level2); Plan{Level; Offset int32; NChars uint32; Backspaces int; Commit []rune}; BuildPlan: caps&engine.CapSurroundingText != 0 → Level1{Offset:-(tail+n), NChars:tail+n, Commit:converted+tail}; иначе Level2{Backspaces:tail+n, Commit:converted+tail}. ГЕОМЕТРИЯ (планировочная поправка к RESEARCH Pattern 1, уровень 1): удалять РОВНО ТОКЕН при непустом хвосте нельзя — после удаления курсор встаёт ПОСЛЕ хвоста и CommitText вставит слово за хвостом (`abc  привет` с потерей порядка); строковый пин D-13 (`ghbdtn ` → `привет `) разрешает только удаление токен+хвост и перекоммит исправленное+хвост. Все счётчики — len([]rune) (Pitfall 2). Сопровить комментарий в plan.go ссылкой на D-13/D-14 и на это геометрическое обоснование.
  GREEN — engine/: эмиттеры DeleteSurroundingText(offset int32, nchars uint32) и RequireSurroundingText() — тело копирует CommitText engine.go:310-317 (conn==nil → return; e.conn.Emit(e.path, ifaceEngine+".…", args…); ошибка → slog.Error). EventHandler (engine.go:66-71) расширяется тремя методами: HandleSurroundingText(text string, cursorPos uint32), HandleCapabilities(caps uint32), AttachEngine(*Engine) — интерфейс внутрирепозиторный, единственный имплементор session.Actor. SetSurroundingText (engine.go:155-160): разварьировать входную dbus.Variant в IBusText (тип уже в types.go), передать handler.HandleSurroundingText(t.Text, cursorPos) при handler != nil (DEBUG-трасса позиций сохраняется). SetCapabilities: дополнительно handler.HandleCapabilities(caps). factory.go CreateEngine: после привязки conn/path — handler.AttachEngine(eng) при handler != nil (sink для эмиттеров актёра). recover-шим на новых путях не нужен (эмиттеры — исходящие сигналы, не экспортированные методы), но НИ ОДИН экспортированный D-Bus-метод не остаётся без существующего шима.
  GREEN — internal/session/actor.go: поля mode-независимой коррекции: buf *correct.Buffer, caps uint32, eng (sink interface{ RequireSurroundingText(); DeleteSurroundingText(int32, uint32); CommitText(engine.IBusText) } — локальный интерфейс в точке использования, go-ultimate), pending *pendingFix{token, tail, converted []rune; deadline *time.Timer}. HandleKey: существующая FSM-логика не меняется + кормление буфера: press && печатный keyval (0x20 ≤ keyval < 0xFE00) && ev.Mods&^engine.MaskShift == 0 → buf.Push(rune(ev.Keyval)); press KeyBackSpace → buf.Backspace(); Shift-клавиши и release буфер не кормят. HandleLifecycle: к FocusOut/Reset (actor.go:71-76) добавляется buf.HardReset() (CORR-09 стартовая часть). HandleCapabilities: сохранить caps. AttachEngine: сохранить sink. HandleSurroundingText: под мьютексом — сравнение при активном pending: MatchesSuffix([]rune(text)[:min(cursorPos, len)] , pending.token) → да: исполнить BuildPlan уровень 1: sink.DeleteSurroundingText(plan.Offset, plan.NChars); sink.CommitText(engine.NewIBusText(string(plan.Commit))); buf.ReplaceToken(converted); лог INFO {"msg":"correction","outcome":"done"} и Debug {"msg":"correction","level":1,"runes":N,"source":…,"result":…,"latency_ms":N} (первый атрибут после msg — level: матрица 02-04 будет грепать точную форму); нет → INFO {"msg":"correction skipped","reason":"verify-mismatch"}, pending=nil. ExpiryAt (actor.go:92-100): в цикле решений — action Double → startCorrection() под мьютексом: token=buf.Token(); пусто → skip empty-buffer; dir,ok=correct.Detect → !ok → skip no-letters|mixed-script; converted=Convert; caps&CapSurroundingText==0 → skip no-surrounding (до 02-03); sink.RequireSurroundingText(); arm 100 мс verifyTimer (const verifyWait = 100 * time.Millisecond; таймер, НЕ блокировка — Pitfall 4); action Single/Triple — пока только slog.Debug (флип приходит планом 02-02). Ответ ProcessKeyEvent остаётся мгновенным: все ожидания — в таймерах актёра (паттерн timer re-entry actor.go:84-108).
  Затем test/e2e/case_word.go: runWordENRU по скелету runM1Gate (case_m1.go:49-74): activateGoswitch → openEntrySurface → focusWitness → injectText("ghbdtn") → injectKeys("shift-r","shift-r") (пейсинг injectKeys уже укладывает тапы в окно 300 мс) → waitForLog `"msg":"correction","outcome":"done"` → closeZenity → stdout-оракул == "привет"; register "word-en-ru" в pickCase (main.go:165-179). mise.toml: [tasks.e2e-word] по конвенции e2e-m1 (описание с "(live GNOME session; NOT in ci)"). mise run ci зелёный. Commit: feat(02-01). REFACTOR при очевидных улучшениях без смены поведения.
  </action>
  <verify>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise exec -- go test ./internal/correct/ ./internal/session/ ./engine/ -race -count=1</automated>
    <fails_when>строка FAIL в выводе или non-zero exit — корпус чистых единиц/актёра/адаптера не зелёный под -race</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise run ci</automated>
    <fails_when>non-zero exit — build/vet/golangci-lint/test -race не зелёные (D-08)</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise run e2e-word</automated>
    <fails_when>non-zero exit или строка FAIL в выводе стенда; либо в выводе отсутствует PASS word-en-ru — сквозной путь (инжекция → FSM → сверка → DeleteSurroundingText+CommitText → оракул stdout "привет") не работает на живом столе</fails_when>
  </verify>
  <done>Сквозной конвейер работает: живой кейс word-en-ru зелёный (ghbdtn→привет в zenity через AT-SPI/stdout-оракул), корпус internal/correct зелёный под -race, INFO-логи не содержат слов, mise run ci зелёный; история test(02-01) до feat(02-01)</done>
  <acceptance_criteria>
  - internal/correct существует и не импортирует godbus/time/goroutines: grep -L по import-блокам пяти .go-файлов не находит github.com/godbus
  - TestConvert_CasePreserved пинит все три регистра в обе стороны и пример D-14 "ghbdtn,"→"привет,"
  - TestBuildPlan_BothLevels пинит Offset/NChars для токена с хвостом (-(tail+n), tail+n) и вырожденный случай tail==""
  - engine.Engine имеет методы DeleteSurroundingText(int32, uint32) и RequireSurroundingText() с Emit по ifaceEngine+".…" (симметрично CommitText)
  - session.Actor реализует HandleSurroundingText/HandleCapabilities/AttachEngine; factory.CreateEngine вызывает AttachEngine
  - actor_test.go фиксирует: сверка не сошлась → ноль вызовов DeleteSurroundingText/CommitText на sink (abort, не мусорить)
  - в логах INFO из актёра нет содержимого слов: только reason/outcome-записи (пинируется тестом лога)
  - живой прогон mise run e2e-word завершается PASS, desktop state восстановлен (стенд сам проверяет verifyRestored)
  </acceptance_criteria>
</task>

<task type="tdd">
  <name>Task 2: Границы слова владельца — слово после пробела (D-13), пунктуация в токене (D-14), цифры (D-15): корпус + хвостовая арифметика + живой кейс word-after-space</name>
  <reversibility rating="costly">Правило «слово живёт после пробела» — пользовательский контракт жеста (reversibility D-13 из CONTEXT: costly); e2e-кейсы «после пробела» завязаны на него</reversibility>
  <files>internal/correct/buffer_test.go, internal/correct/plan_test.go, internal/session/actor_test.go, test/e2e/case_word.go, mise.toml</files>
  <read_first>
  - internal/correct/buffer.go, internal/correct/plan.go (текущее состояние после Task 1)
  - .planning/phases/02-korrektsiya-slova-en-ru/02-RESEARCH.md (§ Ключевая находка №2 — хвост после слова: реальный баг прототипа punto_engine.py:186-199; § Common Pitfalls 1/2)
  - test/e2e/case_word.go (кейс Task 1 — расширяется)
  </read_first>
  <behavior>
  - TestBuffer_WordAfterSpace (D-13): Push "ghbdtn" + ' ' → Token()=="ghbdtn", Tail()==" "; повторный ввод второго слова: "ghbdtn vj" → Token()=="vj", Tail()==""; "ghbdtn vj " → Token()=="vj", Tail()==" "
  - TestBuffer_PunctAndDigitTokens (D-14/D-15): "ghbdtn," → Token()=="ghbdtn," (запятая ёмкая — RU 'б'); "ghbdtn;" → Token()=="ghbdtn;" (';' ёмкая — RU 'ж'); "ghbdtn2026" → Token()=="ghbdtn2026"; "ghbdtn/" → Token()=="ghbdtn", Tail()=="/" ('/' не ёмкая); "ghbdtn v" → Backspace×1 → Token()=="ghbdtn" (вернулись к прошлому слову), Tail()==""
  - TestPlan_TailArithmetic (CORR-07, Pitfall 1): token="ghbdtn", tail=" " → Level1{Offset:-7, NChars:7, Commit:"привет "}; token="ghbdtn", tail="" → {Offset:-6, NChars:6, Commit:"привет"}; token="привет" (RU→EN), tail=" " → Level2{Backspaces:7, Commit:"ghbdtn "} — счёт в рунах
  - TestBuffer_ReplaceTokenToggle: после ReplaceToken("привет") буфер "ghbdtn " → "привет "; Token()=="привет" — повторная коррекция конвертирует обратно (Q3: замена, не очистка)
  - TestActor_AfterSpaceCorrects (session, fake-sink): g..n + ' ' + двойной тап + HandleSurroundingText("abc ghbdtn ", 11) → DeleteSurroundingText(-7,7) и CommitText("привет ") ровно по одному разу
  </behavior>
  <action>
  RED (D-07): расширить корпус (behavior выше) в buffer_test.go/plan_test.go/actor_test.go против текущей реализации — при корректном Task 1 часть кейсов уже зелёные (это НЕ INVALID_RED: новые падающие должны быть — если всё зелёное, значит Task 1 уже закрыл границы и RED-коммит не нужен, зафиксировать это в SUMMARY); падающие минимум: ReplaceTokenToggle, Actor_AfterSpaceCorrects, PunctAndDigitTokens (если Task 1 не закрыл). Commit: test(02-01).
  GREEN: доработать buffer.go/plan.go ровно до корпуса; actёра — если RequiresSurroundingText-флоу не доводил хвост. Живой кейс: runWordAfterSpace в case_word.go (скелет runWordENRU): injectText("ghbdtn") → pressKey("space") → injectKeys("shift-r","shift-r") → waitForLog "correction","outcome":"done" → closeZenity → stdout == "привет " (с завершающим пробелом). Реестр "word-after-space"; mise.toml [tasks.e2e-word-space]. Прогнать живьём: это главный геометрический тест фазы (баг хвоста прототипа — Pitfall 1). Commit: feat(02-01).
  </action>
  <verify>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise exec -- go test ./internal/correct/ ./internal/session/ -race -count=1</automated>
    <fails_when>строка FAIL в выводе — корпус границ слова не зелёный</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise run e2e-word-space</automated>
    <fails_when>non-zero exit или отсутствие PASS word-after-space в выводе; либо stdout-оракул дал "gпривет"/" привет" — хвостовая арифметика неверна (Pitfall 1 живьём)</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise run ci</automated>
    <fails_when>non-zero exit — тройной гейт не зелёный (D-08)</fails_when>
  </verify>
  <done>Слово после пробела исправляется живьём (`ghbdtn ` → `привет `), пунктуация/цифры в токене пиниты корпусом, toggle после коррекции работает; история test до feat</done>
  <acceptance_criteria>
  - buffer_test.go пинит: пробел завершает слово, но не выкидывает его из буфера (D-13); ',' и ';' в токене, '/' и '-' — граница (D-14); цифры в токене (D-15)
  - plan_test.go пинит хвостовую арифметику обоих уровней в рунах, включая кириллический токен (6 рун, не 12 байт)
  - actor_test.go: кейс после пробела даёт ровно один DeleteSurroundingText(-7,7) + один CommitText("привет ")
  - mise run e2e-word-space — PASS, в stdout-оракуле "привет " с сохранённым пробелом
  - mise run ci зелёный
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ibus-daemon → engine (D-Bus) | входящие вызовы ProcessKeyEvent/SetSurroundingText/SetCapabilities — данные клиентского происхождения |
| engine → клиент (сигналы) | исходящие DeleteSurroundingText/CommitText меняют текст в поле пользователя |
| буфер демона | хранит всё, что набрал пользователь (keystroke-данные) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01-01 | Tampering | двухфазная коррекция actor.go | high | mitigate | сверка ADR-004 до удаления: несовпадение/таймаут → тихий отказ, ноль эмиттов удаления (пин TestActor_VerifyPaths) |
| T-02-01-02 | DoS | новые пути обработчиков | medium | mitigate | никаких ожиданий в ProcessKeyEvent (ответ мгновенный, Pitfall 4); recover-шимы на экспортированных методах сохранены |
| T-02-01-03 | Information Disclosure | логи коррекции | high | mitigate | D-20/D-21: INFO — только reason/outcome без слов; исход→результат — только -debug; пинится тестом лога (actor_test.go) |
| T-02-01-04 | Tampering | арифметика диапазона | high | mitigate | счёт в рунах (Pitfall 2), хвостовая геометрия (Pitfall 1) — корпус TestPlan_* + живой word-after-space |
</threat_model>

<verification>
- mise run ci зелёный (build + vet + golangci-lint + test -race, D-08)
- mise run e2e-word && mise run e2e-word-space — оба PASS на живой сессии, desktop state восстановлен
- Полнота среза: непустой buf → Detect → Convert → BuildPlan → RequireSurroundingText → свера → DeleteSurroundingText+CommitText — каждый шаг пинится корпусом или живым кейсом
</verification>

<success_criteria>
- CORR-01 (happy path EN→RU), CORR-04 (чистое направление), CORR-05 (регистр), CORR-07 (уровень 1, ровно по диапазону) — доказаны корпусом + живыми кейсами
- D-13/D-14/D-15/D-20/D-21 реализованы дословно
- Никаких отложенных заглушек в архитектуре: BuildPlan уже вычисляет оба уровня (уровень 2 подключается планом 02-03 без переделки формул)
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (символы плана 02-01)

- Пакет `internal/correct`: `Buffer`, `NewBuffer`, `(*Buffer).Push/Backspace/HardReset/Token/Tail/ReplaceToken`, `letterCapable`; `Dir`, `ENtoRU`, `RUtoEN`, `Detect`; `Convert`; `MatchesSuffix`; `Level`, `Level1`, `Level2`, `Plan`, `BuildPlan`
- `engine.EventHandler` расширен: `HandleSurroundingText(text string, cursorPos uint32)`, `HandleCapabilities(caps uint32)`, `AttachEngine(*Engine)`
- `engine.Engine`: новые методы `DeleteSurroundingText(offset int32, nchars uint32)`, `RequireSurroundingText()`
- `internal/session.Actor`: новые методы `HandleSurroundingText`, `HandleCapabilities`, `AttachEngine`; конвейер `startCorrection`; константа `verifyWait = 100ms`; тип `pendingFix`
- test/e2e: кейсы реестра `word-en-ru`, `word-after-space` (файл case_word.go)
- mise-задачи `e2e-word`, `e2e-word-space`
- Лог-контракты: INFO `{"msg":"correction","outcome":"done"}` / `{"msg":"correction skipped","reason":…}`; DEBUG `{"msg":"correction","level":N,…}` (level — первый атрибут после msg)
</artifacts_produced>

<output>
Create `.planning/phases/02-korrektsiya-slova-en-ru/02-01-SUMMARY.md` when done
</output>
