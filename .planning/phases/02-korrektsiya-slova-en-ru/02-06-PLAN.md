---
phase: 02-korrektsiya-slova-en-ru
plan: 06
type: execute
wave: 5
depends_on: [02-04, 02-05]
files_modified:
  - go.mod
  - go.sum
  - test/e2e/matrix.go
  - test/e2e/matrix_test.go
  - test/e2e/main.go
  - test/e2e/cases/matrix-v1.yaml
  - test/e2e/README.md
  - mise.toml
  - .gitignore
autonomous: true
requirements: [TEST-04]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "e2e-матрица v1 зелёная: YAML-кейсы «ввод → ожидание» (D-18) прогоняются живой сессией, отчёт печатает PASS/FAIL по каждому кейсу, код выхода ≠ 0 при любом падении (TEST-04)"
    - "Изоляция кейсов — явный шаг прогона: КАЖДЫЙ кейс матрицы исполняется в полном цикле setupStand (запуск свежего демона и активация движка) → кейс → teardownStand; каждый кейс стартует в режиме EN с пустым буфером — scriptMode и буфер НЕ переносятся между кейсами, порядок кейсов в файле не влияет на результат"
    - "Набор v1 — полный словесный (D-18): оба направления (`ghbdtn`→`привет`, `привет`→`ghbdtn`), три регистра (`GHBDTN`→`ПРИВЕТ`, `Ghbdtn`→`Привет`, `ghbdtn`→`привет`), слово после пробела (D-13), пунктуация и цифры в токене (D-14/D-15), смешанное слово не тронуто (D-16), сбросы Escape/Enter/Tab/фокус на клиентских классах (CORR-09)"
    - "Критерий успеха 1 фазы покрыт дословно: gnome-text-editor несёт все три регистра — word-gte (`ghbdtn`→`привет`), word-gte-upper (`GHBDTN`→`ПРИВЕТ`), word-gte-capitalized (`Ghbdtn`→`Привет`)"
    - "Поверхности матрицы: zenity (GTK-класс) + chromium (обязателен по ADR-003, фиксирует fallback-уровень 2) + gnome-text-editor (GtkSourceView-класс; драйверы поставлены планом 02-02 и переиспользуются)"
    - "YAML декодируется строго (yaml.v3 decoder.KnownFields(true)): кейс с неизвестным полем или отсутствующим обязательным падает ошибкой декодирования, а не молча — прогон завершается FAIL с именем файла (тест-пин)"
    - "expect_text сравнивается точным равенством UTF-8-строк: readback поля (AT-SPI) или stdout-оракул zenity (кейсы, закрывающие диалог) сравниваются с ожиданием целиком, руна в руну"
    - 'Каждый {tap}-шаг синхронизирован: после тапа раннер ждёт {"msg":"action","n":K} (и mode-запись после single) в логе демона перед следующим шагом — RU-часть кейса никогда не набирается до флипа (Pitfall 5)'
    - "Каждый кейс под watchdog (caseTimeout 180 с) — стенд не вешает живой стол; инжекция только после AT-SPI witness на поверхности, которую стенд открыл сам"
  artifacts:
    - path: test/e2e/matrix.go
      provides: "загрузчик YAML (строгий, мультидок), прогонщик шагов type/key/tap/focus с изоляцией кейса (свежий демон на кейс), сверка expect_text (+expect_level по debug-логу), отчёт PASS/FAIL, exit≠0"
    - path: test/e2e/matrix_test.go
      provides: "пины декодирования: валидный кейс, неизвестное поле → ошибка, отсутствие обязательного → ошибка"
    - path: test/e2e/cases/matrix-v1.yaml
      provides: "полный словесный набор D-18 (16 кейсов) на трёх поверхностях"
    - path: mise.toml
      provides: "[tasks.e2e-matrix] — тот же интерфейс для локального прогона и CI-раннера (D-09)"
  key_links:
    - from: test/e2e/matrix.go
      to: test/e2e/main.go
      via: "флаг -matrix <path> в flag-поверхности main.go:88-93; шаги переиспользуют injectText/injectKeys/pressKey/waitForLog; изоляция — цикл setup/teardown стенда"
      pattern: "injectText|waitForLog"
    - from: test/e2e/matrix.go
      to: go.mod
      via: "gopkg.in/yaml.v3 v3.0.1 — единственная новая зависимость (Package Legitimacy Audit: Approved)"
      pattern: "yaml\\.v3"
  prohibitions:
    - statement: "Стенд НЕ ДОЛЖЕН инжектировать нажатия в окна, которые он сам не открыл: перед каждой инъекцией — AT-SPI witness на собственной поверхности кейса (прецедент Фазы 1); два параллельных стенда на живом столе недопустимы (concurrency — в плане 02-07)"
      status: resolved
      verification: judgment
  assumptions:
    - "FLAGGED (edge-probe, TEST-04, explicit — закрыто критерием): пустой/минимальный ввод: кейс без steps или без name/expect_text отклоняется строгим декодером, прогон падает с именем кейса (не молча)"
    - "FLAGGED (edge-probe, TEST-04, explicit — закрыто критерием): определение длины/равенства: сравнение expect_text — точное равенство целых UTF-8-строк (руна в руну), никаких нормализаций/trim"
    - "FLAGGED (planner, Q2 → RESOLVED расширенно): gnome-text-editor — третья поверхность со всеми тремя регистрами критерия 1; D-17 (минимум zenity+Chromium) не запрещает расширения и сам помечен «ширина матрицы наращивается» — замена формулировки критерия не требуется"
---

<objective>
Приёмочная петля фазы: YAML-матрица e2e-кейсов v1 (TEST-04, D-18) над стендом Фазы 1 — загрузка строгим декодером, шаговый прогон по трём поверхностям (zenity / chromium / gnome-text-editor, драйверы из 02-02) с изоляцией кейса (свежий демон на каждый кейс), отчёт PASS/FAIL по кейсам, код выхода ≠ 0 при падении. Единственная новая зависимость — gopkg.in/yaml.v3 v3.0.1 (Approved в аудите исследования).

Purpose: зелёная матрица v1 — критерий приёмки M2; она же — регрессионный контур для CI-раннера (план 02-07).
Output: matrix.go + matrix_test.go, cases/matrix-v1.yaml (16 кейсов), mise-задача e2e-matrix, обновлённый README стенда.
</objective>

<execution_context>
@/home/nil/DiskD/W/Djarvur/goswitch/.zcode/gsd-core/workflows/execute-plan.md
@/home/nil/DiskD/W/Djarvur/goswitch/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-korrektsiya-slova-en-ru/02-CONTEXT.md (D-17, D-18 — заперты)
@.planning/phases/02-korrektsiya-slova-en-ru/02-PATTERNS.md (§ matrix.go — реестр/exit-code/watchdog/bounded-subprocess/wait-for-condition)
@.planning/phases/02-korrektsiya-slova-en-ru/02-RESEARCH.md (§ Pattern 5 — формат YAML-кейса дословно; § Don't Hand-Roll — yaml.v3 KnownFields)
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Раннер матрицы — строгий YAML-декодер (KnownFields), шаговый прогон type/key/tap/focus с изоляцией кейса (свежий демон), отчёт PASS/FAIL, exit≠0 (TEST-04, D-07)</name>
  <reversibility rating="reversible">Формат кейсов — локальный контракт стенда; расширение полей — аддитивно, строгий декодер ловит опечатки</reversibility>
  <precondition>Планы 02-04/02-05 завершены: все словесные поведения зелёные живьём; драйверы поверхностей (02-02) — mise run e2e-chromium-smoke и e2e-gte-smoke PASS на HEAD</precondition>
  <files>go.mod, go.sum, test/e2e/matrix.go, test/e2e/matrix_test.go, test/e2e/main.go, mise.toml, .gitignore</files>
  <read_first>
  - .planning/phases/02-korrektsiya-slova-en-ru/02-RESEARCH.md (§ Pattern 5 — схема кейса name/surface/mode/steps/expect_text, мультидок через ---, синхронизация тапов; § Validation Architecture — mise run e2e-matrix)
  - test/e2e/main.go (flag-поверхность:88-93, pickCase:165-179, run()→int контракт:86-135, runCaseWatchdog:141-161, runCmd:447-460, waitForLog/waitForNew:486-508, injectText/injectKeys/pressKey:559-594; цикл setup/teardown демона стенда — точка изоляции)
  - test/e2e/case_m1.go (openEntrySurface:101, closeEntrySurface:127)
  - test/e2e/surface.go (драйверы chromium/gnome-text-editor из 02-02 — матрица переиспользует)
  - mise.toml (конвенция e2e-задач:37-51)
  </read_first>
  <behavior>
  - TestMatrixDecode_Valid: YAML из Pattern 5 (word-after-space-en-ru и reset-escape-noop) декодируется в []matrixCase с точными значениями полей
  - TestMatrixDecode_UnknownFieldRejected: кейс с полем typo (например expct_text) → ошибка декодирования (KnownFields), не молча
  - TestMatrixDecode_MissingRequiredRejected: кейс без steps → ошибка; шаг и без type/key/tap/focus → ошибка валидации «ровно один вид шага»
  - TestMatrixDecode_MultiDoc: несколько кейсов в одном файле через --- разделители → len == N
  - TestMatrixStepValidation: {tap: quadruple} → ошибка (только single|double|triple); {surface: terminal} → ошибка (только zenity|chromium|gnome-text-editor); {mode: xx} → ошибка (en|ru)
  - TestMatrixReport_ExitCode: отчёт с ≥1 FAIL даёт код выхода 1, все PASS → 0 (функция расчёта выхода тестируется без живого стола)
  </behavior>
  <action>
  RED (D-07): test/e2e/matrix_test.go (package main — пакет стенда компилируется с тестами) с кейсами behavior; matrix.go — заглушка с типами matrixCase/matrixStep и функциями-нулями → падения на утверждениях. Прогон mise exec -- go test ./test/e2e/ -race -count=1 — подлинный RED; red-evidence. Commit: test(02-06).
  GREEN: go get gopkg.in/yaml.v3@v3.0.1 && go mod tidy (единственный новый require в go.mod — tidy-diff гейт ci это пинит). matrix.go: struct matrixCase{Name, Surface, Mode string; Steps []matrixStep; ExpectText string `yaml:"expect_text"`; ExpectLevel uint8 `yaml:"expect_level"`}; matrixStep{Type/Key/Tap/Focus string — каждое yaml-тег по RESEARCH Pattern 5}; decode: yaml.NewDecoder(bytes.NewReader(data)), dec.KnownFields(true), цикл Decode до io.EOF; валидация: surface ∈ {zenity, chromium, gnome-text-editor}, mode ∈ {en, ru}, каждый шаг ровно один из type|key|tap|focus, tap ∈ {single,double,triple}, expect_text непуст (expect_level — 0 = не проверять). Прогон кейса (runMatrixCase): под runCaseWatchdog; ИЗОЛЯЦИЯ КЕЙСА — явный шаг цикла матрицы: каждый кейс исполняется в полном цикле setupStand (запуск свежего демона + активация движка) → runCase → teardownStand — НЕ один процесс-демон на все кейсы: scriptMode и буфер живут в демоне и переносились бы между кейсами (word-ru-en/word-mixed оставляют режим RU — последующие en-кейсы падали бы); перезапуск демона на кейс даёт детерминированный старт EN/пустой буфер, порядок кейсов в файле не влияет на результат. Внутри кейса: activateGoswitch; mode=="ru" → injectKeys("shift-r") + waitForLog mode:to:ru; surface-драйвер по имени (zenity: openEntrySurface-семейство; chromium/gnome-text-editor: из surface.go); шаги: type → injectText; key → pressKey (space|Escape|Enter|Tab — map на имена ydotool как в pressKey); tap → injectKeys("shift-r" ×K) + waitForLog {"msg":"action","n":K} (Pitfall 5); focus → переключить активную поверхность (открыть целевую и witness — для reset-фокуса); сверка: readback (zenity: если диалог ещё открыт — helper text, если шаги его закрыли — stdout close-оракула; chromium/gnome-text-editor: readText) == ExpectText целиком; ExpectLevel > 0 → waitForLog '"msg":"correction","level":N' (фактический уровень, ADR-003). Отчёт: строки "PASS <name>" / "FAIL <name>: expected %q got %q"; итог; любое FAIL → код 1. main.go: флаг -matrix string — ветка матрицы вместо pickCase (реестр -case сохраняется). .gitignore: e2e-report.txt (матрица дублирует отчёт в файл для артефакта CI 02-07). mise.toml: [tasks.e2e-matrix] = "go run ./test/e2e -matrix test/e2e/cases/matrix-v1.yaml" (описание "(live GNOME session; NOT in ci)"). Минимальный smoke-файл из 2-3 кейсов для первого живого прогона (полный набор — Task 2). Commit: feat(02-06).
  </action>
  <verify>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise exec -- go test ./test/e2e/ -race -count=1</automated>
    <fails_when>строка FAIL — корпус декодера/отчёта не зелёный</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise exec -- go mod tidy && git diff --exit-code -- go.mod go.sum && echo TIDY-CLEAN</automated>
    <fails_when>git diff печатает расхождение — go.mod/go.sum дрейфовали после tidy (лишние зависимости сверх yaml.v3)</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise run e2e-matrix</automated>
    <fails_when>non-zero exit или строка FAIL в отчёте — smoke-набор матрицы не зелёный на живой сессии</fails_when>
  </verify>
  <done>Раннер матрицы работает: строгий декодер пинит, шаговый прогон синхронизирован, кейс изолирован свежим демоном (явный setup/teardown в цикле), отчёт PASS/FAIL с кодом выхода; mise run e2e-matrix зелёный на smoke-наборе</done>
  <acceptance_criteria>
  - go.mod содержит ровно одну новую строку require: gopkg.in/yaml.v3 v3.0.1
  - matrix_test.go пинит: неизвестное поле и отсутствующий обязательный элемент → ошибка декодирования
  - matrix.go: KnownFields(true) присутствует; валидация surface/mode/tap/шагов; цикл матрицы перезапускает демон на каждый кейс (setupStand/teardownStand вокруг runMatrixCase — изоляция не допущение, а шаг)
  - отчёт матрицы печатает PASS/FAIL по каждому кейсу и файл e2e-report.txt
  - mise run e2e-matrix завершается 0 на smoke-наборе; при порче ожидания (руками, для проверки) — код 1
  - mise run ci зелёный (включая tidy-diff)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Полный набор кейсов v1 (D-18) — 16 кейсов на трёх поверхностях, три регистра на gnome-text-editor (критерий 1 дословно) + README</name>
  <reversibility rating="reversible">Ширина матрицы наращивается (D-17); добавление кейсов — аддитивно к YAML</reversibility>
  <files>test/e2e/cases/matrix-v1.yaml, test/e2e/README.md</files>
  <read_first>
  - .planning/phases/02-korrektsiya-slova-en-ru/02-CONTEXT.md (D-18 — дословный перечень; D-17)
  - test/e2e/cases/matrix-v1.yaml (smoke-набор Task 1 — расширяется)
  - test/e2e/README.md (run book — добавляется раздел матрицы)
  - .planning/phases/02-korrektsiya-slova-en-ru/02-RESEARCH.md (§ Pitfall 6 — zenity закрывается от Enter/Escape: сброс-кейсы по классам)
  </read_first>
  <action>
  Полный набор D-18 в test/e2e/cases/matrix-v1.yaml (мультидок), по поверхностям с учётом Pitfall 6:
  zenity (en): word-en-ru (ghbdtn→привет, expect_level:1 — пинит фактический уровень GTK-класса, проверка A1), word-upper (GHBDTN→ПРИВЕТ), word-capitalized (Ghbdtn→Привет), word-after-space ("ghbdtn "→"привет ", D-13), word-punct ("ghbdtn,"→"привет,", D-14), word-digits ("ghbdtn2026"→"привет2026", D-15)
  zenity (ru): word-ru-en (привет→ghbdtn: single-tap флип, ожидание mode-записи, набор, double-tap)
  zenity (en, смешанное): word-mixed-untouched (gfb + флип + ghbdtn → "gfbпривет" не тронуто, D-16)
  chromium (en): fallback-chromium (ghbdtn→привет, expect_level:2 — ADR-003 фиксация), reset-escape (ghbdtn + Escape + double → "ghbdtn", CORR-09), reset-enter (ghbdtn + Enter + double → "ghbdtn" — поле chromium переживает Enter), reset-tab (ghbdtn + Tab + double → "ghbdtn" — Tab: та же таблица keyvals сброса, фокус уходит из поля), reset-focus (ghbdtn + focus zenity + focus chromium + double → "ghbdtn" — сброс по смене фокуса)
  gnome-text-editor (en): word-gte (ghbdtn→привет), word-gte-upper (GHBDTN→ПРИВЕТ), word-gte-capitalized (Ghbdtn→Привет) — критерий успеха 1 фазы ДОСЛОВНО (три регистра на этой поверхности); expect_level по факту — уровень ставится по итогам живого прогона и фиксируется в файле
  Каждый кейс — имя, поверхность, режим, шаги, ожидание. Итого 16 кейсов (6 zenity-en + 1 zenity-ru + 1 mixed + 5 chromium + 3 gnome-text-editor). README стенда: раздел «Матрица v1» — запуск, формат кейса, изоляция кейса (свежий демон на кейс — почему), добавление кейса, интерпретация expect_level, поведение при FAIL. Живой прогон mise run e2e-matrix до зелёного; фактические уровни по поверхностям зафиксировать в файле (expect_level) и SUMMARY.
  </action>
  <verify>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && test -s test/e2e/cases/matrix-v1.yaml && test "$(grep -c '^name:' test/e2e/cases/matrix-v1.yaml)" -ge 16 && echo CASES-COMPLETE</automated>
    <fails_when>non-zero exit — файл кейсов пуст или содержит меньше 16 кейсов (набор D-18 + критерий 1: 6 zenity-en + 1 zenity-ru + 1 mixed + 5 chromium + 3 gnome-text-editor = 16)</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && grep -q 'word-gte-upper' test/e2e/cases/matrix-v1.yaml && grep -q 'word-gte-capitalized' test/e2e/cases/matrix-v1.yaml && grep -q 'reset-tab' test/e2e/cases/matrix-v1.yaml && echo CRITERIA-COVERED</automated>
    <fails_when>non-zero exit — критерий 1 (три регистра на gnome-text-editor) или Tab-сброс не покрыты живыми кейсами</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise run e2e-matrix</automated>
    <fails_when>non-zero exit или любая строка FAIL в отчёте — матрица v1 не зелёная (приёмочный критерий фазы)</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise run ci</automated>
    <fails_when>non-zero exit — тройной гейт не зелёный (D-08)</fails_when>
  </verify>
  <done>Матрица v1 полная (16 кейсов D-18 на трёх поверхностях, три регистра на gnome-text-editor) и зелёная живым прогоном; изоляция кейса работает (порядок кейсов не влияет); фактические уровни лестницы зафиксированы expect_level; README описывает матрицу</done>
  <acceptance_criteria>
  - cases/matrix-v1.yaml содержит кейсы всех шести групп D-18 (направления, регистры, после пробела, пунктуация+цифры, смешанное, сбросы) и chromium-кейс с expect_level:2
  - gnome-text-editor несёт три регистра: word-gte / word-gte-upper / word-gte-capitalized (критерий 1 дословно)
  - сброс по Tab покрыт живым кейсом reset-tab на chromium (та же таблица keyvals, что Enter/Escape)
  - mise run e2e-matrix — все 16 PASS, e2e-report.txt записан; перемешивание порядка кейсов не меняет результат (изоляция)
  - README стенда содержит раздел матрицы с описанием изоляции кейса
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YAML-кейсы → раннер | кейс-файлы читаются из репозитория; опечатка/чужое поле не должно молча исполняться |
| раннер → живой стол | стенд инжектирует нажатия и открывает окна на столе владельца |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-06-01 | Tampering | кейс-инъекция через YAML | medium | mitigate | строгий декодер KnownFields (кейс-пин); кейсы в git — ревью; никакого exec из кейсов: шаги только type/key/tap/focus |
| T-02-06-02 | Elevation | инъекция в чужие окна живого стола | high | mitigate | witness-гейт перед каждой инъекцией (прецедент Фазы 1); поверхности только собственные; watchdog caseTimeout |
| T-02-06-03 | Information Disclosure | отчёт/логи содержат набранный текст | low | accept | отчёт и кейсы — открытые тестовые данные (ghbdtn), не пользовательский ввод; лог демона следует D-20/D-21 |
</threat_model>

<verification>
- mise run ci зелёный; go mod tidy без дрейфа
- mise run e2e-matrix — все 16 кейсов PASS на живой сессии; повторный прогон тоже зелёный (стабильность, изоляция кейсов)
- Кейс с намеренно испорченным ожиданием даёт FAIL и код выхода 1 (проверяется руками один раз при приёмке Task 1)
</verification>

<success_criteria>
- TEST-04 закрыт: YAML-матрица, отчёт PASS/FAIL, код выхода ≠ 0, полный словесный набор D-18 на zenity + chromium + gnome-text-editor
- Критерий успеха 1 фазы покрыт дословно: три регистра на gnome-text-editor
- Изоляция кейсов явная: свежий демон на кейс, scriptMode/буфер не переносятся
- Матрица готова стать CI-задачей раннера (02-07) без изменений формата
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (символы плана 02-06)

- Зависимость `gopkg.in/yaml.v3 v3.0.1` в go.mod
- test/e2e: `matrixCase`, `matrixStep`, `loadMatrixCases`, `runMatrixCase`, `matrixReport` (файл matrix.go); флаг `-matrix <path>`; каталог `cases/matrix-v1.yaml` (16 кейсов); файл отчёта `e2e-report.txt` (gitignored)
- mise-задача `e2e-matrix`
- README стенда: раздел «Матрица v1»
</artifacts_produced>

<output>
Create `.planning/phases/02-korrektsiya-slova-en-ru/02-06-SUMMARY.md` when done
</output>
