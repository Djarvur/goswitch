---
phase: 02-korrektsiya-slova-en-ru
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - engine/engine.go
  - engine/engine_test.go
  - internal/session/actor.go
  - internal/session/actor_test.go
  - internal/correct/direction.go
  - internal/correct/direction_test.go
  - test/e2e/case_word.go
  - mise.toml
autonomous: true
requirements: [CORR-01, CORR-04]

estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Одиночный тап Right Shift переключает внутренний режим движка EN↔RU (флип по ADR-001 Option B): решение FSM Single исполняется на истечении окна, режим — состояние демона, XKB-группа сессии не меняется"
    - "В RU-режиме движок потребляет печатную клавишу и коммитит кириллицу по таблице layouts.ENToRU ('g'→'п', '['→'х', '?'→','), ProcessKeyEvent возвращает true; тождественная карта и непокрытое идут транзитом (false); в EN-режиме всё идёт транзитом — как в Фазе 1"
    - "Буфер хранит реально появившиеся в поле символы (script-true): в RU-режиме — скоммиченные кириллические, в EN-режиме — транзитные латинские; поэтому RU→EN-коррекция работает по составу буфера (CORR-04)"
    - "Оба направления исправляются живьём: `привет`→`ghbdtn` (режим RU, двойной тап) и `ghbdtn`→`привет` (EN) — кейсы word-ru-en и word-en-ru зелёные (CORR-01)"
    - "Слово с буквами обеих раскладок (`gfbпривет`) не трогается вовсе: Detect → (0,false), INFO-отказ reason:mixed-script, поле не меняется — живой кейс word-mixed (D-16)"
    - 'Смена режима логируется на INFO как {"msg":"mode","to":"ru"|"en"} — e2e-стенд ждёт эту запись после {tap: single} перед следующим шагом (Pitfall 5)'
  artifacts:
    - path: internal/session/actor.go
      provides: "scriptMode (modeEN/modeRU), флип на action Single, потребление печатных в RU с коммитом через sink, script-true кормление буфера"
    - path: engine/engine.go
      provides: "ProcessKeyEvent возвращает решение обработчика (consume) — условное потребление вместо всегда-false; контракт observer-false Фазы 1 заменён таблицей режимов"
    - path: internal/correct/direction.go
      provides: "Detect — полный корпус: смешанное слово → отказ (D-16), цифры/пунктуация нейтральны"
    - path: test/e2e/case_word.go
      provides: "живые кейсы word-ru-en и word-mixed"
  key_links:
    - from: internal/session/actor.go
      to: layouts/tables.go
      via: "RU-режим: ENToRU[keyval-rune] → sink.CommitText — паттерн прототипа punto_engine.py:394-414"
      pattern: "ENToRU"
    - from: engine/engine.go
      to: internal/session/actor.go
      via: "ProcessKeyEvent: consume = handler.HandleKey(ev); return consume — решение потребления у логики, транспорт его только возвращает"
      pattern: "HandleKey\\(ev\\)"
  prohibitions:
    - statement: "Определение направления НЕ ДОЛЖНО использовать словари, частотные или лингвистические эвристики — только состав буфера по скрипту букв (CORR-04; SPEC §11: словари — для автокоррекции, которая отвергнута)"
      status: resolved
      verification: judgment
    - statement: "Демон НЕ ДОЛЖЕН показывать интерактивные запросы/диалоги/уведомления при неопределённом направлении или отказе — тихий демон, отказы только в журнал (D-20)"
      status: resolved
      verification: judgment
  assumptions:
    - "FLAGGED (edge-probe, CORR-04, unclassified): автоматическая проба не классифицировала границы детектора направления — предположение: в Фазе 2 «букв какой раскладки больше» (CORR-04) вырождается в чистоту скрипта: любая одна чужая буква → смешанное → тихий отказ (D-16); корпус direction_test.go + кейс word-mixed покрывают границу"
    - "FLAGGED (research A2 → решено планировщиком): триггер флипа в Фазе 2 — проводка FSM Single→флип (рекомендация RESEARCH Open Q1): кейсы D-18 RU→EN и смешанное слово физически требуют режима RU у движка; SWCH-01 (Фаза 3) остаётся за консолидацией источников и UX-доками"
---

<objective>
Внутренний флип скрипта (ADR-001 Option B) в рантайме: одиночный Right Shift переключает режим движка EN↔RU; в RU-режиме движок потребляет печатные клавиши и коммитит кириллицу по таблицам Фазы 1. Это открывает второе направление коррекции (`привет`→`ghbdtn`) и кейс смешанного слова (`gfbпривет` — не трогать, D-16). Буфер становится script-true: хранит символы, реально появившиеся в поле.

Purpose: D-18 запирает кейсы обоих направлений и смешанное слово — без RU-режима они невыполнимы; ADR-001 прямо назначает переключение скрипта выдачи Фазе 2.
Output: условное потребление в ProcessKeyEvent, scriptMode в актёре, полный корпус Direction, живые кейсы word-ru-en / word-mixed, mise-задачи e2e-word-ru / e2e-word-mixed.
</objective>

<execution_context>
@/home/nil/DiskD/W/Djarvur/goswitch/.zcode/gsd-core/workflows/execute-plan.md
@/home/nil/DiskD/W/Djarvur/goswitch/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-korrektsiya-slova-en-ru/02-CONTEXT.md (D-16 — заперт)
@.planning/phases/02-korrektsiya-slova-en-ru/02-PATTERNS.md (§ actor — точка решения ExpiryAt; § engine — RU-mode consumption point engine.go:128-142)
@docs/adr/ADR-001-layout-switching-mechanism.md (Option B — внутренний флип)
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Флип-режим — Single→переключение EN↔RU, потребление печатных в RU с коммитом кириллицы, script-true буфер (CORR-01/CORR-04, D-07)</name>
  <reversibility rating="reversible">Режим — внутреннее состояние актора за стабильным сечением EventHandler; откат или смена триггера — локальная правка</reversibility>
  <precondition>План 02-01 завершён: конвейер коррекции уровня 1 зелёный (mise run e2e-word PASS на HEAD)</precondition>
  <files>engine/engine.go, engine/engine_test.go, internal/session/actor.go, internal/session/actor_test.go</files>
  <read_first>
  - .planning/phases/02-korrektsiya-slova-en-ru/02-RESEARCH.md (§ Pattern 3 — внутренний флип: тело ветки по прототипу punto_engine.py:394-414; § Ключевая находка №1 — почему RU-режим обязателен уже в Фазе 2)
  - engine/engine.go (ProcessKeyEvent:128-142 — точка условного потребления; EventHandler:66-71)
  - engine/engine_test.go (таблица observer-false:54-107 — контракт МЕНЯЕТСЯ, дополняется режимами)
  - internal/session/actor.go (ExpiryAt:92-100 — точка решения Single; HandleKey:43-61 — кормление буфера)
  - internal/session/actor_test.go (tapShift/captureLogs; fake-sink из 02-01)
  - layouts/tables.go (ENToRU: 'g'→'п', '['→'х', '?'→',', '@'→'"', '2'→'2')
  - engine/keys.go (MaskShift, modsMask)
  </read_first>
  <behavior>
  - TestActor_FlipOnSingle: серия из одного Shift_R-тапа + истечение окна → INFO {"msg":"mode","to":"ru"}; повторно → {"msg":"mode","to":"en"}; XKB-группа не участвует (утверждение по отсутствию внешних вызовов на sink, кроме коммитов)
  - TestActor_RUConsumesPrintable: mode RU: HandleKey(press 'g', mods 0) → sink.CommitText("п") и возврат consume=true; '[' → CommitText("х"), consume=true; '?' → CommitText(","), consume=true (shift-уровень уже в keyval); '2' → CommitText("2") допустимо ИЛИ транзит (карта тождественна — пинится: consume=true с коммитом '2' либо false без коммита; выбрать одно и пинить)
  - TestActor_RUTransitUnmapped: mode RU: клавиша вне карты ENToRU (например '\n'-keyval Return) → consume=false, коммита нет (коррекция сброса — план 02-03, здесь только транзит)
  - TestActor_RUCtrlModifiedNotCommitted: mode RU: press 'g' с MaskControl → consume=false, CommitText не вызывался, буфер не кормится (модифицированные — не текст)
  - TestActor_ENTransitUnchanged: mode EN: press 'g' → consume=false, CommitText не вызывался, buf.Push('g') (транзит, как Фаза 1)
  - TestActor_ScriptTrueBuffer: mode RU: 'g','h','b' → буферский Token()=="прив"? — нет: 'g','h','b','d','t','n' → Token()=="привет" (скоммиченные руны), Detect → RUtoEN; двойной тап + свера → CommitText("ghbdtn") на sink (полный конвейер RU→EN на фейке)
  - TestProcessKeyEvent_ConsumePropagation (engine): фейковый handler возвращает true → метод возвращает true; false → false; nil-handler → false (обновлённая таблица бывшего observer-false)
  </behavior>
  <action>
  RED (D-07): сначала тесты. actor_test.go: кейсы behavior против текущего актора — HandleKey ещё возвращает ничего, флипа нет: падения на утверждениях (мод-записи нет, коммитов нет). engine_test.go: таблица ProcessKeyEvent дополняется кейсами consume-пропагации (фейковый handler с настраиваемым возвратом; текущий движок всегда false → кейс true падает). Прогнать mise exec -- go test ./internal/session/ ./engine/ -race -count=1 — подлинный RED; red-evidence (gsd check tdd-red-evidence). Commit: test(02-02).
  GREEN: (1) internal/session/actor.go: поле scriptMode (modeEN старт); HandleKey меняет сигнатуру на (ev engine.EngineEvent) (consume bool) — точка решения потребления у логики: press && печатный (0x20 ≤ keyval < 0xFE00) && ev.Mods&^engine.MaskShift == 0 && mode==RU → ru, ok := layouts.ENToRU[rune(ev.Keyval)]; ok && ru != rune(ev.Keyval) → sink.CommitText(engine.NewIBusText(string(ru))), buf.Push(ru), return true; иначе (ok с тождеством, вне карты, mode EN, модифицированные) → буфер кормится только чистой печатной транзитной руной (mode EN), return false. ExpiryAt: в цикле решений action Single → a.mode = противоположный, slog.Info("mode", "to", "ru"|"en") — единый контракт ожидания стенда (Pitfall 5). Triple → slog.Debug (фразы — Фаза 3). Логирование коммитов флипа — только Debug (D-21: содержимое — не для INFO).
  (2) engine/engine.go ProcessKeyEvent: var consume bool; if e.handler != nil { consume = e.handler.HandleKey(ev) }; return consume, nil — recover-шим и DEBUG-трасса клавиш сохраняются дословно; bare-модификаторы (Shift/Ctrl) не потребляются: актёр возвращает false для них (пин TestActor_RUCtrlModifiedNotCommitted распространяется на все непечатные). Обновить doc-комментарий метода: «наблюдатель Фазы 1» → «потребление решает EventHandler (RU-режим Фазы 2)».
  (3) engine_test.go: обновить контрактную таблицу; паника-сдерживание (:225-280) не трогать. Commit: feat(02-02). REFACTOR: при необходимости вынести печатность в helper (printableRune(keyval, mods) (rune, bool)) без смены поведения.
  </action>
  <verify>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise exec -- go test ./internal/session/ ./engine/ ./internal/correct/ -race -count=1</automated>
    <fails_when>строка FAIL в выводе — корпус флипа/потребления не зелёный под -race</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise run ci</automated>
    <fails_when>non-zero exit — тройной гейт не зелёный (D-08); частный случай: строгий линт ругается на изменение сигнатуры EventHandler — реализация и фейк должны быть согласованы в одном коммите</fails_when>
  </verify>
  <done>Флип на Single работает и логируется, RU потребляет печатные с коммитом кириллицы, EN транзитен, буфер script-true; test(02-02) до feat(02-02)</done>
  <acceptance_criteria>
  - internal/session/actor.go содержит scriptMode и slog.Info("mode", "to", …) на action Single
  - engine.ProcessKeyEvent возвращает значение, возвращённое обработчиком (не константное false)
  - actor_test.go пинит: RU-коммит по таблице ('g'→"п"), Ctrl-модифицированные не кормят буфер и не потребляются, EN-транзит без коммитов
  - engine_test.go: таблица потребления зелёная, паника-тесты Фазы 1 не изменены и зелёные
  - mise run ci зелёный
  </acceptance_criteria>
</task>

<task type="tdd">
  <name>Task 2: Полный корпус направления (D-16) + живые кейсы word-ru-en и word-mixed</name>
  <reversibility rating="reversible">Правило D-16 — логика детектора направления; расширение до CORR-06 (Фаза 3) не меняет хранение</reversibility>
  <files>internal/correct/direction_test.go, internal/correct/direction.go, internal/session/actor_test.go, test/e2e/case_word.go, mise.toml</files>
  <read_first>
  - internal/correct/direction.go (текущий Detect после 02-01)
  - .planning/phases/02-korrektsiya-slova-en-ru/02-CONTEXT.md (D-16 дословно; D-18 — набор кейсов)
  - test/e2e/case_word.go (кейсы 02-01 — расширяются)
  - test/e2e/main.go (waitForLog — ожидание мод-записи)
  </read_first>
  <behavior>
  - TestDetect_MixedRefused (D-16): "gfbпривет"→(_,false); "приветg"→(_,false); одна чужая буква достаточно: "ghbdtnz"→...нет, 'z' латиница: "ghbdtnп"→(_,false)
  - TestDetect_NeutralNotMixed (D-15/D-16): "ghbdtn2026"→(ENtoRU,true); "привет2026"→(RUtoEN,true); "ghbdtn,;"→(ENtoRU,true) — пунктуация нейтральна
  - TestDetect_BothShiftLevels: "GHBdtN"→(ENtoRU,true); "ПриВет"→(RUtoEN,true)
  - TestActor_MixedWordUntouched (session): буфер "gfbпривет" (EN-часть транзитом + RU-часть коммитами) → двойной тап → INFO {"msg":"correction skipped","reason":"mixed-script"}, на sink ноль DeleteSurroundingText и ноль CommitText
  - Живой кейс word-ru-en (D-18): zenity, стартовый режим ru: {tap single} → ждать {"msg":"mode","to":"ru"} → injectText("ghbdtn") (движок коммитит «привет») → {tap double} → stdout-оракул == "ghbdtn"
  - Живой кейс word-mixed (D-18/D-16): zenity: injectText("gfb") → {tap single} → ждать mode:ru → injectText("ghbdtn") → {tap double} → stdout == "gfbпривет" (не тронуто) и в логе correction skipped / reason:mixed-script
  </behavior>
  <action>
  RED: корпус direction_test.go (кейсы выше) + TestActor_MixedWordUntouched против текущей реализации — Detect из 02-01 уже обязан давать (_,false) на смешанном (если корпус зелёный сразу — зафиксировать в SUMMARY, RED-коммит не нужен, GREEN = только живые кейсы). Commit при падениях: test(02-02).
  GREEN: доработки direction.go минимальны (нейтральность уже должна следовать из подсчёта только Latin/Cyrillic). Живые кейсы в case_word.go: runWordRUEN — скелет runWordENRU + шаг флипа: injectKeys("shift-r") → waitForLog("mode","to":"ru") → injectText("ghbdtn") → waitForLog('"msg":"key"' не нужен: ждём коммит не обязательно — сразу двойной тап после инъекции) → injectKeys("shift-r","shift-r") → waitForLog correction outcome:done → closeZenity → stdout=="ghbdtn"; актёр неявно возвращает decode… ВАЖНО: перед двойным тапом убедиться, что флип-окно истекло (waitForLog мод-записи это гарантирует — Pitfall 5). runWordMixed: как в behavior. Реестр: "word-ru-en", "word-mixed". mise.toml: [tasks.e2e-word-ru], [tasks.e2e-word-mixed] (конвенция e2e-m1). Требуется стартовый режим ru для word-ru-en: кейс сам делает single-тап как первый шаг (режим демона стартует EN после запуска — стенд перезапускает демона на каждый кейс, значит режим детерминирован). Commit: feat(02-02).
  </action>
  <verify>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise exec -- go test ./internal/correct/ ./internal/session/ -race -count=1</automated>
    <fails_when>строка FAIL — корпус направления/смешанного не зелёный</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise run e2e-word-ru && mise run e2e-word-mixed</automated>
    <fails_when>non-zero exit или отсутствие PASS word-ru-en / PASS word-mixed; либо оракул word-mixed изменил текст — смешанное слово тронуто (нарушение D-16)</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise run ci</automated>
    <fails_when>non-zero exit — тройной гейт не зелёный (D-08)</fails_when>
  </verify>
  <done>Оба направления коррекции работают живьём; смешанное слово нетронуто с INFO-причиной; корпус D-16 зелёный</done>
  <acceptance_criteria>
  - direction_test.go пинит: одна чужая буква → отказ; цифры и пунктуация не делают слово смешанным; оба регистра
  - actor_test.go: смешанное слово → ноль разрушительных вызовов на sink
  - mise run e2e-word-ru: stdout-оракул "ghbdtn" (полный цикл флип→RU-набор→коррекция)
  - mise run e2e-word-mixed: stdout "gfbпривет" без изменений + запись skipped/mixed-script в логе демона
  - mise run ci зелёный
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| клиент → движок (ProcessKeyEvent) | входящие keyval/keycode/state — решают, что попадёт в поле пользователя (потребление) |
| движок → клиент (CommitText) | в RU-режиме движок сам вставляет текст — новая точка инъекции содержимого |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-02-01 | Tampering | RU-потребление: неверный символ в поле | high | mitigate | коммит только по golden-таблицам layouts.ENToRU (пин Фазы 1); корпус TestActor_RUConsumesPrintable; немапленное — транзит |
| T-02-02-02 | DoS | флип-режим ломает ввод (движок съедает всё) | high | mitigate | потребление ТОЛЬКО чистых печатных в RU; bare-модификаторы и Ctrl-комбо — транзит; живой кейс word-ru-en доказывает возврат в EN |
| T-02-02-03 | Tampering | застрявший RU-режим после ошибки | medium | mitigate | INFO mode-запись наблюдаема; флип — идемпотентный toggle на каждый Single |
</threat_model>

<verification>
- mise run ci зелёный (D-08)
- mise run e2e-word && mise run e2e-word-space && mise run e2e-word-ru && mise run e2e-word-mixed — все четыре кейса слова зелёные (регресс 02-01 не сломан)
</verification>

<success_criteria>
- CORR-01 закрыт в обоих направлениях; CORR-04 закрыт (авто-направление + смешанный отказ D-16)
- Контракт observer-false Фазы 1 осознанно заменён; все существующие тесты engine/session зелёные
- Pitfall 5 закрыт waitForLog на mode-записи
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (символы плана 02-02)

- `internal/session`: `scriptMode`, `modeEN`, `modeRU`, поле `mode` актора; сигнатура `(*Actor).HandleKey(ev engine.EngineEvent) (consume bool)` (сигнатура интерфейса engine.EventHandler меняется вместе)
- Лог-контракт INFO `{"msg":"mode","to":"ru"|"en"}`
- test/e2e: кейсы реестра `word-ru-en`, `word-mixed`
- mise-задачи `e2e-word-ru`, `e2e-word-mixed`
</artifacts_produced>

<output>
Create `.planning/phases/02-korrektsiya-slova-en-ru/02-02-SUMMARY.md` when done
</output>
