## Deferred Items

- Persistent AT-SPI tree wedge after repeated surface SIGKILLs — walks into gnome-shell's bridge block forever; registry restart alone does not heal
  status: open
  **What:** Live matrix runs of 02-06 (2026-09-15) ended with every desktop-wide walk (`focus_helper.py witness`/`focused-inputs`) hanging until the command deadline killed it; per-app bisection isolated the block to gnome-shell's a11y bridge (all other apps, including the owner's Chrome with its 30-child tree, walked fine). Restarting at-spi2-registryd alone did NOT help — the stuck state lives in the shell process; the full a11y BUS restart (kill at-spi-bus-launcher + its dbus-daemon; the stack is dbus-activated and respawns) rebuilt every bridge and walks returned to ~60 ms. The matrix now carries a between-cases quiesce gate (`matrixQuiesce`, test/e2e/matrix.go) that waits out the TRANSIENT form (registry reaping killed surfaces); the PERSISTENT shell-bridge wedge remains an environmental fault — the CI runner book (02-07) should carry the bus-restart remedy, and the root cause (which SIGKILL path leaves the shell bridge stuck) is worth an upstream look if it recurs.
  **Found:** plan 02-06 Task 2 shuffled-order runs; remedy verified live (walk 10 s+ hang → 0.062 s).

- Daemon verify-after round settles on a stale first push — false `correction verify outcome:mismatch` on a healthy correction
  status: open
  **What:** `internal/session/actor.go` `HandleSurroundingText` closes the verify-after round on the FIRST `SetSurroundingText` push after the correction. Live matrix runs of 02-06 (2026-09-15) caught clients pushing the INTERMEDIATE post-delete pre-commit state (cursor_pos:0, empty suffix) as that first answer — the round logs `outcome:mismatch` while the field ends up correct; the final push (cursor 6) arrives ~1 ms after the round closed. Hit gnome-text-editor on run 1 and zenity on run 2 (nondeterministic, any surface). The matrix runner therefore does NOT pin the match record (comment at `verifyMatrixCase`, `test/e2e/matrix.go`); the match contract stays with `TestActor_VerifyAfterLevel1` and the ladder-chromium case. Candidate fix (daemon-side, needs its own plan): require a settled answer — e.g. ignore pushes for a short grace after the correction, or consume the LAST push within the budget instead of the first.
  **Found:** plan 02-06 Task 1 live runs.

- closeEntrySurface presses ydotool key name "Escape" — resolves to the physical E key on ydotool 0.1.8
  status: open
  **What:** `test/e2e/case_m1.go` `closeEntrySurface` (surfaceShell branch) calls `pressKey(ctx, "Escape")`. Live probe 2026-09-15 (plan 02-05): ydotool 0.1.8's name table has no "Escape" entry and falls back to the first letter — the daemon observed keyval 0x65/keycode 18 (the E key), so the locked-session fallback types 'e' into the shell entry instead of dismissing it. The lowercase name "esc" is the one that maps to the real KEY_ESC (0xff1b/keycode 1), proven live in the same probe. Not fixed here: the shell-close path is 02-03 code outside 02-05's file scope and its fallback path was not exercised by this plan's cases. Same trap class as the 02-03 "space"→S finding; fix is a one-word change when the shell surface path is next touched (02-06 matrix runner is the natural moment).
