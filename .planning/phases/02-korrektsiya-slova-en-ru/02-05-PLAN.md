---
phase: 02-korrektsiya-slova-en-ru
plan: 05
type: execute
wave: 5
depends_on: [02-04]
files_modified:
  - .github/workflows/e2e-matrix.yml
  - docs/ci-runner.md
  - SECURITY.md
  - test/e2e/README.md
autonomous: true
requirements: [TEST-04]

estimate:
  tokens: 16000
  raw_tokens: 16000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Матрица v1 прогоняется CI: workflow e2e-matrix.yml на self-hosted GNOME-раннере (ярлык gnome) запускается вручную (workflow_dispatch и ТОЛЬКО он), гонит mise run e2e-matrix и завершается success при зелёной матрице, failed при любом FAIL (D-19)"
    - "Раннер живёт на этой машине в графической сессии владельца: systemd user unit после graphical-session.target с импортом окружения сессии (WAYLAND_DISPLAY, XDG_RUNTIME_DIR, DBUS_SESSION_BUS_ADDRESS), ярлык gnome; gh api видит его со статусом online"
    - "Два прогона стенда одновременно невозможны: concurrency-группа e2e-gnome без cancel-in-progress — один прогон за раз (Pitfall 8)"
    - "Форк-PR и push не могут запустить матрицу: единственный триггер — workflow_dispatch (запускать могут роли с write; раннер печатает на живом столе владельца — это осознанное решение D-19, угроза закрыта ограничением триггера и минимальными permissions)"
    - "Локальный и CI-прогон — одна и та же mise-задача e2e-matrix (D-09); инструкция раннера воспроизводима по docs/ci-runner.md без root"
  artifacts:
    - path: .github/workflows/e2e-matrix.yml
      provides: "workflow: runs-on [self-hosted, gnome], workflow_dispatch-only, permissions contents: read, concurrency e2e-gnome, шаги checkout → mise install → mise run e2e-matrix → upload артефакта e2e-report.txt"
    - path: docs/ci-runner.md
      provides: "инструкция: установка actions-runner в сессию, systemd user unit, импорт окружения, ярлык, runner-группа, обновление"
    - path: SECURITY.md
      provides: "реестр угроз фазы 2 (включая раннер) — создаётся/расширяется"
    - path: ~/.config/systemd/user/goswitch-ci-runner.service
      provides: "user unit раннера (вне git — в репозитории только инструкция и пример в docs)"
  key_links:
    - from: .github/workflows/e2e-matrix.yml
      to: mise.toml
      via: "шаг mise run e2e-matrix — та же задача, что и локальный прогон (D-09, прецедент pr-sanity.yml)"
      pattern: "e2e-matrix"
    - from: docs/ci-runner.md
      to: dist/systemd/user/goswitchd.service
      via: "паттерн systemd user unit после graphical-session.target — прецедент демона"
      pattern: "graphical-session"
  prohibitions:
    - statement: "Матрица НЕ ДОЛЖНА запускаться из недоверенных источников: никаких pull_request/push-триггеров у e2e-workflow — только workflow_dispatch; fork-PR физически не может печатать на столе владельца"
      status: resolved
      verification: judgment
    - statement: "Раннер НЕ ДОЛЖЕН работать от root и НЕ ДОЛЖН хранить регистрационный токен в репозитории: токен одноразовый, живёт минуты, в git не попадает"
      status: resolved
      verification: judgment
  assumptions:
    - "FLAGGED (research A5): self-hosted раннер = ЭТА машина (единственная GNOME-цель, осознанный выбор владельца D-19); если владелец предпочтёт ВМ — требования воспроизводятся по docs/ci-runner.md (GNOME-сессия, группа input, python3-gi, gir1.2-atspi)"
    - "FLAGGED (runner auth): gh должен быть аутентифицирован с правами admin на Djarvur/goswitch для минта registration-token (проверка: gh api repos/Djarvur/goswitch/actions/runners отвечает 200); при 403 — динамический auth-гейт (checkpoint), не молчаливый обход"
---

<objective>
Регрессионный контур фазы: self-hosted GitHub Actions runner на живой GNOME-машине (D-19) + workflow e2e-matrix.yml, запускающий YAML-матрицу v1 той же mise-задачей, что и локальный прогон. Прогон матрицы становится повторяемым CI-артефактом, а не только ручным действием.

Purpose: D-19 — владелец осознанно выбрал CI-раннер уже в Фазе 2 вместо только-локального прогона; зелёная матрица на раннере — часть phase gate перед /gsd:verify-work.
Output: workflow, установленная и подключённая машина-раннер (systemd user unit, ярлык gnome), инструкция docs/ci-runner.md, SECURITY.md, первый зелёный dispatch-прогон.
</objective>

<execution_context>
@/home/nil/DiskD/W/Djarvur/goswitch/.zcode/gsd-core/workflows/execute-plan.md
@/home/nil/DiskD/W/Djarvur/goswitch/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-korrektsiya-slova-en-ru/02-CONTEXT.md (D-19 — заперт)
@.planning/phases/02-korrektsiya-slova-en-ru/02-PATTERNS.md (§ e2e-matrix.yml — скелет pr-sanity.yml и дельты; § docs — форма инструкции)
@.planning/research/STACK.md (требования к раннеру: GNOME-сессия, группа input, python3-gi, gir1.2-atspi)
</context>

<user_setup>
  - service: github-actions-self-hosted-runner
    why: "Прогон e2e-матрицы на живом GNOME-столе (D-19); исполнитель ставит раннер сам, если gh имеет admin на репозитории"
    dashboard_config:
      - task: "Минт registration-token (только если gh api вернул 403)"
        location: "GitHub → Djarvur/goswitch → Settings → Actions → Runners → New self-hosted runner (токен копируется в команду ./config.sh)"
</user_setup>

<tasks>

<task type="auto">
  <name>Task 1: Workflow e2e-matrix.yml (dispatch-only, self-hosted, concurrency) + инструкция docs/ci-runner.md + SECURITY.md</name>
  <reversibility rating="reversible">CI-контур: триггеры и шаги меняются свободно; откат — удалить workflow и снять раннер</reversibility>
  <precondition>План 02-04 завершён: mise run e2e-matrix зелёная локально; mise-задача e2e-matrix существует в mise.toml</precondition>
  <files>.github/workflows/e2e-matrix.yml, docs/ci-runner.md, SECURITY.md, test/e2e/README.md</files>
  <read_first>
  - .github/workflows/pr-sanity.yml (скелет целиком: триггеры, permissions, concurrency, mise install, шаги — копировать с дельтами)
  - .planning/phases/02-korrektsiya-slova-en-ru/02-RESEARCH.md (§ Pattern 6 — параметры раннера дословно; § Security Domain — строки угроз CI)
  - .planning/phases/02-korrektsiya-slova-en-ru/02-PATTERNS.md (§ e2e-matrix.yml — именные дельты против pr-sanity)
  - dist/systemd/user/goswitchd.service (прецедент user unit после graphical-session.target)
  - test/e2e/README.md (раздел матрицы из 02-04 — добавить ссылку на CI-прогон)
  </read_first>
  <action>
  (1) .github/workflows/e2e-matrix.yml — скопировать скелет pr-sanity.yml и применить дельты Pattern 6: name: e2e-matrix; on: workflow_dispatch ТОЛЬКО (никаких push/pull_request — раннер печатает на живом столе); permissions: contents: read; concurrency: group: e2e-gnome, cancel-in-progress: false (один прогон, очередь); jobs.e2e: runs-on: [self-hosted, gnome]; шаги: actions/checkout@v4 → установка mise (curl https://mise.run | sh; PATH) → mise install → mise run e2e-matrix → actions/upload-artifact@v4 (if: always(), path: e2e-report.txt). Заголовочный комментарий-блок в стиле pr-sanity.yml:1-13 объясняет, ПОЧЕМУ dispatch-only и что раннер живёт в сессии владельца.
  (2) docs/ci-runner.md (русский, стиль test/e2e/README.md): требования к машине из STACK (живая GNOME Wayland-сессия, пользователь в группе input, /dev/uinput доступен, python3-gi + gir1.2-atspi, ydotool, zenity/google-chrome/gnome-text-editor); установка: скачать тарболл actions-runner под linux-x64 в ~/actions-runner, ./config.sh --url https://github.com/Djarvur/goswitch --token <одноразовый из gh api или UI> --labels gnome --unattended; systemd USER unit goswitch-ci-runner.service (пример файла в доке): After=graphical-session.target, WantedBy=graphical-session.target, ExecStart=<home>/actions-runner/run.sh, импорт окружения сессии (WAYLAND_DISPLAY, XDG_RUNTIME_DIR, DBUS_SESSION_BUS_ADDRESS — через systemctl --user import-environment при входе или EnvironmentFile); включение: systemctl --user enable --now; проверка: gh api repos/Djarvur/goswitch/actions/runners --jq '.runners[].status'; обновление раннера; снятие с учёта (config.sh remove). Раздел безопасности: почему раннер только для этого репозитория, runner-группа с ограничением.
  (3) SECURITY.md — создать (в дереве его нет): заголовок, контакт, таблица угроз Фазы 2 из threat-моделей планов 02-01..02-05 с диспозициями (формулировки можно сжать до построчного реестра), отдельно блок про раннер: dispatch-only, permissions, concurrency, печать на живом столе — принято владельцем (D-19).
  (4) test/e2e/README.md: подраздел «CI-прогон» — ссылка на docs/ci-runner.md и workflow, напоминание, что локальный и CI-прогон — одна mise-задача.
  </action>
  <verify>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && test -f .github/workflows/e2e-matrix.yml && ! grep -q 'pull_request\|push:' .github/workflows/e2e-matrix.yml && grep -q 'workflow_dispatch' .github/workflows/e2e-matrix.yml && grep -q 'self-hosted' .github/workflows/e2e-matrix.yml && grep -q 'e2e-gnome' .github/workflows/e2e-matrix.yml && grep -q 'contents: read' .github/workflows/e2e-matrix.yml && echo WORKFLOW-OK</automated>
    <fails_when>non-zero exit — workflow отсутствует, содержит push/pull_request-триггеры (запрещено), или не содержит workflow_dispatch/self-hosted/e2e-gnome/contents: read</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && grep -q 'graphical-session' docs/ci-runner.md && grep -q 'WAYLAND_DISPLAY' docs/ci-runner.md && grep -q 'config.sh' docs/ci-runner.md && grep -q 'registration\|token' SECURITY.md && echo DOCS-OK</automated>
    <fails_when>non-zero exit — инструкция раннера не покрывает unit/окружение/установку или SECURITY.md не создан</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise run ci</automated>
    <fails_when>non-zero exit — тройной гейт не зелёный (D-08)</fails_when>
  </verify>
  <done>Workflow dispatch-only на self-hosted ярлыке gnome с concurrency и минимальными permissions; инструкция воспроизводима; SECURITY.md ведёт реестр фазы</done>
  <acceptance_criteria>
  - .github/workflows/e2e-matrix.yml: единственный триггер workflow_dispatch; runs-on содержит self-hosted и gnome; concurrency group e2e-gnome без cancel-in-progress; шаг mise run e2e-matrix; upload артефакта с if: always()
  - docs/ci-runner.md: покрывает установку, user unit с графической сессией, окружение, ярлык, проверку, обновление, снятие
  - SECURITY.md существует и содержит раздел раннера
  - test/e2e/README.md ссылается на CI-прогон
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Раннер живой — установка в сессию, регистрация с ярлыком gnome, первый dispatch-прогон матрицы зелёный (D-19)</name>
  <reversibility rating="costly">Инфраструктура CI: сворачивание раннера позже ломает регрессионный контур (reversibility D-19 из CONTEXT: costly)</reversibility>
  <precondition>gh аутентифицирован и имеет права admin на Djarvur/goswitch: проверка только чтением — gh api repos/Djarvur/goswitch/actions/runners отвечает 200 (при 403/401 — СТОП, auth-гейт к владельцу, токен из Settings → Actions → Runners)</precondition>
  <files>docs/ci-runner.md</files>
  <read_first>
  - docs/ci-runner.md (Task 1 — исполняем написанное)
  - .github/workflows/e2e-matrix.yml (что будет запущено)
  </read_first>
  <action>
  Выполнить инструкцию docs/ci-runner.md на этой машине: mkdir -p ~/actions-runner; скачать и распаковать последний linux-x64 тарболл actions-runner (URL из GitHub releases — без фиксированной версии в доке, версию зафиксировать в SUMMARY); минт токена: TOKEN=$(gh api repos/Djarvur/goswitch/actions/runners/registration-token --jq .token); ./config.sh --url https://github.com/Djarvur/goswitch --token "$TOKEN" --labels gnome --unattended (токен не логировать и не коммитить); создать ~/.config/systemd/user/goswitch-ci-runner.service по образцу из доки (After/WantedBy=graphical-session.target, импорт окружения сессии: при необходимости systemctl --user import-environment WAYLAND_DISPLAY XDG_RUNTIME_DIR DBUS_SESSION_BUS_ADDRESS — заметить в SUMMARY, если в юните использован EnvironmentFile вместо импорта); systemctl --user daemon-reload && systemctl --user enable --now goswitch-ci-runner; дождаться статуса: systemctl --user is-active goswitch-ci-runner == active и gh api repos/Djarvur/goswitch/actions/runners --jq '.runners[] | select(.name!="") | {name,status,labels}' показывает раннер online с ярлыком gnome. Затем первый прогон: закоммитить и запушить workflow (ветка фазы; запуск dispatch возможен с ветки — gh workflow run e2e-matrix.yml --ref <текущая ветка>), дождаться очереди concurrency, gh run watch до завершения; conclusion == success; артефакт e2e-report.txt скачать (gh run download) и приложить путь в SUMMARY. Если прогон упал по окружению (раннер не видит IBus/uinput — окружение юнита), диагностировать по логу воркфлоу: недостающие переменные сессии добавить в импорт; итерировать до зелёного; все отклонения — в SUMMARY.
  </action>
  <verify>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && systemctl --user is-active goswitch-ci-runner && gh api repos/Djarvur/goswitch/actions/runners --jq '[.runners[] | select(.labels[].name=="gnome")] | length' | grep -q '^1$' && echo RUNNER-ONLINE</automated>
    <fails_when>юнит не active, или среди раннеров репозитория нет ровно одного с ярлыком gnome (0 — регистрация не удалась; >1 — дубликат, лишний снять)</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && RUN_ID=$(gh run list --workflow e2e-matrix.yml --limit 1 --json databaseId --jq '.[0].databaseId') && gh run view "$RUN_ID" --json conclusion --jq '.conclusion' | grep -q '^success$' && echo E2E-CI-GREEN</automated>
    <fails_when>последний прогон e2e-matrix не success (failure — кейс матрицы упал на раннере; cancelled — очередь/ручная отмена) — phase gate фазы не достигнут</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise run ci</automated>
    <fails_when>non-zero exit — тройной гейт не зелёный (D-08)</fails_when>
  </verify>
  <done>Раннер online с ярлыком gnome, первый dispatch-прогон e2e-matrix зелёный, артефакт отчёта получен; токен нигде не закоммичен</done>
  <acceptance_criteria>
  - systemctl --user is-active goswitch-ci-runner → active
  - gh api …/actions/runners: ровно один раннер с ярлыком gnome, статус online
  - gh run view последнего e2e-matrix: conclusion success
  - git log/статус не содержит регистрационного токена; ~/actions-runner/.credentials вне репозитория
  - версия раннера и место импорта окружения зафиксированы в SUMMARY
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub → раннер в сессии владельца | workflow-код исполняется на живом столе; источник запуска должен быть только доверенным |
| registration-token | одноразовый секрет подключения раннера к репозиторию |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05-01 | Elevation | fork-PR запускает матричу на столе владельца | high | mitigate | workflow_dispatch — единственный триггер (пин-грейт Task 1); permissions contents: read; раннер подключён только к Djarvur/goswitch |
| T-02-05-02 | DoS | два стенда печатают одновременно (CI + локальный прогон владельца) | high | mitigate | concurrency e2e-gnome без cancel-in-progress; свидетель-гейт стенда остаётся последней линией |
| T-02-05-02b | Information Disclosure | утечка registration-token | high | mitigate | токен одноразовый (минуты жизни), минтится в команду без логирования, не коммитится; проверка Acceptance |
| T-02-05-03 | Elevation | раннер исполняет PR-код из fork (вектор репо-уровня) | medium | mitigate | dispatch-only + ревью PR (осн. ветки защищены, PR-only поток проекта); прогон — только вручную |
</threat_model>

<verification>
- Раннер online (ярлык gnome), юнит active
- Первый dispatch-прогон e2e-matrix: conclusion success, артефакт отчёта скачан
- Локальная регрессия: mise run ci зелёный
</verification>

<success_criteria>
- D-19 закрыт: self-hosted GNOME-раннер поднят в этой фазе, матрица гоняется CI-контуром
- Phase gate фазы достигнут: ci зелёный + e2e-matrix зелёная локально И на раннере
- Безопасность раннера задокументирована (SECURITY.md) и закрыта gates
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (символы плана 02-05)

- Workflow `.github/workflows/e2e-matrix.yml` (dispatch-only, runs-on [self-hosted, gnome], concurrency e2e-gnome)
- Документы `docs/ci-runner.md`, `SECURITY.md`
- Вне git: `~/actions-runner/`, `~/.config/systemd/user/goswitch-ci-runner.service` (юнит), регистрация раннера с ярлыком `gnome`
- CI-контур: ручной dispatch-прогон `e2e-matrix` с артефактом `e2e-report.txt`
</artifacts_produced>

<output>
Create `.planning/phases/02-korrektsiya-slova-en-ru/02-05-SUMMARY.md` when done
</output>
