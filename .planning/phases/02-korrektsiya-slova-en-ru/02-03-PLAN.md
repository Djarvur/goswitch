---
phase: 02-korrektsiya-slova-en-ru
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - engine/engine.go
  - engine/engine_test.go
  - engine/wire_test.go
  - engine/keys.go
  - internal/correct/plan.go
  - internal/correct/plan_test.go
  - internal/correct/buffer.go
  - internal/correct/buffer_test.go
  - internal/session/actor.go
  - internal/session/actor_test.go
  - test/e2e/surface.go
  - test/e2e/fixtures/input.html
  - test/e2e/case_ladder.go
  - test/e2e/preflight.go
  - mise.toml
autonomous: true
requirements: [CORR-07, CORR-09]

estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - 'На клиенте без бита CapSurroundingText коррекция исполняется уровнем 2: ForwardKeyEvent(BackSpace)×(токен+хвост в рунах) бёрстом, затем CommitText(исправленное+хвост) — живой Chromium-кейс фиксирует ФАКТИЧЕСКИЙ уровень лестницы по debug-записи {"msg":"correction","level":2} (ADR-003, CORR-07)'
    - "Пустой ввод конвейера (буфер без токена) на двойной тап — замены нет, ни одного разрушительного эмитта, INFO-отказ reason:empty-buffer (edge-проба CORR-07, D-20)"
    - "Единица длины/равенства в коррекции — руна (кодовый пункт), не байт: все счётчики диапазона и сравнения сверки рунные; кириллический токен из 6 рун удаляется шестью, не двенадцатью удалениями (edge-проба CORR-07, ADR-03/Pitfall 2)"
    - "После каждой коррекции уровня 1 выполняется verify-after: RequireSurroundingText → свежий SetSurroundingText → суффикс сравнивается с исправленным+хвост; расхождение → INFO-счётчик verify-after mismatch, без автопонадобления (ADR-003 остаточный риск; Chromium ibus#2354 ловится именно здесь и матрицей)"
    - "Буфер очищается по Enter, KP_Enter, Tab, Escape (keyvals 0xff0d/0xff8b/0xff09/0xff1b) и по FocusOut/Reset — живой кейс reset-escape: `ghbdtn`+Escape+двойной тап → поле НЕ изменено (CORR-09)"
    - "Backspace-клавиша выталкивает последнюю руну из буфера; Ctrl-модифицированные комбинации буфер не кормят (Pitfall 7) — корпус buffer/actor пинит"
    - "Подсчёт удалений всегда в рунах: кириллический токен из 6 рун удаляется шестью Backspace, не двенадцатью (ADR-003, Pitfall 2)"
  artifacts:
    - path: engine/engine.go
      provides: "эмиттер ForwardKeyEvent(keyval, keycode, state uint32)"
    - path: engine/keys.go
      provides: "KeyKPEnter = 0xff8b (keypad-вариант Enter для сброса)"
    - path: internal/session/actor.go
      provides: "исполнение уровня 2 (доверие буферу без caps — деградация по ADR-004), verify-after с epoch-защитой, триггеры HardReset по keyvals"
    - path: test/e2e/surface.go
      provides: "драйвер поверхности chromium: fresh-instance запуск (--user-data-dir во временный каталог), witness, AT-SPI readback, закрытие по PID"
    - path: test/e2e/fixtures/input.html
      provides: "статическая страница с <input> для Chromium-кейсов"
    - path: test/e2e/case_ladder.go
      provides: "живые кейсы ladder-chromium (фактический уровень) и reset-escape (CORR-09)"
  key_links:
    - from: internal/session/actor.go
      to: engine/engine.go
      via: "уровень 2: sink.ForwardKeyEvent(engine.KeyBackSpace, 14, 0) × N → sink.CommitText — порядок бёрст→коммит (A4)"
      pattern: "ForwardKeyEvent"
    - from: test/e2e/case_ladder.go
      to: test/e2e/focus_helper.py
      via: "readback текста поля chromium через команду text <app> (cmd_text — мост из Фазы 1)"
      pattern: "focus_helper"
  prohibitions:
    - statement: "Коррекция НЕ ДОЛЖНА продолжать разрушающую последовательность, если сверка не сошлась или не была возможна там, где ADR-004 её требует — «abort, не мусорить»: ни одного удаления без зелёной сверки на caps-клиентах и без чистого буфера на no-caps"
      status: resolved
      verification: judgment
    - statement: "Фаза 2 НЕ ДОЛЖНА трогать буфер обмена пользователя — clipboard-уровень лестницы включается только конфигом (которого ещё нет, ADR-003); замена только DeleteSurroundingText / Backspace×N + CommitText"
      status: resolved
      verification: judgment
    - statement: "Демон НЕ ДОЛЖЕН инжектировать текст физическим путём ввода (эмуляция устройств ввода) — только IBus-эмиттеры commit/delete/forward (аксиома SPEC §3.1: совместимость с keyd/xremap); физическая инжекция остаётся исключительной прерогативой e2e-стенда, симулирующего человека"
      status: resolved
      verification: judgment
  assumptions:
    - "FLAGGED (edge-probe, CORR-09, unclassified): триггеры сброса не классифицированы автоматикой — предположение: полный наблюдаемый на IME-уровне набор (keyvals Enter/KP_Enter/Tab/Escape + FocusOut/Reset) исчерпывает спецификацию; клик мыши ненаблюдаем и НЕ является триггером (ADR-004), остаточный риск задокументирован в ADR"
    - "FLAGGED (research A4): порядок ForwardKeyEvent-бёрст → CommitText сохраняется ibus-daemon для всех клиентов — для GTK доказано прототипом, для Chromium доказывает живой кейс ladder-chromium; при нарушении — пейсинг между forward-ами (запасной ход задокументирован в RESEARCH)"
    - "FLAGGED (research A3): Chromium-поверхность = google-chrome (deb 153.x), snap-chromium — запасная; AT-SPI имя приложения Chrome выводится живьём драйвером (walker helper'а толерантен)"
---

<objective>
Лестница замены становится полной: уровень 2 (Backspace×N по рунам + перекоммит) для клиентов без surrounding-text, verify-after после коррекции, и все триггеры сброса буфера (CORR-09). Chromium входит в стенд как вторая поверхность — живой кейс фиксирует фактический fallback-уровень (ADR-003) и закрывает Pitfall 3 (Chromium молча игнорирует delete-surrounding — ibus#2354).

Purpose: CORR-07 требует «ровно по диапазону» на обоих уровнях лестницы; D-17/ADR-003 требуют Chromium-кейс в матрице — он сначала доказывается здесь, в одиночном исполнении.
Output: эмиттер ForwardKeyEvent, исполнение уровня 2 + verify-after в актёре, триггеры сброса, драйвер chromium + fixtures/input.html, кейсы ladder-chromium / reset-escape, mise-задачи.
</objective>

<execution_context>
@/home/nil/DiskD/W/Djarvur/goswitch/.zcode/gsd-core/workflows/execute-plan.md
@/home/nil/DiskD/W/Djarvur/goswitch/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-korrektsiya-slova-en-ru/02-CONTEXT.md (D-17 — Chromium обязателен; D-20/D-21 — логи)
@.planning/phases/02-korrektsiya-slova-en-ru/02-PATTERNS.md (§ surface.go — перенос драйвера zenity; § preflight — таблица проверок; § engine — тесты эмиттеров)
@docs/adr/ADR-003-replacement-capability-ladder.md
@docs/adr/ADR-004-buffer-reset-triggers.md
</context>

<assumption_delta_decision>
**Assumption-delta (checkpoint, advisory):** сигнал «fallback» в цели фазы относится к лестнице замены (уровень 2), а не ко второй платформе/источнику идентичности. Существительное «коррекция» остаётся единственным: (токен, диапазон, направление) — одна идентичность; уровень лестницы — это диспетчеризация по per-context caps, зафиксированная ADR-003. Решение: **no-change** — add-alongside не возникает, промоция не нужна.
</assumption_delta_decision>

<tasks>

<task type="tdd">
  <name>Task 1: Уровень 2 лестницы — ForwardKeyEvent-бёрст Backspace×(токен+хвост) + перекоммит; verify-after; живой Chromium-кейс фиксирует фактический уровень (CORR-07, ADR-003)</name>
  <reversibility rating="costly">Формула диапазона — контракт CORR-07 («ровно по диапазону»); смена после e2e-матрицы 02-04 требует согласованно менять корпус, матрицу и актёр</reversibility>
  <precondition>Планы 02-01/02-02 завершены: конвейер и флип зелёные; google-chrome установлен (google-chrome --version отвечает) — иначе использовать chromium (snap) и зафиксировать выбор в SUMMARY</precondition>
  <files>engine/engine.go, engine/engine_test.go, engine/wire_test.go, internal/correct/plan_test.go, internal/session/actor.go, internal/session/actor_test.go, test/e2e/surface.go, test/e2e/fixtures/input.html, test/e2e/case_ladder.go, test/e2e/preflight.go, mise.toml</files>
  <read_first>
  - .planning/phases/02-korrektsiya-slova-en-ru/02-RESEARCH.md (§ Pattern 1 — оба уровня; § Pitfall 3 — Chromium ibus#2354; § Pitfall 2 — руны; § Environment Availability — google-chrome 153/chromium 152)
  - engine/engine.go (CommitText:307-317 — образец эмиттера), engine/keys.go (KeyBackSpace=0xff08, keycode 14 из RESEARCH Pattern 1), engine/wire_test.go (прецедент пина wire-контрактов)
  - internal/session/actor.go (конвейер 02-01: pendingFix, verifyTimer, startCorrection)
  - internal/session/actor_test.go (fake-sink — расширяется ForwardKeyEvent)
  - test/e2e/case_m1.go (startZenity:175-186, waitZenityEntry:190-207, closeZenity:211-234, reapZenity:238-247, focusWitness:253-260 — форма драйвера)
  - test/e2e/preflight.go (таблица checks:29-49; стиль диагностики checkUinputWritable:80-86)
  - test/e2e/focus_helper.py (cmd_text:122-136, quirks get_text_at_offset:102-119)
  - docs/adr/ADR-003-replacement-capability-ladder.md (уровни, «abort, не мусорить», обязательный Chromium-кейс)
  </read_first>
  <behavior>
  - TestActor_Level2NoCaps: caps без CapSurroundingText, буфер "ghbdtn" + двойной тап → на sink: НОЛЬ RequireSurroundingText перед удалением (сверка невозможна — доверие буферу, деградация ADR-004), ровно 6 × ForwardKeyEvent(0xff08, 14, 0), затем CommitText("привет") — порядок пинится записью вызовов
  - TestActor_Level2WithTailAndRunes: буфер "привет " (RU→EN): 7 Backspace-форвардов и CommitText("ghbdtn ") — счёт в рунах, не байтах (Pitfall 2)
  - TestActor_VerifyAfterLevel1: после DeleteSurroundingText+CommitText на caps-клиенте актёр сам эмитит RequireSurroundingText (epoch растёт); HandleSurroundingText("abc привет", 10) → расхождения нет, счётчик не растёт; HandleSurroundingText("abc ghbdtn", 10) → INFO {"msg":"correction verify","outcome":"mismatch"} — счётчик, БЕЗ повторной коррекции (без автопонадобления)
  - TestForwardKeyEvent_Emitter (engine/wire или engine_test): сигнал ifaceEngine+".ForwardKeyEvent" с аргументами (keyval, keycode, state) — по образцу пина CommitText
  - Живой кейс ladder-chromium: поверхность chromium, injectText("ghbdtn") → двойной тап → readback поля == "привет" И в логе демона debug-запись {"msg":"correction","level":2,…} (фактический уровень фиксирует кейс, не предположение — ADR-003)
  </behavior>
  <action>
  RED (D-07): fake-sink в actor_test.go расширяется методом ForwardKeyEvent(keyval, keycode, state uint32) с записью вызовов; кейсы behavior Level2NoCaps/Level2WithTailAndRunes/VerifyAfterLevel1 падают против текущего актора (уровень 2 не исполняется: skip no-surrounding из 02-01). engine-тест эмиттера падает (метода нет — для подлинного RED на утверждениях добавить в Engine stub-метод ForwardKeyEvent c пустым телом: вызов не эмитится — wire-пин падает). Прогон, red-evidence. Commit: test(02-03).
  GREEN: (1) engine/engine.go: эмиттер ForwardKeyEvent по образцу CommitText (conn==nil → return; Emit(ifaceEngine+".ForwardKeyEvent", keyval, keycode, state); ошибка → slog.Error).
  (2) internal/session/actor.go: startCorrection — ветка caps&CapSurroundingText==0 → BuildPlan уровень 2: немедленно (сверки нет — деградация ADR-004, доверие буферу + явные триггеры сброса) исполнить: for i := 0; i < plan.Backspaces; i++ { sink.ForwardKeyEvent(engine.KeyBackSpace, 14, 0) } затем sink.CommitText(engine.NewIBusText(string(plan.Commit))); buf.ReplaceToken(converted); INFO done + Debug level:2. Ветка уровня 1 после CommitText: verifyAfter — sink.RequireSurroundingText(), verifyEpoch++, pendingAfter{expected: converted+tail}; HandleSurroundingText при активном pendingAfter и свежем epoch: суффикс text[:cursor] == expected → тихо (Debug); нет → INFO {"msg":"correction verify","outcome":"mismatch"} счётчиком; pendingAfter гасится. Verify-таймаут исходной сверы снимает и pendingAfter.
  (3) test/e2e/surface.go: surfaceChromium (enum surfaceKind case_m1.go:36-43 расширяется); startChromium: exec.CommandContext(google-chrome|chromium, "--new-window", "--user-data-dir="+tempdir, "file://"+abs(test/e2e/fixtures/input.html)) — user-data-dir во временный каталог ОБЯЗАТЕЛЕН: без него процесс присоединяется к инстансу владельца и PID-закрытие не работает; waitChromiumInput: witness через focus_helper (имя приложения подбирается живьём — gnome-at-spi walkers толерантны, зафиксировать найденное имя константой в драйвере); readChromiumText: runCmd(/usr/bin/python3, helper, "text", appName); closeChromium: cmd.Process.Kill + wait; reap-путь по образцу reapZenity. Форма — перенос четырёх функций case_m1.go:175-247.
  (4) fixtures/input.html: минимальная страница с <input id="e" type="text"> и <title>goswitch-e2e</title>.
  (5) preflight.go: проверка chromium-launch (какая из двух бинарных присутствует; запуск с fixture → witness → kill; одна строка человеко-читаемой диагностики при отсутствии).
  (6) case_ladder.go: runLadderChromium по скелету runM1Gate: activate → startChromium → witness → injectText("ghbdtn") → двойной тап → waitForLog('"msg":"correction","level":2') → readback == "привет" → close. Реестр "ladder-chromium"; mise.toml [tasks.e2e-ladder-chromium]. Commit: feat(02-03).
  ПЕЙСИНГ (запасной ход A4): если живой прогон покажет, что бёрст форвардов приходит к клиенту не по порядку или не полностью (дубль текста в readback), вставить между ForwardKeyEvent-ами минимальную задержку (например 5–10 мс, константа в актёре) — решение зафиксировать в SUMMARY; это не смена объёма, а реализация запасного хода из исследования.
  </action>
  <verify>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise exec -- go test ./engine/ ./internal/session/ ./internal/correct/ -race -count=1</automated>
    <fails_when>строка FAIL в выводе — корпус уровня 2/verify-after/эмиттера не зелёный под -race</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise run e2e-ladder-chromium</automated>
    <fails_when>non-zero exit или отсутствие PASS ladder-chromium; либо readback ≠ "привет" (дубль "ghbdtn привет" — Chromium не применил удаление, Pitfall 3 живьём: уровень должен был быть 2); либо в логе нет "level":2 — фактический уровень не зафиксирован</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise run ci</automated>
    <fails_when>non-zero exit — тройной гейт не зелёный (D-08)</fails_when>
  </verify>
  <done>Уровень 2 работает на живом Chromium с зафиксированным фактическим уровнем; verify-after пинит расхождение счётчиком; порядок бёрст→коммит и рунный счёт в корпусе</done>
  <acceptance_criteria>
  - engine.Engine содержит ForwardKeyEvent(keyval, keycode, state uint32) с Emit по ifaceEngine+".ForwardKeyEvent"
  - actor_test.go: ровно N=рун(токен+хвост) форвардов Backspace и один CommitText(исправленное+хвост) — порядок пинится
  - verify-after: mismatch → INFO {"msg":"correction verify","outcome":"mismatch"}, повторной коррекции нет
  - test/e2e/surface.go запускает chrome с --user-data-dir во временный каталог (не трогает профиль владельца)
  - mise run e2e-ladder-chromium — PASS с "level":2 в логе демона
  - mise run ci зелёный
  </acceptance_criteria>
</task>

<task type="tdd">
  <name>Task 2: Триггеры сброса буфера — Enter/KP_Enter/Tab/Escape + Backspace-pop + Ctrl-изоляция (CORR-09) и живой кейс reset-escape</name>
  <reversibility rating="reversible">Триггеры — таблица keyval→действие в актёре; расширение списком не меняет архитектуру</reversibility>
  <files>engine/keys.go, internal/correct/buffer.go, internal/correct/buffer_test.go, internal/session/actor.go, internal/session/actor_test.go, test/e2e/case_ladder.go, mise.toml</files>
  <read_first>
  - engine/keys.go (KeyReturn/KeyTab/KeyEscape/KeyBackSpace — добавить KeyKPEnter=0xff8b рядом)
  - internal/session/actor.go (HandleKey — место таблицы сброса; HandleLifecycle FocusOut/Reset уже сбрасывает с 02-01)
  - internal/correct/buffer.go (Backspace/HardReset — семантика уже есть; корпус расширяется)
  - .planning/phases/02-korrektsiya-slova-en-ru/02-RESEARCH.md (§ Pitfall 7 — desync на no-cap клиентах: Backspace-pop и Ctrl-изоляция как меры; § RESEARCH Code Examples — константы триггеров дословно)
  - docs/adr/ADR-004-buffer-reset-triggers.md
  - test/e2e/case_ladder.go (Task 1 — добавляется кейс reset)
  </read_first>
  <behavior>
  - TestActor_HardResetKeyvals: press KeyReturn / 0xff8b / KeyTab / KeyEscape → после каждого buf пуст (проверяется тем, что двойной тап даёт skip empty-buffer); press KP_Enter 0xff8b покрыт явно (keypad-вариант)
  - TestBuffer_BackspacePop: "ghbdtn v" → Backspace×1 → Token()=="ghbdtn"; Backspace×8 → буфер пуст
  - TestBuffer_CtrlIsolation: актёр: press 'a' с MaskControl → буфер не изменился (RU и EN); press 'c' с MaskControl+Shift → тоже нет
  - TestBuffer_ResetByFocusOut: HandleLifecycle(FocusOut) → буфер пуст (уже с 02-01 — пин остаётся в корпусе)
  - Живой кейс reset-escape (CORR-09): chromium: injectText("ghbdtn") → pressKey("Escape") → двойной тап → readback == "ghbdtn" (не изменено) и в логе correction skipped / empty-buffer — сброс до жеста
  </behavior>
  <action>
  RED: кейсы behavior против текущего актора — keyval-сбросы ещё не кормлены (Enter/Tab/Escape сейчас просто транзит, буфер жив) → падения на утверждениях; KP_Enter-константы нет — для RED на утверждениях добавить заглушку константы KeyKPEnter = 0xff8b в engine/keys.go (значение из RESEARCH), а сброс по ней не реализовывать. Прогон, red-evidence. Commit: test(02-03).
  GREEN: internal/session/actor.go HandleKey: press && ev.Keyval ∈ {engine.KeyReturn, engine.KeyKPEnter, engine.KeyTab, engine.KeyEscape} → buf.HardReset() (до FSM-фида не вмешиваться: intervening-key-отмена FSM уже работает этими же событиями) и return false (транзит — сброс это состояние движка, не потребление); press KeyBackSpace → buf.Backspace() (помимо FSM-фиды), return false. Ctrl-изоляция: печатные с ev.Mods&^engine.MaskShift != 0 не кормят буфер (если ещё не сделано в 02-02 — пин есть, реализация могла уже быть). case_ladder.go: runResetEscape (behavior выше; Escape в chromium поле не закрывает окно — Pitfall 6, поэтому поверхность chromium, не zenity). Реестр "reset-escape"; mise.toml [tasks.e2e-reset-escape]. Commit: feat(02-03).
  </action>
  <verify>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise exec -- go test ./internal/session/ ./internal/correct/ ./engine/ -race -count=1</automated>
    <fails_when>строка FAIL — корпус сбросов не зелёный</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise run e2e-reset-escape</automated>
    <fails_when>non-zero exit или readback ≠ "ghbdtn" — после Escape коррекция сработала (сброс не произошёл, CORR-09 нарушен)</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise run ci</automated>
    <fails_when>non-zero exit — тройной гейт не зелёный (D-08)</fails_when>
  </verify>
  <done>Все триггеры сброса пинены корпусом и живьём; Backspace-pop и Ctrl-изоляция работают; CORR-09 закрыт</done>
  <acceptance_criteria>
  - engine/keys.go содержит KeyKPEnter = 0xff8b
  - actor_test.go пинит сброс по каждому из четырёх keyvals и по FocusOut/Reset; Backspace-pop; Ctrl-комбо не кормят буфер
  - mise run e2e-reset-escape — PASS: поле не изменено, в логе причина отказа
  - mise run ci зелёный
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| движок → клиент (ForwardKeyEvent×N) | разрушительная последовательность: удаляет текст пользователя в поле |
| caps-карта клиента → выбор уровня | клиент сообщает свои возможности; бит может не соответствовать поведению (Chromium ibus#2354) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03-01 | Tampering | бёрст Backspace×N стирает чужой текст (desync) | high | mitigate | счёт строго по рунам из буфера; сбросы CORR-09; verify-after счётчиком; живые кейсы reset-*; остаточный риск задокументирован ADR-004 |
| T-02-03-02 | Tampering | caps-бит «врёт» → уровень 1 на клиенте, не применяющем удаление | high | mitigate | кейс ladder-chromium фиксирует фактический уровень; verify-after ловит дубль; матрица 02-04 делает регрессионным |
| T-02-03-03 | DoS | бёрст форвардов перегружает клиента | low | accept | бёрст ≤ длине слова+хвоста (порядка десятков); пейсинг — запасной ход A4 |
| T-02-03-04 | Elevation | стенд открывает chrome в профиле владельца | medium | mitigate | --user-data-dir во временный каталог; закрытие по PID своего процесса |
</threat_model>

<verification>
- mise run ci зелёный (D-08)
- mise run e2e-ladder-chromium && mise run e2e-reset-escape — оба PASS
- Регресс: e2e-word / e2e-word-space / e2e-word-ru / e2e-word-mixed остаются зелёными (смена ProcessKeyEvent-сигнатуры ничего не сломала)
</verification>

<success_criteria>
- CORR-07 закрыт полностью: оба уровня лестницы, verify-after, фактический уровень Chromium зафиксирован живым кейсом
- CORR-09 закрыт: все наблюдаемые триггеры сброса в корпусе и живьём
- Драйвер chromium готов к включению в YAML-матрицу (02-04)
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (символы плана 02-03)

- `engine.Engine.ForwardKeyEvent(keyval, keycode, state uint32)`; `engine.KeyKPEnter = 0xff8b`
- `internal/session.Actor`: исполнение уровня 2, verify-after (`verifyEpoch`, `pendingAfter`), таблица сброса keyvals, INFO-контракт `{"msg":"correction verify","outcome":"mismatch"}`
- test/e2e: `surfaceChromium`, драйвер `startChromium/waitChromiumInput/readChromiumText/closeChromium` (файл surface.go), файл fixtures/input.html, кейсы реестра `ladder-chromium`, `reset-escape` (файл case_ladder.go), префлайт-проверка chromium-launch
- mise-задачи `e2e-ladder-chromium`, `e2e-reset-escape`
</artifacts_produced>

<output>
Create `.planning/phases/02-korrektsiya-slova-en-ru/02-03-SUMMARY.md` when done
</output>
