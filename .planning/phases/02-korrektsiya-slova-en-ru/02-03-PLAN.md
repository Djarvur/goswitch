---
phase: 02-korrektsiya-slova-en-ru
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - engine/engine.go
  - engine/engine_test.go
  - engine/factory.go
  - internal/session/actor.go
  - internal/session/actor_test.go
  - test/e2e/case_word.go
  - test/e2e/main.go
  - mise.toml
autonomous: true
requirements: [CORR-01, CORR-07]

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Двойной тап Right Shift исправляет последнее слово EN→RU сквозным конвейером: живой e2e-кейс word-en-ru (zenity, инжекция ydotool, чтение результата) даёт ghbdtn→привет (CORR-01)"
    - "Замена уровня 1 (клиент с битом CapSurroundingText) удаляет токен+хвост и коммитит исправленный_токен+хвост одной транзакцией — ровно по диапазону неверного текста, без визуального прыжка (CORR-07)"
    - "Перед любой заменой выполняется сверка ADR-004: RequireSurroundingText → ожидание свежего SetSurroundingText ≤ 100 мс (таймер актёра, ответ ProcessKeyEvent всегда мгновенный) → суффиксное сравнение; несовпадение или таймаут → тихий отказ, НИ ОДИН символ не удаляется (edge-проба no-silent-drop equality; D-20)"
    - "Слово, уже отделённое пробелом, исправляется живьём: кейс word-after-space даёт `ghbdtn ` → `привет ` (D-13)"
    - "Отказ коррекции логируется на INFO причиной без содержимого слова (reason: empty-buffer | no-letters | mixed-script | verify-mismatch | verify-timeout | no-surrounding); детали удачной коррекции (исход→результат, уровень лестницы, latency) — только в -debug; на INFO — только счётчики (D-20/D-21)"
    - "Конвейер использует internal/correct из 02-01 как есть: Token/Tail → Detect → Convert → BuildPlan — формулы не дублируются в актёре"
  artifacts:
    - path: engine/engine.go
      provides: "эмиттеры DeleteSurroundingText(offset int32, nchars uint32) и RequireSurroundingText() симметрично CommitText; EventHandler расширен HandleSurroundingText/HandleCapabilities/AttachEngine"
    - path: engine/factory.go
      provides: "CreateEngine после привязки conn/path вызывает handler.AttachEngine(eng) — sink для эмиттеров актёра"
    - path: internal/session/actor.go
      provides: "двухфазная коррекция в актёре: Double→токен→направление→Require→(таймер 100 мс)→сверка→удаление+коммит; кормление буфера печатными клавишами; INFO/D-21-логи"
    - path: test/e2e/case_word.go
      provides: "живые кейсы word-en-ru и word-after-space (zenity, оракул stdout)"
  key_links:
    - from: internal/session/actor.go
      to: internal/correct/buffer.go
      via: "актёр кормит Buffer печатными клавишами и берёт Token()/Tail() при решении Double"
      pattern: "\\.Token\\(\\)"
    - from: internal/session/actor.go
      to: engine/engine.go
      via: "актёра sink = attached engine: RequireSurroundingText → SetSurroundingText (handler) → DeleteSurroundingText + CommitText"
      pattern: "AttachEngine"
    - from: internal/correct/plan.go
      to: engine/keys.go
      via: "BuildPlan выбирает уровень по биту CapSurroundingText (1 << 5)"
      pattern: "CapSurroundingText"
  prohibitions:
    - statement: "Коррекция НЕ ДОЛЖНА срабатывать без явного жеста — никакой автокоррекции при наборе: владелец осознанно отверг автокоррекцию (Out of Scope в REQUIREMENTS.md); конвейер запускается только решением FSM Double"
      status: resolved
      verification: judgment
    - statement: "Журнал НЕ ДОЛЖЕН содержать содержимое набранных/исправленных слов на уровне INFO — только причины отказов без слов и счётчики; исход→результат и трассировка — исключительно -debug (D-20/D-21)"
      status: resolved
      verification: judgment
  assumptions:
    - "FLAGGED (research A1): zenity 4.0.1 (GTK4) применяет DeleteSurroundingText — если нет, кейс word-en-ru падает живым прогоном и лестница вырождается в уровень 2 (самодостаточен по ADR-003); матрица 02-06 закрепит фактический уровень"
---

<objective>
Трассер ядра ценности: двойной тап Right Shift исправляет последнее слово EN→RU (`ghbdtn`→`привет`) в живом поле ввода zenity — сквозной конвейер буфер→направление→сверка→лестница (уровень 1) через эмиттеры engine/, с логами D-20/D-21. Вторая задача расширяет срез живьём: слово после пробела (D-13) — главный геометрический тест фазы (баг хвоста прототипа, Pitfall 1).

**As a** пользователь, набирающий в неправильной раскладке, **I want** двойным нажатием Right Shift исправить последнее слово с сохранением регистра, **so that** не перепечатывать его вручную — и замена идёт ровно по диапазону неверного текста, без прыжка курсора.

Purpose: CORR-01 — ядро продукта; каждый последующий план фазы расширяет этот доказанный срез (направления 02-04, уровень 2 02-05, матрица 02-06).
Output: два эмиттера engine/, двухфазная коррекция в актёре, живые кейсы word-en-ru / word-after-space, mise-задачи e2e-word / e2e-word-space.
</objective>

<execution_context>
@/home/nil/DiskD/W/Djarvur/goswitch/.zcode/gsd-core/workflows/execute-plan.md
@/home/nil/DiskD/W/Djarvur/goswitch/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-korrektsiya-slova-en-ru/02-CONTEXT.md (D-13, D-20, D-21 — заперты)
@.planning/phases/02-korrektsiya-slova-en-ru/02-PATTERNS.md (§ actor, § engine — образцы с номерами строк)
@docs/adr/ADR-003-replacement-capability-ladder.md
@docs/adr/ADR-004-buffer-reset-triggers.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Трассер — двойной Right Shift исправляет ghbdtn→привет в zenity: буфер→направление→сверка→DeleteSurroundingText+CommitText (CORR-01/CORR-07, D-07)</name>
  <reversibility rating="costly">Сечение EventHandler (HandleSurroundingText/HandleCapabilities/AttachEngine) — кросс-модульный контракт engine↔session; смена позже требует согласованной правки в обоих пакетах и стенде</reversibility>
  <precondition>План 02-01 завершён: internal/correct зелёный headless (mise exec -- go test ./internal/correct/ -race -count=1 PASS). Живая GNOME Wayland-сессия на этой машине доступна: непусто $WAYLAND_DISPLAY, ibus-daemon отвечает (ibus address), префлайт стенда `go run ./test/e2e -case m1-gate` зелёный на текущем HEAD</precondition>
  <files>engine/engine.go, engine/engine_test.go, engine/factory.go, internal/session/actor.go, internal/session/actor_test.go, test/e2e/case_word.go, test/e2e/main.go, mise.toml</files>
  <read_first>
  - .planning/phases/02-korrektsiya-slova-en-ru/02-RESEARCH.md (§ Pattern 1 — арифметика лестницы, § Pattern 4 — двухфазная сверка, § Common Pitfalls 1/2/4, § Code Examples — эмиттеры)
  - internal/correct/buffer.go, internal/correct/direction.go, internal/correct/convert.go, internal/correct/verify.go, internal/correct/plan.go (пакет 02-01 — используется как есть)
  - .planning/phases/02-korrektsiya-slova-en-ru/02-PATTERNS.md (§ actor — timer re-entry actor.go:84-108; § engine — образец эмиттера engine.go:310-317)
  - engine/engine.go (ProcessKeyEvent:128-142, SetSurroundingText:152-160, SetCapabilities:162-170, CommitText:307-317, EventHandler:66-71)
  - engine/types.go (IBusText, NewIBusText)
  - engine/factory.go (CreateEngine binds conn/path — точка AttachEngine)
  - internal/session/actor.go (весь; мьютекс-сериализация, ExpiryAt — место решения)
  - internal/session/actor_test.go (captureLogs/syncBuffer/tapShift — расширяемый корпус)
  - test/e2e/case_m1.go (startZenity/waitZenityEntry/closeZenity/focusWitness — драйвер кейса)
  - test/e2e/main.go (pickCase-реестр:165-179, waitForLog:486, injectText/injectKeys/pressKey:559-594, runCaseWatchdog:141)
  - docs/adr/ADR-003-replacement-capability-ladder.md, docs/adr/ADR-004-buffer-reset-triggers.md
  </read_first>
  <behavior>
  - TestActor_DoubleTapCorrects (session, fake-sink): кормление печатных нажатий g..n → двойной тап → expiry → на sink наблюдается RequireSurroundingText; затем HandleSurroundingText("abc ghbdtn", 9) → на sink ровно DeleteSurroundingText(-6,6) (токен без хвоста) и CommitText("привет"); в логе INFO {"msg":"correction","outcome":"done"}; в -debug запись с "level":1 первой после msg
  - TestActor_VerifyPaths (D-20): сверка не сошлась → INFO {"msg":"correction skipped","reason":"verify-mismatch"}, НИ ОДНОГО вызова DeleteSurroundingText/CommitText на sink; таймаут 100 мс (инъекция времени таймера) → reason:"verify-timeout"; пустой буфер → reason:"empty-buffer"; ни одна INFO-запись не содержит содержимого слова
  - TestActor_EmptyBufferNoDestructive: буфер без токена + двойной тап → skip empty-buffer, ноль разрушительных вызовов на sink (edge-проба CORR-07: пустой ввод — замены нет)
  - TestEmitters_DeleteAndRequire (engine): сигналы ifaceEngine+".DeleteSurroundingText" с аргументами (offset, nchars) и ifaceEngine+".RequireSurroundingText" — по образцу пина CommitText; conn==nil → тихий возврат
  </behavior>
  <action>
  RED (D-07): internal/session/actor_test.go: расширить фейковым sink-интерфейсом (двойник эмиттеров engine: RequireSurroundingText/DeleteSurroundingText/CommitText — вызовы записываются слайсом под мьютексом, стиль syncBuffer actor_test.go:27) и тестами behavior против текущего актёра (методы-заглушки: HandleSurroundingText/HandleCapabilities/AttachEngine добавляются как no-op — RED падает на утверждениях: коррекции нет, вызовов sink нет). engine_test.go: пины двух эмиттеров (для подлинного RED на утверждениях добавить в Engine stub-методы DeleteSurroundingText/RequireSurroundingText с пустым телом — вызов не эмитится). Прогнать mise exec -- go test ./internal/session/ ./engine/ -race -count=1 — подлинный RED на утверждениях; записать red-evidence (gsd check tdd-red-evidence). Commit: test(02-03).
  GREEN — engine/: эмиттеры DeleteSurroundingText(offset int32, nchars uint32) и RequireSurroundingText() — тело копирует CommitText engine.go:310-317 (conn==nil → return; e.conn.Emit(e.path, ifaceEngine+".…", args…); ошибка → slog.Error). EventHandler (engine.go:66-71) расширяется тремя методами: HandleSurroundingText(text string, cursorPos uint32), HandleCapabilities(caps uint32), AttachEngine(*Engine) — интерфейс внутрирепозиторный, единственный имплементор session.Actor. SetSurroundingText (engine.go:155-160): разварьировать входную dbus.Variant в IBusText (тип уже в types.go), передать handler.HandleSurroundingText(t.Text, cursorPos) при handler != nil (DEBUG-трасса позиций сохраняется). SetCapabilities: дополнительно handler.HandleCapabilities(caps). factory.go CreateEngine: после привязки conn/path — handler.AttachEngine(eng) при handler != nil (sink для эмиттеров актёра). recover-шим на новых путях не нужен (эмиттеры — исходящие сигналы, не экспортированные методы), но НИ ОДИН экспортированный D-Bus-метод не остаётся без существующего шима.
  GREEN — internal/session/actor.go: поля: buf *correct.Buffer, caps uint32, eng (sink interface{ RequireSurroundingText(); DeleteSurroundingText(int32, uint32); CommitText(engine.IBusText) } — локальный интерфейс в точке использования, go-ultimate), pending *pendingFix{token, tail, converted []rune; deadline *time.Timer}. HandleKey: существующая FSM-логика не меняется + кормление буфера: press && печатный keyval (0x20 ≤ keyval < 0xFE00) && ev.Mods&^engine.MaskShift == 0 → buf.Push(rune(ev.Keyval)); press KeyBackSpace → buf.Backspace(); Shift-клавиши и release буфер не кормят. HandleLifecycle: к FocusOut/Reset (actor.go:71-76) добавляется buf.HardReset() (CORR-09 стартовая часть). HandleCapabilities: сохранить caps. AttachEngine: сохранить sink. HandleSurroundingText: под мьютексом — сравнение при активном pending: MatchesSuffix([]rune(text)[:min(cursorPos, len)], pending.token) → да: исполнить BuildPlan уровень 1: sink.DeleteSurroundingText(plan.Offset, plan.NChars); sink.CommitText(engine.NewIBusText(string(plan.Commit))); buf.ReplaceToken(converted); лог INFO {"msg":"correction","outcome":"done"} и Debug {"msg":"correction","level":1,"runes":N,"source":…,"result":…,"latency_ms":N} (первый атрибут после msg — level: матрица 02-06 будет грепать точную форму); нет → INFO {"msg":"correction skipped","reason":"verify-mismatch"}, pending=nil. ExpiryAt (actor.go:92-100): в цикле решений — action Double → startCorrection() под мьютексом: token=buf.Token(); пусто → skip empty-buffer; dir,ok=correct.Detect → !ok → skip no-letters|mixed-script; converted=Convert; caps&CapSurroundingText==0 → skip no-surrounding (до 02-05); sink.RequireSurroundingText(); arm 100 мс verifyTimer (const verifyWait = 100 * time.Millisecond; таймер, НЕ блокировка — Pitfall 4); action Single/Triple — пока только slog.Debug (флип приходит планом 02-04). Ответ ProcessKeyEvent остаётся мгновенным: все ожидания — в таймерах актёра (паттерн timer re-entry actor.go:84-108).
  Затем test/e2e/case_word.go: runWordENRU по скелету runM1Gate (case_m1.go:49-74): activateGoswitch → openEntrySurface → focusWitness → injectText("ghbdtn") → injectKeys("shift-r","shift-r") (пейсинг injectKeys уже укладывает тапы в окно 300 мс) → waitForLog `"msg":"correction","outcome":"done"` → closeZenity → stdout-оракул == "привет"; register "word-en-ru" в pickCase (main.go:165-179). mise.toml: [tasks.e2e-word] по конвенции e2e-m1 (описание с "(live GNOME session; NOT in ci)"). mise run ci зелёный. Commit: feat(02-03). REFACTOR при очевидных улучшениях без смены поведения.
  </action>
  <verify>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise exec -- go test ./internal/session/ ./engine/ ./internal/correct/ -race -count=1</automated>
    <fails_when>строка FAIL в выводе или non-zero exit — корпус актёра/адаптера не зелёный под -race</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise run ci</automated>
    <fails_when>non-zero exit — build/vet/golangci-lint/test -race не зелёные (D-08)</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise run e2e-word</automated>
    <fails_when>non-zero exit или строка FAIL в выводе стенда; либо в выводе отсутствует PASS word-en-ru — сквозной путь (инжекция → FSM → сверка → DeleteSurroundingText+CommitText → оракул stdout "привет") не работает на живом столе</fails_when>
  </verify>
  <done>Сквозной конвейер работает: живой кейс word-en-ru зелёный (ghbdtn→привет в zenity через AT-SPI/stdout-оракул), корпус актёра/эмиттеров зелёный под -race, INFO-логи не содержат слов, mise run ci зелёный; история test(02-03) до feat(02-03)</done>
  <acceptance_criteria>
  - engine.Engine имеет методы DeleteSurroundingText(int32, uint32) и RequireSurroundingText() с Emit по ifaceEngine+".…" (симметрично CommitText)
  - session.Actor реализует HandleSurroundingText/HandleCapabilities/AttachEngine; factory.CreateEngine вызывает AttachEngine
  - actor_test.go фиксирует: сверка не сошлась → ноль вызовов DeleteSurroundingText/CommitText на sink (abort, не мусорить); пустой буфер → ноль разрушительных вызовов
  - в логах INFO из актёра нет содержимого слов: только reason/outcome-записи (пинируется тестом лога)
  - актёр вызывает Token/Detect/Convert/BuildPlan из internal/correct (формулы 02-01), не дублирует их
  - живой прогон mise run e2e-word завершается PASS, desktop state восстановлен (стенд сам проверяет verifyRestored)
  </acceptance_criteria>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Слово после пробела живьём (D-13) — кейс word-after-space: хвостовая геометрия на настоящем столе (Pitfall 1)</name>
  <reversibility rating="costly">Правило «слово живёт после пробела» — пользовательский контракт жеста (reversibility D-13 из CONTEXT: costly); e2e-кейсы «после пробела» завязаны на него</reversibility>
  <files>internal/session/actor_test.go, test/e2e/case_word.go, test/e2e/main.go, mise.toml</files>
  <read_first>
  - internal/session/actor.go (конвейер Task 1 — проверка хвостового пути)
  - .planning/phases/02-korrektsiya-slova-en-ru/02-RESEARCH.md (§ Ключевая находка №2 — хвост после слова: реальный баг прототипа punto_engine.py:186-199; § Common Pitfalls 1)
  - test/e2e/case_word.go (кейс Task 1 — расширяется)
  </read_first>
  <behavior>
  - TestActor_AfterSpaceCorrects (session, fake-sink): g..n + ' ' + двойной тап + HandleSurroundingText("abc ghbdtn ", 11) → DeleteSurroundingText(-7,7) и CommitText("привет ") ровно по одному разу
  - TestActor_ToggleRepeat: после успешной коррекции (буфер стал "привет") повторный двойной тап + сверка → CommitText("ghbdtn") — toggle на актёре (инвариант ReplaceToken из 02-01 в конвейере)
  - Живой кейс word-after-space (D-13): zenity: injectText("ghbdtn") → pressKey("space") → двойной тап → waitForLog correction outcome:done → stdout-оракул == "привет " (с завершающим пробелом)
  </behavior>
  <action>
  RED (D-07): расширить actor_test.go кейсами behavior против текущего актора — при корректном Task 1 часть кейсов уже зелёная (это НЕ INVALID_RED: новые падающие должны быть; если всё зелёное — Task 1 уже закрыл хвостовой путь, зафиксировать это в SUMMARY, RED-коммит не нужен). Commit при падениях: test(02-03).
  GREEN: доработать актёра ровно до корпуса (хвостовой путь startCorrection должен доводить tail до BuildPlan и ReplaceToken). Живой кейс: runWordAfterSpace в case_word.go (скелет runWordENRU) — шаги в behavior; register "word-after-space" в pickCase (main.go). mise.toml [tasks.e2e-word-space]. Прогнать живьём: это главный геометрический тест фазы — баг хвоста прототипа (Pitfall 1) ловится именно здесь. Commit: feat(02-03).
  </action>
  <verify>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise exec -- go test ./internal/session/ -race -count=1</automated>
    <fails_when>строка FAIL в выводе — корпус хвостового пути/toggle не зелёный</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise run e2e-word-space</automated>
    <fails_when>non-zero exit или отсутствие PASS word-after-space в выводе; либо stdout-оракул дал "gпривет"/" привет" — хвостовая арифметика неверна (Pitfall 1 живьём)</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise run ci</automated>
    <fails_when>non-zero exit — тройной гейт не зелёный (D-08)</fails_when>
  </verify>
  <done>Слово после пробела исправляется живьём (`ghbdtn ` → `привет `), toggle после коррекции работает на актёре; история test до feat</done>
  <acceptance_criteria>
  - actor_test.go: кейс после пробела даёт ровно один DeleteSurroundingText(-7,7) + один CommitText("привет "); toggle-повтор конвертирует обратно
  - mise run e2e-word-space — PASS, в stdout-оракуле "привет " с сохранённым пробелом
  - mise run e2e-word остаётся PASS (регресс)
  - mise run ci зелёный
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ibus-daemon → engine (D-Bus) | входящие вызовы ProcessKeyEvent/SetSurroundingText/SetCapabilities — данные клиентского происхождения |
| engine → клиент (сигналы) | исходящие DeleteSurroundingText/CommitText меняют текст в поле пользователя |
| буфер демона | хранит всё, что набрал пользователь (keystroke-данные) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03-01 | Tampering | двухфазная коррекция actor.go | high | mitigate | сверка ADR-004 до удаления: несовпадение/таймаут → тихий отказ, ноль эмиттов удаления (пин TestActor_VerifyPaths) |
| T-02-03-02 | DoS | новые пути обработчиков | medium | mitigate | никаких ожиданий в ProcessKeyEvent (ответ мгновенный, Pitfall 4); recover-шимы на экспортированных методах сохранены |
| T-02-03-03 | Information Disclosure | логи коррекции | high | mitigate | D-20/D-21: INFO — только reason/outcome без слов; исход→результат — только -debug; пинится тестом лога (actor_test.go) |
| T-02-03-04 | Tampering | арифметика диапазона в проводке | high | mitigate | формулы поставлены корпусом 02-01 (руны, хвост); актёр только вызывает BuildPlan — дублей арифметики нет |
</threat_model>

<verification>
- mise run ci зелёный (build + vet + golangci-lint + test -race, D-08)
- mise run e2e-word && mise run e2e-word-space — оба PASS на живой сессии, desktop state восстановлен
- Полнота среза: непустой buf → Detect → Convert → BuildPlan → RequireSurroundingText → сверка → DeleteSurroundingText+CommitText — каждый шаг пинится корпусом или живым кейсом
</verification>

<success_criteria>
- CORR-01 (happy path EN→RU), CORR-07 (уровень 1, ровно по диапазону) — доказаны корпусом + живыми кейсами
- D-13/D-20/D-21 реализованы дословно в проводке
- Никаких отложенных заглушек в архитектуре: BuildPlan уже вычисляет оба уровня (уровень 2 подключается планом 02-05 без переделки формул)
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (символы плана 02-03)

- `engine.EventHandler` расширен: `HandleSurroundingText(text string, cursorPos uint32)`, `HandleCapabilities(caps uint32)`, `AttachEngine(*Engine)`
- `engine.Engine`: новые методы `DeleteSurroundingText(offset int32, nchars uint32)`, `RequireSurroundingText()`
- `internal/session.Actor`: новые методы `HandleSurroundingText`, `HandleCapabilities`, `AttachEngine`; конвейер `startCorrection`; константа `verifyWait = 100ms`; тип `pendingFix`
- test/e2e: кейсы реестра `word-en-ru`, `word-after-space` (файл case_word.go)
- mise-задачи `e2e-word`, `e2e-word-space`
- Лог-контракты: INFO `{"msg":"correction","outcome":"done"}` / `{"msg":"correction skipped","reason":…}`; DEBUG `{"msg":"correction","level":N,…}` (level — первый атрибут после msg)
</artifacts_produced>

<output>
Create `.planning/phases/02-korrektsiya-slova-en-ru/02-03-SUMMARY.md` when done
</output>
