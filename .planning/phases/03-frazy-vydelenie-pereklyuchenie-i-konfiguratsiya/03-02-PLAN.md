---
phase: 03-frazy-vydelenie-pereklyuchenie-i-konfiguratsiya
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/config/config.go
  - internal/config/config_test.go
  - internal/config/load.go
  - internal/config/load_test.go
  - internal/config/watch.go
  - internal/config/watch_test.go
  - internal/hotkey/names.go
  - internal/hotkey/names_test.go
  - cmd/goswitchd/main.go
  - go.mod
  - go.sum
  - docs/CONFIG.md
# scope_note: 12 files accepted (checker scope_sanity) — 4 *_test.go pairs mandated by strict TDD (D-07);
# go.mod/go.sum are the single approved dependency add (fsnotify v1.10.1); estimate 26k = 26% of 100k.
autonomous: true
requirements: [CONF-01, CONF-02, CONF-03, SWCH-04]

estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Все хоткеи, таймауты и параметры демона задаются YAML-конфигом: секции ровно hotkeys / timeouts / correction / macr (D-31); окно тапов, комбо-биндинг, кап Backspace (D-27), вкл/выкл clipboard-ступени (D-28), MACR-флаги — всё в схеме (CONF-01)"
    - "Опечатка в ключе YAML = невалидный конфиг целиком: strict-декодер KnownFields отклоняет, ошибка называет поле (D-33); валидация диапазонов: tap_window_ms ∈ (0,2000], verify_wait_ms ∈ (0,2000], backspace_cap ∈ [1,500] — гигантские значения отклонены (DoS-потолок ASVS V5)"
    - "Hot reload: правка конфига применяется без перезапуска — watcher (fsnotify directory-watch + debounce) публикует иммутабельный снапшот через atomic.Pointer; невалидная правка → WARN в лог + last-good остаётся действовать (D-32); ошибка видна позже в goswitchctl status (план 03-06)"
    - "Демон принимает флаг -config: путь к YAML; окно тапов для FSM берётся из конфига при старте (tap_window_ms, дефолт 300 = DefaultWindow ADR-002/D-35); без флага — встроенные дефолты, поведение Фазы 2 не меняется (SWCH-04)"
    - "Биндинги — имена-строки из закрытой таблицы (shift_r, ctrl_r, …; комбо через +), неизвестное имя = невалидный конфиг (D-33); разбор живёт в чистом internal/hotkey, значения keyval из ibuskeysyms.h с комментарием-провенансом (ловушка 02-05: имя ≠ клавиша)"
    - "Совместимость с Caramba (CONF-03, wish) — таблицей соответствия в docs/CONFIG.md, БЕЗ клонирования Caramba-имён: их схема привязана к модели «свитч на первый тап», наша — к ADR-002 (D-31)"
    - "Корпус FSM (fsm_test.go, 11 тестов) остаётся зелёным без правок семантики — конфигурируемость окна не ломает исполненный ADR-002 (D-35)"
  artifacts:
    - path: internal/config/config.go
      provides: "тип Config (hotkeys/timeouts/correction/macr, yaml-теги snake_case), Defaults(), Validate() с названными полями в ошибках"
    - path: internal/config/load.go
      provides: "Load(path) (*Config, error) — yaml.NewDecoder + KnownFields(true) по паттерну matrix.go:113-116"
    - path: internal/config/watch.go
      provides: "Watcher: fsnotify на КАТАЛОГ + фильтр по имени файла + debounce 200 мс + atomic.Pointer[Config] (внутреннее хранилище) + WARN при отклонении; контракт закреплён производителем: Snapshot() config.Config (значение-копия — потребитель 03-04 ждёт interface{ Snapshot() config.Config }); LastError() для статуса"
    - path: internal/hotkey/names.go
      provides: "закрытая таблица имён → keyval/mask + ParseBinding(string) (Binding, error); маски и keyval с провенансом из ibuskeysyms.h (Control_R = 0xffe4)"
    - path: docs/CONFIG.md
      provides: "референс схемы + таблица соответствия Caramba (CONF-03)"
    - path: cmd/goswitchd/main.go
      provides: "флаг -config; стартовая проводка: Load → NewActor(window из конфига) → watcher на signal-ctx"
  key_links:
    - from: internal/config/load.go
      to: internal/hotkey/names.go
      via: "Validate() резолвит имена биндингов через hotkey.ParseBinding — неизвестное имя отклоняет конфиг целиком"
      pattern: "ParseBinding"
    - from: cmd/goswitchd/main.go
      to: internal/config
      via: "-config → config.Load → window → NewActor; watcher публикует снапшоты (потребление актором — план 03-04)"
      pattern: "config\\.Load"
    - from: internal/config/watch.go
      to: gopkg.in/yaml.v3
      via: "каждое событие после debounce → полный ре-парс strict-декодером; невалид = WARN + last-good (D-32)"
      pattern: "config reload rejected"
  prohibitions:
    - statement: "Опечатка в ключе конфига НЕ ДОЛЖНА молча применяться — пользователь, поправивший таймаут с опечаткой, обязан получить ошибку, а не иллюзию применённой настройки (D-33; transparent-failure запрет)"
      status: resolved
      verification: judgment
    - statement: "Невалидная правка НЕ ДОЛЖНА оставлять демона без рабочей конфигурации — last-good продолжает действовать, WARN пишется всегда (D-32); пустой/отсутствующий конфиг при явном -config — ошибка старта, не тихие дефолты"
      status: resolved
      verification: judgment
  assumptions:
    - "FLAGGED (probe CONF-01 / D-31 costly): схема ключей = публичный контракт пользователей; смена после релиза = миграция. Схема ниже зафиксирована планом; владелец подтверждает её на verify-гейте по docs/CONFIG.md"
    - "FLAGGED (probe CONF-02 / research Pitfall 8): reload окна действует только на серии, начатые после reload; взведённый таймер доживает по старому окну (stale-timer no-op инвариант FSM). Юнит-пин — в плане 03-04; здесь пинится уровень watcher'а"
    - "FLAGGED (probe CONF-03): таблица соответствия в документации признаётся достаточной реализацией wish-уровня CONF-03 (D-31 запрещает клонирование имён); владелец подтверждает на verify"
    - "FLAGGED (probe SWCH-04 / D-35): корпус fsm_test.go (11 тестов) — исполненный ADR-002; конфигурируемость окна не должна требовать семантических правок корпуса (разрешены только механические правки конструктора в 03-04)"
---

<objective>
Новый чистый пакет internal/config: YAML-схема всех параметров демона (D-31), strict-декод с отклонением целиком (D-33), hot reload c directory-watch + debounce + last-good (D-32), разбор биндингов по закрытой таблице имён; флаг -config у демона со стартовой проводкой окна тапов (SWCH-04/D-35); документация схемы с таблицей соответствия Caramba (CONF-03). Единственная новая зависимость фазы — fsnotify v1.10.1 (легитимность: proxy.golang.org + STACK, Approved в аудите RESEARCH).

**As a** пользователь, **I want** настроить все горячие клавиши и таймаусы в одном YAML-файле и править их на лету, **so that** демон не нужно перезапускать, а опечатка не ломает рабочую конфигурацию молча.

Purpose: CONF-01/CONF-02/CONF-03 — фундамент конфигурируемости; планы 03-03 (clipboard-флаг) и 03-04 (потребление снапшотов актором, комбо-биндинг) читают этот пакет.
Output: internal/config (config/load/watch + корпус), internal/hotkey/names.go, -config у демона, docs/CONFIG.md, fsnotify в go.mod.
</objective>

<execution_context>
@/home/nil/DiskD/W/Djarvur/goswitch/.zcode/gsd-core/workflows/execute-plan.md
@/home/nil/DiskD/W/Djarvur/goswitch/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-frazy-vydelenie-pereklyuchenie-i-konfiguratsiya/03-CONTEXT.md (D-31, D-32, D-33 — заперты; дискреция схемы — у планировщика, зафиксирована ниже)
@.planning/phases/03-frazy-vydelenie-pereklyuchenie-i-konfiguratsiya/03-PATTERNS.md (§ internal/config — строгий декод matrix.go:113-136, цикл conn.go:132-145, дисциплина armTimer actor.go:529-535)
@.planning/research/STACK.md (паттерн directory-watch + debounce)
@docs/adr/ADR-002-tap-semantics.md (окно 300 мс — DefaultWindow остаётся документированным дефолтом)
</context>

<config_schema>
Схема фиксируется дословно (дискреция планировщика по D-31 исполнена; yaml-теги snake_case — tagliatelle):

- hotkeys.tap_key: строка-имя ключевой клавиши серии тапов (single/double/triple), дефолт shift_r
- hotkeys.word_layout_combo: строка-биндинг комбо «слово+раскладка» (SWCH-02/D-36), дефолт shift+ctrl_r
- timeouts.tap_window_ms: окно дискриминации тапов, дефолт 300 (ADR-002)
- timeouts.verify_wait_ms: ожидание свежего SetSurroundingText при сверке, дефолт 100 (фаза 2)
- correction.backspace_cap: кап серии Backspace (D-27), дефолт 50
- correction.clipboard_rung: вкл/выкл clipboard-ступени (D-28), дефолт false
- macr.enabled: дефолт false; macr.letters: строка букв через запятую (подмножество a–z), дефолт пусто; macr.apps: список имён приложений (ADR-005 per-app), дефолт []; macr.alt_modifier: имя модификатора или пусто — пусто = НЕ вводить альтернативный модификатор (ADR-005 b.3: дефолт не вводится до живой проверки)

Validate(): диапазоны tap_window_ms ∈ (0,2000], verify_wait_ms ∈ (0,2000], backspace_cap ∈ [1,500]; tap_key и word_layout_combo резолвятся hotkey.ParseBinding; macr.letters ⊆ a–z (пусто допустимо только при enabled=false); macr.apps ≤ 64 записей (DoS-потолок); alt_modifier ∈ {пусто, ctrl_l, ctrl_r} (дефолт пусто). Каждая ошибка называет поле (стиль matrix.go:139-167).
</config_schema>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Схема Config + strict-декод + таблица имён биндингов в hotkey (CONF-01/D-31/D-33, D-07)</name>
  <reversibility rating="costly">Схема = публичный контракт конфига пользователей (D-31 costly); смена после релиза = миграция конфигов</reversibility>
  <files>internal/config/config.go, internal/config/config_test.go, internal/config/load.go, internal/config/load_test.go, internal/hotkey/names.go, internal/hotkey/names_test.go</files>
  <read_first>
  - .planning/phases/03-frazy-vydelenie-pereklyuchenie-i-konfiguratsiya/03-RESEARCH.md (§ Code Examples §3 — строгий декод; § Open Questions Q4 — формат биндингов принят: имена-строки)
  - .planning/phases/03-frazy-vydelenie-pereklyuchenie-i-konfiguratsiya/03-PATTERNS.md (§ internal/config — Analog A matrix.go:113-136 дословно; словари как ФУНКЦИИ не vars matrix.go:37-39)
  - test/e2e/matrix.go (строгий декод :113-136; validate :139-167; закрытые словари :39-51)
  - test/e2e/matrix_test.go (таблицы отвержения :108-165 — образец корпусов strict-тестов)
  - internal/hotkey/fsm.go (KeyvalShiftR = 0xffe2 — дублирование константы в чистом пакете уже прецедент; MaxTaps, DefaultWindow)
  - engine/keys.go (блок масок :5-12 и keyval :24-37 — значения и стиль провенанс-комментариев)
  - /usr/include/ibus-1.0/ibuskeysyms.h (строка 191: Control_R = 0xffe4 — брать значения ТОЛЬКО отсюда, не из памяти)
  </read_first>
  <behavior>
  - TestLoad_ValidDoc: YAML из config_schema выше декодируется в Config со всеми полями; Defaults() совпадает с документированными дефолтами (tap_window_ms 300, backspace_cap 50, clipboard_rung false, macr.enabled false)
  - TestLoad_UnknownKeyRejected: любой неизвестный ключ (например timeots:) → ошибка, конфиг целиком невалиден (D-33)
  - TestValidate_Ranges: tap_window_ms 0/2001, verify_wait_ms 0, backspace_cap 0/501, macr.apps на 65 записей — каждая ошибка называет поле
  - TestValidate_Bindings: tap_key "shift_x" / word_layout_combo "shift+ctrl_q" → ошибка с именем поля; корректные shift_r и shift+ctrl_r проходят
  - TestParseBinding (hotkey): shift_r → Keyval 0xffe2, ModMask Shift; shift+ctrl_r → Keyval 0xffe4, ModMask Shift; ctrl_l/alt_l/super_l/super_r/alt_r разбираются; пустая строка и неизвестный токен → ошибка; таблица имён — функция, не var (строгий линт)
  - TestValidate_MACRFields: enabled=true при пустых letters → ошибка; letters "a,z" ок; letters "a,я" → ошибка (только латиница a–z); alt_modifier "ctrl_l" ок, "shift" (не модификатор-кандидат) → ошибка
  </behavior>
  <action>
  RED (D-07): names_test.go + load_test.go + config_test.go по behavior против пустых пакетов (заглушки-методы для подлинного RED на утверждениях). Прогон mise exec -- go test ./internal/hotkey/ ./internal/config/ -race -count=1; red-evidence в red-evidence/ фазы 03. Commit: test(03-02).
  GREEN — internal/hotkey/names.go: закрытая таблица имён → keyval (shift_l 0xffe1, shift_r 0xffe2, ctrl_l 0xffe3, ctrl_r 0xffe4, alt_l 0xffe9, alt_r 0xffea, super_l 0xffeb, super_r 0xffec — значения из ibuskeysyms.h, провенанс-комментарий у каждой) и имена → маска (shift, ctrl, alt, super — битовые значения как в engine/keys.go:5-12, задокументировано комментарием о дублировании в чистый пакет — прецедент KeyvalShiftR); ParseBinding(name string) (Binding, error): сплит по +, последний токен — ключ, остальные — модификаторы; Binding{Keyval uint32, ModMask uint32}. Пакет остаётся без часов/горутин/D-Bus (док fsm.go:48-51).
  GREEN — internal/config/config.go: типы секций + Config, yaml-теги snake_case, Defaults() по config_schema, Validate() — диапазоны и резолв биндингов через hotkey.ParseBinding, ошибки fmt.Errorf с именем поля (%w-обёртки — go-ultimate). Внутренние словари — функции.
  GREEN — internal/config/load.go: Load(path string) (*Config, error) — os.Open, yaml.NewDecoder, dec.KnownFields(true) (дословно паттерн matrix.go:113-116), Decode → Validate; ошибки "open config: %w" / "decode config: %w" / "validate config: %w" (стиль engine/conn.go:69-117). Пустой документ → ошибка validate (явный -config обязан дать рабочий конфиг).
  mise run ci зелёный. Commit: feat(03-02).
  </action>
  <verify>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise exec -- go test ./internal/hotkey/ ./internal/config/ -race -count=1</automated>
    <fails_when>строка FAIL в выводе или non-zero exit — корпус схемы/декода/биндингов не зелёный</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise exec -- go test ./internal/hotkey/ -run TestFSM -race -count=1</automated>
    <fails_when>строка FAIL — fsm-корпус ADR-002 сломан добавлением names.go (D-35: 11 тестов зелёные без правок семантики)</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise run ci</automated>
    <fails_when>non-zero exit — зелёная итерация D-08 не достигнута</fails_when>
  </verify>
  <done>Схема декодируется strict-режимом с названными полями в ошибках; биндинги резолвятся закрытой таблицей; корпус FSM не тронут; история test до feat</done>
  <acceptance_criteria>
  - internal/config/load.go содержит dec.KnownFields(true) — греп-пин (D-33)
  - TestLoad_UnknownKeyRejected и TestValidate_Ranges зелёные — опечатка/диапазоны отклоняются целиком
  - internal/hotkey/names.go: keyval-константы ссылаются на ibuskeysyms.h в комментариях; Control_R == 0xffe4
  - Defaults() пинит tap_window_ms==300 (DefaultWindow ADR-002), backspace_cap==50 (D-27), clipboard_rung==false (D-28)
  - macr.alt_modifier по умолчанию пуст — альтернативный модификатор НЕ вводится (ADR-005 b.3)
  </acceptance_criteria>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Watcher — fsnotify directory-watch + debounce + last-good + atomic.Pointer (CONF-02/D-32)</name>
  <files>internal/config/watch.go, internal/config/watch_test.go, go.mod, go.sum</files>
  <read_first>
  - .planning/phases/03-frazy-vydelenie-pereklyuchenie-i-konfiguratsiya/03-RESEARCH.md (§ Architecture Patterns — Pattern 2/§3: скелет watcher'а дословно; fsnotify API из module cache)
  - .planning/phases/03-frazy-vydelenie-pereklyuchenie-i-konfiguratsiya/03-PATTERNS.md (§ internal/config — Analog B conn.go:132-145: цикл-с-ctx; Wave 0 gap note: watcher за интерфейсом для синтетических событий)
  - engine/conn.go (waitBusLoss :132-145 — идиома цикла над каналом с ctx-отменой)
  - internal/session/actor.go (armTimer :529-535 — дисциплина взвода/остановки таймера для debounce)
  - .planning/research/STACK.md (directory-watch + debounce: vim/kwrite/gedit пишут write-temp+rename — вотчинг пути теряет inode)
  </read_first>
  <behavior>
  - TestWatch_ValidRewritePublishes: синтетическое событие Write по имени файла → после debounce watcher.Snapshot() возвращает НОВЫЙ конфиг (поле tap_window_ms изменилось)
  - TestWatch_InvalidRewriteKeepsLastGood: событие с битым содержимым (неизвестный ключ) → WARN в лог (captureLogs), Snapshot() НЕ изменился, LastError() содержит текст ошибки (D-32)
  - TestWatch_IgnoresOtherFiles: события по чужому имени в каталоге игнорируются (без ре-парса — пин: Load не вызывается, счётчик вызовов)
  - TestWatch_DebounceCoalesces: серия из 3 событий в окне debounce → один ре-парс (счётчик == 1)
  - TestWatch_RemoveAndRenameTrigger: события Remove/Rename по имени файла обрабатываются (atomic-rename редактора: сохранение = Rename в тот же каталог)
  - fsnotify за маленьким интерфейсом (событийный источник) — корпус головless, без реального inotify
  </behavior>
  <action>
  Зависимость: mise exec -- go get github.com/fsnotify/fsnotify@v1.10.1 && mise exec -- go mod tidy (единственная новая зависимость фазы; аудит легитимности — RESEARCH Package Legitimacy: Approved, proxy.golang.org сверен; golangci gomodguard_v2 disable-комментарий актуален — модуль разрешает ровно godbus+yaml+fsnotify).
  RED (D-07): watch_test.go по behavior — watcher-заглушка с пустым телом для подлинного RED; счётчик ре-парсов через инъекцию функции загрузки (loadFn field). Прогон mise exec -- go test ./internal/config/ -race -count=1; red-evidence. Commit: test(03-02).
  GREEN — internal/config/watch.go: NewWatcher(ctx context.Context, path string, opts …) (*Watcher, error): fsnotify.NewWatcher, w.Add(filepath.Dir(path)) — КАТАЛОГ, не файл; goroutine-цикл по w.Events/w.Errors с ctx.Done (идиома conn.go:132-145); фильтр filepath.Base(ev.Name) == Base(path); операции Write|Create|Remove|Rename взводят debounce time.AfterFunc (200 мс, дисциплина arm/stop actor.go:529-535 — повторное событие перезаряжает таймер); по истечении — полный Load(path): ok → atomic.Pointer.Store + LastError очищается; ошибка → slog.Warn("config reload rejected", "error", err) + last-good остаётся (D-32) + LastError() сохраняет текст. Публикация Snapshot() config.Config — значение-копия (разыменование atomic.Pointer.Load()); ТИП ВОЗВРАТА ЗАКРЕПЛЁН здесь на производителе: потребитель 03-04 — interface{ Snapshot() config.Config }, сигнатуры обязаны совпадать дословно. Ошибки w.Errors → slog.Warn. Горутина имеет выход (ctx) — go-ultimate concurrency hygiene. Watcher не тянет D-Bus/движок — пакет чистый.
  mise run ci + tidy-diff зелёные. Commit: feat(03-02).
  </action>
  <verify>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise exec -- go test ./internal/config/ -race -count=1</automated>
    <fails_when>строка FAIL в выводе — корпус watcher'а не зелёный под -race (включая горутинную гигиену)</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise exec -- go mod tidy && git diff --exit-code -- go.mod go.sum && echo TIDY-CLEAN</automated>
    <fails_when>строки diff в выводе или отсутствие TIDY-CLEAN — go.mod/go.sum дрейфуют после добавления fsnotify</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise run ci</automated>
    <fails_when>non-zero exit — D-08 не зелёный</fails_when>
  </verify>
  <done>Watcher публикует валидные снапшоты атомарно, сохраняет last-good при битых правках с WARN, игнорирует чужие файлы, коалесит серию в один ре-парс; fsnotify в go.mod без дрейфа</done>
  <acceptance_criteria>
  - watch.go использует w.Add на каталог (filepath.Dir) — греп-пин паттерна atomic-rename
  - TestWatch_InvalidRewriteKeepsLastGood зелёный: WARN + Snapshot не изменился + LastError непуст
  - debounce: 3 события → 1 ре-парс (пин счётчиком)
  - go.mod содержит github.com/fsnotify/fsnotify v1.10.1; go.sum без ручных правок (tidy-clean)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Флаг -config у демона + стартовая проводка окна + docs/CONFIG.md с таблицей Caramba (CONF-01/SWCH-04/CONF-03)</name>
  <files>cmd/goswitchd/main.go, docs/CONFIG.md</files>
  <read_first>
  - cmd/goswitchd/main.go (весь — engineConfig main.go:48-60, run() signal-ctx :33-44, NewActor(window) :53 — точка врезки)
  - internal/hotkey/fsm.go (DefaultWindow :8-11 — остаётся документированным дефолтом)
  - internal/config/config.go, internal/config/watch.go (результат Tasks 1–2)
  - .planning/phases/03-frazy-vydelenie-pereklyuchenie-i-konfiguratsiya/03-CONTEXT.md (D-31 — таблица соответствия БЕЗ клонирования имён; D-35 — окно теперь реально из YAML)
  </read_first>
  <action>
  cmd/goswitchd/main.go: флаг -config string (пусто = встроенные дефолты, поведение Фазы 2 сохранено). При заданном пути: config.Load — ошибка старта → stderr + exit 1 (видимая, не тихая); окно тапов = time.Duration(cfg.Timeouts.TapWindowMs) * time.Millisecond → NewActor(window) (сегодня main.go:53 передаёт hotkey.DefaultWindow — заменить на значение из конфига); запустить config.NewWatcher на существующем signal-ctx цикла run(); watcher-снапшоты пока никем не потребляются актором — потребление снапшотов (окно на новые серии, кап, clipboard-флаг, комбо-биндинг) выполняет план 03-04, это зафиксировано в SUMMARY как граница плана. Завершение по ctx — watcher останавливается (goroutine exit).
  docs/CONFIG.md: референс схемы из config_schema (все ключи, дефолты, диапазоны, пример); раздел совместимости с Caramba — таблица соответствия НАШИХ ключей их назначениям Caramba (например timeouts.tap_window_ms ↔ их интервал тапов) с явной оговоркой: их схема привязана к «свитч на первый тап», goswitch — к ADR-002 «все действия на истечении окна», имена НЕ клонируются (D-31); пример конфига целиком. Приватность: заметка, что содержимое конфига в логи не попадает.
  Проверка живьём: во временный файл — конфиг с tap_window_ms: 450 → собрать демон → запуск с -config не падает, в -debug видна стартовая запись применённого окна (если стартовой записи нет — добавить одну INFO-запись "config loaded" с путём и окном, без содержимого). Commit: feat(03-02).
  </action>
  <verify>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && grep -q '\-config' cmd/goswitchd/main.go && grep -q 'TapWindowMs' cmd/goswitchd/main.go && grep -q 'config.NewWatcher' cmd/goswitchd/main.go && echo WIRED</automated>
    <fails_when>отсутствие WIRED в выводе — флаг/проводка окна/watcher не врезаны в main.go</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && grep -q 'Caramba' docs/CONFIG.md && grep -q 'tap_window_ms' docs/CONFIG.md && grep -q 'clipboard_rung' docs/CONFIG.md && echo DOCS-OK</automated>
    <fails_when>отсутствие DOCS-OK — таблица Caramba или референс схемы неполны (CONF-03/D-31)</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise run ci</automated>
    <fails_when>non-zero exit — D-08 не зелёный</fails_when>
  </verify>
  <done>Демон стартует с -config (окно из YAML), отказ старта на битом конфиге виден, docs/CONFIG.md документирует схему и таблицу Caramba; watcher запущен на ctx</done>
  <acceptance_criteria>
  - main.go: -config задан → окно FSM из tap_window_ms; не задан → hotkey.DefaultWindow (регресс Фазы 2 невозможен)
  - битый путь -config → non-zero exit со структурной ошибкой (не тихие дефолты)
  - docs/CONFIG.md содержит: все ключи схемы, таблицу соответствия Caramba, оговорку о различии моделей (D-31)
  - watcher стартует с демоном и останавливается по ctx (нет утечки горутины — пин -race в ci)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| файл конфига → демон | YAML-файл в $HOME — внешний вход: опечатки, гигантские значения, billion-laughs-класс эксплоитов YAML |
| watcher → актор | снапшоты конфигурации управляют поведением ввода (окно, кап разрушительной серии) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-02-01 | Tampering | подмена/битый конфиг соседним процессом пользователя | medium | accept | однопользовательская сессия (ASVS L1): в рамках угрозы принят; strict-parse ограничивает влияние, last-good предотвращает поломку (RESEARCH security-таблица) |
| T-03-02-02 | DoS | гигантские значения (window 10^9, кап 10^6) | high | mitigate | Validate() потолки: window ≤ 2000 мс, cap ≤ 500, apps ≤ 64 (пин TestValidate_Ranges) |
| T-03-02-03 | DoS | вредоносный YAML (billion-laughs) | medium | mitigate | yaml.v3 v3.0.1 содержит фиксы известных DoS (STACK); строгая схема ограничивает поверхность |
| T-03-02-04 | Information Disclosure | конфиг в логах | low | mitigate | логируются путь/окно/статус валидности, не содержимое секций; заметка в docs/CONFIG.md |
| T-03-02-SC | Tampering | новая зависимость fsnotify (supply chain) | high | mitigate | Package Legitimacy Audit RESEARCH: proxy.golang.org v1.10.1 сверен (2026-05-04), официальный репозиторий, де-факто стандарт; пин точной версии в go.mod; строка T-…-SC ведена по контракту, [SLOP]-пакетов нет — блокирующий чекпоинт не требуется |
</threat_model>

<verification>
- mise run ci зелёный; tidy-diff зелёный (fsnotify — единственное изменение go.mod)
- Корпус FSM (11 тестов) зелёный без семантических правок (D-35)
- Строгий декод пинит D-33: опечатка отклоняет конфиг целиком, поле названо
- docs/CONFIG.md — референс + Caramba-таблица (CONF-03)
</verification>

<success_criteria>
- CONF-01: вся схематика фазы (hotkeys/timeouts/correction/macr) в YAML со strict-контролем
- CONF-02 (инфраструктурная половина): watcher публикует снапшоты, last-good при ошибках; потребление актором — план 03-04
- CONF-03: таблица соответствия в документации, имена не клонированы
- SWCH-04/D-35: окно из конфига при старте; DefaultWindow остаётся документированным дефолтом; корпус FSM нетронут
</success_criteria>

<artifacts_produced>
## Artifacts this plan produces (символы плана 03-02)

- `internal/config`: типы `Config`, `Hotkeys`, `Timeouts`, `Correction`, `MACR`; `Defaults()`, `Validate()`, `Load(path)`, `NewWatcher(ctx, path)`, `Watcher.Snapshot() config.Config` (значение-копия; контракт потребителя 03-04), `Watcher.LastError()`; WARN-строка "config reload rejected"; дебаунс 200 мс
- `internal/hotkey/names.go`: `ParseBinding(string) (Binding, error)`, тип `Binding{Keyval, ModMask uint32}`, закрытая таблица имён (shift_l…super_r), собственные keyval/mask-константы с провенансом ibuskeysyms.h
- `cmd/goswitchd`: флаг `-config`; проводка window → NewActor; старт watcher на signal-ctx
- YAML-ключи (публичный контракт): hotkeys.tap_key, hotkeys.word_layout_combo, timeouts.tap_window_ms, timeouts.verify_wait_ms, correction.backspace_cap, correction.clipboard_rung, macr.enabled, macr.letters, macr.apps, macr.alt_modifier
- Новая зависимость: github.com/fsnotify/fsnotify v1.10.1 (go.mod)
- docs/CONFIG.md (схема + таблица Caramba)
</artifacts_produced>

<output>
Create `.planning/phases/03-frazy-vydelenie-pereklyuchenie-i-konfiguratsiya/03-02-SUMMARY.md` when done
</output>
