---
phase: 03-frazy-vydelenie-pereklyuchenie-i-konfiguratsiya
plan: 07
type: execute
wave: 6
depends_on: [03-03, 03-04, 03-05, 03-06]
files_modified:
  - test/e2e/matrix.go
  - test/e2e/matrix_test.go
  - test/e2e/main.go
  - test/e2e/cases/matrix-v2.yaml
  - .github/workflows/e2e-matrix.yml
  - mise.toml
autonomous: true
requirements: [CORR-02, CORR-03, CORR-06, SWCH-01, SWCH-02, SWCH-03, CONF-02, MACR-01, INST-02]

estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "e2e-матрица v2 зелёная во всей широте: фразы (однородные/смешанные D-26, регистры), выделение (обе стороны Pitfall 6, per-surface фактическая ступень из спайка 03-03), смешанный текст (слово/фраза), комбо SWCH-02, одиночный тап (mode-оракул D-34), super-space живучесть, MACR, clipboard-ступень (opt-in конфиг), ctl-reload — на поверхностях zenity + gnome-text-editor + chromium (критерий №5 ROADMAP)"
    - "Схема шагов матрицы расширена видами select / combo / reload: каждый новый вид = одно поле matrixStep + словарная статья + ветка runMatrixStep + рука stepSummary + рост счётчика «ровно один вид шага» — strict-декодер матрицы отвергает неизвестные поля (T-02-06 линия)"
    - "Имена ydotool для селект/комбо-инъекций канонизированы в matrixKeyNames() по живым пробам (Pitfall 5) — кейсы не зависят от ловушки первой-буквы"
    - "Отчёт PASS/FAIL по кейсам, код выхода ≠ 0 при любом FAIL, изоляция кейса свежим демоном (setupStand → case → teardown на кейс) — контракт матрицы v1 сохранён"
    - "CI-раннер green106 прогоняет matrix-v2.yaml через dispatch-workflow e2e-matrix (обновлён вход на v2-файл) — первый зелёный прогон на раннере зафиксирован"
  artifacts:
    - path: test/e2e/cases/matrix-v2.yaml
      provides: "полная матрица v2: ~18–22 кейса всей широты фазы на трёх поверхностях"
    - path: test/e2e/matrix.go
      provides: "шаги select/combo/reload в схеме+диспетчере; канонизация имён селект-клавиш; expect-поля фактической ступени выделения"
    - path: .github/workflows/e2e-matrix.yml
      provides: "dispatch-вход на cases/matrix-v2.yaml для self-hosted GNOME-раннера"
  key_links:
    - from: test/e2e/cases/matrix-v2.yaml
      to: test/e2e/matrix.go
      via: "кейсы используют только строгую схему шагов (select/combo/reload/type/tap/…) — неизвестное поле = ошибка декода"
      pattern: "matrix-v2"
    - from: .github/workflows/e2e-matrix.yml
      to: test/e2e/cases/matrix-v2.yaml
      via: "workflow input file → go run ./test/e2e -matrix <файл> на раннере green106"
      pattern: "matrix-v2\\.yaml"
  prohibitions:
    - statement: "Матрица НЕ ДОЛЖНА отпускать сломанный стол: teardown каждого кейса восстанавливает desktop state (никаких осиротевших имён IBus/процессов) — фазовый гейт верит только зелёному прогону на восстановленном столе (живой урок 02-06)"
      status: resolved
      verification: judgment
    - statement: "Матрица НЕ ДОЛЖНА краснеть по вине стенда: нестабильные имена инъекций канонизируются до включения в кейсы (Pitfall 5) — FAIL кейса означает дефект демона, а не стенда"
      status: resolved
      verification: judgment
  assumptions:
    - "FLAGGED (probe агрегат): широта матрицы опирается на спайк-таблицы 03-03 (ступень выделения per-surface) и 03-05 (буква MACR) — исполнитель включает кейсы только с доказанными живьём именами/ступенями; поверхности за пределами zenity/GTE/chromium (Electron и др.) НЕ добавляются (минимальная широта CONTEXT)"
    - "FLAGGED (research A3): кейс long-phrase (~60–100 рун) пинирует фактическое поведение surrounding-окна клиентов: если сверка режет длинные фразы — кейс фиксирует фактический отказ с причиной (документированная деградация D-25), не молчание"
---

<objective>
Приёмочная матрица фазы: matrix-v2.yaml полной широты поверх готовых драйверов и кейсов планов 03-01..03-06. Схема шагов матрицы расширяется (select/combo/reload), имена инъекций канонизируются по живым пробам, dispatch-workflow раннера переключается на v2. Фазовый гейт: матрица v2 зелёная на живом столе И на CI-раннере green106.

**As a** владелец, **I want** одну команду, прогоняющую ВСЮ широту жестов фазы по трём приложениям, **so that** приёмка M3 — это зелёный отчёт, а не ручная проверка по списку.

Purpose: критерий №5 ROADMAP (матрица v2 зелёная во всей широте) — приёмочная база фазы; пере-доказывает требования CORR-02/03/06, SWCH-01..03, CONF-02, MACR-01, INST-02 в комплексе.
Output: cases/matrix-v2.yaml, шаги select/combo/reload в матрице, обновлённый e2e-matrix workflow, mise-задача e2e-matrix-v2.
</objective>

<execution_context>
@/home/nil/DiskD/W/Djarvur/goswitch/.zcode/gsd-core/workflows/execute-plan.md
@/home/nil/DiskD/W/Djarvur/goswitch/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-frazy-vydelenie-pereklyuchenie-i-konfiguratsiya/03-PATTERNS.md (§ test/e2e — дословные правила расширения matrixStep :90-107, validate :171-203, runMatrixStep :611-624, matrixKeyNames :67-73, tapMatrix-оракул :648-673, изоляция :490-530)
@.planning/phases/03-frazy-vydelenie-pereklyuchenie-i-konfiguratsiya/03-CONTEXT.md (ширина матрицы: минимум zenity + Chromium; D-34 mode-оракул)
@.planning/phases/02-korrektsiya-slova-en-ru/02-06-SUMMARY.md (живые ловушки матрицы v1: флип после открытия поверхности, orphaned name, серийные SIGKILL)
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Шаги select/combo/reload в схеме матрицы + канонизация имён (Pitfall 5)</name>
  <files>test/e2e/matrix.go, test/e2e/matrix_test.go, test/e2e/main.go</files>
  <read_first>
  - test/e2e/matrix.go (matrixStep :90-107; закрытые словари :39-51; validate :171-203 и счётчик «ровно один вид шага» :173-178; runMatrixStep :611-624; stepSummary :562-575; matrixKeyNames :67-73; tapMatrix :648-673)
  - test/e2e/matrix_test.go (таблицы отвержения :108-165 и словарей :208-244 — расширить)
  - SUMMARY 03-03 (спайк-таблица: канонические имена селект-инъекций + фактическая ступень per-surface)
  - SUMMARY 03-04 (каноническое имя инъекции комбо)
  - test/e2e/main.go (injectKeys/pressKey; startDaemon args)
  </read_first>
  <behavior>
  - TestMatrixDecode_SelectComboReload: YAML-документы с шагами select (keys: имя инъекции селекта), combo (binding-строка), reload (файл-фрагмент: строка для дозаписи в temp-config + ожидание applied/rejected) декодируются; каждый шаг проходит validate
  - TestMatrixDecode_RejectsNew: неизвестное поле/вид в новых шагах → ошибка декода (strict-линия); два вида в одном шаге → ошибка счётчика
  - TestMatrixKeyNames_Canonical: имена из спайков (селект/комбо) резолвятся через matrixKeyNames — таблица пополняется, неизвестное имя отклоняется на валидации кейса (не в рантайме стенда)
  - TestMatrixStep_RunDispatch: select/combo/reload-шаги диспетчеризуются (runMatrixStep-ветки; reload-шаг пишет фрагмент и ждёт log-форму применения или WARN config reload rejected)
  - stepSummary отражает новые виды в отчёте (руки сводки)
  </behavior>
  <action>
  RED (D-07): matrix_test.go по behavior. Прогон mise exec -- go test ./test/e2e/ -race -count=1; red-evidence. Commit: test(03-07).
  GREEN — matrix.go: три новых вида шагов по правилу PATTERNS (поле + словарная статья + validate-ветка + runMatrixStep-рука + stepSummary-рука + рост счётчика видов); select-шаг: инъекция канонического селект-имени (из спайк-таблицы 03-03) перед tap-шагом; combo-шаг: инъекция комбо-имени (из 03-04) с гейтом на лог-формы "combo" и mode; reload-шаг: дозапись фрагмента в temp-config демона кейса (стенд уже умеет -config) + waitForLog применённого снапшота или config reload rejected (обе формы валидны для кейсов last-good). matrixKeyNames: пополнить каноническими именами по спайкам. Комбо-оракул: tapMatrix-подобный гейт на лог-форму combo/mode — БЕЗ переименования существующих форм action/mode (matrix.go:663). mise run ci. Commit: feat(03-07).
  </action>
  <verify>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise exec -- go test ./test/e2e/ -race -count=1</automated>
    <fails_when>строка FAIL — корпус декодера/диспетчера шагов не зелёный</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise exec -- go test ./test/e2e/ -run 'TestMatrixDecode' -race -count=1 -v 2>&1 | grep -c '^--- PASS'</automated>
    <fails_when>счётчик PASS меньше, чем было до плана (регресс таблиц отвержения), или 0 — новые таблицы не написаны</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise run ci</automated>
    <fails_when>non-zero exit — D-08 не зелёный</fails_when>
  </verify>
  <done>Схема принимает select/combo/reload со strict-контролем; диспетчер и сводка исполняют новые виды; имена канонизированы</done>
  <acceptance_criteria>
  - три новых вида шагов декодируются и отвергают неизвестные поля (пин)
  - счётчик «ровно один вид шага» вырос и валиден (пин двухвидового шага — ошибка)
  - matrixKeyNames пополнена именами из спайк-таблиц; несуществующее имя — ошибка валидации кейса
  - reload-шаг различает applied и config reload rejected (обе лог-формы)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: matrix-v2.yaml полной широты + зелёный прогон на столе и раннере (критерий №5)</name>
  <precondition>Планы 03-01..03-06 завершены; все живые кейсы фазы PASS; mise exec -- go test ./... -race -count=1 зелёный</precondition>
  <files>test/e2e/cases/matrix-v2.yaml, .github/workflows/e2e-matrix.yml, mise.toml, test/e2e/matrix.go</files>
  <read_first>
  - test/e2e/cases/matrix-v1.yaml (формат кейсов, expect-поля, регистры)
  - test/e2e/matrix.go (expect_level-подобные поля :820-829 — модель для expect_sel_step: фактическая ступень замены выделения per-surface из спайка 03-03)
  - SUMMARY 03-03 (спайк-таблица поверхностей), SUMMARY 03-05 (буква MACR/доставка), SUMMARY 03-04 (комбо-имя)
  - .github/workflows/e2e-matrix.yml (текущий dispatch-вход matrix-v1)
  - docs/ci-runner.md (02-07: живые команды раннера; gh api --method POST для токенов)
  </read_first>
  <action>
  cases/matrix-v2.yaml — полная широта фазы (по одной строке на способность; поверхности zenity/gte/chromium по спайк-таблицам): фразы: phrase-en-ru (однородная), phrase-mixed (D-26 прогоны), phrase-ru (RU→EN), phrase-registers (GHB…/Ghb…), phrase-after-space (хвостовая геометрия), long-phrase (~60–100 рун — A3-пин: фактическое поведение, включая валидный отказ с причиной); выделение: select-all-correct (zenity + gte + chromium с expect_sel_step по спайку), select-partial, select-reverse (право-в-лево); смешанный текст: word-mixed (паипривет), phrase-mixed; переключение: layout-single (mode-оракул, оба направления), combo-word-layout (слово + mode), super-space-alive (имя живо + коррекция после); конфиг: reload-window (reload-шаг: смена tap_window_ms → применение), reload-invalid-last-good (битая правка → rejected + поведение сохранено); MACR: macr-super-letter (конфиг-включённый, буква из спайка); ctl: ctl-status-reload (goswitchctl status/reload из reload-шага); clipboard: select-clipboard (только если спайк 03-03 нашёл поверхность с primary-несовместимостью; иначе строка исключена с комментарием-ссылкой на SUMMARY 03-03 — молчаливое исключение запрещено).
  mise.toml: [tasks.e2e-matrix-v2] → go run ./test/e2e -matrix test/e2e/cases/matrix-v2.yaml (конвенция живых задач). Живой прогон на столе: зелёный отчёт, ноль FAIL, desktop state восстановлен (teardown-контракт матрицы). При фейлах — классификация: дефект демона (чинить в коде фазы) vs среда (префлайт/имена — Pitfall 5).
  .github/workflows/e2e-matrix.yml: dispatch-вход/дефолт файла → test/e2e/cases/matrix-v2.yaml (path-инпут сохраняется для v1-регрессии). Запустить dispatch на раннере green106 (workflow_dispatch с ref фазовой ветки — bootstrap-паттерн 02-07), зафиксировать первый зелёный прогон (run URL в SUMMARY). Commit: feat(03-07).
  </action>
  <verify>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && grep -c '^- name:\|- id:' test/e2e/cases/matrix-v2.yaml; grep -q 'select' test/e2e/cases/matrix-v2.yaml && grep -q 'combo' test/e2e/cases/matrix-v2.yaml && grep -q 'reload' test/e2e/cases/matrix-v2.yaml && echo V2-BREADTH-OK</automated>
    <fails_when>отсутствие V2-BREADTH-OK — матрица не покрывает новые способности фазы</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise run e2e-matrix-v2</automated>
    <fails_when>non-zero exit или любой FAIL в отчёте, или отчёт не содержит строк PASS по всем кейсам v2 — матрица не зелёная на живом столе</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && grep -q 'matrix-v2' .github/workflows/e2e-matrix.yml && echo WF-V2</automated>
    <fails_when>отсутствие WF-V2 — workflow раннера не переключён на v2</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise run ci && mise run e2e-matrix</automated>
    <fails_when>non-zero exit — зелёная итерация или регресс v1-матрицы не выполнены</fails_when>
  </verify>
  <done>matrix-v2 зелёная на живом столе (все кейсы PASS, ноль FAIL, стол восстановлен), workflow раннера на v2, первый зелёный прогон green106 зафиксирован URL-ом в SUMMARY; v1-матрица остаётся зелёной</done>
  <acceptance_criteria>
  - matrix-v2.yaml покрывает: фразы (5+), выделение (3+ поверхности/направления), смешанный текст, комбо, одиночный тап, super-space, reload (валидный+битый), MACR, ctl; исключения (clipboard) — только с комментарием-ссылкой на спайк-вердикт
  - reload-invalid-last-good кейс: WARN-форма + сохранённое поведение (D-32 в матрице)
  - long-phrase: фактическое поведение запинено (прогон или документированный отказ с причиной — не молчание)
  - e2e-matrix.yml указывает на matrix-v2.yaml; зелёный прогон раннера green106 зафиксирован
  - отчёт: PASS/FAIL по каждому кейсу; exit ≠ 0 при FAIL (контракт v1 сохранён)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| matrix-v2.yaml → стенд | YAML-файл кейсов — вход декодера стенда (strict уже обязателен) |
| стенд → живой стол владельца | инъекции управляют реальным GNOME-столом: изоляция и teardown — границы доверия |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-07-01 | DoS | нестабильные имена инъекций роняют кейсы | medium | mitigate | канонизация matrixKeyNames по спайк-таблицам до включения в кейсы; валидация кейса отвергает неизвестное имя на декоде (Pitfall 5) |
| T-03-07-02 | Tampering | кейс с полем вне схемы молча игнорируется | high | mitigate | strict KnownFields матрицы (линия 02-06) + новые таблицы отвержения в корпусе (Task 1) |
| T-03-07-03 | DoS | осиротевшие имена IBus/процессы между кейсами | high | mitigate | изоляция fresh-daemon + teardown с проверками (контракт v1, matrix.go:490-530); префлайт ловит orphaned name, ibus restart лечит (урок 02-06) |
</threat_model>

<verification>
- mise run ci зелёный; mise run e2e-matrix-v2 зелёная на живом столе; mise run e2e-matrix (v1) зелёная
- Первый зелёный прогон green106 по matrix-v2.yaml (URL в SUMMARY)
- Все способности фазы имеют строку в v2 (или задокументированное исключение со ссылкой)
</verification>

<success_criteria>
- Критерий №5 ROADMAP: матрица v2 зелёная во всей широте — фаза готова к /gsd:verify-work
- Регресс-арест: v1-матрица остаётся зелёной (сукцессия word-mixed уже в 03-01)
- Фактические ступени/имена/доставка — запинены, не предполагаемы
</success_criteria>

<artifacts_produced>
## Artifacts this plan produces (символы плана 03-07)

- test/e2e: `cases/matrix-v2.yaml` (~18–22 кейса); шаги матрицы select/combo/reload (schema+dispatch+summary); пополненная matrixKeyNames; expect_sel_step-поле фактической ступени выделения
- mise-задача `e2e-matrix-v2`
- .github/workflows/e2e-matrix.yml: вход по умолчанию → matrix-v2.yaml
</artifacts_produced>

<output>
Create `.planning/phases/03-frazy-vydelenie-pereklyuchenie-i-konfiguratsiya/03-07-SUMMARY.md` when done
</output>
