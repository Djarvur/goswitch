---
phase: 03-frazy-vydelenie-pereklyuchenie-i-konfiguratsiya
plan: 03
type: execute
wave: 2
depends_on: [03-01, 03-02]
files_modified:
  - engine/engine.go
  - engine/engine_test.go
  - internal/clipboard/clipboard.go
  - internal/clipboard/clipboard_test.go
  - internal/correct/verify.go
  - internal/correct/verify_test.go
  - internal/session/actor.go
  - internal/session/actor_test.go
  - cmd/goswitchd/main.go
  - test/e2e/case_select.go
  - test/e2e/main.go
  - mise.toml
# scope_note: 12 files accepted (checker scope_sanity) — 4 *_test.go pairs mandated by strict TDD (D-07);
# engine.go+engine_test.go seam widening must update all EventHandler implementors in one iteration
# (Pitfall 1 — compilation unit cannot be split); estimate 30k = 30% of 100k.
autonomous: true
requirements: [CORR-03]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Провод SetSurroundingText доносит anchorPos до актора: выделение активно ⇔ anchor ≠ cursor (D-30); двойной тап при активном выделении исправляет ВЫДЕЛЕННЫЙ текст — живой кейс select-correct даёт выделенный ghbdtn → привет (CORR-03)"
    - "Двойной тап БЕЗ активного выделения продолжает исправлять последнее слово — поведение Фазы 2 не меняется (D-30, пин корпуса: кейсы Фазы 2 зелёные без правок ожиданий)"
    - "Приложение не сообщает выделение (anchor == cursor или surrounding недоступен) → диапазон выделения не образуется; выделение не наблюдается → тихий отказ D-20 с причиной в лог только если замена выделения была единственным путём (D-30)"
    - "Геометрия выделения учитывает порядок: cursor < anchor (выделение правее курсора, offset ≥ 0) и cursor > anchor (offset < 0) — оба направления пинятся юнит-тестами (Pitfall 6)"
    - "Clipboard round-trip — opt-in ступень ПОСЛЕ commit/delete (D-28): в дефолт-конфиге ВЫКЛЮЧЕН; включается correction.clipboard_rung; после round-trip демон best-effort восстанавливает исходный буфер (D-29), неудача восстановления — только WARN, не ошибка операции"
    - "Контент буфера обмена НИКОГДА не попадает в лог ни на одном уровне (расширение D-20/D-21); замена передаётся в wl-copy ТОЛЬКО через stdin-пайп, никогда argv (утечка в ps / парсинг как флагов)"
  artifacts:
    - path: engine/engine.go
      provides: "EventHandler.HandleSurroundingText(text string, cursorPos, anchorPos uint32) — seam расширен, якорь с провода доходит до актора (Pitfall 1)"
    - path: internal/correct/verify.go
      provides: "SelectionRange(cursor, anchor uint32) (start, end uint32, active bool) и VerifyRangeAt(text []rune, start uint32, want []rune) bool — сверка выделения в обе стороны"
    - path: internal/clipboard/clipboard.go
      provides: "Save(ctx)/Set(ctx, content через stdin)/Clear(ctx)/Restore(ctx, saved, had) поверх wl-paste --no-newline / wl-copy --trim-newline / wl-copy --clear; раннер за интерфейсом для юнит-фиков"
    - path: internal/session/actor.go
      provides: "ветка выделения в конвейере (активный якорь → диапазон выделения), clipboard-ступень после verify-mismatch, session.Options{BackspaceCap, ClipboardRung} + SetOptions"
    - path: test/e2e/case_select.go
      provides: "живые кейсы select-smoke (спайк: пин фактического поведения поверхностей) и select-correct (замена выделения)"
  key_links:
    - from: engine/engine.go
      to: internal/session/actor.go
      via: "SetSurroundingText(text, cursorPos, anchorPos) → handler.HandleSurroundingText(text, cursorPos, anchorPos) — якорь в кэше сверки актора"
      pattern: "anchorPos"
    - from: internal/session/actor.go
      to: internal/clipboard/clipboard.go
      via: "verify-mismatch при включённой ступени → Save → Set(converter) → ForwardKeyEvent Ctrl+V → Restore best-effort (D-28/D-29)"
      pattern: "clipboard\\."
    - from: cmd/goswitchd/main.go
      to: internal/session/actor.go
      via: "SetOptions(BackspaceCap, ClipboardRung из config.Load) — стартовая подача опций коррекции (снапшот-потребление — 03-04)"
      pattern: "SetOptions"
  prohibitions:
    - statement: "Содержимое буфера обмена НЕ ДОЛЖНО попадать в журнал ни на одном уровне — ни исходное, ни подменённое (D-20/D-21 расширены на clipboard; privacy)"
      status: resolved
      verification: judgment
    - statement: "Замена выделения НЕ ДОЛЖНА трогать текст за пределами выделения — ровно диапазон [min(cursor,anchor), max(cursor,anchor)) и ни руной больше (CORR-07 точность, применённая к выделению)"
      status: resolved
      verification: judgment
    - statement: "Исходный буфер пользователя НЕ ДОЛЖЕН остаться безвозвратно затёртым: перед подменой сохраняется байт-точно, после — восстанавливается best-effort; пустой исходник восстанавливается wl-copy --clear (D-29)"
      status: resolved
      verification: judgment
  assumptions:
    - "FLAGGED (probe CORR-03 / research A1): заменяет ли CommitText активное выделение (семантика «набор поверх выделения») в GTK/Chromium-классе и применим ли DeleteSurroundingText к диапазону выделения со знаком — НЕИЗВЕСТНО до живой пробы; Task 1 (спайк) пинирует фактическую ступень per-surface ДО фиксации порядка ступеней; матрица v2 фиксирует фактическое"
    - "FLAGGED (probe CORR-03 / research A2): GTK-класс пушит anchorPos, отражающий выделение; поведение Chromium 153 с якорем неизвестно — если якорь не шлёт, кейсы выделения Chromium тихо отказывают D-30 (документированная деградация, не блокер)"
    - "FLAGGED (probe CORR-03 / research A5): имена ydotool для селект-инъекций (ctrl+a, shift+Right, Control_R) разрешаются не в те физические клавиши (ловушка space→S, Escape→E); Task 1 пробует живьём и канонизирует рабочие имена"
    - "FLAGGED (research A8): wl-copy форкает и сервит буфер в фоне — восстановление может быть перекрыто буфер-менеджером GNOME; D-29 квалифицирует restore как best-effort (только WARN)"
---

<objective>
Выделение как третий диапазон коррекции: якорь с провода доходит до актора (Pitfall 1), двойной тап при активном выделении исправляет выделенное (D-30), геометрия в обе стороны (Pitfall 6), clipboard-ступень opt-in с best-effort восстановлением (D-28/D-29, Pitfall 3). Спайк Task 1 пинирует фактическое поведение замены выделения per-surface (A1/A2/A5) ДО встраивания основной логики — по образцу 02-05 «ladder-chromium».

**As a** пользователь, выделивший неверный текст, **I want** двойным Right Shift исправить выделенный фрагмент, **so that** чинить можно любую часть набранного, а не только последнее слово — и там, где commit не заменяет выделение, опциональная clipboard-ступень доводит замену до конца, не губя мой буфер обмена.

Purpose: CORR-03 — третий диапазон единого конвейера 03-01; clipboard-ступень закрывает rung C ADR-003.
Output: расширенный seam anchorPos, ветка выделения, internal/clipboard, кейсы select-smoke/select-correct, session.Options + стартовая подача опций.
</objective>

<execution_context>
@/home/nil/DiskD/W/Djarvur/goswitch/.zcode/gsd-core/workflows/execute-plan.md
@/home/nil/DiskD/W/Djarvur/goswitch/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-frazy-vydelenie-pereklyuchenie-i-konfiguratsiya/03-CONTEXT.md (D-28, D-29, D-30 — заперты)
@.planning/phases/03-frazy-vydelenie-pereklyuchenie-i-konfiguratsiya/03-PATTERNS.md (§ engine.go — seam widening; § clipboard — адаптация runCmd main.go:475-488; § actor — startCorrection/executeLevel2)
@.planning/phases/03-frazy-vydelenie-pereklyuchenie-i-konfiguratsiya/03-RESEARCH.md (§ Code Examples §2/§4; Pitfalls 1/3/6; Pattern 4)
@docs/adr/ADR-003-replacement-capability-ladder.md (rung C — clipboard только по конфигу)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seam anchorPos + живой спайк селекта — пин фактического поведения поверхностей (Pitfall 1/5, A1/A2/A5)</name>
  <reversibility rating="costly">Сечение EventHandler — кросс-модульный контракт engine↔session; все имплементоры/дубли обновляются в этой же задаче (Pitfall 1)</reversibility>
  <precondition>Планы 03-01/03-02 завершены: mise exec -- go test ./... -race -count=1 зелёный. Живая сессия: m1-gate префлайт зелёный; ydotool доступен (пользователь в группе input)</precondition>
  <files>engine/engine.go, engine/engine_test.go, internal/session/actor.go, internal/session/actor_test.go, test/e2e/case_select.go, test/e2e/main.go, mise.toml</files>
  <read_first>
  - .planning/phases/03-frazy-vydelenie-pereklyuchenie-i-konfiguratsiya/03-RESEARCH.md (§ Code Examples §2 — дословные текущие формы engine.go:187-197 и seam :88-94; Pitfall 1; §8 — ydotool синтаксис)
  - engine/engine.go (SetSurroundingText :187-197, EventHandler :82-95)
  - engine/engine_test.go (дубли EventHandler — обновить вместе с seam)
  - internal/session/actor.go (HandleSurroundingText :185-195, кэш surr)
  - internal/session/actor_test.go (двойники-имплементоры EventHandler)
  - test/e2e/case_ladder.go (02-05 ladder-chromium — образец спайк-кейса, пинящего фактический уровень)
  - test/e2e/main.go (injectKeys/pressKey, waitForLog, startDaemon :299 — расширить аргументами спавна для -config)
  - /usr/include/ibus-1.0/ibusengine.h (:430 — семантика anchor_pos: «Anchor position of selection in @text»)
  </read_first>
  <action>
  Seam: engine/engine.go — сигнатура HandleSurroundingText(text string, cursorPos, anchorPos uint32); вызов в SetSurroundingText передаёт anchorPos (сегодня теряется, engine.go:192); ВСЕ имплементоры (session.Actor + тестовые дубли в engine_test.go/actor_test.go) обновить в этой же задаче; DEBUG-трасса surrounding_text уже пишет anchor_pos — сохранить. internal/session/actor.go: кэш сверки surr дополняется якорем; поведение без выделения НЕ меняется (якорь только хранится).
  Спайк-кейс select-smoke (test/e2e/case_select.go): zenity: injectText("ghbdtn привет") → последовательно инжектировать ctrl+a, затем Home+shift+End (или shift+Right серию) → после каждой инъекции ждать -debug записи surrounding_text и фиксировать cursor_pos/anchor_pos; затем двойной тап при активном выделении → наблюдать фактическую ступень: применился ли commit поверх выделения, DeleteSurroundingText, или замены не произошло (readback + лог). Имена ydotool пробовать живьём (кандидаты: ctrl+a, ctrl+A, Control+a; shift+Right, Shift_R+Right) — нерабочие имена (ловушка первой буквы) выявить по отсутствию изменения anchor в трассе; рабочие захардкодить в кейсе с комментарием. Результаты спайка записать в SUMMARY (таблица: поверхность → якорь шлёт/нет → фактическая ступень замены) — они фиксируют порядок ступеней Task 2 и поля матрицы v2 (03-07). register "select-smoke" в pickCase; mise.toml [tasks.e2e-select-smoke].
  Если спайк покажет, что zenity НЕ пушит якорь при выделении — зафиксировать в SUMMARY и продолжать: выделение на этой поверхности ненаблюдаемо (D-30 отказ), кейс select-correct переносится на поверхность со работающим якорем (gnome-text-editor/chromium — проверить те же шаги спайка там, у стенда есть оба драйвера).
  Commit: feat(03-03).
  </action>
  <verify>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise exec -- go test ./engine/ ./internal/session/ -race -count=1</automated>
    <fails_when>строка FAIL — seam-правка сломала корпус engine/session (какой-то имплементор не обновлён)</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && grep -n 'anchorPos' engine/engine.go | head -3</automated>
    <fails_when>пустой вывод или меньше двух вхождений — якорь не протащен через seam</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise run e2e-select-smoke</automated>
    <fails_when>non-zero exit — спайк не отработал (инъекция/трасса/таблица результатов); сам факт «якорь не шлётся» НЕ является FAIL кейса — кейс фиксирует фактическое и PASS-ит с вердиктом</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise run ci</automated>
    <fails_when>non-zero exit — D-08 не зелёный</fails_when>
  </verify>
  <done>Якорь доходит до актора на всех поверхностях провода; спайк-таблица (якорь/ступень per-surface) в SUMMARY; рабочие имена ydotool канонизированы в кейсе; поведение без выделения не изменилось (корпус Фазы 2 зелёный)</done>
  <acceptance_criteria>
  - engine.EventHandler сигнатура содержит anchorPos uint32; все имплементоры обновлены (компиляция + корпус зелёные)
  - select-smoke PASS с зафиксированным вердиктом per-surface в выводе/SUMMARY (якорь: да/нет; ступень: commit/delete/нет)
  - DEBUG-трасса surrounding_text сохраняет оба поля (cursor_pos, anchor_pos)
  - двойной тап без выделения ведёт себя как в Фазе 2 (никаких новых отказов в существующих кейсах — e2e-word регресс-прогон зелёный)
  </acceptance_criteria>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Ветка выделения в конвейере — диапазон = выделение, геометрия в обе стороны (D-30, Pitfall 6)</name>
  <files>internal/correct/verify.go, internal/correct/verify_test.go, internal/session/actor.go, internal/session/actor_test.go, test/e2e/case_select.go, mise.toml</files>
  <read_first>
  - internal/correct/verify.go (MatchesSuffix — образец; beforeCursor-idioma clamping actor.go:565-574)
  - internal/correct/runs.go (03-01 ConvertRuns — конверсия диапазона выделения тем же единым код-путём D-23)
  - internal/session/actor.go (конвейер Task 1 плана 03-01: параметризованный запуск token/tail/replace — выделение врезается сюда; executeCorrection/executeLevel2)
  - .planning/phases/03-frazy-vydelenie-pereklyuchenie-i-konfiguratsiya/03-RESEARCH.md (Pitfall 6 — signed offset; § Архитектурная карта: детект выделения = engine → session)
  - test/e2e/case_select.go (спайк Task 1 — пинирует фактическую ступень)
  </read_first>
  <behavior>
  - TestSelectionRange (correct): cursor=0,anchor=6 → (0,6,true); cursor=6,anchor=0 → (0,6,true); cursor==anchor → (_,_,false); VerifyRangeAt("привет мир", 0, "привет") == true; VerifyRangeAt со сдвинутым диапазоном == false
  - TestActor_DoubleTapSelectionCorrects (session, fake-sink): поле "ghbdtn привет", выделен диапазон [0,6) (cursor=6, anchor=0 — правое-в-лево), двойной тап + сверка → DeleteSurroundingText(-6, 6) и CommitText("привет") — ровно по выделению, хвост " привет" НЕ удаляется
  - TestActor_SelectionLeftToRight (Pitfall 6): cursor=0, anchor=6 (выделение правее курсора) → offset удаления = 0, nchars = 6 — положительный offset-случай пинен отдельно
  - TestActor_NoSelectionStillWord (D-30): cursor == anchor → путь слова ровно как Фаза 2 (геометрия токена, не выделения) — существующий корпус не правится, добавляется явный пин
  - TestActor_SelectionMixedConverts: выделение "gfbпривет" → CommitText("паипривет") — единая конверсия ConvertRuns на диапазоне выделения (D-23)
  - Живой кейс select-correct (по результатам спайка): поверхность с работающим якорем: ввод → селект-инъекция (каноническое имя из Task 1) → двойной тап → readback == сконвертированному выделению, текст вне выделения нетронут
  </behavior>
  <action>
  RED (D-07): verify_test.go + actor_test.go по behavior (SelectionRange/VerifyRangeAt — заглушки; актор без ветки выделения — падения на утверждениях CommitText/offset). Прогон mise exec -- go test ./internal/correct/ ./internal/session/ -race -count=1; red-evidence. Commit: test(03-03).
  GREEN — internal/correct/verify.go: SelectionRange(cursorPos, anchorPos uint32) (start, end uint32, active bool) — start=min, end=max, active = cursor ≠ anchor (D-30); VerifyRangeAt(text []rune, start uint32, want []rune) bool — сверка verify-after для выделения: наличие сконвертированного текста на месте диапазона (Pitfall 6 — MatchesSuffix проверяет только префикс-до-курсора, его недостаточно).
  GREEN — internal/session/actor.go: в конвейере (точка входа Double) — при активном якоре из последнего пуша (SelectionRange(surr.cursor, surr.anchor).active) диапазон = выделение [start,end) из surrounding-текста (НЕ из буфера — выделение сообщает приложение); конверсия — ConvertRuns(диапазон) (единый путь 03-01); D-24/отказы — те же правила; лестница: DeleteSurroundingText(offset = start − cursorPos — ЗНАКОВЫЙ по взаимному положению, nchars = end − start) + CommitText(сконвертированное) — ровно диапазон, без хвостов; verify-after — VerifyRangeAt по свежему пушу; якорь == cursor → путь слова, поведение Фазы 2 (пин TestActor_NoSelectionStillWord). Отказы: выделенный диапазон без букв → quiet D-20; сверка не сошлась → verify-mismatch, ноль эмиттов.
  Живой кейс select-correct в case_select.go (поверхность/ступень/имя селекта — из спайк-таблицы Task 1); register; mise.toml [tasks.e2e-select-correct]. Прогоны: e2e-select-correct, e2e-word (регресс). Commit: feat(03-03).
  </action>
  <verify>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise exec -- go test ./internal/correct/ ./internal/session/ -race -count=1</automated>
    <fails_when>строка FAIL — корпус выделения/геометрии не зелёный</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise exec -- go test ./internal/session/ -run 'Selection' -race -count=1 -v</automated>
    <fails_when>non-zero exit или отсутствие PASS-строк TestActor_DoubleTapSelectionCorrects/TestActor_SelectionLeftToRight в выводе — фильтр пуст или корпус красный</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise run e2e-select-correct && mise run e2e-word</automated>
    <fails_when>non-zero exit или отсутствие PASS select-correct/PASS word-en-ru — замена выделения не работает живьём либо сломан путь слова (D-30)</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise run ci</automated>
    <fails_when>non-zero exit — D-08 не зелёный</fails_when>
  </verify>
  <done>Выделение исправляется ровно по диапазону в обе стороны (offset знак пинен), без выделения — путь слова Фазы 2; живой кейс select-correct PASS; смешанное выделение конвертируется прогонами</done>
  <acceptance_criteria>
  - правильная геометрия: правое-в-лево → отрицательный offset, левое-в-право → offset 0 + nchars — оба пина зелёные (Pitfall 6 закрыт)
  - текст вне выделения не удаляется (пин: DeleteSurroundingText ровно nchars выделения)
  - cursor == anchor → ни один новый отказ не появляется; корпус Фазы 2 без правок ожиданий
  - e2e select-correct PASS на поверхности из спайк-таблицы; readback: вне выделения байт-то же
  </acceptance_criteria>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: Clipboard-ступень opt-in — wl-copy/wl-paste round-trip + best-effort restore (D-28/D-29, Pitfall 3)</name>
  <files>internal/clipboard/clipboard.go, internal/clipboard/clipboard_test.go, engine/keys.go, internal/session/actor.go, internal/session/actor_test.go, cmd/goswitchd/main.go, test/e2e/case_select.go, test/e2e/main.go, mise.toml</files>
  <read_first>
  - .planning/phases/03-frazy-vydelenie-pereklyuchenie-i-konfiguratsiya/03-RESEARCH.md (§ Code Examples §4 — прототип punto_engine.py:273-305 и флаги wl-clipboard из man; Pitfall 3; anti-pattern «argv»)
  - .planning/phases/03-frazy-vydelenie-pereklyuchenie-i-konfiguratsiya/03-PATTERNS.md (§ clipboard — адаптация runCmd main.go:475-488: ctx, stdin-пайп, диагностика)
  - internal/session/actor.go (executeCorrection/verify-after — точка включения ступени ПОСЛЕ commit/delete при mismatch; comboMask :35 — Mod4 уже исключён из буфера)
  - engine/keys.go (KeyControlL :35 — есть; добавить KeyV = 0x0076 с провенансом из ibuskeysyms.h)
  - engine/engine.go (ForwardKeyEvent — эмиттер уровня 2 Фазы 2 — последовательность Ctrl+V)
  - internal/config/config.go (correction.clipboard_rung — флаг уже в схеме 03-02)
  - test/e2e/main.go (startDaemon :299 — расширить аргументами для -config)
  </read_first>
  <behavior>
  - TestClipboard_SaveSetRestore (fake-раннер): Save → wl-paste --no-newline (байт-точно); Set → wl-copy --trim-newline с контентом ЧЕРЕЗ STDIN (пин: контент отсутствует в argvfake-раннера); пустой буфер (wl-paste exit ≠ 0) → had=false → Restore зовёт wl-copy --clear; Restore с had=true → wl-copy с сохранёнными байтами в stdin
  - TestClipboard_Timeouts: каждый вызов с ctx-дедлайном — отмена контекста отменяет subprocess (пин: раннер видит ctx)
  - TestActor_ClipboardRungDisabledByDefault: verify-mismatch при ClipboardRung=false (дефолт) → ноль вызовов clipboard-раннера (D-28 — opt-in)
  - TestActor_ClipboardRungAfterMismatch: ClipboardRung=true + verify-mismatch → последовательность Save → Set(converted) → ForwardKeyEvent Ctrl+V (KeyControlL press → v press c MaskControl → v release → ControlL release) → Restore; лог: INFO-записи не содержат контента; WARN restore-неудачи не возвращает ошибку операции (D-29)
  - Живой кейс select-clipboard (опционально по спайку): демон с -config (correction.clipboard_rung: true), поверхность, где primary-ступени не заменяют выделение (по спайк-таблице Task 1); до кейса записать буфер wl-paste, после — сверить восстановление best-effort
  </behavior>
  <action>
  RED (D-07): clipboard_test.go по behavior (пакет-заглушка); actor_test.go — кейсы ступени против актора без неё. Прогон mise exec -- go test ./internal/clipboard/ ./internal/session/ -race -count=1; red-evidence. Commit: test(03-03).
  GREEN — internal/clipboard/clipboard.go: раннер-интерфейс (run(ctx, name, args, stdin []byte) ([]byte, error)) + production-реализация на os/exec.CommandContext (адаптация runCmd: буферы, контекстный таймаут, ошибка с именем команды и stderr); API: Save(ctx) (saved []byte, had bool, err error) — wl-paste --no-newline; had=false при ненулевом exit пустого буфера; Set(ctx, content) error — wl-copy --trim-newline, content ТОЛЬКО в cmd.Stdin (argv-запрет); Clear(ctx) — wl-copy --clear; Restore(ctx, saved, had) — had ? Set(saved) : Clear(); ошибка восстановления возвращается, но классифицируется вызывающим как best-effort (WARN). wl-copy форкает — быстрый возврат Run() нормален (man, RESEARCH §4).
  GREEN — engine/keys.go: KeyV = 0x0076 (провенанс ibuskeysyms.h, Latin-1 прямой) — для ForwardKeyEvent-последовательности Ctrl+V.
  GREEN — internal/session/actor.go: session.Options{BackspaceCap int; ClipboardRung bool} + метод SetOptions(Options); ступень: при verify-mismatch && Options.ClipboardRung → clipboard.Save → ConvertRuns-результат (уже вычислен) clipboard.Set → ForwardKeyEvent-бёрст Ctrl+V (по прототипу: KeyControlL press mods 0 → KeyV press MaskControl → KeyV release MaskControl|MaskRelease → KeyControlL release) → clipboard.Restore (ошибка → slog.Warn("clipboard restore failed", "error", err), НЕ отказ операции — D-29). Контент в лог не пишется НИГДЕ (запрещено). main.go: SetOptions из config.Load (correction.backspace_cap → BackspaceCap, correction.clipboard_rung → ClipboardRung); полное снапшот-потребление reload — план 03-04 (зафиксировать в SUMMARY). startDaemon стенда получает args-параметр: case-кейс пишет temp-config (clipboard_rung: true) и спавнит демон с -config.
  Живой кейс select-clipboard: если спайк-таблица нашла поверхность с поверхностью, где primary-ступени не заменяют выделение — кейс прогоняет полный round-trip на ней; если все поверхности заменяют primary-ступенью — кейс помечается unreachable-primary (пин механики Save/Set/Restore на живом столе без замены: прямая последовательность через демон невозможна — тогда кейс проверяет subprocess-механику запуском Save/Set/Restore из стенда и в SUMMARY фиксируется, что ступень живьём не достижима до появления несовместимой поверхности; решение о включении в матрицу v2 — 03-07). register; mise.toml [tasks.e2e-select-clipboard]. Прогоны + ci. Commit: feat(03-03).
  </action>
  <verify>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise exec -- go test ./internal/clipboard/ ./internal/session/ -race -count=1</automated>
    <fails_when>строка FAIL — корпус clipboard-ступени не зелёный</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && grep -q 'cmd.Stdin' internal/clipboard/clipboard.go && echo STDIN-ONLY</automated>
    <fails_when>отсутствие STDIN-ONLY — контент уходит argv-путём (утечка в ps; запрещено)</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && test "$(grep -c 'clipboard restore failed' internal/session/actor.go)" -ge 1 && echo LOG-SHAPES-OK</automated>
    <fails_when>отсутствие LOG-SHAPES-OK — WARN-форма восстановления D-29 не заведена</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise run e2e-select-clipboard && mise run ci</automated>
    <fails_when>non-zero exit — живая механика round-trip или зелёная итерация не выполнены</fails_when>
  </verify>
  <done>Ступень opt-in (дефолт OFF — пин нулевых вызовов), порядок commit/delete → clipboard (D-28), байт-точное восстановление с --clear для пустого, WARN-only неудача (D-29), контент никогда в лог и никогда в argv; Options стартово подаются из конфига</done>
  <acceptance_criteria>
  - дефолт: verify-mismatch → ноль вызовов clipboard (пин TestActor_ClipboardRungDisabledByDefault, D-28)
  - wl-paste --no-newline / wl-copy --trim-newline / wl-copy --clear — флаги пинены тестами fake-раннера (Pitfall 3 закрыт)
  - контент передаётся только через cmd.Stdin (греп-пин STDIN-ONLY)
  - последовательность ForwardKeyEvent Ctrl+V соответствует прототипу (4 события, порядок пинен sink-тестом)
  - ClipboardRung=true включает ступень только после verify-mismatch (пин порядка вызовов)
  - goswitchd/main.go подаёт SetOptions из config.Load; SUMMARY фиксирует границу: reload-потребление — 03-04
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| клиент → engine (SetSurroundingText с anchorPos) | якорь — клиентские данные: приложение может слать несогласованные cursor/anchor |
| демон → буфер обмена пользователя (wl-copy/wl-paste) | подмена системного буфера: содержимое пользователя проходит через демон |
| демон → клиент (ForwardKeyEvent Ctrl+V) | вставка поверх выделения — физическая инъекция в поле |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-03-01 | Information Disclosure | clipboard round-trip | high | mitigate | контент никогда в лог (пин); передача только stdin-пайпом — argv не виден в ps (Pitfall 3 + anti-pattern) |
| T-03-03-02 | Tampering | подмена буфера без восстановления | high | mitigate | Save байт-точно ДО подмены, Restore ПОСЛЕ (D-29); пустой исходник → wl-copy --clear; неудача — WARN (осознанный best-effort, D-29 владельца) |
| T-03-03-03 | Tampering | несогласованные cursor/anchor от клиента | medium | mitigate | SelectionRange: start=min/end=max + клэмп к len(text) (идиома beforeCursor actor.go:565-574); сверка VerifyRangeAt до/после |
| T-03-03-04 | Elevation of Privilege | subprocess-выполнение wl-copy/wl-paste | low | mitigate | фиксированные имена бинарников из PATH, аргументы — только флаги без пользовательского ввода; ctx-таймаут на каждый вызов |
| T-03-03-05 | DoS | зависший wl-clipboard | medium | mitigate | каждый subprocess с контекстным дедлайном (пин TestClipboard_Timeouts); ступень после таймаута — тихий отказ |
</threat_model>

<verification>
- mise run ci зелёный; e2e-select-smoke / e2e-select-correct / e2e-select-clipboard прогнаны живьём
- Спайк-таблица per-surface (якорь/ступень) зафиксирована в SUMMARY — вход для матрицы v2 (03-07)
- Порядок ступеней D-28 (commit/delete → clipboard) пинен тестами последовательности вызовов
</verification>

<success_criteria>
- CORR-03: двойной тап при активном выделении исправляет выделенное; без выделения — слово (D-30)
- Геометрия в обе стороны (Pitfall 6) и точность диапазона пинены корпусом
- Clipboard-ступень opt-in с best-effort restore (D-28/D-29), контент защищён (лог/argv)
- Фактическое поведение поверхностей запинено спайком (A1/A2/A5 закрыты живыми пробами)
</success_criteria>

<artifacts_produced>
## Artifacts this plan produces (символы плана 03-03)

- `engine.EventHandler.HandleSurroundingText(text string, cursorPos, anchorPos uint32)` — расширенный seam; `engine/keys.go` + `KeyV = 0x0076`
- `internal/correct`: `SelectionRange(cursor, anchor) (start, end, active)`, `VerifyRangeAt(text, start, want) bool`
- `internal/clipboard`: `Save/Set/Clear/Restore` + раннер-интерфейс; флаги wl-clipboard пинены
- `internal/session`: тип `Options{BackspaceCap int, ClipboardRung bool}`, `Actor.SetOptions(Options)`; ветка выделения; clipboard-ступень; WARN-форма "clipboard restore failed"
- test/e2e: кейсы `select-smoke`, `select-correct`, `select-clipboard`; startDaemon принимает args для -config
- mise-задачи `e2e-select-smoke`, `e2e-select-correct`, `e2e-select-clipboard`
</artifacts_produced>

<output>
Create `.planning/phases/03-frazy-vydelenie-pereklyuchenie-i-konfiguratsiya/03-03-SUMMARY.md` when done
</output>
