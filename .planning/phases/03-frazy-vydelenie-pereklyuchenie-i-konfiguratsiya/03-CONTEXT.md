# Phase 3: Фразы, выделение, переключение и конфигурация - Context

**Gathered:** 2026-09-15
**Status:** Ready for planning

<domain>
## Phase Boundary

Полный словарь жестов поверх каркаса и пословной коррекции Фаз 1–2: тройной Right Shift исправляет всю фразу из буфера, двойной при активном выделении исправляет выделенное, смешанный текст корректируется по точной семантике (закрыт Q-маркер SPEC §4.2), одиночный Right Shift переключает внутренний режим EN↔RU (механизм ADR-001 «один хозяин»), Shift+RightCtrl исправляет слово и переключает раскладку; все хоткеи/таймауты/параметры — YAML-конфиг с hot reload; `goswitchctl` (статус, reload, принудительная коррекция); MACR-01 по механизму ADR-005. Приёмка — зелёная e2e-матрица v2 полной широты. Терминальный класс (clipboard-уровень для терминалов, Q3) остаётся отложенным; поставка/приёмка — Фаза 4.

</domain>

<decisions>
## Implementation Decisions

### Смешанный текст (CORR-06 — закрыт Q-маркер SPEC §4.2)
- **D-22:** Якорь «правильной» раскладки — раскладка последней БУКВЫ корректируемого текста (буфера/фразы/выделения); конвертируются только прогоны «чужих» букв. Буквальное чтение SPEC §4.2 «относительно последней использованной». Цифры и общая пунктуация якорь не сдвигают (продолжение D-15): `ghbdtn2026привет` — якорь RU от `…вет`, конвертируется только `ghbdtn`. — **Reversibility:** costly — золотой корпус юнит-кейсов и e2e-кейсы v2 завязаны на семантику; смена после приёмки = переделка корпуса и переучивание
  - **Уточнение D-22 (2026-09-15, по итогам исследования, авто-разрешено без владельца):** якорь управляет ТОЛЬКО смешанным текстом (прогоны обеих раскладок). Однораскладочный текст конвертируется целиком по составу — поведение Фазы 2, зелёная матрица v1 (`ghbdtn`→`привет`, `привет`→`ghbdtn`) неприкосновенна. Основание: строгий якорь везде превращает базовый кейс `ghbdtn`→`привет` в no-op и ломает принятую приёмку Фазы 2; вопрос владельца D-22 был задан о смешанном тексте (CORR-06), не о чистых словах. Рекомендация исследователя (03-RESEARCH.md, Open Question Q1) принята.
- **D-23:** Гранулярность — прогонами: каждый непрерывный прогон букв одной раскладки конвертируется независимо, если он чужой относительно якоря. ЕДИНЫЙ код-путь для слова (двойной тап), фразы (тройной) и выделения: `ghbdtnпривет` → `приветпривет`; `gfbпривет` → `паипривет`. — **Reversibility:** costly — тот же корпус, что D-22
- **D-24:** «Нечего конвертировать» (все буквы уже в якорной раскладке) — УСПЕШНАЯ операция без изменений, не отказ и не WARN. Владелец выбрал это вопреки рекомендации «тихий отказ». Пустой буфер/отсутствие текста остаются тихим отказом по D-20. — **Reversibility:** reversible — правило в логике решения

### Границы фразы (CORR-02)
- **D-25:** «Вся фраза» = весь буфер с последнего жёсткого сброса (Enter, Tab, смена фокуса, Escape — SPEC §4.3), без искусственного капа на длину. Обязательная сверка с surrounding text (ADR-004) сама отрезает неваличный хвост — «abort, не мусорить». — **Reversibility:** reversible — правило в логике диапазона
- **D-26:** Фраза со словами в разных раскладках конвертируется той же семантикой прогонов (D-22/D-23): якорь = последняя буква фразы, чужие прогоны конвертируются, свои не трогаются. Отдельной «фразовой» семантики нет. — **Reversibility:** costly — единый корпус с D-22/D-23
- **D-27:** Лестница ADR-003 для фраз: DeleteSurroundingText приоритетен; ступень Backspace×N получает настраиваемый кап серии (дефолт ~50 нажатий, YAML); если обе ступени недоступны и clipboard-ступень выключена — тихий отказ D-20 с причиной в лог. — **Reversibility:** reversible — значение капа в конфиге

### Выделение и clipboard (CORR-03)
- **D-28:** Clipboard round-trip для замены выделения — opt-in по конфигу (последовательность с ADR-003 rung C «только по конфигу»); в дефолт-конфиге ВЫКЛЮЧЕН. Порядок ступеней: commit_text/DeleteSurroundingText → clipboard round-trip (если включён). — **Reversibility:** reversible — дефолт в конфиге
- **D-29:** После clipboard round-trip демон best-effort восстанавливает исходное содержимое буфера обмена (сохранить перед → вернуть после); неудача восстановления — только запись в лог, не ошибка операции. — **Reversibility:** reversible
- **D-30:** Приложение не сообщает выделение (anchor == cursor в SetSurroundingText или surrounding text недоступен) → тихий отказ D-20 с причиной в лог. Двойной тап БЕЗ активного выделения продолжает исправлять последнее слово — поведение Фазы 2 не меняется. — **Reversibility:** reversible

### YAML-конфиг и Caramba (CONF-01..03)
- **D-31:** Своя чистая схема ключей (секции уровня `hotkeys` / `timeouts` / `correction` / `macr` — точную структуру выбирает планировщик); совместимость с Caramba (CONF-03, wish) — таблицей соответствия в документации, БЕЗ клонирования Caramba-имён: их схема привязана к их модели «свитч на первый тап», наша — к ADR-002. — **Reversibility:** costly — схема = публичный контракт конфига пользователей; смена после релиза = миграция
- **D-32:** Hot reload при невалидной правке — last-good + WARN: демон продолжает работать на последней валидной конфигурации, WARN в лог, ошибка видна в `goswitchctl status`. Atomic-rename редакторов уже покрыт паттерном STACK (directory-watch + debounce, fsnotify). — **Reversibility:** reversible
- **D-33:** Строгий парсер: неизвестный ключ = невалидный конфиг (отклоняется целиком, last-good остаётся). yaml.v3 strict-режим (KnownFields) — опечатка в ключе не молчит. — **Reversibility:** reversible

### Переключение раскладки (SWCH-01..04) — примирение с ADR (перенос, не новые вопросы)
- **D-34 (перенос ADR-001/D-03, BIND):** Одиночный Right Shift переключает ВНУТРЕННИЙ режим EN↔RU за интерфейсом `layoutset`. «Один хозяин»: в источниках ввода только goswitch; Super+Space ничего не переключает (и не обязан — переключать нечего); системный индикатор GNOME всегда показывает один источник goswitch и внутренний режим НЕ отражает — индикатору не верить, компенсации нет (OSD отложена). Формулировки SWCH-03 («Super+Space и MRU продолжают работать») и критерия №3 ROADMAP («родное Super+Space… продолжают работать», «святой порядок: свитч на первый тап») УСТАРЕЛИ против принятых ADR-001/ADR-002 — верификатор фазы НЕ должен требовать их буквального исполнения; рабочая интерпретация: нативный механизм Super+Space не сломан присутствием goswitch (если пользователь добавит другие источники — механизм жив), e2e-оракул переключения — внутреннее состояние движка (решение FSM/`layoutset` в логе), НЕ gsettings. — **Reversibility:** costly — пользовательский контракт «один хозяин» (ADR-001/D-03 дословно)
- **D-35 (перенос ADR-002, BIND):** Конфликт таймингов single/double/triple уже разрешён ADR-002: классика с ожиданием, ВСЕ действия на истечении окна (дефолт 300 мс, настраивается — теперь реально через YAML), ничего на самом тапе; модификаторная дискриминация, правило четвёртого тапа, отмена посторонней клавишей — исполняются существующим корпусом `internal/hotkey/fsm_test.go`.
- **D-36 (SWCH-02, из SPEC §4.1):** Комбо Shift+RightCtrl (переназначается): ИСПРАВИТЬ последнее слово (семантика двойного тапа: сверка ADR-004, границы D-13..D-15), ЗАТЕМ переключить внутренний режим (toggle EN↔RU, тот же `layoutset`). Порядок зафиксирован именем действия в SPEC («исправить слово и переключить раскладку»). Q-маркер SPEC `[Q: конфликтует с прошлым опытом дублирования]` — дефолт остаётся Shift+RightCtrl (переназначение в YAML покрывает неудобство); живую эргономику проверяет e2e. — **Reversibility:** reversible — дефолт в конфиге

### Claude's Discretion
- Точная структура YAML-схемы (имена ключей внутри секций, формат биндингов) — планировщик; принципы D-31..D-33 фиксированы. В конфиг обязаны вместиться: окно тапов, все биндинги (вкл. комбо SWCH-02), кап Backspace (D-27), вкл/выкл clipboard-ступени (D-28), MACR: глобальный вкл/выкл + per-app списки + альтернативные модификаторы (ADR-005).
- Транспорт `goswitchctl` ↔ демон (кандидат по STACK: D-Bus на session bus — свой интерфейс демона; альтернатива — локальный сокет) — исследователь/планировщик.
- Формат вывода `goswitchctl status` (человекочитаемый; `--json` опционален) и состав: режим EN/RU, конфиг (путь, валидность, last-good ошибка), счётчики коррекций, перехваченные Super-комбинации (ADR-005).
- Механика восстановления буфера (D-29) и clipboard round-trip — исполнитель (wl-copy/wl-paste по STACK).
- Реализация MACR-01 по ADR-005: перехват Super+Буква в ProcessKeyEvent, эмуляция Ctrl+Буква через ForwardKeyEvent, AT-SPI-наблюдатель только при per-app списках; дефолт альтернативного модификатора НЕ вводится до живой проверки (ADR-005 b.3) — фаза проверяет и решает.
- Ширина e2e-матрицы v2 «разные приложения»: минимум zenity (GTK) + Chromium (уже в стенде); расширение (Electron и др.) — планировщик по результатам исследования поверхностей.
- Порядок задач, разбиение на планы, детали self-hosted раннера — исполнитель.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Спецификация и требования
- `docs/SPEC.md` §4.1 — хоткеи (тройной = фраза, двойной при выделении, одиночный = раскладка, Shift+RightCtrl с Q-маркером), §4.2 — смешанный текст (Q-маркер закрыт D-22/D-23), §4.3 — сброс буфера и «abort, не мусорить», §4.5 — MACR, §5 — NFR в редакции spec-delta ADR-002, §6 Q3 — терминалы (отложено), §7 — тестирование
- `.planning/REQUIREMENTS.md` — требования Фазы 3: CORR-02, CORR-03, CORR-06, SWCH-01..04, CONF-01..03, MACR-01, INST-02
- `.planning/ROADMAP.md` §Phase 3 — цель и критерии успеха (см. D-34/D-35: две формулировки критериев устарели против ADR)

### ADR (Accepted, гейт M0 2026-09-10 — BIND)
- `docs/adr/ADR-001-layout-switching-mechanism.md` — внутренний флип, «один хозяин», e2e-оракул по внутреннему состоянию (D-34)
- `docs/adr/ADR-002-tap-semantics.md` — классика с ожиданием, окно 300 мс, корпус FSM (D-35)
- `docs/adr/ADR-003-replacement-capability-ladder.md` — лестница замены, clipboard только по конфигу (D-27/D-28)
- `docs/adr/ADR-004-buffer-reset-triggers.md` — обязательная сверка, best-effort сброс по скачку курсора
- `docs/adr/ADR-005-macr-01-owner-checkpoint.md` — механизм MACR: глобальные правила + per-app YAML + AT-SPI-наблюдатель + лестница деградации перехвата
- `docs/adr/d01-experiment-log.md` — доказательная база отклонения two-engine

### Контексты предыдущих фаз
- `.planning/phases/01-adr-paket-i-karkas-ibus-dvizhka/01-CONTEXT.md` — D-01..D-12 (механизм, тайминг, TDD/mise/lint/CI директивы)
- `.planning/phases/02-korrektsiya-slova-en-ru/02-CONTEXT.md` — D-13..D-21 (границы слова, тихий отказ, приватность логов, e2e-матрица v1)

### Исследования
- `.planning/research/STACK.md` — yaml.v3 + fsnotify паттерн hot reload, wl-copy/wl-paste, e2e-стенд
- `.planning/research/ARCHITECTURE.md` — карта D-Bus IBus 1.5.29 (живая интроспекция)

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `internal/hotkey/fsm.go` — FSM уже эмитит Single/Double/Triple на истечении окна (ADR-002 исполнен корпусом `fsm_test.go`); Фаза 3 подключает обработчики Single (layoutset toggle) и Triple (фраза), Double расширяется веткой выделения
- `internal/correct/buffer.go` — Buffer хранит ВСЮ фразу с указателем на токен: `Token()`, `Tail()`, `ReplaceToken()`; тройной тап = замена всего диапазона — потребуется API уровня «заменить весь буфер» (расширение, не переделка)
- `internal/correct/convert.go` / `direction.go` — `Convert()` по таблицам и `Detect()`; D-23 требует обобщения до прогонов (detect по сегментам), таблицы `layouts/` переиспользуются как есть
- `engine/engine.go` — `SetSurroundingText(text, cursorPos, anchorPos)` уже декодирует anchor: детект выделения = anchor ≠ cursor; `Emitter` (CommitText/ForwardKeyEvent/DeleteSurroundingText) — ступени лестницы; recover-шимы
- `test/e2e/` — стенд: `matrix.go`/`matrix_test.go` (YAML-кейсы), `surface.go` (zenity/Chromium драйверы), `case_word.go`, `case_ladder.go`; матрица v2 надстраивается над этим каркасом
- `cmd/goswitchd/main.go` — точка сборки; `internal/session/actor.go` — сессионный актор (перерегистрация)

### Established Patterns
- Строгий TDD + гейт итерации `go build`/`go test -race`/`golangci-lint` (D-07/D-08); mise-задачи — единственный интерфейс сценариев (D-09)
- Приватность логов: INFO — счётчики/причины без содержимого, детали — `-debug` (D-20/D-21 продолжаются в D-24/D-27/D-30)
- Тихий отказ «abort, не мусорить» — общий инвариант всех новых путей (фраза, выделение, clipboard)

### Integration Points
- FSM Action → маршрутизатор действий → (Single) `layoutset` toggle; (Double) выделение-или-слово; (Triple) фраза; (комбо SWCH-02) слово+toggle
- Новый пакет `internal/config` (yaml.v3 strict + fsnotify directory-watch/debounce) → инъекция параметров во FSM (окно), correct (капы), MACR, clipboard — hot reload по D-32
- Новый `cmd/goswitchctl` ↔ демон (транспорт — на исследование) — INST-02
- MACR: ProcessKeyEvent перехват Super+Буква → ForwardKeyEvent Ctrl+Буква; AT-SPI-наблюдатель опционален при per-app списках (ADR-005)

</code_context>

<specifics>
## Specific Ideas

- Золотые примеры смешанного текста: `ghbdtnпривет` → `приветпривет`; `gfbпривет` → `паипривет` (якорь RU от последней буквы); `приветghbdtn` → `ghbdtnghbdtn` (якорь EN); цифры якорь не сдвигают: `ghbdtn2026привет` → `привет2026привет`
- Punto Switcher — референс «исправить после пробела» (D-13); Caramba — референс схемы биндингов для таблицы соответствия в доках (D-31), но НЕ модель поведения (D-04/ADR-002)
- Владелец просил пояснять незнакомые термины без жаргона (из 01-CONTEXT, действует)

</specifics>

<deferred>
## Deferred Ideas

- Терминальный класс (clipboard-уровень для терминалов, SPEC §6 Q3) — остаётся отложенным из Фазы 2 (там владелец снял терминал из v1); в требования Фазы 3 НЕ входит, кандидат на Фазу 4/v2
- OSD-уведомление при переключении раскладки (компенсация «необучаемого» индикатора, ADR-001) — вне объёма, переносится

</deferred>

---

*Phase: 3-Фразы, выделение, переключение и конфигурация*
*Context gathered: 2026-09-15*
