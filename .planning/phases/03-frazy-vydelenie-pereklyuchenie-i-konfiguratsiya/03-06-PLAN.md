---
phase: 03-frazy-vydelenie-pereklyuchenie-i-konfiguratsiya
plan: 06
type: execute
wave: 5
depends_on: [03-04, 03-05]
files_modified:
  - internal/ctlsvc/ctlsvc.go
  - internal/ctlsvc/ctlsvc_test.go
  - internal/config/watch.go
  - internal/session/actor.go
  - internal/session/actor_test.go
  - cmd/goswitchctl/main.go
  - cmd/goswitchd/main.go
  - test/e2e/case_ctl.go
  - test/e2e/main.go
  - mise.toml
autonomous: true
requirements: [INST-02]

estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`goswitchctl status` показывает: режим EN/RU, конфиг (путь, валидность, last-good ошибка), счётчики коррекций, перехваченные Super-комбинации — человекочитаемо, с опцией --json (INST-02, состав — дискреция CONTEXT)"
    - "`goswitchctl reload` принуждает перечитывание конфига: валидный — применяется (снапшот обновлён), невалидный — last-good остаётся, ошибка видна в status (D-32)"
    - "`goswitchctl correct` запускает конвейер слова принудительно (семантика двойного тапа без тапа) и сообщает исход"
    - "Транспорт — D-Bus session bus: демон владеет именем org.djarvur.goswitch (RequestNameReplyPrimaryOwner, guard по образцу engine/conn.go:93-101), объект /org/djarvur/goswitch, методы Status/ReloadConfig/CorrectNow; повторный экземпляр демона не захватывает имя (single-instance)"
    - "Каждый экспортированный метод ctlsvc начинается recover-широм — паника метода не убивает демон и ввод (INTEG-05 продолжение)"
    - "goswitchctl недоступен → понятная ошибка («демон не запущен?»), exit ≠ 0; живой кейс ctl-smoke гоняет status/reload/correct против настоящего демона"
  artifacts:
    - path: internal/ctlsvc/ctlsvc.go
      provides: "D-Bus сервис на session bus: org.djarvur.goswitch, методы Status() (s), ReloadConfig() (s), CorrectNow() (s); recover-шимы; guard первичного владельца"
    - path: cmd/goswitchctl/main.go
      provides: "CLI: подкоманды status | reload | correct, флаг --json у status; D-Bus-клиент; exit-контракт"
    - path: internal/session/actor.go
      provides: "StatusSnapshot() session.Status (mode, счётчики коррекций, super-перехваты), CorrectNow() — принудительный запуск конвейера слова под мьютексом актора"
    - path: internal/config/watch.go
      provides: "Reload() — синхронный принудительный ре-парс для ctl (публикация или last-good + ошибка)"
    - path: test/e2e/case_ctl.go
      provides: "живой кейс ctl-smoke: демон + goswitchctl status/reload/correct, включая видимость last-good ошибки"
  key_links:
    - from: internal/ctlsvc
      to: internal/session
      via: "методы зовут actor.StatusSnapshot()/CorrectNow() под мьютексом актора — никогда второй FSM"
      pattern: "StatusSnapshot"
    - from: cmd/goswitchctl
      to: internal/ctlsvc
      via: "ConnectSessionBus → Object(org.djarvur.goswitch, /org/djarvur/goswitch) → CallWithContext методов"
      pattern: "org\\.djarvur\\.goswitch"
    - from: cmd/goswitchd/main.go
      to: internal/ctlsvc
      via: "второе godbus-соединение демона (session bus) стартует/останавливается на своём ctx рядом с engine.Run"
      pattern: "ctlsvc\\."
  prohibitions:
    - statement: "goswitchctl status НЕ ДОЛЖЕН раскрывать содержимое набранного/исправленного текста или буфера обмена — только режим, счётчики, состояние конфига (D-20/D-21; privacy)"
      status: resolved
      verification: judgment
    - statement: "ctl-интерфейс НЕ ДОЛЖЕН принимать команды от других пользователей сессии — session bus с SO_PEERCRED-изоляцией того же uid; имя захвачено primary-owner guard'ом (V4)"
      status: resolved
      verification: judgment
  assumptions:
    - "FLAGGED (probe INST-02 / research A9): методов без свойств/сигналов достаточно для goswitchctl (Status/ReloadConfig/CorrectNow); расширение позже — дешёвое (новый метод на том же объекте); владелец видит состав статуса на verify"
    - "FLAGGED (транспорт): D-Bus session bus вместо локального сокета — вердикт исследователя (STACK-кандидат подтверждён: godbus уже в дереве, peer-creds/автозапуск бесплатно); владелец подтверждает на verify"
---

<objective>
Управление демоном без перезапуска: internal/ctlsvc — второй godbus-сокет демона на session bus (org.djarvur.goswitch), cmd/goswitchctl — CLI-клиент (status/reload/correct, --json). Status собирает режим, состояние конфига (last-good ошибка D-32 видна), счётчики коррекций и Super-перехватов (ADR-005 b.2). Живой кейс гоняет полный цикл против настоящего демона.

**As a** владелец/пользователь демона, **I want** командой goswitchctl посмотреть статус, перечитать конфиг и принудительно скорректировать слово, **so that** диагностика и управление не требуют перезапуска и чтения логов вручную.

Purpose: INST-02 — CLI-эксплуатация; D-32 замыкается видимостью last-good ошибки в status.
Output: internal/ctlsvc, cmd/goswitchctl, actor.StatusSnapshot/CorrectNow, watcher.Reload, кейс ctl-smoke.
</objective>

<execution_context>
@/home/nil/DiskD/W/Djarvur/goswitch/.zcode/gsd-core/workflows/execute-plan.md
@/home/nil/DiskD/W/Djarvur/goswitch/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-frazy-vydelenie-pereklyuchenie-i-konfiguratsiya/03-CONTEXT.md (дискреция: транспорт и состав статуса; D-32)
@.planning/phases/03-frazy-vydelenie-pereklyuchenie-i-konfiguratsiya/03-PATTERNS.md (§ ctlsvc — Analogs A/B: conn.go:93-101 guard, factory.go:47-56 Export, engine.go:125-133 recoverHandler; § goswitchctl — main.go:20-44 skeleton, matrix.go:388-423 клиент)
@.planning/phases/03-frazy-vydelenie-pereklyuchenie-i-konfiguratsiya/03-RESEARCH.md (§ Code Examples §5 — свереные сигнатуры ConnectSessionBus)
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: internal/ctlsvc — сервис на session bus + actor.StatusSnapshot/CorrectNow + watcher.Reload (INST-02)</name>
  <reversibility rating="costly">D-Bus-имя org.djarvur.goswitch и сигнатуры Status/ReloadConfig/CorrectNow — публичный контракт CLI; смена после релиза = миграция клиентов</reversibility>
  <files>internal/ctlsvc/ctlsvc.go, internal/ctlsvc/ctlsvc_test.go, internal/config/watch.go, internal/session/actor.go, internal/session/actor_test.go</files>
  <read_first>
  - engine/conn.go (RequestName guard :93-101 — дословный образец; sentinel errNotPrimaryOwner :24; errors.Is в serve-цикле :47-61)
  - engine/factory.go (Export :47-56 — образец экспорта)
  - engine/engine.go (recoverHandler :125-133 — шим; сигнатуры сервисных методов с (err *dbus.Error) named returns)
  - internal/session/actor.go (мьютекс-дисциплина; счётчики 03-05; mode/scriptMode :47-53)
  - internal/config/watch.go (03-02: Snapshot/LastError — Reload добавляется)
  - cmd/goswitchd/main.go (run() ctx-цикл :33-44 — место старта ctlsvc)
  - .planning/research/ARCHITECTURE.md (карта шин: имя org.djarvur.goswitch предписано)
  </read_first>
  <behavior>
  - TestSvc_Status (unit, fake actor): Status() возвращает человекочитаемую строку, содержащую mode (en/ru), счётчики коррекций done/skipped, super-перехваты, конфиг-строку (путь + ok/last-good error)
  - TestSvc_ReloadConfig: валидный путь → watcher-снапшот обновлён, ответ содержит "applied"; невалидный → last-good остался, ответ содержит ошибку (D-32)
  - TestSvc_CorrectNow: актор запускает конвейер слова (пин: RequireSurroundingText на fake sink) без тапа; исход возвращается строкой
  - TestSvc_NameGuard: RequestName не primary → ошибка запуска (sentinel errors.Is)
  - TestSvc_RecoverShim: паника внутри метода → *dbus.Error, демон жив (пин вызовом метода, паникующего через fake)
  - TestActor_StatusSnapshot: снимок содержит mode/счётчики; после коррекции+отказа счётчики инкрементированы корректно
  </behavior>
  <action>
  RED (D-07): ctlsvc_test.go + actor_test.go по behavior (пакет-заглушки). Прогон mise exec -- go test ./internal/ctlsvc/ ./internal/session/ -race -count=1; red-evidence. Commit: test(03-06).
  GREEN — internal/session/actor.go: StatusSnapshot() Status{Mode string; CorrectionsDone, CorrectionsSkipped int; SkipReasons map[string]int; SuperIntercepted, SuperUpstreamConsumed int; ConfigPath string; ConfigValid bool; ConfigError string} — заполнение под мьютексом из уже ведущихся счётчиков (счётчики 03-05; при отсутствии — завести инкременты в существующих путях done/skipped); CorrectNow() string — запуск конвейера слова (та же внутренняя точка, что Double, без FSM) — ответ немедленный (конвейер асинхронный, статус отражается в счётчиках; характер ответа зафиксировать в доке метода).
  GREEN — internal/config/watch.go: Reload() (string, error) — синхронный Load + публикация или last-good + ошибка (переиспользует общий путь с debounce-обработчиком; без гонок — под существующими атомиками).
  GREEN — internal/ctlsvc/ctlsvc.go: Run(ctx, deps) — dbus.ConnectSessionBus(), RequestName("org.djarvur.goswitch", NameFlagReplaceExisting) с guard-ом PrimaryOwner (sentinel errNotPrimaryOwner по образцу conn.go:93-101), Export(&svc{actor, watcher}, "/org/djarvur/goswitch", "org.djarvur.goswitch"); методы Status() (string, *dbus.Error), ReloadConfig() (string, *dbus.Error), CorrectNow() (string, *dbus.Error) — каждый с defer recoverHandler-аналогом (named return *dbus.Error); зависимости через маленькие интерфейсы в точке использования (StatusSnapshotProvider, Reloader, Corrector) — ctlsvc не импортирует session-типы напрямую, только интерфейсы. Останов по ctx (второе соединение закрывается).
  mise run ci зелёный. Commit: feat(03-06).
  </action>
  <verify>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise exec -- go test ./internal/ctlsvc/ ./internal/session/ ./internal/config/ -race -count=1</automated>
    <fails_when>строка FAIL — корпус ctlsvc/снапшота/Reload не зелёный</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && grep -c 'recover' internal/ctlsvc/ctlsvc.go && grep -q 'RequestNameReplyPrimaryOwner' internal/ctlsvc/ctlsvc.go && echo GUARD-OK</automated>
    <fails_when>отсутствие GUARD-OK или recover-счётчик 0 — шимы/guard не заведены (INTEG-05/V4)</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise run ci</automated>
    <fails_when>non-zero exit — D-08 не зелёный</fails_when>
  </verify>
  <done>Сервис экспортирует три метода с шимами и guard-ом; снапшот несёт mode/счётчики/конфиг-состояние; Reload синхронный; история test до feat</done>
  <acceptance_criteria>
  - имя org.djarvur.goswitch, путь /org/djarvur/goswitch — свереные строки (греп)
  - каждый метод: recover-шим + named *dbus.Error (пин паник-тестом)
  - StatusSnapshot не содержит текста коррекций (только счётчики/режим/конфиг) — код-ревью пин
  - Reload(): валид → применён; невалид → last-good + текст ошибки (пины)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: cmd/goswitchctl — CLI-клиент + живой кейс ctl-smoke + проводка в демоне</name>
  <files>cmd/goswitchctl/main.go, cmd/goswitchd/main.go, test/e2e/case_ctl.go, test/e2e/main.go, mise.toml</files>
  <read_first>
  - cmd/goswitchd/main.go (main/run skeleton :20-44 — образец; место ctlsvc.Run)
  - test/e2e/matrix.go (goswitchNameOwner :388-423 — клиентский godbus-идиом)
  - internal/ctlsvc/ctlsvc.go (Task 1 — имена методов/шина)
  - test/e2e/main.go (startDaemon :299 c args — temp-config для кейса; runCmd :475-488)
  </read_first>
  <action>
  cmd/goswitchctl/main.go: stdlib только (flag-запрет CLI-фреймворков): подкоманды os.Args[1]: status (флаг --json — однострочный JSON-вывод), reload, correct; вся логика в run(ctx) error; D-Bus-клиент: dbus.ConnectSessionBus → conn.Object("org.djarvur.goswitch", "/org/djarvur/goswitch").CallWithContext(ctx, "org.djarvur.goswitch.Status|ReloadConfig|CorrectNow", 0); ошибка отсутствия владельца имени → дружелюбный вывод "daemon not running?" + exit 1; успех — печать ответа; тестопригодная run() без os.Exit внутри (main тонкий — go-ultimate).
  cmd/goswitchd/main.go: старт ctlsvc.Run(ctx, actor, watcher) рядом с engine-циклом (второе godbus-соединение процесса; watcher передаётся при -config, иначе nil → ReloadConfig сообщает "no config file"); останов по signal-ctx.
  Живой кейс ctl-smoke (test/e2e/case_ctl.go): temp-config; spawn демона с -config; шаги: (1) runCmd go run ./cmd/goswitchctl status → exit 0, вывод содержит mode и config-строку; (2) порча конфига (неизвестный ключ) + goswitchctl reload → вывод содержит ошибку, goswitchctl status показывает last-good ошибку (D-32 видимость); (3) goswitchctl correct при набранном слове — коррекция выполняется (waitForLog correction outcome:done); (4) восстановление конфига + reload → applied. register "ctl-smoke"; mise.toml [tasks.e2e-ctl]. Прогоны живьём + ci. Commit: feat(03-06).
  </action>
  <verify>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise run build && go vet ./cmd/goswitchctl/ && grep -q 'org.djarvur.goswitch' cmd/goswitchctl/main.go && echo CTL-OK</automated>
    <fails_when>отсутствие CTL-OK или non-zero exit — клиент не собирается или имя не то</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise run e2e-ctl</automated>
    <fails_when>non-zero exit или отсутствие PASS ctl-smoke — полный цикл status/reload(ошибка→last-good)/correct не работает живьём</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise run ci</automated>
    <fails_when>non-zero exit — D-08 не зелёный</fails_when>
  </verify>
  <done>goswitchctl работает живьём против демона (status/reload/correct), last-good ошибка видна в status (D-32), демоном владеет ctl-соединение на ctx</done>
  <acceptance_criteria>
  - подкоманды status/reload/correct + --json у status; exit ≠ 0 при ошибке/демон-недоступен
  - вывод "daemon not running?" при отсутствии владельца имени (пин)
  - e2e ctl-smoke PASS: все 4 шага включая видимость last-good ошибки
  - cmd/goswitchctl без CLI-фреймворков (stdlib flag/os.Args — греп-пин отсутствия сторонних импортов)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session bus → ctlsvc | входящие D-Bus-вызовы Methods — любой процесс того же пользователя |
| goswitchctl → демон | CLI-клиент запускается пользователем; команда CorrectNow запускает конвейер замены текста |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-06-01 | Spoofing | захват имени org.djarvur.goswitch чужим процессом | medium | mitigate | RequestNameReplyPrimaryOwner guard + sentinel (conn.go-паттерн); session bus SO_PEERCRED — тот же uid |
| T-03-06-02 | DoS | паника в методе ctlsvc убивает демон | high | mitigate | recover-шим на каждом экспортированном методе (INTEG-05; пин паник-тестом) |
| T-03-06-03 | Information Disclosure | status раскрывает пользовательский текст | high | mitigate | StatusSnapshot — режим/счётчики/конфиг только; текст коррекций не включается (запрет D-20) |
| T-03-06-04 | Tampering | CorrectNow от любого процесса пользователя | low | accept | однопользовательская сессия (ASVS L1): любой процесс пользователя и так владеет той же сессией; воздействие в рамках uid |
</threat_model>

<verification>
- mise run ci зелёный; e2e-ctl живьём PASS
- Имя/путь/методы сверены с ARCHITECTURE.md; goswitchctl человекочитаем + --json
- Last-good ошибка видна в status при битом конфиге (D-32 закрыт видимостью)
</verification>

<success_criteria>
- INST-02: три подкоманды работают против живого демона; exit-контракт соблюдён
- D-32: last-good диагностируется извне демона
- Счётчики MACR видны в статусе (ADR-005 b.2 замкнут)
</success_criteria>

<artifacts_produced>
## Artifacts this plan produces (символы плана 03-06)

- `internal/ctlsvc`: `Run(ctx, deps)`, D-Bus имя org.djarvur.goswitch, путь /org/djarvur/goswitch, методы Status/ReloadConfig/CorrectNow, sentinel errNotPrimaryOwner
- `internal/session`: тип `Status` (Mode, CorrectionsDone/CorrectionsSkipped, SkipReasons, SuperIntercepted, SuperUpstreamConsumed, ConfigPath, ConfigValid, ConfigError), `Actor.StatusSnapshot()`, `Actor.CorrectNow()`
- `internal/config`: `Watcher.Reload() (string, error)`
- `cmd/goswitchctl`: бинарник, подкоманды status (--json) / reload / correct
- `cmd/goswitchd`: старт ctlsvc.Run на signal-ctx
- test/e2e: кейс `ctl-smoke`; mise-задача `e2e-ctl`
</artifacts_produced>

<output>
Create `.planning/phases/03-frazy-vydelenie-pereklyuchenie-i-konfiguratsiya/03-06-SUMMARY.md` when done
</output>
