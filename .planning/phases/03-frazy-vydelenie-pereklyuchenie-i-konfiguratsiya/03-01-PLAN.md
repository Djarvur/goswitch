---
phase: 03-frazy-vydelenie-pereklyuchenie-i-konfiguratsiya
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/correct/runs.go
  - internal/correct/runs_test.go
  - internal/correct/buffer.go
  - internal/correct/buffer_test.go
  - internal/correct/plan.go
  - internal/correct/plan_test.go
  - internal/session/actor.go
  - internal/session/actor_test.go
  - test/e2e/case_phrase.go
  - test/e2e/case_word.go
  - test/e2e/cases/matrix-v1.yaml
  - test/e2e/main.go
  - mise.toml
# scope_note: 13 files accepted (checker scope_sanity) — 4 *_test.go pairs mandated by strict TDD (D-07);
# case_word.go/matrix-v1.yaml carry this task's own verify (D-16→D-23 succession must land in the same
# iteration as the semantics change, else e2e-matrix goes red — green iteration D-08); estimate 30k = 30% of 100k.
autonomous: true
requirements: [CORR-02, CORR-06]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Тройной Right Shift исправляет всю фразу из буфера с последнего жёсткого сброса (D-25): живой e2e-кейс phrase-en-ru даёт `ghbdtn ghbdtn` → `привет привет` в zenity (CORR-02)"
    - "Смешанный текст корректируется прогонами (D-22/D-23): якорь = раскладка последней БУКВЫ, чужие прогоны конвертируются каждый независимо, свои не трогаются; золотой корпус пинит дословно: `ghbdtnпривет`→`приветпривет`, `gfbпривет`→`паипривет`, `приветghbdtn`→`ghbdtnghbdtn`, `ghbdtn2026привет`→`привет2026привет` (цифры якорь не сдвигают) (CORR-06)"
    - "Однородный диапазон конвертируется целиком в другую раскладку (уточнение D-22: якорь управляет ТОЛЬКО смешанным текстом) — базовые кейсы Фазы 2 `ghbdtn`→`привет`, `привет`→`ghbdtn` и матрица v1 остаются зелёными"
    - "«Нечего конвертировать» (все буквы уже в якорной раскладке) — УСПЕШНАЯ операция без изменений, не отказ и не WARN (D-24); пустой буфер/нет букв — тихий отказ D-20 с причиной в лог"
    - "Ступень Backspace×N для фраз получает кап серии (дефолт 50, D-27); превышение капа при недоступном DeleteSurroundingText → тихий отказ с причиной backspace-cap, НИ ОДИН символ не удаляется"
    - "ЕДИНЫЙ код-путь конверсии для слова (двойной тап) и фразы (тройной): актёр зовёт correct.ConvertRuns для любого диапазона — арифметика Detect+Convert не дублируется в актёре (D-23)"
    - "Смена семантики смешанного слова (D-16 Фазы 2 → D-23 Фазы 3): кейс word-mixed и строка матрицы v1 обновлены на ожидание конверсии чужих прогонов (`gfbпривет`→`паипривет`) — запланированная сукцессия, зафиксированная в SUMMARY"
  artifacts:
    - path: internal/correct/runs.go
      provides: "ConvertRuns(text []rune) (out []rune, changed bool, ok bool) — run-сегментация с якорем последней буквы; ok=false = отказ (нет букв/немапленная буква), changed=false = успех-без-изменений D-24"
    - path: internal/correct/buffer.go
      provides: "Phrase() []rune — клон всей фразы; ReplacePhrase(repl []rune) — замена [0, len) c recompute()"
    - path: internal/correct/plan.go
      provides: "BuildPlan принимает кап серии Backspace; const DefaultBackspaceCap = 50; план уровня 2 не строится при n > кап"
    - path: internal/session/actor.go
      provides: "конвейер, параметризованный диапазоном (слово/фраза): Triple → фраза целиком; конверсия через ConvertRuns; причина отказа backspace-cap"
    - path: test/e2e/case_phrase.go
      provides: "живые кейсы phrase-en-ru (однородная фраза) и phrase-mixed (флип → RU-набор → тройной тап)"
  key_links:
    - from: internal/session/actor.go
      to: internal/correct/runs.go
      via: "актёр конвертирует любой диапазон (слово/фраза) вызовом ConvertRuns — единый код-путь D-23"
      pattern: "ConvertRuns"
    - from: internal/session/actor.go
      to: internal/correct/buffer.go
      via: "Triple → buf.Phrase() → замена buf.ReplacePhrase(converted)"
      pattern: "\\.(Phrase|ReplacePhrase)\\("
    - from: internal/correct/plan.go
      to: internal/session/actor.go
      via: "кап Backspace ограничивает Plan уровня 2; актёр отказывается причиной backspace-cap (D-27)"
      pattern: "backspace-cap"
  prohibitions:
    - statement: "Коррекция НЕ ДОЛЖНА срабатывать без явного жеста — конвейер запускается только решением FSM Double/Triple или комбо; никакой автокоррекции при наборе (осознанно отвергнуто владельцем, Out of Scope REQUIREMENTS.md)"
      status: resolved
      verification: judgment
    - statement: "Журнал НЕ ДОЛЖЕН содержать содержимое фраз/слов на уровне INFO — только причины/счётчики; исход→результат — исключительно -debug (D-20/D-21)"
      status: resolved
      verification: judgment
  assumptions:
    - "FLAGGED (probe CORR-02 / research A3): surrounding-окно клиента может не покрывать длинную фразу — сверка ADR-004 обязана сверять доступный суффикс и тихо отказывать при невалидной голове; живая проба на фразах ~50–100 рун включается в матрицу v2 (03-07); до неё поведение пинится юнит-кейсом сверки усечённого окна"
    - "FLAGGED (probe CORR-06 / research A6 / Open Question Q1): якорь D-22 применяется ТОЛЬКО к смешанным диапазонам, однородный конвертируется целиком — интерпретация закреплена уточнением D-22 в 03-CONTEXT.md (2026-09-15); золотой корпус пинит её, на verify-гейте владелец подтверждает корпус"
---

<objective>
Трассер фазы: тройной Right Shift исправляет всю фразу из буфера (`ghbdtn ghbdtn` → `привет привет`) сквозным конвейером актёра — FSМ Triple → диапазон-фраза → сверка ADR-004 → лестница ADR-003 — живьём в zenity. Вторая задача — голова чистой логики фазы: run-сегментация смешанного текста (D-22/D-23/D-24) как единый код-путь слова/фразы с золотым корпусом дословно из CONTEXT.

**As a** пользователь, набравший целую фразу не в той раскладке, **I want** тройным нажатием Right Shift исправить всю фразу разом, **so that** не исправлять слово за словом — а смешанные фразы корректируются точечно: конвертируется только часть, набранная не той раскладкой.

Purpose: CORR-02 + CORR-06 — два из трёх новых диапазонов коррекции; последующие планы (03-03 выделение) надстраиваются на этот доказанный срез.
Output: internal/correct/runs.go + расширения Buffer/BuildPlan, конвейер фразы в актёре, живые кейсы phrase-en-ru / phrase-mixed, mise-задача e2e-phrase, обновлённая семантика word-mixed (сукцессия D-16 → D-23).
</objective>

<execution_context>
@/home/nil/DiskD/W/Djarvur/goswitch/.zcode/gsd-core/workflows/execute-plan.md
@/home/nil/DiskD/W/Djarvur/goswitch/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-frazy-vydelenie-pereklyuchenie-i-konfiguratsiya/03-CONTEXT.md (D-22..D-27 — заперты)
@.planning/phases/03-frazy-vydelenie-pereklyuchenie-i-konfiguratsiya/03-PATTERNS.md (§ runs.go, § buffer.go, § plan.go — образцы с номерами строк)
@docs/adr/ADR-003-replacement-capability-ladder.md
@docs/adr/ADR-004-buffer-reset-triggers.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Трассер — тройной Right Shift исправляет фразу `ghbdtn ghbdtn`→`привет привет` в zenity: Triple→диапазон-фраза→сверка→лестница (CORR-02, D-07)</name>
  <reversibility rating="costly">Конвейер, параметризованный диапазоном, — сечение актёра, на которое опираются фраза (здесь) и выделение (03-03); смена позже = переделка трёх диапазонов сразу</reversibility>
  <precondition>Фазы 1–2 завершены: mise exec -- go test ./... -race -count=1 зелёный на текущем HEAD. Живая GNOME Wayland-сессия доступна: непусто $WAYLAND_DISPLAY, ibus-daemon отвечает (ibus address), префлайт go run ./test/e2e -case m1-gate зелёный</precondition>
  <files>internal/correct/buffer.go, internal/correct/buffer_test.go, internal/session/actor.go, internal/session/actor_test.go, test/e2e/case_phrase.go, test/e2e/main.go, mise.toml</files>
  <read_first>
  - .planning/phases/03-frazy-vydelenie-pereklyuchenie-i-konfiguratsiya/03-RESEARCH.md (§ Code Examples §1/§7 — врезка Triple actor.go:253-269; Pitfall 9 — длинная фраза)
  - .planning/phases/03-frazy-vydelenie-pereklyuchenie-i-konfiguratsiya/03-PATTERNS.md (§ buffer.go — ReplaceToken buffer.go:100-111 и slices.Clone идиома Token() buffer.go:75-82; § actor.go — диспетчеризация actor.go:253-269, startCorrection actor.go:413-464)
  - internal/correct/buffer.go (весь — активный диапазон, recompute)
  - internal/correct/plan.go (BuildPlan plan.go:43-61 — фраза передаётся как token с пустым tail)
  - internal/correct/verify.go (MatchesSuffix — сверка фразы целиком)
  - internal/session/actor.go (весь — особенно startCorrection, executeCorrection, pendingFix)
  - internal/session/actor_test.go (fake-sink и captureLogs — расширяемый корпус)
  - test/e2e/case_word.go (runWordENRU — скелет кейса; word-mixed — обновляется в Task 2)
  - test/e2e/main.go (pickCase-реестр, waitForLog, injectText/injectKeys)
  - docs/SPEC.md §4.1 (тройной тап = фраза), §4.3 (границы жёсткого сброса — D-25)
  </read_first>
  <behavior>
  - TestBuffer_PhraseReplace (correct): push "ghbdtn ghbdtn" → Phrase() возвращает всю строку; ReplacePhrase("привет привет") → Phrase() == "привет привет", последующий Token() пересчитан от новой руны (recompute-инвариант)
  - TestActor_TripleTapCorrectsPhrase (session, fake-sink): кормление печатных "ghbdtn ghbdtn" → тройной тап shift-r → expiry → RequireSurroundingText на sink; HandleSurroundingText("abc ghbdtn ghbdtn", 19) → ровно DeleteSurroundingText(-13, 13) и CommitText("привет привет"); INFO {"msg":"correction","outcome":"done"}
  - TestActor_TripleTapEmptyBuffer: тройной тап при пустом буфере → INFO {"msg":"correction skipped","reason":"empty-buffer"}, ноль разрушительных вызовов на sink (D-20/D-25)
  - TestActor_PhraseVerifyMismatch: сверка не сошлась (невалидная голова surrounding) → reason:"verify-mismatch", ноль вызовов Delete/Commit («abort, не мусорить», D-25)
  - Живой кейс phrase-en-ru: zenity: injectText("ghbdtn ghbdtn") → три нажатия shift-r в окне → waitForLog correction outcome:done → stdout-оракул == "привет привет"
  </behavior>
  <action>
  RED (D-07): buffer_test.go — кейсы Phrase/ReplacePhrase против текущего Buffer (методов нет — компиляция теста падает на отсутствии поведения: добавить методы-заглушки с пустым телом для подлинного RED на утверждениях, стиль 02-03). actor_test.go — TestActor_TripleTap… против текущего актора (Triple сейчас stub «action deferred» — падает на утверждениях: вызовов sink нет). Прогнать mise exec -- go test ./internal/correct/ ./internal/session/ -race -count=1 — подлинный RED; записать red-evidence (gsd check tdd-red-evidence, каталог red-evidence/ фазы 03). Commit: test(03-01).
  GREEN — internal/correct/buffer.go: метод Phrase() []rune — клон b.runes (идиома slices.Clone из Token() buffer.go:75-82); метод ReplacePhrase(repl []rune) — заменить [0, len(b.runes)) по образцу ReplaceToken buffer.go:100-111 (next-слайс, затем b.recompute() — токены пересчитываются, буфер зеркалит поле).
  GREEN — internal/session/actor.go: диспетчер actor.go:253-269 — case hotkey.Triple вместо Debug-заглушки зовёт конвейер фразы. Рефакторинг без смены семантики слова: тело startCorrection расчленить на параметризованный внутренний запуск (token, tail []rune, replace func(converted []rune)) — путь слова передаёт Token()/Tail()/ReplaceToken КАК СЕГОДНЯ (корпус Фазы 2 остаётся зелёным без правок ожиданий), путь фразы — Phrase()/nil/ReplacePhrase (D-23: один конвейер, разные диапазоны). Направление фразы в трассере: homogeneous — Detect(letters) даёт dir, Convert(phrase, dir) проходит пробел тождественно (convert.go:15-40); смешанная фраза в трассере — отказ mixed-script (Task 2 заменяет на ConvertRuns). Сверка ADR-004: MatchesSuffix по всему диапазону фразы; невалидная голова/хвост → verify-mismatch отказ (D-25). Логи: существующие контракты {"msg":"correction","outcome":"done"} / {"msg":"correction skipped","reason":…} не переименовывать (матрица грепает точную форму, matrix.go:663).
  Затем test/e2e/case_phrase.go: runPhraseENRU по скелету runWordENRU (case_word.go): activateGoswitch → openEntrySurface → focusWitness → injectText("ghbdtn ghbdtn") → injectKeys("shift-r","shift-r","shift-r") → waitForLog correction outcome:done → closeZenity → stdout-оракул == "привет привет"; register "phrase-en-ru" в pickCase (main.go). mise.toml: [tasks.e2e-phrase] по конвенции e2e-word (описание с "(live GNOME session; NOT in ci)"). mise run ci зелёный. Commit: feat(03-01).
  </action>
  <verify>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise exec -- go test ./internal/correct/ ./internal/session/ -race -count=1</automated>
    <fails_when>строка FAIL в выводе или non-zero exit — корпус буфера/актёра не зелёный под -race</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise run ci</automated>
    <fails_when>non-zero exit — build/vet/golangci-lint/test -race не зелёные (D-08)</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise run e2e-phrase</automated>
    <fails_when>non-zero exit или отсутствие PASS phrase-en-ru в выводе; либо stdout-оракул отличен от "привет привет" — сквозной путь фразы не работает на живом столе</fails_when>
  </verify>
  <done>Тройной тап исправляет фразу живьём (phrase-en-ru PASS, оракул "привет привет"), корпус Buffer/актёра зелёный под -race, путь слова не изменил ожиданий корпуса Фазы 2, mise run ci зелёный; история test(03-01) до feat(03-01)</done>
  <acceptance_criteria>
  - internal/correct/buffer.go экспортирует Phrase() []rune и ReplacePhrase([]rune); после ReplacePhrase вызван recompute (Token() отражает новую фразу — пин тестом)
  - actor.go: case hotkey.Triple запускает конвейер с диапазоном-фразой; путь Double использует те же внутренности с диапазоном-словом (единый код-путь D-23)
  - actor_test.go: пустой буфер → ноль вызовов Delete/Commit на sink; verify-mismatch → ноль разрушительных вызовов
  - двойной тап после Тask 1 ведёт себя ровно как до (корпус Фазы 2 зелёный без правок ожиданий — регресс-арест)
  - лог-контракты "correction"/"correction skipped" не переименованы (греп-стабильность матрицы)
  - mise run e2e-word остаётся PASS (регресс слова)
  </acceptance_criteria>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Run-сегментация D-22/D-23/D-24 — золотой корпус + единая конверсия всех диапазонов + кап Backspace D-27 (CORR-06/CORR-02)</name>
  <reversibility rating="costly">Семантика якоря и прогонов (D-22/D-23) — золотой корпус и матрица v2 завязаны на неё; смена после приёмки = переделка корпуса и переучивание (reversibility D-22/D-23/D-26 из CONTEXT)</reversibility>
  <files>internal/correct/runs.go, internal/correct/runs_test.go, internal/correct/plan.go, internal/correct/plan_test.go, internal/session/actor.go, internal/session/actor_test.go, test/e2e/case_phrase.go, test/e2e/case_word.go, test/e2e/cases/matrix-v1.yaml, mise.toml</files>
  <read_first>
  - .planning/phases/03-frazy-vydelenie-pereklyuchenie-i-konfiguratsiya/03-CONTEXT.md (D-22 + уточнение 2026-09-15, D-23, D-24, D-26, D-27 — дословно)
  - .planning/phases/03-frazy-vydelenie-pereklyuchenie-i-konfiguratsiya/03-PATTERNS.md (§ runs.go — скан direction.go:22-42, letterCapable split buffer.go:152-168, стиль корпуса buffer_test.go:11-23 и таблиц buffer_test.go:142-166)
  - internal/correct/direction.go (Detect — скан по рунам как образец)
  - internal/correct/convert.go (Convert — переиспользуется КАК ЕСТЬ per прогон; не форкать)
  - internal/correct/plan.go (BuildPlan + #nosec G115 комментарии — переоценить для фразы, Pitfall 9)
  - internal/session/actor.go (место вызова Detect/Convert в конвейере Task 1)
  - test/e2e/case_word.go (кейс word-mixed — сукцессия D-16 → D-23)
  - test/e2e/cases/matrix-v1.yaml (строка word-mixed — ожидание меняется)
  </read_first>
  <behavior>
  - TestConvertRuns_GoldenCorpus (correct, дословно из CONTEXT specifics): ghbdtnпривет→приветпривет (якорь RU от …вет); gfbпривет→паипривет; приветghbdtn→ghbdtnghbdtn (якорь EN); ghbdtn2026привет→привет2026привет (цифры нейтральны, D-15/D-22)
  - TestConvertRuns_HomogeneousWholesale: ghbdtn→привет и привет→ghbdtn целиком (уточнение D-22: якорь — только для смешанных; непрерывность CORR-01/04 и матрицы v1); регистр посимвольно: Gfbпривет→Паипривет
  - TestConvertRuns_NothingToConvert (D-24): все буквы в якорной раскладке (например привет, якорь RU) → out == input, changed=false, ok=true — УСПЕХ без изменений, НЕ отказ
  - TestConvertRuns_Refusals: нет букв вообще (только цифры/пунктуация) → ok=false; немапленная буква → ok=false, конвертация не выполнена частично (D-20 — тихий отказ на стороне актора)
  - TestBuildPlan_BackspaceCap (D-27): token+tail длиной > кап → уровень 2 не строится; длина == кап → строится ровно на кап; DefaultBackspaceCap == 50
  - TestActor_MixedWordConvertsForeignRuns (сукцессия D-16→D-23): буфер/поле "gfbпривет", двойной тап, сверка → CommitText("паипривет") ровно по диапазону
  - TestActor_PhraseMixedCorrects (D-26): фраза "ghbdtn привет", тройной тап, сверка → CommitText("привет привет") — той же семантикой прогонов, отдельной фразовой семантики нет
  - Живой кейс phrase-mixed: zenity: injectText("ghbdtn") → одиночный shift-r (флип в RU, SWCH-01 путь 02-04) → injectText("привет") (RU-коммиты движка) → тройной тап → stdout-оракул == "привет привет"
  </behavior>
  <action>
  RED (D-07): internal/correct/runs_test.go — золотой корпус behavior (файл runs.go с пустыми заглушками для подлинного RED на утверждениях); plan_test.go — кейсы капа (BuildPlan ещё без капа — падение на утверждении «уровень 2 не строится»); actor_test.go — TestActor_MixedWord… и TestActor_PhraseMixed… против текущего актора (смешанное сейчас отказывает mixed-script — падение на утверждении CommitText). Прогнать mise exec -- go test ./internal/correct/ ./internal/session/ -race -count=1; red-evidence; Commit: test(03-01).
  GREEN — internal/correct/runs.go: скан прогонов — каждый максимальный прогон букв одного письма (unicode.Is Latin/Cyrillic по образцу direction.go:22-42) есть сегмент; нейтральные руны (цифры/общая пунктуация — размечивание по letterCapable-разбиению buffer.go:152-168) не образуют сегментов и якорь не сдвигают; якорь = письмо последней буквы диапазона; чужие сегменты конвертируются каждый независимо вызовом Convert(convert.go:15-40) КАК ЕСТЬ; API ConvertRuns(text []rune) (out []rune, changed bool, ok bool) —语义 D-22/D-23/D-24: ok=false (нет букв/немапленная буква), changed=false (D-24 успех-без-изменений), иначе changed=true. Пакет остаётся чистым: ни D-Bus, ни часов, ни горутин (контракт doc buffer.go:1-9).
  GREEN — internal/correct/plan.go: BuildPlan получает кап (параметр backspaceCap int; константа DefaultBackspaceCap = 50); при len(token)+len(tail) > backspaceCap уровень 2 не строится (план помечает уровень 2 недоступным); #nosec G115-обоснования переоценены для фразы (Pitfall 9 — surrounding-окно клиента ограничено).
  GREEN — internal/session/actor.go: шаг конверсии конвейера (оба диапазона — слово и фраза) заменяет Detect+Convert на ConvertRuns: отказ ok=false → тихий отказ D-20 (причины no-letters / convert-failed в INFO); changed=false → УСПЕХ без изменений — выполнить ReplacePhrase/ReplaceToken не нужно, лог outcome:done, отдельный WARN не ставить (D-24 — выбор владельца); смешанное больше не отказывает. Кап: актёр передаёт correct.DefaultBackspaceCap (конфиг-подача — план 03-04); отказ уровня 2 по капу при недоступном уровне 1 → reason:"backspace-cap" (D-27).
  Сукцессия D-16 → D-23 (запланированная, не регресс): test/e2e/case_word.go — кейс word-mixed: оракул меняется с «нетронуто» на "паипривет"; test/e2e/cases/matrix-v1.yaml — строка word-mixed: ожидание конверсии чужих прогонов; actor_test.go — TestActor_MixedWordUntouched переименован/заменён по behavior. Изменение семантики зафиксировать в SUMMARY отдельным пунктом (владелец видит на verify). Живой кейс phrase-mixed в case_phrase.go (behavior, шаги выше); register "phrase-mixed" в pickCase; mise.toml [tasks.e2e-phrase-mixed]. mise run ci зелёный; живые прогоны e2e-phrase, e2e-phrase-mixed, e2e-word-mixed, e2e-matrix. Commit: feat(03-01). REFACTOR при очевидных улучшениях без смены поведения.
  </action>
  <verify>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise exec -- go test ./internal/correct/ ./internal/session/ -race -count=1</automated>
    <fails_when>строка FAIL в выводе — золотой корпус runs/кап/актёр не зелёный</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise exec -- go test ./internal/correct/ -run 'TestConvertRuns|TestBuildPlan' -race -count=1 -v</automated>
    <fails_when>non-zero exit или отсутствие строк PASS TestConvertRuns_GoldenCorpus/TestBuildPlan_BackspaceCap в -v-выводе — фильтр не нашёл тесты (пустой прогон)</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise run ci</automated>
    <fails_when>non-zero exit — зелёная итерация D-08 не достигнута</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise run e2e-phrase-mixed && mise run e2e-word-mixed</automated>
    <fails_when>non-zero exit или отсутствие PASS phrase-mixed/PASS word-mixed в выводе — сукцессия D-23 или фразовая семантика D-26 не работает живьём</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise run e2e-matrix</automated>
    <fails_when>non-zero exit или любой FAIL в отчёте матрицы v1 — обновлённое ожидание word-mixed или базовые кейсы сломаны</fails_when>
  </verify>
  <done>Золотой корпус прогонов зелёный дословно по CONTEXT; слово/фраза/смешанный текст идут одним код-путем ConvertRuns; кап Backspace пинится; матрица v1 зелёная с обновлённой строкой word-mixed; история test до feat</done>
  <acceptance_criteria>
  - internal/correct/runs.go экспортирует ConvertRuns; золотые примеры CONTEXT specifics пиниты в runs_test.go с комментарием D-22/D-23 у каждого кейса
  - однородный диапазон конвертируется целиком (ghbdtn→привет) — уточнение D-22 исполнено, матрица v1 зелёная
  - D-24: changed=false возвращает ok=true — актёр логирует outcome:done без WARN и без вызовов замены (пин actor_test)
  - BuildPlan(backspaceCap): n > кап → уровень 2 не строится; DefaultBackspaceCap == 50 (D-27)
  - actor.go не содержит вызовов Detect (греп-пин: конверсия только через ConvertRuns) — за исключением, если Detect остаётся для иных целей, зафиксировать в SUMMARY
  - word-mixed живой кейс даёт "паипривет"; в SUMMARY — пункт о сукцессии D-16 → D-23 для владельца
  - mise run e2e-matrix зелёная (обновлённая v1)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| клиент → engine (SetSurroundingText) | текст поля — данные клиентского происхождения; фраза может превышать surrounding-окно клиента |
| engine → клиент (Delete/Commit/Forward) | исходящие сигналы меняют текст в поле пользователя — фраза = самый большой диапазон замены фазы |
| конфигурация капа | значение из YAML (появляется планом 03-02) ограничивает разрушительную серию Backspace |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01-01 | Tampering | конвейер фразы (большой диапазон) | high | mitigate | сверка ADR-004 по всему диапазону ДО удаления: невалидная голова/хвост → verify-mismatch, ноль эмиттов (пин TestActor_PhraseVerifyMismatch) |
| T-03-01-02 | DoS | серия Backspace×N для длинной фразы | high | mitigate | кап серии D-27 (дефолт 50, DefaultBackspaceCap): превышение → тихий отказ backspace-cap, ноль удалений (пин TestBuildPlan_BackspaceCap) |
| T-03-01-03 | Tampering | run-конвертация смешанного текста | medium | mitigate | Convert не форкается: немапленная буква валит конвертацию диапазона целиком (ok=false → отказ), частичная порча невозможна |
| T-03-01-04 | Information Disclosure | логи коррекции фраз | high | mitigate | D-20/D-21: INFO — только outcome/reason; исход→результат — -debug; пин тестом лога (продолжение корпуса Фазы 2) |
</threat_model>

<verification>
- mise run ci зелёный (build + vet + golangci-lint + test -race, D-08)
- mise run e2e-phrase && mise run e2e-phrase-mixed && mise run e2e-word-mixed && mise run e2e-matrix — все PASS на живой сессии
- Золотой корпус ConvertRuns соответствует примерам CONTEXT specifics дословно (четыре примера прогонов + D-24 + однородные)
</verification>

<success_criteria>
- CORR-02 доказан трассером (фраза целиком, сверка, лестница, живой кейс) и капом D-27
- CORR-06 доказан золотым корпусом прогонов + живыми кейсами (phrase-mixed, word-mixed)
- Единый код-путь D-23: слово и фраза идут через один конвейер с разными диапазонами; выделение (03-03) врежется в то же место
- Сукцессия D-16 → D-23 задокументирована в SUMMARY для verify-гейта владельца
</success_criteria>

<artifacts_produced>
## Artifacts this plan produces (символы плана 03-01)

- `internal/correct`: новые `ConvertRuns([]rune) ([]rune, bool, bool)` (runs.go), `Buffer.Phrase() []rune`, `Buffer.ReplacePhrase([]rune)`, `BuildPlan` с параметром backspaceCap, константа `DefaultBackspaceCap = 50`, причина отказа `backspace-cap`
- `internal/session/actor.go`: конвейер, параметризованный диапазоном (внутренний запуск token/tail/replace); ветка Triple; конверсия через ConvertRuns; причины no-letters / convert-failed
- test/e2e: кейсы реестра `phrase-en-ru`, `phrase-mixed` (case_phrase.go); обновлённый ораколь `word-mixed` (case_word.go, matrix-v1.yaml)
- mise-задачи `e2e-phrase`, `e2e-phrase-mixed`
- Лог-контракты: без новых INFO-форм — используются существующие "correction"/"correction skipped" (расширен словарь причин)
</artifacts_produced>

<output>
Create `.planning/phases/03-frazy-vydelenie-pereklyuchenie-i-konfiguratsiya/03-01-SUMMARY.md` when done
</output>
