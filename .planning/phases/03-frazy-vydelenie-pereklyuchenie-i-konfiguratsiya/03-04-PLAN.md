---
phase: 03-frazy-vydelenie-pereklyuchenie-i-konfiguratsiya
plan: 04
type: execute
wave: 3
depends_on: [03-02, 03-03]
files_modified:
  - internal/hotkey/fsm.go
  - internal/hotkey/fsm_test.go
  - internal/hotkey/names.go
  - engine/keys.go
  - internal/session/actor.go
  - internal/session/actor_test.go
  - cmd/goswitchd/main.go
  - test/e2e/case_combo.go
  - test/e2e/main.go
  - mise.toml
# scope_note: 10 files accepted (checker scope_sanity) — fsm_test.go is the guarded ADR-002 corpus
# (mechanical constructor edits only, D-35) and cannot be split from fsm.go; names.go/keys.go are constant
# tables consumed by the combo branch in the same task; estimate 28k = 28% of 100k.
autonomous: true
requirements: [SWCH-01, SWCH-02, SWCH-03, CONF-02]

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Одиночный Right Shift переключает ВНУТРЕННИЙ режим EN↔RU (SWCH-01, D-34/ADR-001): e2e-оракул — запись mode в логе демона, НЕ gsettings; живой кейс layout-single пинит флип в обе стороны"
    - "Комбо Shift+RightCtrl (переназначается через hotkeys.word_layout_combo): ИСПРАВИТЬ последнее слово (семантика двойного тапа: сверка ADR-004, границы D-13..D-15), ЗАТЕМ переключить режим — порядок зафиксирован именем действия SPEC (D-36); живой кейс combo-word-layout даёт ghbdtn→привет + смену mode"
    - "Родное GNOME-переключение Super+Space не сломано присутствием goswitch (рабочая интерпретация D-34): инъекция super+space не роняет демон и не снимает имя; буквальный MRU-критерий устарел и не проверяется"
    - "Hot reload конфига действует на живом демоне (CONF-02): правка tap_window_ms/комбо-биндинга/капа/clipboard-флага применяется без перезапуска — НОВЫЕ серии используют новое окно; взведённый таймер доживает по старому (Pitfall 8); невалидная правка — last-good (D-32)"
    - "Ключевая клавиша серии тапов конфигурируема (hotkeys.tap_key, дефолт shift_r): корпус FSM (11 тестов) зелёный — правки только механические (аргумент конструктора), семантика ADR-002 не тронута (D-35)"
    - "Распознавание комбо не ломает модификаторную дискриминацию остальных клавиш (Pitfall 4): TestFSM_ModifierUse и весь корпус остаются зелёными; Control_R-press при зажатом Shift_R даёт действие, а не молчаливое гашение серии"
  artifacts:
    - path: internal/hotkey/fsm.go
      provides: "NewFSM(window, tapKeyval uint32) — конфигурируемая ключевая клавиша; DefaultWindow остаётся документированным дефолтом"
    - path: engine/keys.go
      provides: "KeyControlR = 0xffe4 (провенанс ibuskeysyms.h:191) для форварда и сверки"
    - path: internal/session/actor.go
      provides: "распознавание комбо до FSM-ветки (Reset серии + слово→флип), потребление конфиг-снапшотов (AttachConfig: окно/кап/clipboard/биндинги), комбо-флип после завершения конвейера слова"
    - path: test/e2e/case_combo.go
      provides: "живые кейсы combo-word-layout, layout-single, super-space-alive"
  key_links:
    - from: internal/session/actor.go
      to: internal/config
      via: "AttachConfig(configSource) — актор читает Snapshot() один раз на событие; окно для новых серий, Options, биндинг комбо (Pitfall 8)"
      pattern: "Snapshot\\(\\)"
    - from: internal/session/actor.go
      to: internal/hotkey/names.go
      via: "биндинг комбо из конфига резолвится в Binding{Keyval, ModMask} — сравнение на входящем KeyPress"
      pattern: "ParseBinding"
    - from: cmd/goswitchd/main.go
      to: internal/session/actor.go
      via: "actor.AttachConfig(watcher) — reload-канал конфига в актора"
      pattern: "AttachConfig"
  prohibitions:
    - statement: "Переключение раскладки НЕ ДОЛЖНО трогать gsettings/dconf — внутренний флип за «один хозяин» (ADR-001/D-34 дословно); чтение dconf как оракула переключения запрещено (Pitfall 3 ADR-001)"
      status: resolved
      verification: judgment
    - statement: "Комбо-распознавание НЕ ДОЛЖНО ослаблять модификаторную дискриминацию FSM для остальных клавиш — серия с посторонней клавишей по-прежнему гаснет (D-35, корпус fsm_test.go — исполненный ADR-002)"
      status: resolved
      verification: judgment
  assumptions:
    - "FLAGGED (probe SWCH-01 / D-34): системный индикатор GNOME всегда показывает один источник goswitch и внутренний режим НЕ отражает — компенсации нет (OSD отложена); владелец принял на M0; e2e-оракул только mode-запись лога"
    - "FLAGGED (probe SWCH-02 / D-36 Q-маркер + A5): живая эргономика Shift+RightCtrl и разрешение имён ydotool для инъекции комбо (Control_R vs ctrl_r) — живая проба в кейсе до фиксации имени; переназначение в YAML покрывает неудобство"
    - "FLAGGED (probe SWCH-03 / D-34 рабочая интерпретация): «родное Super+Space продолжает работать» проверяется как «механизм не сломан присутствием goswitch» (инъекция не роняет демон/имя); полный MRU-цикл с одним источником ввода неверифицируем — формулировка SWCH-03 устарела против ADR-001, верификатор фазы не требует буквального MRU"
---

<objective>
Третий жест фазы: комбо «исправить слово и переключить раскладку» (D-36) — распознавание Control_R-press при зажатом Shift РАНЬШЕ FSM-ветки, порядок слово→флип; живое потребление конфиг-снапшотов актором (hot reload CONF-02: окно/кап/clipboard/биндинги); конфигурируемая ключевая клавиша серии (D-35 — только механические правки корпуса FSM); живые кейсы SWCH-01 (mode-оракул D-34) и SWCH-03 (super+space не ломается).

**As a** пользователь, закончивший писать русское слово в английской раскладке, **I want** одним комбо Shift+RightCtrl исправить слово И переключить раскладку, **so that** не делать два жеста подряд — а все тайминги и биндинги правятся в YAML на лету без перезапуска демона.

Purpose: SWCH-01..04 + CONF-02 (живая половина) — жесты переключения и конфигурируемость замыкаются на потребителя-актора.
Output: комбо-ветка актора, AttachConfig-потребление снапшотов, NewFSM(window, tapKeyval), KeyControlR, кейсы combo-word-layout / layout-single / super-space-alive.
</objective>

<execution_context>
@/home/nil/DiskD/W/Djarvur/goswitch/.zcode/gsd-core/workflows/execute-plan.md
@/home/nil/DiskD/W/Djarvur/goswitch/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-frazy-vydelenie-pereklyuchenie-i-konfiguratsiya/03-CONTEXT.md (D-34, D-35, D-36 — заперты BIND)
@.planning/phases/03-frazy-vydelenie-pereklyuchenie-i-konfiguratsiya/03-PATTERNS.md (§ fsm.go — Pitfall 4 дословно, корпус 11 тестов; § actor — диспетчеризация :253-269, armTimer :529-535, timer/config snapshot rule)
@.planning/phases/03-frazy-vydelenie-pereklyuchenie-i-konfiguratsiya/03-RESEARCH.md (Pitfall 8 — reload окна; §6 — Control_R = 0xffe4)
@docs/adr/ADR-001-layout-switching-mechanism.md (один хозяин, mode-оракул)
@docs/adr/ADR-002-tap-semantics.md (все действия на истечении окна)
@docs/SPEC.md §4.1 (комбо — порядок зафиксирован именем действия)
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Комбо-распознавание до FSM + конфигурируемый tap_key + порядок слово→флип (SWCH-02/D-36, D-35)</name>
  <reversibility rating="costly">Комбо-жест и его порядок (слово→флип) — пользовательский контракт SPEC §4.1/D-36; e2e-кейсы и матрица v2 завязаны</reversibility>
  <files>internal/hotkey/fsm.go, internal/hotkey/fsm_test.go, engine/keys.go, internal/session/actor.go, internal/session/actor_test.go, test/e2e/case_combo.go, test/e2e/main.go, mise.toml</files>
  <read_first>
  - .planning/phases/03-frazy-vydelenie-pereklyuchenie-i-konfiguratsiya/03-PATTERNS.md (§ fsm.go: keyPress :85-98 — где Control_R сегодня ставит sawKey; § actor: feedKey :356-398 — точка ветки выше mode-ветвей; дисциплина «The ProcessKeyEvent answer never waits» actor.go:69-70)
  - internal/hotkey/fsm.go (NewFSM :60-63, DefaultWindow :8-11, словарь событий :32-46)
  - internal/hotkey/fsm_test.go (11 тестов — какие правки механические: конструктор)
  - internal/hotkey/names.go (03-02: Binding, ParseBinding — резолв биндинга комбо)
  - internal/session/actor.go (HandleKey/feedKey, диспетчер :253-269, flipScript :333-349, startCorrection-конвейер 03-01)
  - engine/keys.go (KeyControlL :35 — рядом добавить KeyControlR)
  - test/e2e/case_word.go (runWordENRU — скелет кейса комбо)
  - /usr/include/ibus-1.0/ibuskeysyms.h (:191 — Control_R 0xffe4)
  </read_first>
  <behavior>
  - TestFSM_ConfigurableTapKey: NewFSM(window, 0xffe1) — серия по Shift_L даёт Single/Double; серия по Shift_R НЕ даёт действий; корпус с 0xffe2 (Shift_R) даёт прежние Single/Double/Triple (механическое обновление конструктора, семантика нетронута — D-35)
  - TestFSM_ModifierUse зелёный БЕЗ правок ожиданий (Pitfall 4 — дискриминация выживает)
  - TestActor_ComboWordThenFlip (fake-sink): press Shift_R → press Control_R (Mods: Shift) → на sink последовательность: RequireSurroundingText → (сверка HandleSurroundingText) → DeleteSurroundingText+CommitText("привет") → запись mode-флипа; НИКАКОГО действия серии (таймер серии погашен Reset; тройной/двойной не срабатывают)
  - TestActor_ComboEmptyBufferStillFlips: пустой буфер + комбо → конвейер отказывает empty-buffer, флип ВСЁ РАВНО выполняется (дискреция: переключение — первичный интент при отсутствии слова); порядок: отказ слова → флип
  - TestActor_ComboConfigurableBinding: SetOptions/снапшот с word_layout_combo "alt+ctrl_l" → комбо срабатывает на Ctrl_L+Alt, а Shift+Control_R — нет
  - TestActor_ComboDoesNotFeedBuffer: буквы с Mod4/комбо-клавиши буфер не кормят (comboMask-инвариант сохранён)
  - Живой кейс combo-word-layout: zenity: injectText("ghbdtn") → инъекция комбо (имя-кандидат из живой пробы: shift_r+ctrl_r или иное рабочее — канонизировать в кейсе) → waitForLog correction outcome:done + mode-запись → stdout-оракул "привет"; лог-оракул: mode изменилась ПОСЛЕ correction done (порядок D-36)
  </behavior>
  <action>
  RED (D-07): fsm_test.go — TestFSM_ConfigurableTapKey (конструктор ещё двухаргументный — механический RED компиляции допустим как подлинный: тест не собирается = тест падает; зафиксировать в red-evidence с указанием «compile-RED, механическая правка конструктора разрешена D-35»); actor_test.go — комбо-кейсы против актора без ветки (падения на утверждении CommitText/mode). Прогон mise exec -- go test ./internal/hotkey/ ./internal/session/ -race -count=1; red-evidence. Commit: test(03-04).
  GREEN — internal/hotkey/fsm.go: NewFSM(window time.Duration, tapKeyval uint32); внутренние сравнения с KeyvalShiftR заменяются на поле f.tapKeyval; DefaultWindow и KeyvalShiftR остаются документированными дефолтами; fsm_test.go — МЕХАНИЧЕСКИЕ правки вызовов конструктора (передать KeyvalShiftR), НИ ОДНОГО изменения ожиданий (D-35; TestFSM_DefaultWindow остаётся).
  GREEN — engine/keys.go: KeyControlR = 0xffe4 рядом с KeyControlL :35, провенанс-комментарий ibuskeysyms.h:191 (ловушка класса 02-05: значения только из заголовка).
  GREEN — internal/session/actor.go: в feedKey ВЫШЕ FSM-ветки и mode-ветвей: press && ev.Keyval == combo.Keyval && ev.Mods&combo.ModMask == combo.ModMask (лэтчи разрешены — прецедент NumLock 02-04: масочное сравнение через &, не через &^) → a.fsm.Feed(hotkey.Reset{}) (погасить серию — Pitfall 4), выполнить комбо: запуск конвейера слова с флагом comboPending; по завершении конвейера (done ИЛИ skipped — обе развязки) → flipScript() + comboPending=false (порядок слово→флип фиксирован D-36: флип строго после settled-записи конвейера); таймаут сверки также снимает comboPending с флипом (конвейер завершён отказом). Биндинг комбо: из конфиг-снапшота (word_layout_combo через hotkey.ParseBinding — уже в схеме 03-02), дефолт Binding{Keyval 0xffe4, ModMask Shift}. Лог: новая греп-стабильная форма slog.Info("combo", "kind", "word-layout") на входе комбо (аддитивно; формы "action"/"mode" не переименовывать — matrix.go:663 грепает их). Ответ ProcessKeyEvent по-прежнему мгновенный: комбо запускает конвейер через таймеры/мьютекс, без ожиданий в обработчике.
  Живой кейс combo-word-layout в case_combo.go (скелет runWordENRU; имя инъекции комбо канонизировать живой пробой в кейсе — кандидаты в Pitfall 5); register; mise.toml [tasks.e2e-combo]. Прогоны: e2e-combo, e2e-word (регресс), e2e-phrase (регресс). Commit: feat(03-04).
  </action>
  <verify>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise exec -- go test ./internal/hotkey/ ./internal/session/ -race -count=1</automated>
    <fails_when>строка FAIL — комбо-корпус или FSM-корпус не зелёный (11+ тестов FSM обязаны остаться зелёными, D-35)</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise exec -- go test ./internal/hotkey/ -run 'TestFSM_ModifierUse|TestFSM_DefaultWindow' -race -count=1 -v 2>&1 | grep -c '^--- PASS'</automated>
    <fails_when>выведенный счётчик меньше 2 — ключевые инвариантные тесты FSM сломаны правкой конструктора (D-35)</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise run e2e-combo && mise run e2e-word</automated>
    <fails_when>non-zero exit или отсутствие PASS combo-word-layout/PASS word-en-ru — комбо не работает живьём либо сломан путь слова</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise run ci</automated>
    <fails_when>non-zero exit — D-08 не зелёный</fails_when>
  </verify>
  <done>Комбо даёт слово→флип в зафиксированном порядке (пин последовательности sink+лог), биндинг переназначается, серия гасится без действия, корпус FSM зелёный с механическими правками; живой кейс PASS</done>
  <acceptance_criteria>
  - NewFSM(window, tapKeyval); fsm_test.go — ноль изменений ожиданий (греп-ревью диффа: только аргументы конструктора)
  - TestFSM_ModifierUse и TestFSM_DefaultWindow зелёные без правок
  - комбо-ветка в feedKey выше mode-ветвей; после комбо ноль действий серии (пин)
  - KeyControlR == 0xffe4 в engine/keys.go с провенанс-комментарием
  - порядок пинен: CommitText/mode-флип; INFO "combo" kind word-layout присутствует
  - e2e combo-word-layout PASS; stdout "привет"; mode-запись после correction done
  </acceptance_criteria>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Живое потребление конфиг-снапшотов актором (CONF-02) + кейсы layout-single / super-space-alive (SWCH-01/SWCH-03)</name>
  <files>internal/session/actor.go, internal/session/actor_test.go, cmd/goswitchd/main.go, test/e2e/case_combo.go, test/e2e/main.go, mise.toml</files>
  <read_first>
  - internal/config/watch.go (03-02: Watcher.Snapshot() config.Config — источник снапшотов; тип возврата закреплён производителем 03-02)
  - internal/session/actor.go (armTimer :529-535, epoch-теги :293-302, NewActor :116-123 — timer/config snapshot rule из PATTERNS)
  - .planning/phases/03-frazy-vydelenie-pereklyuchenie-i-konfiguratsiya/03-RESEARCH.md (§ Pattern 2 — снапшоты через atomic.Pointer; Pitfall 8 — reload окна: НИКОГДА не перевзводить живой таймер)
  - .planning/phases/03-frazy-vydelenie-pereklyuchenie-i-konfiguratsiya/03-CONTEXT.md (D-34 — mode-оракул, индикатору не верить; рабочая интерпретация SWCH-03)
  - test/e2e/main.go (goswitchNameOwner — матрица :388-423: живая проверка владения именем для super-space-alive)
  </read_first>
  <behavior>
  - TestActor_HotReloadWindowNewSeries (fake configSource): серия начата при окне 300 → до reload истекает по 300; после смены снапшота на 150 — новая серия истекает по 150; ВЗВЕДЁННЫЙ до reload таймер не перевзводится (stale no-op инвариант)
  - TestActor_HotReloadOptionsAndCombo: смена backspace_cap/clipboard_rung/word_layout_combo в снапшоте подхватывается следующим событием (кап применён к новому BuildPlan, комбо-биндинг сменился)
  - TestActor_HotReloadInvalidKeepsLastGood: источник отдаёт тот же (последний валидный) снапшот при битом файле — поведение неизменно (уровень watcher'а пинит 03-02; здесь пин потребления: один Snapshot() на событие)
  - Живой кейс layout-single (SWCH-01/D-34): zenity: одиночный shift-r → waitForLog mode-запись (смена en→ru); повторный одиночный → обратно ru→en; НИКАКОГО gsettings-чтения в оракуле
  - Живой кейс super-space-alive (SWCH-03): инъекция super+space → демон жив: имя org.freedesktop.IBus.goswitch всё ещё принадлежит процессу (goswitchNameOwner), последующая коррекция слова работает (injectText + двойной тап → PASS-часть кейса)
  - Живой reload в combo-word-layout-расширении: демон с -config; правка tap_window_ms в temp-файле → waitForLog config reload rejected ИЛИ применение без перезапуска (пин: снапшот подхвачен — новое окно действует на новую серию)
  </behavior>
  <action>
  RED (D-07): actor_test.go — кейсы behavior против актора без AttachConfig (fake configSource — маленький интерфейс в точке использования: Snapshot() config.Config; go-ultimate). Прогон mise exec -- go test ./internal/session/ -race -count=1; red-evidence. Commit: test(03-04).
  GREEN — internal/session/actor.go: метод AttachConfig(src interface{ Snapshot() config.Config }) — хранить источник; каждое событие (HandleKey/ExpiryAt/tap-взвод) читает снапшот ОДИН раз (локальная переменная, не держать указатель через мьютекс — Pattern 2): окно для ВЗВОДА НОВОЙ серии, Options (замена SetOptions-стартовой подачи из 03-03: SetOptions остаётся для тестов/без-конфига стартового режима, но при подключённом источнике приоритет у снапшота — сукцессию зафиксировать в SUMMARY), биндинг комбо. ЗАПРЕТ (Pitfall 8): никогда не перевзводить уже взведённый таймер при reload — новые серии/новое окно; взведённый доживает (stale-timer no-op уже переживает — TestFSM_WindowEdges). main.go: actor.AttachConfig(watcher) после старта watcher'а (при -config).
  Кейсы в case_combo.go: runLayoutSingle (двойная последовательность одиночных тапов с mode-оракулом), runSuperSpaceAlive (инъекция + goswitchNameOwner + коррекция после), расширение runComboWordLayout опцией -config + правкой окна на живом демоне (шаг: записать tap_window_ms: 500 в temp-config ДО старта, после PASS-части — переписать на 200, waitForLog применения, одиночный тап истекает по новому окну — грубая проверка < 300 мс/+ гонки не нужна: пин факт применения reload-записью лога). register "layout-single", "super-space-alive"; mise.toml [tasks.e2e-layout-single], [tasks.e2e-super-space]. Прогоны живьём + ci. Commit: feat(03-04).
  </action>
  <verify>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise exec -- go test ./internal/session/ ./internal/config/ -race -count=1</automated>
    <fails_when>строка FAIL — reload-потребление не зелёное под -race</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise run e2e-layout-single && mise run e2e-super-space</automated>
    <fails_when>non-zero exit или отсутствие PASS layout-single/PASS super-space-alive — mode-оракул D-34 или живучесть при super+space не доказаны</fails_when>
    <automated>cd /home/nil/DiskD/W/Djarvur/goswitch && mise run e2e-combo && mise run ci</automated>
    <fails_when>non-zero exit — расширенный комбо-кейс с reload или зелёная итерация не выполнены</fails_when>
  </verify>
  <done>Reload действует на живом демоне (окно/опции/биндинги, новые серии), last-good сохраняется, mode-оракул и super-space живучесть пинены живыми кейсами; история test до feat</done>
  <acceptance_criteria>
  - TestActor_HotReloadWindowNewSeries: старая серия — старое окно, новая — новое; перевзвода нет (пин)
  - AttachConfig читает Snapshot() один раз на событие (код-ревью пин; no pointer across mutex)
  - layout-single: mode-запись en→ru→en; оракуль не читает gsettings (греп кейса: отсутствие gsettings в case_combo.go)
  - super-space-alive: имя владельца сохранено после super+space; коррекция после инъекции работает
  - SetOptions → снапшот-приоритет сукцессия задокументирована в SUMMARY
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| конфиг-снапшот → поведение ввода | reload меняет окно/биндинги/капы на живом вводе рабочего стола |
| клавиатурный ввод → комбо-ветка | распознавание жеста до FSM — неверное распознавание гасит пользовательские серии |
| инъекция super+space (e2e) | системная комбинация GNOME — стенд не должен ломать оболочку |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04-01 | DoS | reload-путь в обработчике клавиш | high | mitigate | снапшот читается без блокировок (atomic), обработчик мгновенен; перевзвод таймеров запрещён (Pitfall 8) — гонки исключены конструкцией, пин -race |
| T-03-04-02 | Tampering | комбо-ветка ослабляет дискриминацию серий | high | mitigate | комбо гасит серию Reset-ом осознанно и только на точном биндинге; для остальных клавиш путь в FSM не меняется — корпус fsm_test зелёный (Pitfall 4) |
| T-03-04-03 | Information Disclosure | mode/комбо-логи | low | mitigate | логи несут kind/mode/счётчики, не содержимое — продолжение D-20/D-21 |
</threat_model>

<verification>
- mise run ci зелёный; e2e-combo / e2e-layout-single / e2e-super-space живьём PASS
- Дифф fsm_test.go — только механические правки конструктора (D-35)
- Порядок слово→флип пинен sink-последовательностью и лог-порядком
</verification>

<success_criteria>
- SWCH-02: комбо работает живьём, переназначается, порядок D-36 исполнен
- SWCH-01: mode-оракул живьём в обе стороны (D-34)
- SWCH-03: super+space не ломает демон (рабочая интерпретация D-34)
- SWCH-04/CONF-02: окно из конфига + живой reload новых серий + last-good
</success_criteria>

<artifacts_produced>
## Artifacts this plan produces (символы плана 03-04)

- `internal/hotkey`: `NewFSM(window, tapKeyval uint32)`; поле tapKeyval
- `engine/keys.go`: `KeyControlR = 0xffe4`
- `internal/session`: `Actor.AttachConfig(interface{ Snapshot() config.Config })`; комбо-ветка feedKey; флаг comboPending; лог-форма INFO "combo" kind=word-layout
- `cmd/goswitchd`: проводка actor.AttachConfig(watcher) при -config
- test/e2e: кейсы `combo-word-layout` (с живой reload-секцией), `layout-single`, `super-space-alive`
- mise-задачи `e2e-combo`, `e2e-layout-single`, `e2e-super-space`
</artifacts_produced>

<output>
Create `.planning/phases/03-frazy-vydelenie-pereklyuchenie-i-konfiguratsiya/03-04-SUMMARY.md` when done
</output>
