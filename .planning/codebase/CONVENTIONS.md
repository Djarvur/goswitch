# Conventions

Project conventions established during development. This file is the source of the
`## Conventions` section in `AGENTS.md` (regenerated by `generate-claude-md`; edit here,
not between the GSD markers).

## CI gates (go-ultimate skill)

CI-гейты скилла go-ultimate — build / vet / test -race -cover / go mod tidy +
git diff --exit-code / govulncheck — включены с Фазы 1 (`.github/workflows/test.yml`).

Отклонение (единственное): **golangci-lint** отложен — инструмент не установлен на целевой
машине; текущий lint-гейт `go vet` + `go test -race` (стадийность явно разрешена исследованием
фазы 1, см. `.planning/phases/01-adr-paket-i-karkas-ibus-dvizhka/01-RESEARCH.md`, Environment
Availability). При появлении инструмента — добавить пиннированную версию в отдельный job CI.
